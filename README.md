# STATE OF PLAY — UI & Motion Library

**v4.14.0 / 37カテゴリ・817パーツ**。触って選び、ソースと意図を自分の開発へ持ち出すUIパーツ集です。
表現と素材感を重視するAタイプと、落ち着いた実用的なBタイプを収録しています。

> ベースとなるv4.0.0は、未保存だった追加分をv3.8.0から再実装した版です。以前の未保存ソースと同一ではありません。
> 既存280パーツの本体は維持しています。経緯と照合は [RECOVERY.md](docs/RECOVERY.md) を参照してください。

## v4.14.0の追加 — ホバーで動く装飾10種類

既存の装飾20種類にAタイプ10種類を追加し、装飾は30種類（A22・B8）になりました。ホバーで近づく・開く・ほどける表現を中心に、React/Vanillaの配布コードと個別ZIP、使い方、AI用プロンプトを用意しています。v4.13.0基点の受領差分に含まれていた削除済みPaper Loaderは戻さず、v4.13.4までのサイト修正を保持しました。受領した新作10件のVanilla初期化コードにあった構文エラーも修正しています。[一覧と導入](docs/ORNAMENTS.md)を参照してください。

## v4.13.4の修正 — ツールチップ／ポップオーバーの配置

24種類すべてで、出現アニメーション中の縮んだ高さを配置計算に使い、元の大きさへ戻ったパネルが「詳しく見る」のボタンへ重なる問題を修正しました。Folio、Botanical、Ceramicのボタンは、通常時もホバー時もサイトに合う暗い配色にしました。パーツ詳細の配布コード、使い方、AI用プロンプトも更新しています。[原因と確認](docs/HINTS-REDESIGN.md)を参照してください。

## v4.13.3の修正 — 通知Aタイプ

通知Aタイプ16種類を、知らせが届く場面に合わせて作り直しました。光の到着帯、紙の伝票、製図の注記、電極、案内帯など、それぞれ通知専用の構図と短い出現・退出動作を持ちます。静止見本と実際の`notify()`で出る通知を揃え、本文・操作・表示時間の処理は維持しました。配布コード・使い方・AI用プロンプトも更新しています。[設計と確認](docs/NOTICE-REDESIGN.md)を参照してください。

## v4.13.2の修正 — ツールチップ／ポップオーバー

24種類すべてで、展開直後に候補面の右・下へ一瞬出ていたスクロールバーを抑えました。外枠は装飾をクリップし、長い本文や操作だけを内側でスクロールします。Aタイプ16種類は、情報が現れる位置と動き、輪郭や文字組みを素材ごとに作り分け、ラジオ・コンボボックスと同じ面の繰り返しを減らしました。好評だったApertureとPrismの幾何学的な表現は維持しています。パーツ本体と使い方・AI用プロンプトも更新しました。[変更内容](docs/HINTS-REDESIGN.md)を参照してください。

## v4.13.1の修正 — コンボボックスの候補

24種類すべてで、候補一覧の内側をスクロールするときは開いたまま、ページや周囲の領域をスクロールすると候補パネルを閉じるようにしました。Nixie Finderの候補面には電極線を重ねず、入力面の演出と候補文字の読みやすさを両立しています。配布コード・使い方・AI用プロンプトも更新しました。[原因と確認](docs/COMBOBOX-SCROLL-FIX.md)を参照してください。

## v4.13.0の追加 — 装飾 / ORNAMENTS

意味を持たない視覚的なアクセントとして、装飾20種類（Aタイプ12、Bタイプ8）を追加しました。React / Vanillaの配布コード、使い方、AI用プロンプト、個別ZIPを備え、`paused` / `data-paused` で動きを止められます。v4.12.0向け差分から必要な追加だけを統合し、v4.12.5までのサイト修正、カテゴリ別読み込み、削除済みの重複ローダーはそのまま維持しています。受領ファイルのVanilla初期化コードにあった構文の欠落も修正しました。[一覧と導入](docs/ORNAMENTS.md)を参照してください。

## v4.12.5の修正 — ナビゲーションの子項目

「もっと見る」の子項目をクリックする前に一覧が閉じる問題を修正しました。選択後は現在地の名前と親の選択面が更新されます。展開一覧はページや親領域をスクロールすると閉じ、一覧内のスクロールでは開いたままです。ナビゲーション20種類の配布用共通コード・使い方・AI用プロンプトも更新しました。[APIと動作](docs/NAVIGATOR.md)を参照してください。

## v4.12.4の修正 — ナビゲーションの展示

ナビゲーション20種類で、リンク操作後に追加されていた「移動する操作例」の文言をなくしました。デモではリンク先へ遷移せず、パーツ内の現在地表示を更新します。これにより展示カードの幅と形が操作後も変わりません。配布用パーツ・使い方・AI用プロンプトは変更していません。

## v4.12.3の修正 — 右クリックメニューの展示

ギャラリーで操作結果のデモ文言がカードの横へ入り、右クリックメニューの幅を縮めていた問題を修正しました。メニュー本体が持つ結果表示を利用し、重複した文言を出さないようにしています。他の作業台パーツのデモ文言もカードの下に配置します。パーツ本体と配布用コード・使い方・AI用プロンプトは変更していません。

## v4.12.2の修正 — 期間選択の日付配置

日時選択20種類の期間モードで、開始日・区切り線・終了日を均等に配置しました。狭い部品幅では日付を縦に並べ、さらに狭い場合はカレンダーボタンを下へ移して、日付を切らずに表示します。各パーツの配布CSS・使い方・AI用プロンプトも同じ配置へ更新しました。[構成と確認](docs/DATE-PICKER-CUSTOM-UI.md)を参照してください。

## v4.12.1の修正 — カテゴリ選択の配置

サイト全体の検索欄を廃止し、その場所へAurora Selectによるカテゴリ選択を移しました。タブ列右端の幅に合わせてコンパクトにし、見出しを表示せず選択中のカテゴリ名と件数だけを見せます。サイト内だけ黄緑系のベースカラーへ調整し、Aurora Selectの配布用デザインと36カテゴリ・787パーツの本体は変更していません。[接続と確認](docs/CATEGORY-SELECT-INTEGRATION.md)を参照してください。

## v4.12.0の追加 — NAVIGATOR

検索バー20、コマンドパレット16、右クリックメニュー16、ナビゲーション20、テーブル16の計88パーツを追加しました。検索の非同期結果、コマンド実行、対象要素へのメニュー接続、実リンク、表の並べ替え・選択は利用先のデータや処理へ接続できます。5カテゴリともカテゴリ別の遅延読み込みを維持し、コード・使い方・AI用プロンプト・個別ZIPを提供します。以前削除した重複ローダーを戻していないため、全体は787種類です。[API](docs/NAVIGATOR.md)と[統合結果](docs/NAVIGATOR-INTEGRATION-VERIFICATION.md)を参照してください。

## v4.11.0の追加 — SIGNATURE

アバター、評価、カラーピッカー、スケルトン、タイムライン、ステップ／ウィザードを各16種類、計96種類追加しました。6カテゴリともA10・B6で、値・読み込み状態・履歴・工程は利用先のデータへ接続できます。ギャラリーではカテゴリ別の遅延読み込みと既存の起動表示を維持し、新作のコード・使い方・AI用プロンプト・パーツZIPを提供します。以前に重複のため削除したPaper Loaderは戻していないので、全体は699種類です。[API](docs/SIGNATURE.md)と[このリポジトリへの統合結果](docs/SIGNATURE-INTEGRATION-VERIFICATION.md)を参照してください。

## v4.10.10の修正 — 日時選択の独自UI

日時選択20種類すべてで、日付・期間・日時・時刻の入力を独自パネルに統一しました。日付欄を押すと独自カレンダーが開き、時刻は時・分の選択面で編集できます。ブラウザー標準の日時ピッカーが重複して開くことはありません。直接入力、フォーム送信、制約、リセットを維持し、各パーツのコード・使い方・AI用プロンプトと配布ZIPにも反映しました。[構成と検証](docs/DATE-PICKER-CUSTOM-UI.md)を参照してください。

## v4.10.9の修正 — コンボボックス展開時のスクロールバー

候補を開く瞬間に右端・下端へ一瞬だけ出ていたスクロールバーを抑えました。コンボボックス24種類すべてで、表示パネルの外枠はクリップし、候補が多い場合は候補一覧だけが縦にスクロールします。コード・使い方・AI用プロンプトにも同じ構造を反映しました。[原因と確認](docs/COMBOBOX-SCROLL-FIX.md)を参照してください。

## v4.10.8の修正 — Mercury Selectorの光沢移動

セグメント「Mercury Selector」で隣の項目を選ぶ際、金属グラデーションの大きな回転と強い光沢が走る動きを抑えました。素材感を保ちながら、選択面の伸縮と反射角を小さくしています。使い方・AI用プロンプト・配布コードも同じ動きに合わせて更新しました。[原因と確認](docs/MERCURY-SELECTOR-MOTION.md)を参照してください。

## v4.10.7の追加 — カテゴリ選択にAurora Selectを導入

サイト上部のカテゴリ移動をネイティブselectから、既存のAurora Selectに切り替えました。日本語名・英語カテゴリ名・件数を表示し、一覧切替・直接リンク・キーボード操作を既存のカテゴリ状態へ接続しています。Auroraの共有コントローラーとスタイルを使い、初回表示時に「プルダウン」カテゴリの実装を先読みしない構成です。[統合と確認](docs/CATEGORY-SELECT-INTEGRATION.md)を参照してください。

## v4.10.6の修正 — 数値入力のフォーカス

数値入力の＋／−ボタンを押すと中央の入力欄が自動でフォーカス状態になる問題を修正しました。共有コントローラーから入力欄への自動focusを外し、押した操作ボタンにフォーカスを残します。全20種類の使い方・AI用プロンプトと、各形式に同梱される共有コードを更新しました。[原因と確認](docs/NUMBER-FOCUS-FIX.md)を参照してください。

## v4.10.5の追加 — 進捗表示のデモ再生

「進捗表示」カテゴリに「デモ再生／デモ停止」ボタンを追加しました。24種類の表示値を素材ごとに位相をずらして同時に巡回させるため、塗り・水位・面の反応を一覧で比べられます。値はプレビュー内だけで、手動操作・カテゴリ変更・詳細表示・ページ非表示で停止します。[動作と確認](docs/PROGRESS-DEMO.md)を参照してください。

## v4.10.4の修正 — ポップアップ表示時のスクロールバー

ポップアップが開くときに右端・下端へ一瞬だけスクロールバーが出る問題を修正しました。全24種類で、外側のダイアログ枠はスクロールを切り、内容面だけを縦方向にスクロールさせます。短い入場アニメーションと各スキンの素材表現は維持し、長い本文も引き続き読めます。詳細コード・使い方・AI用プロンプトと、短い画面高での確認を更新しました。[実装と確認](docs/POPUP-SCROLL-FIX.md)を参照してください。

## v4.10.3の修正 — タブのスクロールバー

Aurora TabsでNotesを選ぶだけで横スクロールバーが出る問題を修正しました。24種類すべてで、項目が実際に幅を超える場合だけ横スクロールを有効にし、つまみとレールは各パーツの素材色・形に合わせています。詳細のコード・使い方・AI用プロンプトも更新しました。[原因と検証](docs/TAB-SCROLL-FIX.md)を参照してください。

## v4.10.2の修正 — プルダウンの閉じ際

Kinetic系12種類で、閉じた候補一覧を退出演出用に再表示していたため、一瞬だけ全項目が見える問題を修正しました。閉じた瞬間に候補面を非表示にし、短い反応はトリガーに残します。全36種類の実クリック・閉じた次のフレーム・Escapeを確認し、使い方・AI用プロンプト・配布ソースも同期しました。[原因と検証](docs/DROPDOWN-CLOSE-FIX.md)を参照してください。

## v4.10.1の統合 — WAYFINDER

パンくずAタイプ10種類と数値入力Aタイプ13種類を改修しました。パンくずは階層線・紙の重なり・現在地の文字組み、数値入力は操作キー・ダイヤル・水位計など、配置と動きから作り分けています。直接入力、長い階層名、省略項目のリンクを維持し、詳細コード・使い方・AI用プロンプト・配布ZIPも更新しました。同名素材を両カテゴリで併用しても色が混ざらないCSS境界と、設定更新時に省略メニューを閉じる動作も補いました。ローカルで削除済みのPaper Loaderは戻していないため、全603パーツ・ローダー39種類です。[差分の原記録](docs/PATCH-v4.10.0.md)と[このリポジトリの統合検証](docs/WAYFINDER-INTEGRATION-VERIFICATION.md)を区別してください。

## v4.9.3の修正 — カテゴリの読み込み表示

カテゴリや検索条件を切り替えてパーツを読み込む間に、Orbital Loomの3本の軌道と中心の球を表示します。添付されたVanilla TS／portable構成のプロンプトと既存のパーツを基準にし、表示・終了・縮小モーション・画面外での停止を接続しました。初回起動のNixie Loaderと、カテゴリ別の遅延読み込みは維持しています。[実装と検証](docs/CATEGORY-LOADING.md)を参照してください。

## v4.9.2の修正 — 起動表示

初回表示と再読み込みでCSS適用前のページが見える問題を修正しました。アプリ本体と初期カテゴリが使えるまで、Nixie Loaderを起動表示として見せます。詳細画面にあるVanilla TS用プロンプトを参照し、琥珀色の正方形と連続した回転を保っています。動きを減らす設定、読み込み失敗、JavaScript無効時にも対応します。既存のカテゴリ別遅延読み込みは維持しています。[実装と検証](docs/BOOT-LOADING.md)を参照してください。

## v4.9.1の統合 — SEQUENCE

ページ送りのAタイプ10種類と、タグ／チップのAタイプ16種類を改修しました。
紙・金属・ガラス・布・結晶などの面が、ページ切り替えや選択・削除に反応します。
大きな装飾アイコンを必須にせず、文字・現在値・フォームの操作を保っています。

- ページ送り：実際の番号位置へ選択面が追従。ボタン／ネイティブリンク、総ページ数の変更、先頭・末尾、RTLに対応します。
- タグ：文字のみでも成立する16素材。表示・選択・削除・選択＋削除を切り替えられ、同じ項目のinputとフォーカスを保持します。
- 詳細画面：総ページ数、タグの機能、長いラベル、リセットを実際のコントローラーで試せます。
- ローダー39種類を含む対象外577パーツの本体は変更していません。依存ライブラリも変更していません。

`SEQUENCE`で今回の26種類を検索できます。APIと導入例は [SEQUENCE.md](docs/SEQUENCE.md)、このリポジトリへの取り込み結果は [SEQUENCE-INTEGRATION-VERIFICATION.md](docs/SEQUENCE-INTEGRATION-VERIFICATION.md) を参照してください。元の差分の検証記録は [SEQUENCE-VERIFICATION.md](docs/SEQUENCE-VERIFICATION.md) に残しています。

## v4.8.1の修正 — ローダー

Nixie LoaderとCeramic Loaderの2番目の正方形が周回の継ぎ目で跳ぶ問題を修正しました。見た目と動きがほぼ同じだったPaper Loaderは削除し、Ceramic Loaderを残しました。この時点ではローダー39種、全603パーツです。詳細のコード・使い方・AI用プロンプト・ZIPも更新しています。[変更と検証](docs/LOADER-ORBIT-FIX.md)を参照してください。

## v4.8.0の改修 — CONTINUUM（履歴）

進捗表示16種、ファイル選択13種、日時選択13種の**Aタイプ42種**を改修しました。
表示値・入力・フォーカス位置は即時に更新し、素材の面だけが展開・変形します。
ローダーは既存24種を維持し、**A8種＋B8種を追加して40種**になりました。
ギャラリーで `CONTINUUM` を検索すると今回の58種、`MOTION STUDIES` で新しいローダー16種に絞り込めます。

- 進捗：実際の進捗率に応じて、水位・折り目・金属板・結晶面などが変化します。数値とネイティブprogressは演出を待たずに更新します。
- ファイル選択：ドラッグ・フォーカス・選択で受け入れ面が開きます。選択内容はローカルのFile[]で、送信は行いません。
- 日時：背景の展開・月の切替・確定した期間の帯が変化します。日付セルと文字の操作位置は動かしません。
- ローダー：折り畳み立方体・軌道・紙などのAと、通常のリング・ドット・細いラインなどのB。CSSアニメーションは画面外・ページ非表示・縮小モーション・一時停止に対応します。

導入とAPIは [CONTINUUM.md](docs/CONTINUUM.md)、今回実施した検証と未確認範囲は [CONTINUUM-VERIFICATION.md](docs/CONTINUUM-VERIFICATION.md) を参照してください。
共有スキンには、CONTINUUM / RESONANCEへ古い装飾が入り込まない境界も追加しました。
独自変更、.git、package-lock.jsonを保護してから更新してください。依存ライブラリの追加・バージョン変更はありません。

## v4.7.0の改修 — RESONANCE

ラジオ／選択カード・コンボボックス・通知・ツールチップ／ポップオーバーのAタイプを各16種類、計64種類改修しました。操作によってガラスの膜、紙の層、結晶面、布のひだなどが変化します。入力と本文は実際のDOMのままです。

候補一覧と選択済みタグの配置、通知のhover/focus中の残り時間、tooltipとinteractive popoverのARIA・フォーカスも整理しています。`RESONANCE`で検索すると対象を確認できます。Bタイプと他カテゴリのパーツ本体は維持しています。

[設計と使い方](docs/RESONANCE.md) / [今回の検証](docs/RESONANCE-VERIFICATION.md)

## v4.6.0の改修 — TRANSFORM（履歴）

タブ・セグメント・ポップアップ・スライダーのAタイプを各16種類、計64種類改修しました。選択片・背景の構造・展開する面・操作に応答するレールを作り込み、公開APIと値の確定は維持しています。

`TRANSFORM`で検索すると対象をまとめて確認できます。対象外524パーツの本体5,240ファイルはv4.5.0と同一です。Bタイプのデザインは変更しません。

[設計・使い方・改修一覧](docs/TRANSFORM.md) / [今回の検証](docs/TRANSFORM-VERIFICATION.md)

## v4.5.0の改修 — UNFOLD / RESPONSIVE（履歴）

Aタイプのアコーディオン16種類を、紙の展開、ガラスの分離、シャッター、波の膜など、それぞれ異なる動きと造形へ改修しました。新規IDの追加ではありません。

テキストボックスは6種類（Obsidian / Capillary / Prism / Ribbon / Orbit Search / Contour）だけを改修しました。入力する文字の位置やキャレットは固定したまま、外側の素材がフォーカスと入力へ反応します。残り18種類と、アコーディオンのBタイプ8種類は本体を変更していません。

検索欄で `UNFOLD` または `RESPONSIVE` を検索すると、今回の対象を探せます。対象外566パーツの本体5,660ファイルはv4.4.0と同一です。React共通ビュー2ファイルに、任意の内部参照と装飾レイヤー受け渡しだけを追加しています。既存の公開props・ID・フォームAPIを変更する更新ではありません。

[設計・組み込み・更新方法](docs/UNFOLD-AND-RESPONSIVE.md) / [今回の検証範囲](docs/UNFOLD-VERIFICATION.md)

## v4.4.0の追加 — KINETIC（履歴）

スクロールバー12種類とプルダウン12種類を新規追加しました。既存564パーツは削除せず、元のスタイル・ID・APIを維持しています。対象カテゴリはそれぞれ36種類です。

新作には `KINETIC` タグがあり、検索でまとめて探せます。スクロールバー／プルダウンのカテゴリ内では新作が先に並びます。従来のA/B分類を変更する更新ではありません。

レール自体の変形、磁力線、液面、分光する軌跡、紙の展開、絞り状の開口、立体的な選択面など、入力の状態とは独立した反応を実装しています。既存のデモ枠を変えるのではなく、パーツ自体に含まれる描画です。

[新作とAPI](docs/KINETIC-COLLECTION.md) / [今回の検証](docs/KINETIC-VERIFICATION.md)

## v4.3.0の変更（履歴）

スクロールバーとプルダウンのAタイプ32種類を、比率・面・動きを中心に再設計しました。可視レール9〜12px、つまみ15〜18pxに整え、8種類ではスクロール位置に応じてレールの質感が変わります。

プルダウンは文字を主役にし、大きな自動アイコンを標準から外しました。候補背景・光・下線・紙の展開などが操作に連動します。任意の小さなアイコンや見出しを追加することもできます。

[設計と導入](docs/REFINED-SCROLL-SELECT.md) / [今回の確認範囲](docs/REFINEMENT-VERIFICATION.md)

## v4.2.0の変更（履歴）

スクロールバー16種類・プルダウン16種類の**Aタイプ**を造形から改修しました。ガラス管、切削金属、ケーブル、フィルム、レンズ鏡筒、顔料見本、布、切符など、レールや選択面そのものを素材として表現します。Bタイプと他カテゴリの532パーツは維持しています。

配布ソースとAI用プロンプトも更新しました。詳細は [SCULPTED-SCROLL-SELECT.md](docs/SCULPTED-SCROLL-SELECT.md)、今回の確認範囲は [SCULPTURE-VERIFICATION.md](docs/SCULPTURE-VERIFICATION.md) を参照してください。

## v4.1.0の変更

v4で追加されたAタイプのうち、ローダーを除く171種類の材質・造形・状態表現を改訂しました。Bタイプとローダーはデザインを維持し、CSSの相互干渉だけを修正しています。詳しくは [ATELIER.md](docs/ATELIER.md) を参照してください。

## サイト構造の更新と外部差分の取り込み

初期表示はトグルです。カテゴリの実物はタブ選択時、配布コード・説明・プロンプトは詳細表示時に取得します。「すべて」は24件ずつ追加表示します。
ChatGPT側で作成された差分には旧サイト構造が含まれる場合があります。上書き前に [カテゴリ別読み込み・差分統合手順](docs/LAZY-LOADING.md) を確認してください。

## 開発

Node.js 22.12.0以上を使います。PowerShellでも同じコマンドです。

```sh
npm install
npx playwright install chromium
npm run dev
```

ターミナルに表示されるURLを開きます。ソースの `index.html` をダブルクリックする方式ではありません。
`npm run build` が配信用の `dist/` を作り、`npm run preview` でビルド結果を確認できます。
この配布環境ではnpm接続が制限されているため、ロックファイルは捏造せず同梱していません。
既存のロックファイルがあれば保持し、`npm install` で更新したものをGit管理してください。

## 検証と全体ZIP

```sh
npm run typecheck
npm test
npm run verify
npm run package
```

`verify` は全体の型チェック・単体テスト・実Viteビルド・各ブラウザー検証を実行します。
`package` はリポジトリ全体を `release/` に出力し、CRC・SHA-256を照合します。
今回のテスト範囲は [REFINEMENT-VERIFICATION.md](docs/REFINEMENT-VERIFICATION.md) を確認してください。v4.2の記録は [SCULPTURE-VERIFICATION.md](docs/SCULPTURE-VERIFICATION.md) にあります。v4.1の記録は [ATELIER-VERIFICATION.md](docs/ATELIER-VERIFICATION.md) にあります。従来版の記録は [FINAL-VERIFICATION.md](docs/FINAL-VERIFICATION.md) に残しています。
ネットワーク制約下のオフライン検証は実Vite・指定依存バージョンの検証と区別しています。

## パーツを持ち出す

カード内のパーツはその場で操作でき、余白や「CODE」から詳細を開きます。
コード、使い方、AI用プロンプト、ファイル一覧を整理して表示します。

- React TSX / JSX、通常サイト向けTS / JSの4形式。
- 「導入向け」はパーツ本体と必要な補助処理が1つのフォルダーで完結。「元の構成」は元の階層を保持。
- コピーに成功したときだけ、成功表示を出します。拒否された場合は手動コピーできます。
- 「ファイルを保存」は選択中のファイル。「パーツZIP」は依存・使用例・説明・プロンプトを階層ごと含みます。
- ZIPのテキスト保管用形式は閲覧用です。通常ソースの代わりに直接実行する形式ではありません。

プレビューに入力した値・ファイルは送信・永続保存しません。実際の検索、保存、通知の発行、アップロード、検証などは利用先へ接続してください。
A/Bは見た目・使いどころの方向性であり、対応する公開APIを無視した実装や安全性の保証ではありません。

## 構成

```text
src/app/           ギャラリー・詳細・コード・保存UI
src/catalog/       掲載定義・配布レイアウト・共通プロンプト
src/parts/         各パーツの正本（CSS、HTML、TSX、TS、説明）
src/shared/        共有コントローラー
scripts/           Viteカタログ、ソース変換、ZIP生成
tests/             単体・ブラウザー・移動配置の検証
public/            アイコン、コード表示／ZIP用ライブラリ
```

`packages/`を常時生成しません。配布形式を正本から生成し、コード表示とパーツZIPで同じ内容を使います。
長い共有ソースはカタログの転送時だけ重複をまとめ、表示／配布するときには元の本文を損失なく復元します。
`node_modules`、`.test-output`、`dist`、`release`、`.git`、秘密情報は全体ZIPへ含めません。

## 既存リポジトリの置き換え

まずバックアップし、未コミットの独自変更を退避してください。**`.git`は削除せず、既存の`package-lock.json`も保持**します。
ZIP内の `STATE-OF-PLAY/` の中身を既存リポジトリのルートへ配置してください。
ZIPのフォルダー自体をルートの中へもう一段入れないように注意してください。
v3.8.0からの移行では元パーツを削除せず、新規カテゴリと関連コードを追加しています。
異なる変更履歴がある場合は、無条件の全削除ではなく差分を確認して統合してください。
GitHubへのコミット・pushは、このZIPの取得や展開では行われません。

## ドキュメント

[全807パーツ](docs/COLLECTION.md) / [追加13カテゴリのAPI](docs/FOUNDATIONS.md) / [パーツ追加](docs/ADDING-PARTS.md) /
[既存プロジェクトへの組み込み](docs/INTEGRATION.md) / [移行](docs/MIGRATION.md) / [ライセンス](THIRD-PARTY-NOTICES.md)
