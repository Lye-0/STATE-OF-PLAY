import { createStaticSurface } from '../../../../shared/static-surface';
/** Initialization is optional: this container is styled entirely by CSS. */
export function init(element: HTMLElement) { return createStaticSurface(element); }
