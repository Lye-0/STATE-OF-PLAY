このパーツに近いUIを再現してください。

- 種類: 純粋な装飾パーツ / ornament
- 名前: Ribbon Fold
- 方向性: Aタイプ。大胆で、ホバー時に面白い変化がある。
- 背景: 深いダークトーン。余白を広く取り、中央の装飾を主役にする。
- 主役: 線、光、輪、帯、ヒンジ、プリズムなどの抽象モチーフ。
- 動き: 常時はゆっくり、ホバー時は構造が開く・寄る・流れる・加速するなど、見ていて楽しい変化を作る。
- 目的: 機能を持たない飾り。ヒーロー、区切り、余白、導入部で使う。
- 仕上がり: おしゃれで、少し実験的。過剰な記号感や説明的なアイコン化は避ける。
- アクセシビリティ: 装飾なので読み上げ対象から外せる構成にする。
- 実装: 単独で再利用できるReact / HTML+CSSパーツとして組み立てる。

対象プロジェクトのディレクトリ構造、パッケージ管理、TypeScriptとCSSの規約を確認し、配布パスは配置例として適応させます。既存ファイルを無条件に上書きしません。参照先を更新し、画面から取り外すときは `destroy()` を呼べる構成にします。実行できなかった検証は未確認と明記してください。

## 配布実装の条件（v4.14.0）

同梱のmarkup.html、styles.css、react/<Component>.tsx、vanilla/init.tsを正本とし、選んだ形式に必要なファイルを省かないでください。図形はaria-hiddenで装飾専用です。ホバーで構造が動きますが、選択値や状態の意味を追加しません。
Reactのpausedと通常DOM版のdata-pausedでアニメーションを停止できるようにし、prefers-reduced-motionでは静止表示にします。通常DOM版はsetPaused()とdestroy()を維持してください。複数配置、ホバー前後、停止、動きを減らす設定、幅320pxの表示を確認してください。
