# Three Dots — B / MOTION STUDIES 4.8.0

三つの点が順番に少し持ち上がる。
外観とアニメーションの正本は `continuum/loaders.css` と、このパーツのstyles.cssです。構造は `continuum/loader.ts` の `three-dots` 分岐。一般的な別のスピナーや絵文字に置換しないでください。
普通のアプリで使う控えめなBタイプです。大きな枠・派手な発光・追加オブジェクトへ変更しないでください。
ARIAのstatusと読込メッセージを保ち、見た目の要素はaria-hiddenにします。停止は完了ではありません。実際のロード処理に合わせて利用先が表示／非表示にします。画面外、非表示タブ、縮小モーション、明示的pauseの制御を維持します。
配置は利用先のディレクトリ構成と規約を確認し、importとCSSを同時に調整します。既存のコンポーネントを上書きしないでください。型チェックと実際の表示・取り外しを確認し、未実行の項目は区別してください。
