import {initNavigationLink} from '../../../../shared/navigation-link';
/** Optional. href + CSS work without JavaScript. */
export function init(root:HTMLElement) { return initNavigationLink(root); }
