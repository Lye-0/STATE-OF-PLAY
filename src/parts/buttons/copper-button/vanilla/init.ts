import {createActionButton,type ActionButtonOptions} from '../../../../shared/action-button';
export function init(root:HTMLElement,options:ActionButtonOptions={}) { return createActionButton(root,options); }
