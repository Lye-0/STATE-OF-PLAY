import {createCheckboxController,type CheckboxOptions} from '../../../../shared/checkbox-controller';
export function init(root:HTMLElement,options:CheckboxOptions={}) {return createCheckboxController(root,options);}
