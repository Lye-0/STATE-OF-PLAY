# v4.9.0 → v4.10.0 再構築差分

> これは受領した差分の原記録です。604パーツ・40ローダー、オフライン検証などは差分作成時の環境を指します。現在のv4.10.1リポジトリ（603パーツ・39ローダー）への取り込みは [WAYFINDER-INTEGRATION-VERIFICATION.md](WAYFINDER-INTEGRATION-VERIFICATION.md) を参照してください。

## この配布物について
前の返信で案内したv4.10.0のZIPは実際には保存されていませんでした。
この版は、保存済みのv4.9.0ソースと、残っていたパンくず・数値入力の表示画像を基に、改修を再構築したものです。
失われた前回の実装ファイルそのものの再送ではありません。以前の画像・細かな動作と完全一致は保証しません。

## 対象
パンくずのAタイプ10種類、数値入力のAタイプ13種類。
Bタイプ、他カテゴリ、ローダーは変更しません。総数604パーツ・25カテゴリです。
両カテゴリの構成を分け、パンくずは経路と階層、数値入力は単一値の操作機構として設計しています。

## 適用
1. v4.9.0の作業フォルダーをバックアップします。
2. ZIPの `STATE-OF-PLAY/` 内の中身を、リポジトリのルートへ重ねて配置します。
3. `.git`、独自変更、`package-lock.json`は保護してください。削除対象はありません。
4. 依存ライブラリの追加・バージョン変更はありません。
5. `npm run typecheck:app`、`npm test`、`npm run test:wayfinder`で確認できます。

## 検証
再構築したソースで次を実行しました。
- TypeScript 5.8.3によるアプリのstrict型チェック: 成功。
- WAYFINDERと既存Atelierの関連単体テスト: 16件成功。
- 実Chromiumでの操作・配置・フォーム・縮小モーション・後片付けの検証: 11項目成功。
- 全体ZIPと差分ZIPのCRC、SHA-256、v4.9.0へ差分を適用した結果の完全一致を確認。

ブラウザーテストはTypeScriptの実ソースをコンパイルしたテスト用文書によるもので、Viteの本番ビルド確認ではありません。
IMEは合成イベントで確認しました。Windows/iOSの実IME、指定React環境の実行、公式依存一式での全体型チェック、npm audit、GitHub Actionsの完走は未確認です。
既存の全件ZIPラウンドトリップを含むnpm testは途中で停止しており、今回「全体のテストが完走した」とはしていません。
関連テストの実行ログは `docs/wayfinder-checks/` にあります。

## 実装
- `src/shared/foundation/wayfinding/breadcrumbs.ts` / `.css`
- `src/shared/foundation/wayfinding/number.ts` / `.css`
パーツ個別のReact/Vanilla入口から上記を参照し、コード表示・コピー・パーツZIPも同じ元ソースを使用します。
