/* Generated from src/parts. */
window.SOP_CATALOG = [{"id":"chrome","name":"Chrome","category":"toggles","order":1,"version":"2.3.0","tagline":"硬質な、余韻。","description":"削り出しの金属と、吸い付くスナップ。","material":"MACHINED METAL","motion":"SNAP","accent":"209,230,178","initial":true,"componentName":"ChromeToggle","tags":["Chrome","chrome","トグル","MACHINED METAL","削り出しの金属と、吸い付くスナップ。"],"config":{"id":"chrome","name":"Chrome","initial":true,"stiffness":380,"damping":25,"tone":220,"travel":128},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Chrome / 2.3.0\n\n削り出しの金属と、吸い付くスナップ。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`ChromeToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/chrome/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/chrome/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/chrome/react/ChromeToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/chrome/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/chrome/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/chrome/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Chrome — 再現仕様\n\n削り出しの金属と、吸い付くスナップ。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 128px、ばねの剛性 380、減衰 25。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-chrome` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 260px;\nheight: 110px;\nleft: 0;\ntop: 4px;\nwidth: 260px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #717677, #3d4243, #141717, #282c2c, #505556, #191d1d, #0009, #070909, #c1c5bc55, #c1c5bc38, #080b0b, #030606, #111817, #151e1c, #000, #777e7666, #030505, #c8f09328, #3c493b, #bdd6a5, #c7ffa560, #d8dbd6, #616765, #202725, #8c948e, #000a, #e4e7dd, #fff7, #343b38, #282e2d, #c2c9c0, #3a4240, #e3e9dd, #59615e, #232b29, #b0b9b1, #eef1e6, #38403d\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/chrome/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/chrome/react/ChromeToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"chrome\",\n    \"name\": \"Chrome\",\n    \"initial\": true,\n    \"stiffness\": 380,\n    \"damping\": 25,\n    \"tone\": 220,\n    \"travel\": 128\n};\n/** 削り出しの金属と、吸い付くスナップ。 */\nexport default function ChromeToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Chrome トグル'} aria-checked={checked} className={`sop-toggle sop-chrome ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"chrome-body\">\u003ci className=\"screw s1\">\u003c/i>\u003ci className=\"screw s2\">\u003c/i>\u003ci className=\"screw s3\">\u003c/i>\u003ci className=\"screw s4\">\u003c/i>\u003cspan className=\"chrome-slot\">\u003cspan className=\"chrome-power\">I\u003c/span>\u003cspan className=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan className=\"chrome-led\">\u003c/span>\u003cspan className=\"chrome-knob\">\u003cspan className=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan className=\"chrome-ticks\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/chrome/styles.css","code":"/* chrome: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-chrome {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-chrome[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-chrome:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-chrome:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-chrome {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-chrome:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-chrome:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-chrome .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-chrome .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-chrome .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-chrome .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-chrome .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-chrome .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-chrome .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-chrome,\n  .sop-toggle.sop-chrome * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-chrome {\n  width: 260px;\n  height: 110px;\n}\n\n.sop-toggle.sop-chrome .chrome-body {\n  position: absolute;\n  left: 0;\n  top: 4px;\n  width: 260px;\n  height: 96px;\n  border-radius: 48px;\n  background: linear-gradient(175deg,#717677 0%,#3d4243 3%,#141717 14%,#282c2c 74%,#505556 94%,#191d1d 98%);\n  box-shadow: 0 15px 24px #0009,0 3px 0 #070909,inset 0 1px 1px #c1c5bc55,inset 0 -1px 1px #c1c5bc38;\n  border: 1px solid #080b0b;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s3 {\n  left: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s2,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  right: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s2 {\n  top: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s3,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  bottom: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-slot {\n  position: absolute;\n  left: 29px;\n  right: 29px;\n  top: 12px;\n  height: 70px;\n  border-radius: 40px;\n  background: linear-gradient(#030606,#111817 50%,#151e1c);\n  box-shadow: inset 0 5px 8px #000,inset 0 0 3px #000,0 1px 1px #777e7666;\n  border-top: 1px solid #030505;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-chrome .chrome-slot:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(ellipse at 25% 80%,#c8f09328,transparent 70%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-chrome .chrome-power,\n.sop-toggle.sop-chrome .chrome-zero {\n  position: absolute;\n  top: 26px;\n  font: 13px Arial;\n  color: #3c493b;\n  text-shadow: 0 -1px 0 #000;\n}\n\n.sop-toggle.sop-chrome .chrome-power {\n  left: 27px;\n  color: #bdd6a5;\n  opacity: calc(.2 + var(--p)*.8);\n  text-shadow: 0 0 9px #c7ffa560;\n}\n\n.sop-toggle.sop-chrome .chrome-zero {\n  right: 27px;\n}\n\n.sop-toggle.sop-chrome .chrome-knob {\n  position: absolute;\n  width: 74px;\n  height: 74px;\n  left: 28px;\n  top: 8px;\n  transform: translateX(calc(var(--p)*128px)) rotate(calc(var(--p)*100deg));\n  border-radius: 50%;\n  background: repeating-conic-gradient(from 5deg,#d8dbd6 0deg,#616765 2deg,#202725 4deg,#8c948e 6deg);\n  box-shadow: 2px 7px 7px #000a,0 1px 2px #e4e7dd,inset 0 0 0 1px #fff7,inset 0 0 0 3px #343b38;\n}\n\n.sop-toggle.sop-chrome .chrome-center {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  background: conic-gradient(from -50deg,#282e2d,#c2c9c0 12%,#3a4240 24%,#e3e9dd 41%,#59615e 51%,#232b29 66%,#b0b9b1 80%,#eef1e6 86%,#38403d);\n  border: 1px solid #c3cbc170;\n  box-shadow: inset 0 0 0 3px #e4e9df0c;\n}\n\n.sop-toggle.sop-chrome .chrome-center:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #a9b4a83a;\n  border-radius: 50%;\n  background: repeating-radial-gradient(circle,#ddddcc0d 0 1px,transparent 1px 2px);\n}\n\n.sop-toggle.sop-chrome .chrome-center i {\n  position: absolute;\n  inset: 24px;\n  border-radius: 50%;\n  border: 1px solid #c1c8b840;\n  box-shadow: 0 0 0 2px #303c321f;\n  background: radial-gradient(circle at 35% 25%,#d7ddcf,#747d74 55%,#37413a);\n}\n\n.sop-toggle.sop-chrome .chrome-led {\n  position: absolute;\n  bottom: 5px;\n  left: 112px;\n  width: 32px;\n  height: 2px;\n  background: #b9da8c;\n  opacity: calc(.13 + var(--p)*.8);\n  box-shadow: 0 0 9px #c8ed98aa;\n}\n\n.sop-toggle.sop-chrome .chrome-ticks {\n  position: absolute;\n  left: 87px;\n  bottom: -10px;\n  width: 88px;\n  height: 3px;\n  background: repeating-linear-gradient(90deg,#454e42 0 1px,transparent 1px 8px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/chrome/react/Example.tsx","code":"import React, { useState } from 'react';\nimport ChromeToggle from './ChromeToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cChromeToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/chrome/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/chrome/react/ChromeToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"chrome\",\n    \"name\": \"Chrome\",\n    \"initial\": true,\n    \"stiffness\": 380,\n    \"damping\": 25,\n    \"tone\": 220,\n    \"travel\": 128\n};\n/** 削り出しの金属と、吸い付くスナップ。 */\nexport default function ChromeToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Chrome トグル'} aria-checked={checked} className={`sop-toggle sop-chrome ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"chrome-body\">\u003ci className=\"screw s1\">\u003c/i>\u003ci className=\"screw s2\">\u003c/i>\u003ci className=\"screw s3\">\u003c/i>\u003ci className=\"screw s4\">\u003c/i>\u003cspan className=\"chrome-slot\">\u003cspan className=\"chrome-power\">I\u003c/span>\u003cspan className=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan className=\"chrome-led\">\u003c/span>\u003cspan className=\"chrome-knob\">\u003cspan className=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan className=\"chrome-ticks\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/chrome/styles.css","code":"/* chrome: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-chrome {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-chrome[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-chrome:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-chrome:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-chrome {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-chrome:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-chrome:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-chrome .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-chrome .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-chrome .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-chrome .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-chrome .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-chrome .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-chrome .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-chrome,\n  .sop-toggle.sop-chrome * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-chrome {\n  width: 260px;\n  height: 110px;\n}\n\n.sop-toggle.sop-chrome .chrome-body {\n  position: absolute;\n  left: 0;\n  top: 4px;\n  width: 260px;\n  height: 96px;\n  border-radius: 48px;\n  background: linear-gradient(175deg,#717677 0%,#3d4243 3%,#141717 14%,#282c2c 74%,#505556 94%,#191d1d 98%);\n  box-shadow: 0 15px 24px #0009,0 3px 0 #070909,inset 0 1px 1px #c1c5bc55,inset 0 -1px 1px #c1c5bc38;\n  border: 1px solid #080b0b;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s3 {\n  left: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s2,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  right: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s2 {\n  top: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s3,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  bottom: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-slot {\n  position: absolute;\n  left: 29px;\n  right: 29px;\n  top: 12px;\n  height: 70px;\n  border-radius: 40px;\n  background: linear-gradient(#030606,#111817 50%,#151e1c);\n  box-shadow: inset 0 5px 8px #000,inset 0 0 3px #000,0 1px 1px #777e7666;\n  border-top: 1px solid #030505;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-chrome .chrome-slot:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(ellipse at 25% 80%,#c8f09328,transparent 70%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-chrome .chrome-power,\n.sop-toggle.sop-chrome .chrome-zero {\n  position: absolute;\n  top: 26px;\n  font: 13px Arial;\n  color: #3c493b;\n  text-shadow: 0 -1px 0 #000;\n}\n\n.sop-toggle.sop-chrome .chrome-power {\n  left: 27px;\n  color: #bdd6a5;\n  opacity: calc(.2 + var(--p)*.8);\n  text-shadow: 0 0 9px #c7ffa560;\n}\n\n.sop-toggle.sop-chrome .chrome-zero {\n  right: 27px;\n}\n\n.sop-toggle.sop-chrome .chrome-knob {\n  position: absolute;\n  width: 74px;\n  height: 74px;\n  left: 28px;\n  top: 8px;\n  transform: translateX(calc(var(--p)*128px)) rotate(calc(var(--p)*100deg));\n  border-radius: 50%;\n  background: repeating-conic-gradient(from 5deg,#d8dbd6 0deg,#616765 2deg,#202725 4deg,#8c948e 6deg);\n  box-shadow: 2px 7px 7px #000a,0 1px 2px #e4e7dd,inset 0 0 0 1px #fff7,inset 0 0 0 3px #343b38;\n}\n\n.sop-toggle.sop-chrome .chrome-center {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  background: conic-gradient(from -50deg,#282e2d,#c2c9c0 12%,#3a4240 24%,#e3e9dd 41%,#59615e 51%,#232b29 66%,#b0b9b1 80%,#eef1e6 86%,#38403d);\n  border: 1px solid #c3cbc170;\n  box-shadow: inset 0 0 0 3px #e4e9df0c;\n}\n\n.sop-toggle.sop-chrome .chrome-center:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #a9b4a83a;\n  border-radius: 50%;\n  background: repeating-radial-gradient(circle,#ddddcc0d 0 1px,transparent 1px 2px);\n}\n\n.sop-toggle.sop-chrome .chrome-center i {\n  position: absolute;\n  inset: 24px;\n  border-radius: 50%;\n  border: 1px solid #c1c8b840;\n  box-shadow: 0 0 0 2px #303c321f;\n  background: radial-gradient(circle at 35% 25%,#d7ddcf,#747d74 55%,#37413a);\n}\n\n.sop-toggle.sop-chrome .chrome-led {\n  position: absolute;\n  bottom: 5px;\n  left: 112px;\n  width: 32px;\n  height: 2px;\n  background: #b9da8c;\n  opacity: calc(.13 + var(--p)*.8);\n  box-shadow: 0 0 9px #c8ed98aa;\n}\n\n.sop-toggle.sop-chrome .chrome-ticks {\n  position: absolute;\n  left: 87px;\n  bottom: -10px;\n  width: 88px;\n  height: 3px;\n  background: repeating-linear-gradient(90deg,#454e42 0 1px,transparent 1px 8px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/chrome/react/Example.jsx","code":"import React, { useState } from 'react';\nimport ChromeToggle from './ChromeToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cChromeToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/chrome/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/chrome/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"chrome\",\n    \"name\": \"Chrome\",\n    \"initial\": true,\n    \"stiffness\": 380,\n    \"damping\": 25,\n    \"tone\": 220,\n    \"travel\": 128\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for ChromeToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/chrome/styles.css","code":"/* chrome: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-chrome {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-chrome[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-chrome:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-chrome:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-chrome {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-chrome:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-chrome:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-chrome .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-chrome .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-chrome .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-chrome .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-chrome .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-chrome .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-chrome .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-chrome,\n  .sop-toggle.sop-chrome * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-chrome {\n  width: 260px;\n  height: 110px;\n}\n\n.sop-toggle.sop-chrome .chrome-body {\n  position: absolute;\n  left: 0;\n  top: 4px;\n  width: 260px;\n  height: 96px;\n  border-radius: 48px;\n  background: linear-gradient(175deg,#717677 0%,#3d4243 3%,#141717 14%,#282c2c 74%,#505556 94%,#191d1d 98%);\n  box-shadow: 0 15px 24px #0009,0 3px 0 #070909,inset 0 1px 1px #c1c5bc55,inset 0 -1px 1px #c1c5bc38;\n  border: 1px solid #080b0b;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s3 {\n  left: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s2,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  right: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s2 {\n  top: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s3,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  bottom: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-slot {\n  position: absolute;\n  left: 29px;\n  right: 29px;\n  top: 12px;\n  height: 70px;\n  border-radius: 40px;\n  background: linear-gradient(#030606,#111817 50%,#151e1c);\n  box-shadow: inset 0 5px 8px #000,inset 0 0 3px #000,0 1px 1px #777e7666;\n  border-top: 1px solid #030505;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-chrome .chrome-slot:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(ellipse at 25% 80%,#c8f09328,transparent 70%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-chrome .chrome-power,\n.sop-toggle.sop-chrome .chrome-zero {\n  position: absolute;\n  top: 26px;\n  font: 13px Arial;\n  color: #3c493b;\n  text-shadow: 0 -1px 0 #000;\n}\n\n.sop-toggle.sop-chrome .chrome-power {\n  left: 27px;\n  color: #bdd6a5;\n  opacity: calc(.2 + var(--p)*.8);\n  text-shadow: 0 0 9px #c7ffa560;\n}\n\n.sop-toggle.sop-chrome .chrome-zero {\n  right: 27px;\n}\n\n.sop-toggle.sop-chrome .chrome-knob {\n  position: absolute;\n  width: 74px;\n  height: 74px;\n  left: 28px;\n  top: 8px;\n  transform: translateX(calc(var(--p)*128px)) rotate(calc(var(--p)*100deg));\n  border-radius: 50%;\n  background: repeating-conic-gradient(from 5deg,#d8dbd6 0deg,#616765 2deg,#202725 4deg,#8c948e 6deg);\n  box-shadow: 2px 7px 7px #000a,0 1px 2px #e4e7dd,inset 0 0 0 1px #fff7,inset 0 0 0 3px #343b38;\n}\n\n.sop-toggle.sop-chrome .chrome-center {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  background: conic-gradient(from -50deg,#282e2d,#c2c9c0 12%,#3a4240 24%,#e3e9dd 41%,#59615e 51%,#232b29 66%,#b0b9b1 80%,#eef1e6 86%,#38403d);\n  border: 1px solid #c3cbc170;\n  box-shadow: inset 0 0 0 3px #e4e9df0c;\n}\n\n.sop-toggle.sop-chrome .chrome-center:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #a9b4a83a;\n  border-radius: 50%;\n  background: repeating-radial-gradient(circle,#ddddcc0d 0 1px,transparent 1px 2px);\n}\n\n.sop-toggle.sop-chrome .chrome-center i {\n  position: absolute;\n  inset: 24px;\n  border-radius: 50%;\n  border: 1px solid #c1c8b840;\n  box-shadow: 0 0 0 2px #303c321f;\n  background: radial-gradient(circle at 35% 25%,#d7ddcf,#747d74 55%,#37413a);\n}\n\n.sop-toggle.sop-chrome .chrome-led {\n  position: absolute;\n  bottom: 5px;\n  left: 112px;\n  width: 32px;\n  height: 2px;\n  background: #b9da8c;\n  opacity: calc(.13 + var(--p)*.8);\n  box-shadow: 0 0 9px #c8ed98aa;\n}\n\n.sop-toggle.sop-chrome .chrome-ticks {\n  position: absolute;\n  left: 87px;\n  bottom: -10px;\n  width: 88px;\n  height: 3px;\n  background: repeating-linear-gradient(90deg,#454e42 0 1px,transparent 1px 8px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/chrome/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-chrome');\nif (!element) throw new Error('The Chrome root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/chrome/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/chrome/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Chrome\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/chrome/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"chrome\",\n    \"name\": \"Chrome\",\n    \"initial\": true,\n    \"stiffness\": 380,\n    \"damping\": 25,\n    \"tone\": 220,\n    \"travel\": 128\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for ChromeToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/chrome/styles.css","code":"/* chrome: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-chrome {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-chrome[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-chrome:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-chrome:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-chrome {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-chrome:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-chrome:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-chrome .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-chrome .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-chrome .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-chrome .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-chrome .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-chrome .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-chrome .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-chrome,\n  .sop-toggle.sop-chrome * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-chrome {\n  width: 260px;\n  height: 110px;\n}\n\n.sop-toggle.sop-chrome .chrome-body {\n  position: absolute;\n  left: 0;\n  top: 4px;\n  width: 260px;\n  height: 96px;\n  border-radius: 48px;\n  background: linear-gradient(175deg,#717677 0%,#3d4243 3%,#141717 14%,#282c2c 74%,#505556 94%,#191d1d 98%);\n  box-shadow: 0 15px 24px #0009,0 3px 0 #070909,inset 0 1px 1px #c1c5bc55,inset 0 -1px 1px #c1c5bc38;\n  border: 1px solid #080b0b;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s3 {\n  left: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s2,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  right: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s2 {\n  top: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s3,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  bottom: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-slot {\n  position: absolute;\n  left: 29px;\n  right: 29px;\n  top: 12px;\n  height: 70px;\n  border-radius: 40px;\n  background: linear-gradient(#030606,#111817 50%,#151e1c);\n  box-shadow: inset 0 5px 8px #000,inset 0 0 3px #000,0 1px 1px #777e7666;\n  border-top: 1px solid #030505;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-chrome .chrome-slot:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(ellipse at 25% 80%,#c8f09328,transparent 70%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-chrome .chrome-power,\n.sop-toggle.sop-chrome .chrome-zero {\n  position: absolute;\n  top: 26px;\n  font: 13px Arial;\n  color: #3c493b;\n  text-shadow: 0 -1px 0 #000;\n}\n\n.sop-toggle.sop-chrome .chrome-power {\n  left: 27px;\n  color: #bdd6a5;\n  opacity: calc(.2 + var(--p)*.8);\n  text-shadow: 0 0 9px #c7ffa560;\n}\n\n.sop-toggle.sop-chrome .chrome-zero {\n  right: 27px;\n}\n\n.sop-toggle.sop-chrome .chrome-knob {\n  position: absolute;\n  width: 74px;\n  height: 74px;\n  left: 28px;\n  top: 8px;\n  transform: translateX(calc(var(--p)*128px)) rotate(calc(var(--p)*100deg));\n  border-radius: 50%;\n  background: repeating-conic-gradient(from 5deg,#d8dbd6 0deg,#616765 2deg,#202725 4deg,#8c948e 6deg);\n  box-shadow: 2px 7px 7px #000a,0 1px 2px #e4e7dd,inset 0 0 0 1px #fff7,inset 0 0 0 3px #343b38;\n}\n\n.sop-toggle.sop-chrome .chrome-center {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  background: conic-gradient(from -50deg,#282e2d,#c2c9c0 12%,#3a4240 24%,#e3e9dd 41%,#59615e 51%,#232b29 66%,#b0b9b1 80%,#eef1e6 86%,#38403d);\n  border: 1px solid #c3cbc170;\n  box-shadow: inset 0 0 0 3px #e4e9df0c;\n}\n\n.sop-toggle.sop-chrome .chrome-center:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #a9b4a83a;\n  border-radius: 50%;\n  background: repeating-radial-gradient(circle,#ddddcc0d 0 1px,transparent 1px 2px);\n}\n\n.sop-toggle.sop-chrome .chrome-center i {\n  position: absolute;\n  inset: 24px;\n  border-radius: 50%;\n  border: 1px solid #c1c8b840;\n  box-shadow: 0 0 0 2px #303c321f;\n  background: radial-gradient(circle at 35% 25%,#d7ddcf,#747d74 55%,#37413a);\n}\n\n.sop-toggle.sop-chrome .chrome-led {\n  position: absolute;\n  bottom: 5px;\n  left: 112px;\n  width: 32px;\n  height: 2px;\n  background: #b9da8c;\n  opacity: calc(.13 + var(--p)*.8);\n  box-shadow: 0 0 9px #c8ed98aa;\n}\n\n.sop-toggle.sop-chrome .chrome-ticks {\n  position: absolute;\n  left: 87px;\n  bottom: -10px;\n  width: 88px;\n  height: 3px;\n  background: repeating-linear-gradient(90deg,#454e42 0 1px,transparent 1px 8px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/chrome/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-chrome');\nif (!element)\n    throw new Error('The Chrome root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/chrome/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/chrome/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Chrome\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Chrome — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Chrome · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"true\" aria-label=\"Chrome トグル\" class=\"sop-toggle sop-chrome\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"chrome-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"chrome-slot\">\u003cspan class=\"chrome-power\">I\u003c/span>\u003cspan class=\"chrome-zero\">O\u003c/span>\u003c/span>\u003cspan class=\"chrome-led\">\u003c/span>\u003cspan class=\"chrome-knob\">\u003cspan class=\"chrome-center\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"chrome-ticks\">\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* chrome: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-chrome {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-chrome[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-chrome:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-chrome:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-chrome {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-chrome:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-chrome:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-chrome .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-chrome .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-chrome .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-chrome .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-chrome .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-chrome .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-chrome .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-chrome .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-chrome,\n  .sop-toggle.sop-chrome * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-chrome {\n  width: 260px;\n  height: 110px;\n}\n\n.sop-toggle.sop-chrome .chrome-body {\n  position: absolute;\n  left: 0;\n  top: 4px;\n  width: 260px;\n  height: 96px;\n  border-radius: 48px;\n  background: linear-gradient(175deg,#717677 0%,#3d4243 3%,#141717 14%,#282c2c 74%,#505556 94%,#191d1d 98%);\n  box-shadow: 0 15px 24px #0009,0 3px 0 #070909,inset 0 1px 1px #c1c5bc55,inset 0 -1px 1px #c1c5bc38;\n  border: 1px solid #080b0b;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s3 {\n  left: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s2,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  right: 18px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s1,\n.sop-toggle.sop-chrome .chrome-body .s2 {\n  top: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-body .s3,\n.sop-toggle.sop-chrome .chrome-body .s4 {\n  bottom: 19px;\n}\n\n.sop-toggle.sop-chrome .chrome-slot {\n  position: absolute;\n  left: 29px;\n  right: 29px;\n  top: 12px;\n  height: 70px;\n  border-radius: 40px;\n  background: linear-gradient(#030606,#111817 50%,#151e1c);\n  box-shadow: inset 0 5px 8px #000,inset 0 0 3px #000,0 1px 1px #777e7666;\n  border-top: 1px solid #030505;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-chrome .chrome-slot:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(ellipse at 25% 80%,#c8f09328,transparent 70%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-chrome .chrome-power,\n.sop-toggle.sop-chrome .chrome-zero {\n  position: absolute;\n  top: 26px;\n  font: 13px Arial;\n  color: #3c493b;\n  text-shadow: 0 -1px 0 #000;\n}\n\n.sop-toggle.sop-chrome .chrome-power {\n  left: 27px;\n  color: #bdd6a5;\n  opacity: calc(.2 + var(--p)*.8);\n  text-shadow: 0 0 9px #c7ffa560;\n}\n\n.sop-toggle.sop-chrome .chrome-zero {\n  right: 27px;\n}\n\n.sop-toggle.sop-chrome .chrome-knob {\n  position: absolute;\n  width: 74px;\n  height: 74px;\n  left: 28px;\n  top: 8px;\n  transform: translateX(calc(var(--p)*128px)) rotate(calc(var(--p)*100deg));\n  border-radius: 50%;\n  background: repeating-conic-gradient(from 5deg,#d8dbd6 0deg,#616765 2deg,#202725 4deg,#8c948e 6deg);\n  box-shadow: 2px 7px 7px #000a,0 1px 2px #e4e7dd,inset 0 0 0 1px #fff7,inset 0 0 0 3px #343b38;\n}\n\n.sop-toggle.sop-chrome .chrome-center {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  background: conic-gradient(from -50deg,#282e2d,#c2c9c0 12%,#3a4240 24%,#e3e9dd 41%,#59615e 51%,#232b29 66%,#b0b9b1 80%,#eef1e6 86%,#38403d);\n  border: 1px solid #c3cbc170;\n  box-shadow: inset 0 0 0 3px #e4e9df0c;\n}\n\n.sop-toggle.sop-chrome .chrome-center:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #a9b4a83a;\n  border-radius: 50%;\n  background: repeating-radial-gradient(circle,#ddddcc0d 0 1px,transparent 1px 2px);\n}\n\n.sop-toggle.sop-chrome .chrome-center i {\n  position: absolute;\n  inset: 24px;\n  border-radius: 50%;\n  border: 1px solid #c1c8b840;\n  box-shadow: 0 0 0 2px #303c321f;\n  background: radial-gradient(circle at 35% 25%,#d7ddcf,#747d74 55%,#37413a);\n}\n\n.sop-toggle.sop-chrome .chrome-led {\n  position: absolute;\n  bottom: 5px;\n  left: 112px;\n  width: 32px;\n  height: 2px;\n  background: #b9da8c;\n  opacity: calc(.13 + var(--p)*.8);\n  box-shadow: 0 0 9px #c8ed98aa;\n}\n\n.sop-toggle.sop-chrome .chrome-ticks {\n  position: absolute;\n  left: 87px;\n  bottom: -10px;\n  width: 88px;\n  height: 3px;\n  background: repeating-linear-gradient(90deg,#454e42 0 1px,transparent 1px 8px);\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/chrome/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/chrome/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/chrome/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"chrome\",\n    \"name\": \"Chrome\",\n    \"initial\": true,\n    \"stiffness\": 380,\n    \"damping\": 25,\n    \"tone\": 220,\n    \"travel\": 128\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for ChromeToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/chrome/demo/main.ts\");})();\n"}},{"id":"liquid","name":"Liquid","category":"toggles","order":2,"version":"2.3.0","tagline":"光を、ひとしずく。","description":"伸びて、揺れて、透明なかたちに戻る。","material":"LIQUID GLASS","motion":"FLOW","accent":"133,210,231","initial":true,"componentName":"LiquidToggle","tags":["Liquid","liquid","トグル","LIQUID GLASS","伸びて、揺れて、透明なかたちに戻る。"],"config":{"id":"liquid","name":"Liquid","initial":true,"stiffness":195,"damping":15,"tone":540,"travel":153},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Liquid / 2.3.0\n\n伸びて、揺れて、透明なかたちに戻る。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`LiquidToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/liquid/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/liquid/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/liquid/react/LiquidToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/liquid/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/liquid/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/liquid/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Liquid — 再現仕様\n\n伸びて、揺れて、透明なかたちに戻る。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 153px、ばねの剛性 195、減衰 15。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-liquid` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 258px;\nheight: 108px;\nleft: 10px;\ntop: 76px;\nheight: 34px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #0009, #b4f3ee30, #1b455126, #13232a, #28404a, #8cbae02b, #aecbda51, #c4edf767, #adcdff55, #000, #aae7d436, #b2e2ff29, #8bdebd88, #5fc6e899, #d7b9eb66, #b5ebf094, #418eca3b, #b2f5dd5e, #c4efed, #bce6f012, #87d8e30b, #d4fbffab, #eefffa80, #54bbd733, #9db4ce55, #042c3b05, #b8ffda66, #ecfff7cc, #a9dff7a6, #afeeff75, #020e2080, #99fcf130, #b7eee450, #dcfcff50, #e3fff5c4, #b7f9fa00, #e6fffaa1, #b9d3ff00\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/liquid/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/liquid/react/LiquidToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"liquid\",\n    \"name\": \"Liquid\",\n    \"initial\": true,\n    \"stiffness\": 195,\n    \"damping\": 15,\n    \"tone\": 540,\n    \"travel\": 153\n};\n/** 伸びて、揺れて、透明なかたちに戻る。 */\nexport default function LiquidToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Liquid トグル'} aria-checked={checked} className={`sop-toggle sop-liquid ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"liquid-shadow\">\u003c/span>\u003cspan className=\"liquid-track\">\u003cspan className=\"liquid-grid\">\u003c/span>\u003cspan className=\"liquid-stream\">\u003c/span>\u003cspan className=\"liquid-mark\">FLOW\u003c/span>\u003cspan className=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan className=\"liquid-bridge\">\u003c/span>\u003cspan className=\"liquid-lens\">\u003cspan className=\"lens-inner\">\u003c/span>\u003cspan className=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan className=\"liquid-droplet drop-one\">\u003c/span>\u003cspan className=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/liquid/styles.css","code":"/* liquid: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-liquid {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-liquid[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-liquid:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-liquid:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-liquid {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-liquid:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-liquid:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-liquid .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-liquid .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-liquid .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-liquid .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-liquid .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-liquid .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-liquid .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-liquid,\n  .sop-toggle.sop-liquid * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-liquid {\n  width: 258px;\n  height: 108px;\n}\n\n.sop-toggle.sop-liquid .liquid-shadow {\n  position: absolute;\n  left: 10px;\n  right: 10px;\n  top: 76px;\n  height: 34px;\n  border-radius: 50%;\n  background: #0009;\n  filter: blur(13px);\n}\n\n.sop-toggle.sop-liquid .liquid-track {\n  position: absolute;\n  left: 0;\n  top: 9px;\n  width: 258px;\n  height: 91px;\n  border-radius: 50px;\n  background: linear-gradient(152deg,#b4f3ee30 0,#1b455126 22%,#13232a 45%,#28404a 80%,#8cbae02b);\n  border: 1px solid #aecbda51;\n  box-shadow: inset 0 1px 1px #c4edf767,inset 0 -1px 2px #adcdff55,0 1px 2px #000;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .liquid-grid {\n  position: absolute;\n  inset: 0;\n  opacity: .4;\n  background-image: linear-gradient(90deg,#aae7d436 1px,transparent 1px),linear-gradient(#b2e2ff29 1px,transparent 1px);\n  background-size: 16px 16px;\n  transform: perspective(200px) rotateY(-14deg) rotateZ(-18deg) scale(1.6);\n  transition: opacity .4s;\n}\n\n.sop-toggle.sop-liquid .liquid-stream {\n  position: absolute;\n  inset: -60px -20px;\n  background: conic-gradient(from 20deg at 32% 44%,transparent,#8bdebd88 7%,#5fc6e899 14%,transparent 27%,#d7b9eb66 40%,#b5ebf094 45%,transparent 53%,#418eca3b 77%,#b2f5dd5e);\n  filter: blur(6px);\n  transform: translateX(calc(var(--p)*24px)) rotate(calc(var(--p)*42deg));\n  opacity: calc(.3 + var(--p)*.65);\n}\n\n.sop-toggle.sop-liquid .liquid-mark {\n  position: absolute;\n  left: 40px;\n  top: 38px;\n  font: 9px var(--sop-mono);\n  letter-spacing: 4px;\n  color: #c4efed;\n  opacity: calc(.15 + var(--p)*.5);\n}\n\n.sop-toggle.sop-liquid .liquid-waterline {\n  position: absolute;\n  inset: 4px;\n  border: 1px solid #bce6f012;\n  border-radius: 50px;\n  box-shadow: inset 0 -12px 14px #87d8e30b;\n}\n\n.sop-toggle.sop-liquid .liquid-lens {\n  position: absolute;\n  left: 9px;\n  top: 12px;\n  width: 85px;\n  height: 85px;\n  transform: translateX(calc(var(--p)*153px)) scaleX(calc(1 + var(--energy)*.22)) scaleY(calc(1 - var(--energy)*.09));\n  border: 1px solid #d4fbffab;\n  border-radius: 50%;\n  background: radial-gradient(ellipse at 25% 13%,#eefffa80,transparent 28%),radial-gradient(ellipse at 60% 75%,#54bbd733,transparent 56%),linear-gradient(130deg,#9db4ce55,#042c3b05 40%,#b8ffda66);\n  backdrop-filter: blur(1.7px) saturate(1.9);\n  -webkit-backdrop-filter: blur(1.7px) saturate(1.9);\n  box-shadow: inset 2px 2px 4px #ecfff7cc,inset -2px -3px 4px #a9dff7a6,inset 0 0 10px #afeeff75,2px 7px 12px #020e2080,-3px 0 15px #99fcf130;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .lens-inner {\n  position: absolute;\n  inset: 6px;\n  border-radius: 50%;\n  border: 1px solid #b7eee450;\n  box-shadow: inset 0 0 5px #dcfcff50;\n  transform: rotate(-30deg);\n}\n\n.sop-toggle.sop-liquid .lens-inner:before {\n  content: \"\";\n  position: absolute;\n  left: 9px;\n  right: 9px;\n  top: 3px;\n  height: 28px;\n  border-top: 2px solid #e3fff5c4;\n  border-radius: 50%;\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-liquid .lens-glint {\n  position: absolute;\n  right: 4px;\n  top: 29px;\n  width: 14px;\n  height: 43px;\n  border-radius: 50%;\n  transform: rotate(30deg);\n  background: linear-gradient(#b7f9fa00,#e6fffaa1,#b9d3ff00);\n  filter: blur(1px);\n}\n\n.sop-toggle.sop-liquid .liquid-bridge {\n  position: absolute;\n  top: 32px;\n  left: 46px;\n  width: 100px;\n  height: 45px;\n  border-radius: 50%;\n  background: linear-gradient(#c0fbf855,#64c3e260,#d0e8ee77);\n  border: 1px solid #b4f8fd70;\n  filter: blur(3px);\n  transform: translateX(calc(var(--p)*123px - var(--v)*4px)) scaleX(calc(var(--energy)*1.6));\n  transform-origin: 0 50%;\n  opacity: var(--energy);\n}\n\n.sop-toggle.sop-liquid .liquid-droplet {\n  position: absolute;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 20%,#e8fffb,#83e5e04d 24%,#91d9fb20 61%,#bbeffa);\n  box-shadow: inset 0 0 2px #dafbfbb3,0 3px 8px #0003;\n  opacity: calc(var(--energy)*.85);\n}\n\n.sop-toggle.sop-liquid .drop-one {\n  width: 12px;\n  height: 12px;\n  left: calc(32px + var(--p)*164px);\n  top: 3px;\n  transform: translate(calc(var(--v)*-9px),calc(var(--energy)*-10px));\n}\n\n.sop-toggle.sop-liquid .drop-two {\n  width: 7px;\n  height: 7px;\n  left: calc(43px + var(--p)*135px);\n  top: 98px;\n  transform: translate(calc(var(--v)*-13px),calc(var(--energy)*8px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/liquid/react/Example.tsx","code":"import React, { useState } from 'react';\nimport LiquidToggle from './LiquidToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cLiquidToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/liquid/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/liquid/react/LiquidToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"liquid\",\n    \"name\": \"Liquid\",\n    \"initial\": true,\n    \"stiffness\": 195,\n    \"damping\": 15,\n    \"tone\": 540,\n    \"travel\": 153\n};\n/** 伸びて、揺れて、透明なかたちに戻る。 */\nexport default function LiquidToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Liquid トグル'} aria-checked={checked} className={`sop-toggle sop-liquid ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"liquid-shadow\">\u003c/span>\u003cspan className=\"liquid-track\">\u003cspan className=\"liquid-grid\">\u003c/span>\u003cspan className=\"liquid-stream\">\u003c/span>\u003cspan className=\"liquid-mark\">FLOW\u003c/span>\u003cspan className=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan className=\"liquid-bridge\">\u003c/span>\u003cspan className=\"liquid-lens\">\u003cspan className=\"lens-inner\">\u003c/span>\u003cspan className=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan className=\"liquid-droplet drop-one\">\u003c/span>\u003cspan className=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/liquid/styles.css","code":"/* liquid: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-liquid {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-liquid[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-liquid:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-liquid:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-liquid {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-liquid:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-liquid:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-liquid .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-liquid .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-liquid .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-liquid .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-liquid .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-liquid .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-liquid .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-liquid,\n  .sop-toggle.sop-liquid * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-liquid {\n  width: 258px;\n  height: 108px;\n}\n\n.sop-toggle.sop-liquid .liquid-shadow {\n  position: absolute;\n  left: 10px;\n  right: 10px;\n  top: 76px;\n  height: 34px;\n  border-radius: 50%;\n  background: #0009;\n  filter: blur(13px);\n}\n\n.sop-toggle.sop-liquid .liquid-track {\n  position: absolute;\n  left: 0;\n  top: 9px;\n  width: 258px;\n  height: 91px;\n  border-radius: 50px;\n  background: linear-gradient(152deg,#b4f3ee30 0,#1b455126 22%,#13232a 45%,#28404a 80%,#8cbae02b);\n  border: 1px solid #aecbda51;\n  box-shadow: inset 0 1px 1px #c4edf767,inset 0 -1px 2px #adcdff55,0 1px 2px #000;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .liquid-grid {\n  position: absolute;\n  inset: 0;\n  opacity: .4;\n  background-image: linear-gradient(90deg,#aae7d436 1px,transparent 1px),linear-gradient(#b2e2ff29 1px,transparent 1px);\n  background-size: 16px 16px;\n  transform: perspective(200px) rotateY(-14deg) rotateZ(-18deg) scale(1.6);\n  transition: opacity .4s;\n}\n\n.sop-toggle.sop-liquid .liquid-stream {\n  position: absolute;\n  inset: -60px -20px;\n  background: conic-gradient(from 20deg at 32% 44%,transparent,#8bdebd88 7%,#5fc6e899 14%,transparent 27%,#d7b9eb66 40%,#b5ebf094 45%,transparent 53%,#418eca3b 77%,#b2f5dd5e);\n  filter: blur(6px);\n  transform: translateX(calc(var(--p)*24px)) rotate(calc(var(--p)*42deg));\n  opacity: calc(.3 + var(--p)*.65);\n}\n\n.sop-toggle.sop-liquid .liquid-mark {\n  position: absolute;\n  left: 40px;\n  top: 38px;\n  font: 9px var(--sop-mono);\n  letter-spacing: 4px;\n  color: #c4efed;\n  opacity: calc(.15 + var(--p)*.5);\n}\n\n.sop-toggle.sop-liquid .liquid-waterline {\n  position: absolute;\n  inset: 4px;\n  border: 1px solid #bce6f012;\n  border-radius: 50px;\n  box-shadow: inset 0 -12px 14px #87d8e30b;\n}\n\n.sop-toggle.sop-liquid .liquid-lens {\n  position: absolute;\n  left: 9px;\n  top: 12px;\n  width: 85px;\n  height: 85px;\n  transform: translateX(calc(var(--p)*153px)) scaleX(calc(1 + var(--energy)*.22)) scaleY(calc(1 - var(--energy)*.09));\n  border: 1px solid #d4fbffab;\n  border-radius: 50%;\n  background: radial-gradient(ellipse at 25% 13%,#eefffa80,transparent 28%),radial-gradient(ellipse at 60% 75%,#54bbd733,transparent 56%),linear-gradient(130deg,#9db4ce55,#042c3b05 40%,#b8ffda66);\n  backdrop-filter: blur(1.7px) saturate(1.9);\n  -webkit-backdrop-filter: blur(1.7px) saturate(1.9);\n  box-shadow: inset 2px 2px 4px #ecfff7cc,inset -2px -3px 4px #a9dff7a6,inset 0 0 10px #afeeff75,2px 7px 12px #020e2080,-3px 0 15px #99fcf130;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .lens-inner {\n  position: absolute;\n  inset: 6px;\n  border-radius: 50%;\n  border: 1px solid #b7eee450;\n  box-shadow: inset 0 0 5px #dcfcff50;\n  transform: rotate(-30deg);\n}\n\n.sop-toggle.sop-liquid .lens-inner:before {\n  content: \"\";\n  position: absolute;\n  left: 9px;\n  right: 9px;\n  top: 3px;\n  height: 28px;\n  border-top: 2px solid #e3fff5c4;\n  border-radius: 50%;\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-liquid .lens-glint {\n  position: absolute;\n  right: 4px;\n  top: 29px;\n  width: 14px;\n  height: 43px;\n  border-radius: 50%;\n  transform: rotate(30deg);\n  background: linear-gradient(#b7f9fa00,#e6fffaa1,#b9d3ff00);\n  filter: blur(1px);\n}\n\n.sop-toggle.sop-liquid .liquid-bridge {\n  position: absolute;\n  top: 32px;\n  left: 46px;\n  width: 100px;\n  height: 45px;\n  border-radius: 50%;\n  background: linear-gradient(#c0fbf855,#64c3e260,#d0e8ee77);\n  border: 1px solid #b4f8fd70;\n  filter: blur(3px);\n  transform: translateX(calc(var(--p)*123px - var(--v)*4px)) scaleX(calc(var(--energy)*1.6));\n  transform-origin: 0 50%;\n  opacity: var(--energy);\n}\n\n.sop-toggle.sop-liquid .liquid-droplet {\n  position: absolute;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 20%,#e8fffb,#83e5e04d 24%,#91d9fb20 61%,#bbeffa);\n  box-shadow: inset 0 0 2px #dafbfbb3,0 3px 8px #0003;\n  opacity: calc(var(--energy)*.85);\n}\n\n.sop-toggle.sop-liquid .drop-one {\n  width: 12px;\n  height: 12px;\n  left: calc(32px + var(--p)*164px);\n  top: 3px;\n  transform: translate(calc(var(--v)*-9px),calc(var(--energy)*-10px));\n}\n\n.sop-toggle.sop-liquid .drop-two {\n  width: 7px;\n  height: 7px;\n  left: calc(43px + var(--p)*135px);\n  top: 98px;\n  transform: translate(calc(var(--v)*-13px),calc(var(--energy)*8px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/liquid/react/Example.jsx","code":"import React, { useState } from 'react';\nimport LiquidToggle from './LiquidToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cLiquidToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/liquid/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/liquid/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"liquid\",\n    \"name\": \"Liquid\",\n    \"initial\": true,\n    \"stiffness\": 195,\n    \"damping\": 15,\n    \"tone\": 540,\n    \"travel\": 153\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for LiquidToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/liquid/styles.css","code":"/* liquid: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-liquid {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-liquid[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-liquid:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-liquid:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-liquid {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-liquid:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-liquid:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-liquid .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-liquid .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-liquid .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-liquid .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-liquid .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-liquid .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-liquid .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-liquid,\n  .sop-toggle.sop-liquid * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-liquid {\n  width: 258px;\n  height: 108px;\n}\n\n.sop-toggle.sop-liquid .liquid-shadow {\n  position: absolute;\n  left: 10px;\n  right: 10px;\n  top: 76px;\n  height: 34px;\n  border-radius: 50%;\n  background: #0009;\n  filter: blur(13px);\n}\n\n.sop-toggle.sop-liquid .liquid-track {\n  position: absolute;\n  left: 0;\n  top: 9px;\n  width: 258px;\n  height: 91px;\n  border-radius: 50px;\n  background: linear-gradient(152deg,#b4f3ee30 0,#1b455126 22%,#13232a 45%,#28404a 80%,#8cbae02b);\n  border: 1px solid #aecbda51;\n  box-shadow: inset 0 1px 1px #c4edf767,inset 0 -1px 2px #adcdff55,0 1px 2px #000;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .liquid-grid {\n  position: absolute;\n  inset: 0;\n  opacity: .4;\n  background-image: linear-gradient(90deg,#aae7d436 1px,transparent 1px),linear-gradient(#b2e2ff29 1px,transparent 1px);\n  background-size: 16px 16px;\n  transform: perspective(200px) rotateY(-14deg) rotateZ(-18deg) scale(1.6);\n  transition: opacity .4s;\n}\n\n.sop-toggle.sop-liquid .liquid-stream {\n  position: absolute;\n  inset: -60px -20px;\n  background: conic-gradient(from 20deg at 32% 44%,transparent,#8bdebd88 7%,#5fc6e899 14%,transparent 27%,#d7b9eb66 40%,#b5ebf094 45%,transparent 53%,#418eca3b 77%,#b2f5dd5e);\n  filter: blur(6px);\n  transform: translateX(calc(var(--p)*24px)) rotate(calc(var(--p)*42deg));\n  opacity: calc(.3 + var(--p)*.65);\n}\n\n.sop-toggle.sop-liquid .liquid-mark {\n  position: absolute;\n  left: 40px;\n  top: 38px;\n  font: 9px var(--sop-mono);\n  letter-spacing: 4px;\n  color: #c4efed;\n  opacity: calc(.15 + var(--p)*.5);\n}\n\n.sop-toggle.sop-liquid .liquid-waterline {\n  position: absolute;\n  inset: 4px;\n  border: 1px solid #bce6f012;\n  border-radius: 50px;\n  box-shadow: inset 0 -12px 14px #87d8e30b;\n}\n\n.sop-toggle.sop-liquid .liquid-lens {\n  position: absolute;\n  left: 9px;\n  top: 12px;\n  width: 85px;\n  height: 85px;\n  transform: translateX(calc(var(--p)*153px)) scaleX(calc(1 + var(--energy)*.22)) scaleY(calc(1 - var(--energy)*.09));\n  border: 1px solid #d4fbffab;\n  border-radius: 50%;\n  background: radial-gradient(ellipse at 25% 13%,#eefffa80,transparent 28%),radial-gradient(ellipse at 60% 75%,#54bbd733,transparent 56%),linear-gradient(130deg,#9db4ce55,#042c3b05 40%,#b8ffda66);\n  backdrop-filter: blur(1.7px) saturate(1.9);\n  -webkit-backdrop-filter: blur(1.7px) saturate(1.9);\n  box-shadow: inset 2px 2px 4px #ecfff7cc,inset -2px -3px 4px #a9dff7a6,inset 0 0 10px #afeeff75,2px 7px 12px #020e2080,-3px 0 15px #99fcf130;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .lens-inner {\n  position: absolute;\n  inset: 6px;\n  border-radius: 50%;\n  border: 1px solid #b7eee450;\n  box-shadow: inset 0 0 5px #dcfcff50;\n  transform: rotate(-30deg);\n}\n\n.sop-toggle.sop-liquid .lens-inner:before {\n  content: \"\";\n  position: absolute;\n  left: 9px;\n  right: 9px;\n  top: 3px;\n  height: 28px;\n  border-top: 2px solid #e3fff5c4;\n  border-radius: 50%;\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-liquid .lens-glint {\n  position: absolute;\n  right: 4px;\n  top: 29px;\n  width: 14px;\n  height: 43px;\n  border-radius: 50%;\n  transform: rotate(30deg);\n  background: linear-gradient(#b7f9fa00,#e6fffaa1,#b9d3ff00);\n  filter: blur(1px);\n}\n\n.sop-toggle.sop-liquid .liquid-bridge {\n  position: absolute;\n  top: 32px;\n  left: 46px;\n  width: 100px;\n  height: 45px;\n  border-radius: 50%;\n  background: linear-gradient(#c0fbf855,#64c3e260,#d0e8ee77);\n  border: 1px solid #b4f8fd70;\n  filter: blur(3px);\n  transform: translateX(calc(var(--p)*123px - var(--v)*4px)) scaleX(calc(var(--energy)*1.6));\n  transform-origin: 0 50%;\n  opacity: var(--energy);\n}\n\n.sop-toggle.sop-liquid .liquid-droplet {\n  position: absolute;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 20%,#e8fffb,#83e5e04d 24%,#91d9fb20 61%,#bbeffa);\n  box-shadow: inset 0 0 2px #dafbfbb3,0 3px 8px #0003;\n  opacity: calc(var(--energy)*.85);\n}\n\n.sop-toggle.sop-liquid .drop-one {\n  width: 12px;\n  height: 12px;\n  left: calc(32px + var(--p)*164px);\n  top: 3px;\n  transform: translate(calc(var(--v)*-9px),calc(var(--energy)*-10px));\n}\n\n.sop-toggle.sop-liquid .drop-two {\n  width: 7px;\n  height: 7px;\n  left: calc(43px + var(--p)*135px);\n  top: 98px;\n  transform: translate(calc(var(--v)*-13px),calc(var(--energy)*8px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/liquid/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-liquid');\nif (!element) throw new Error('The Liquid root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/liquid/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/liquid/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Liquid\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/liquid/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"liquid\",\n    \"name\": \"Liquid\",\n    \"initial\": true,\n    \"stiffness\": 195,\n    \"damping\": 15,\n    \"tone\": 540,\n    \"travel\": 153\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for LiquidToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/liquid/styles.css","code":"/* liquid: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-liquid {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-liquid[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-liquid:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-liquid:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-liquid {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-liquid:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-liquid:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-liquid .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-liquid .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-liquid .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-liquid .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-liquid .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-liquid .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-liquid .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-liquid,\n  .sop-toggle.sop-liquid * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-liquid {\n  width: 258px;\n  height: 108px;\n}\n\n.sop-toggle.sop-liquid .liquid-shadow {\n  position: absolute;\n  left: 10px;\n  right: 10px;\n  top: 76px;\n  height: 34px;\n  border-radius: 50%;\n  background: #0009;\n  filter: blur(13px);\n}\n\n.sop-toggle.sop-liquid .liquid-track {\n  position: absolute;\n  left: 0;\n  top: 9px;\n  width: 258px;\n  height: 91px;\n  border-radius: 50px;\n  background: linear-gradient(152deg,#b4f3ee30 0,#1b455126 22%,#13232a 45%,#28404a 80%,#8cbae02b);\n  border: 1px solid #aecbda51;\n  box-shadow: inset 0 1px 1px #c4edf767,inset 0 -1px 2px #adcdff55,0 1px 2px #000;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .liquid-grid {\n  position: absolute;\n  inset: 0;\n  opacity: .4;\n  background-image: linear-gradient(90deg,#aae7d436 1px,transparent 1px),linear-gradient(#b2e2ff29 1px,transparent 1px);\n  background-size: 16px 16px;\n  transform: perspective(200px) rotateY(-14deg) rotateZ(-18deg) scale(1.6);\n  transition: opacity .4s;\n}\n\n.sop-toggle.sop-liquid .liquid-stream {\n  position: absolute;\n  inset: -60px -20px;\n  background: conic-gradient(from 20deg at 32% 44%,transparent,#8bdebd88 7%,#5fc6e899 14%,transparent 27%,#d7b9eb66 40%,#b5ebf094 45%,transparent 53%,#418eca3b 77%,#b2f5dd5e);\n  filter: blur(6px);\n  transform: translateX(calc(var(--p)*24px)) rotate(calc(var(--p)*42deg));\n  opacity: calc(.3 + var(--p)*.65);\n}\n\n.sop-toggle.sop-liquid .liquid-mark {\n  position: absolute;\n  left: 40px;\n  top: 38px;\n  font: 9px var(--sop-mono);\n  letter-spacing: 4px;\n  color: #c4efed;\n  opacity: calc(.15 + var(--p)*.5);\n}\n\n.sop-toggle.sop-liquid .liquid-waterline {\n  position: absolute;\n  inset: 4px;\n  border: 1px solid #bce6f012;\n  border-radius: 50px;\n  box-shadow: inset 0 -12px 14px #87d8e30b;\n}\n\n.sop-toggle.sop-liquid .liquid-lens {\n  position: absolute;\n  left: 9px;\n  top: 12px;\n  width: 85px;\n  height: 85px;\n  transform: translateX(calc(var(--p)*153px)) scaleX(calc(1 + var(--energy)*.22)) scaleY(calc(1 - var(--energy)*.09));\n  border: 1px solid #d4fbffab;\n  border-radius: 50%;\n  background: radial-gradient(ellipse at 25% 13%,#eefffa80,transparent 28%),radial-gradient(ellipse at 60% 75%,#54bbd733,transparent 56%),linear-gradient(130deg,#9db4ce55,#042c3b05 40%,#b8ffda66);\n  backdrop-filter: blur(1.7px) saturate(1.9);\n  -webkit-backdrop-filter: blur(1.7px) saturate(1.9);\n  box-shadow: inset 2px 2px 4px #ecfff7cc,inset -2px -3px 4px #a9dff7a6,inset 0 0 10px #afeeff75,2px 7px 12px #020e2080,-3px 0 15px #99fcf130;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .lens-inner {\n  position: absolute;\n  inset: 6px;\n  border-radius: 50%;\n  border: 1px solid #b7eee450;\n  box-shadow: inset 0 0 5px #dcfcff50;\n  transform: rotate(-30deg);\n}\n\n.sop-toggle.sop-liquid .lens-inner:before {\n  content: \"\";\n  position: absolute;\n  left: 9px;\n  right: 9px;\n  top: 3px;\n  height: 28px;\n  border-top: 2px solid #e3fff5c4;\n  border-radius: 50%;\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-liquid .lens-glint {\n  position: absolute;\n  right: 4px;\n  top: 29px;\n  width: 14px;\n  height: 43px;\n  border-radius: 50%;\n  transform: rotate(30deg);\n  background: linear-gradient(#b7f9fa00,#e6fffaa1,#b9d3ff00);\n  filter: blur(1px);\n}\n\n.sop-toggle.sop-liquid .liquid-bridge {\n  position: absolute;\n  top: 32px;\n  left: 46px;\n  width: 100px;\n  height: 45px;\n  border-radius: 50%;\n  background: linear-gradient(#c0fbf855,#64c3e260,#d0e8ee77);\n  border: 1px solid #b4f8fd70;\n  filter: blur(3px);\n  transform: translateX(calc(var(--p)*123px - var(--v)*4px)) scaleX(calc(var(--energy)*1.6));\n  transform-origin: 0 50%;\n  opacity: var(--energy);\n}\n\n.sop-toggle.sop-liquid .liquid-droplet {\n  position: absolute;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 20%,#e8fffb,#83e5e04d 24%,#91d9fb20 61%,#bbeffa);\n  box-shadow: inset 0 0 2px #dafbfbb3,0 3px 8px #0003;\n  opacity: calc(var(--energy)*.85);\n}\n\n.sop-toggle.sop-liquid .drop-one {\n  width: 12px;\n  height: 12px;\n  left: calc(32px + var(--p)*164px);\n  top: 3px;\n  transform: translate(calc(var(--v)*-9px),calc(var(--energy)*-10px));\n}\n\n.sop-toggle.sop-liquid .drop-two {\n  width: 7px;\n  height: 7px;\n  left: calc(43px + var(--p)*135px);\n  top: 98px;\n  transform: translate(calc(var(--v)*-13px),calc(var(--energy)*8px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/liquid/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-liquid');\nif (!element)\n    throw new Error('The Liquid root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/liquid/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/liquid/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Liquid\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Liquid — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Liquid · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"true\" aria-label=\"Liquid トグル\" class=\"sop-toggle sop-liquid\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"liquid-shadow\">\u003c/span>\u003cspan class=\"liquid-track\">\u003cspan class=\"liquid-grid\">\u003c/span>\u003cspan class=\"liquid-stream\">\u003c/span>\u003cspan class=\"liquid-mark\">FLOW\u003c/span>\u003cspan class=\"liquid-waterline\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-bridge\">\u003c/span>\u003cspan class=\"liquid-lens\">\u003cspan class=\"lens-inner\">\u003c/span>\u003cspan class=\"lens-glint\">\u003c/span>\u003c/span>\u003cspan class=\"liquid-droplet drop-one\">\u003c/span>\u003cspan class=\"liquid-droplet drop-two\">\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* liquid: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-liquid {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-liquid[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-liquid:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-liquid:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-liquid {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-liquid:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-liquid:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-liquid .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-liquid .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-liquid .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-liquid .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-liquid .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-liquid .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-liquid .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-liquid .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-liquid,\n  .sop-toggle.sop-liquid * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-liquid {\n  width: 258px;\n  height: 108px;\n}\n\n.sop-toggle.sop-liquid .liquid-shadow {\n  position: absolute;\n  left: 10px;\n  right: 10px;\n  top: 76px;\n  height: 34px;\n  border-radius: 50%;\n  background: #0009;\n  filter: blur(13px);\n}\n\n.sop-toggle.sop-liquid .liquid-track {\n  position: absolute;\n  left: 0;\n  top: 9px;\n  width: 258px;\n  height: 91px;\n  border-radius: 50px;\n  background: linear-gradient(152deg,#b4f3ee30 0,#1b455126 22%,#13232a 45%,#28404a 80%,#8cbae02b);\n  border: 1px solid #aecbda51;\n  box-shadow: inset 0 1px 1px #c4edf767,inset 0 -1px 2px #adcdff55,0 1px 2px #000;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .liquid-grid {\n  position: absolute;\n  inset: 0;\n  opacity: .4;\n  background-image: linear-gradient(90deg,#aae7d436 1px,transparent 1px),linear-gradient(#b2e2ff29 1px,transparent 1px);\n  background-size: 16px 16px;\n  transform: perspective(200px) rotateY(-14deg) rotateZ(-18deg) scale(1.6);\n  transition: opacity .4s;\n}\n\n.sop-toggle.sop-liquid .liquid-stream {\n  position: absolute;\n  inset: -60px -20px;\n  background: conic-gradient(from 20deg at 32% 44%,transparent,#8bdebd88 7%,#5fc6e899 14%,transparent 27%,#d7b9eb66 40%,#b5ebf094 45%,transparent 53%,#418eca3b 77%,#b2f5dd5e);\n  filter: blur(6px);\n  transform: translateX(calc(var(--p)*24px)) rotate(calc(var(--p)*42deg));\n  opacity: calc(.3 + var(--p)*.65);\n}\n\n.sop-toggle.sop-liquid .liquid-mark {\n  position: absolute;\n  left: 40px;\n  top: 38px;\n  font: 9px var(--sop-mono);\n  letter-spacing: 4px;\n  color: #c4efed;\n  opacity: calc(.15 + var(--p)*.5);\n}\n\n.sop-toggle.sop-liquid .liquid-waterline {\n  position: absolute;\n  inset: 4px;\n  border: 1px solid #bce6f012;\n  border-radius: 50px;\n  box-shadow: inset 0 -12px 14px #87d8e30b;\n}\n\n.sop-toggle.sop-liquid .liquid-lens {\n  position: absolute;\n  left: 9px;\n  top: 12px;\n  width: 85px;\n  height: 85px;\n  transform: translateX(calc(var(--p)*153px)) scaleX(calc(1 + var(--energy)*.22)) scaleY(calc(1 - var(--energy)*.09));\n  border: 1px solid #d4fbffab;\n  border-radius: 50%;\n  background: radial-gradient(ellipse at 25% 13%,#eefffa80,transparent 28%),radial-gradient(ellipse at 60% 75%,#54bbd733,transparent 56%),linear-gradient(130deg,#9db4ce55,#042c3b05 40%,#b8ffda66);\n  backdrop-filter: blur(1.7px) saturate(1.9);\n  -webkit-backdrop-filter: blur(1.7px) saturate(1.9);\n  box-shadow: inset 2px 2px 4px #ecfff7cc,inset -2px -3px 4px #a9dff7a6,inset 0 0 10px #afeeff75,2px 7px 12px #020e2080,-3px 0 15px #99fcf130;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-liquid .lens-inner {\n  position: absolute;\n  inset: 6px;\n  border-radius: 50%;\n  border: 1px solid #b7eee450;\n  box-shadow: inset 0 0 5px #dcfcff50;\n  transform: rotate(-30deg);\n}\n\n.sop-toggle.sop-liquid .lens-inner:before {\n  content: \"\";\n  position: absolute;\n  left: 9px;\n  right: 9px;\n  top: 3px;\n  height: 28px;\n  border-top: 2px solid #e3fff5c4;\n  border-radius: 50%;\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-liquid .lens-glint {\n  position: absolute;\n  right: 4px;\n  top: 29px;\n  width: 14px;\n  height: 43px;\n  border-radius: 50%;\n  transform: rotate(30deg);\n  background: linear-gradient(#b7f9fa00,#e6fffaa1,#b9d3ff00);\n  filter: blur(1px);\n}\n\n.sop-toggle.sop-liquid .liquid-bridge {\n  position: absolute;\n  top: 32px;\n  left: 46px;\n  width: 100px;\n  height: 45px;\n  border-radius: 50%;\n  background: linear-gradient(#c0fbf855,#64c3e260,#d0e8ee77);\n  border: 1px solid #b4f8fd70;\n  filter: blur(3px);\n  transform: translateX(calc(var(--p)*123px - var(--v)*4px)) scaleX(calc(var(--energy)*1.6));\n  transform-origin: 0 50%;\n  opacity: var(--energy);\n}\n\n.sop-toggle.sop-liquid .liquid-droplet {\n  position: absolute;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 20%,#e8fffb,#83e5e04d 24%,#91d9fb20 61%,#bbeffa);\n  box-shadow: inset 0 0 2px #dafbfbb3,0 3px 8px #0003;\n  opacity: calc(var(--energy)*.85);\n}\n\n.sop-toggle.sop-liquid .drop-one {\n  width: 12px;\n  height: 12px;\n  left: calc(32px + var(--p)*164px);\n  top: 3px;\n  transform: translate(calc(var(--v)*-9px),calc(var(--energy)*-10px));\n}\n\n.sop-toggle.sop-liquid .drop-two {\n  width: 7px;\n  height: 7px;\n  left: calc(43px + var(--p)*135px);\n  top: 98px;\n  transform: translate(calc(var(--v)*-13px),calc(var(--energy)*8px));\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/liquid/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/liquid/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/liquid/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"liquid\",\n    \"name\": \"Liquid\",\n    \"initial\": true,\n    \"stiffness\": 195,\n    \"damping\": 15,\n    \"tone\": 540,\n    \"travel\": 153\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for LiquidToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/liquid/demo/main.ts\");})();\n"}},{"id":"eclipse","name":"Eclipse","category":"toggles","order":3,"version":"2.3.0","tagline":"夜と、朝のあいだ。","description":"月が滑り、静かな夜に朝焼けが満ちる。","material":"CELESTIAL LIGHT","motion":"RISE","accent":"246,191,111","initial":false,"componentName":"EclipseToggle","tags":["Eclipse","eclipse","トグル","CELESTIAL LIGHT","月が滑り、静かな夜に朝焼けが満ちる。"],"config":{"id":"eclipse","name":"Eclipse","initial":false,"stiffness":155,"damping":23,"tone":330,"travel":154},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Eclipse / 2.3.0\n\n月が滑り、静かな夜に朝焼けが満ちる。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`EclipseToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/eclipse/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/eclipse/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/eclipse/react/EclipseToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/eclipse/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/eclipse/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/eclipse/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Eclipse — 再現仕様\n\n月が滑り、静かな夜に朝焼けが満ちる。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 154px、ばねの剛性 155、減衰 23。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-eclipse` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 260px;\nheight: 112px;\nborder-radius: 60px;\nborder-radius: inherit;\nleft: var(--x);\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #343b70, #121a36, #0008, #0007, #7786b151, #bc6334, #e9a666, #efd2a4, #fff0be, #eeecff, #bad0ff99, #f2edff, #f4ebd6, #d7d9d9, #8793ad, #656d8c, #39425b55, #adb8fe27, #070e2277, #fff8df, #fff0bb, #f8c264, #e69243, #fff8d4, #7b85975c, #62718488, #e6e6d747, #14214177, #fff6c966, #ffe8b829, #fff2b41c, #ffeec508, #fff5dba8, #3f4b76\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/eclipse/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/eclipse/react/EclipseToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"eclipse\",\n    \"name\": \"Eclipse\",\n    \"initial\": false,\n    \"stiffness\": 155,\n    \"damping\": 23,\n    \"tone\": 330,\n    \"travel\": 154\n};\n/** 月が滑り、静かな夜に朝焼けが満ちる。 */\nexport default function EclipseToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Eclipse トグル'} aria-checked={checked} className={`sop-toggle sop-eclipse ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"eclipse-sky\">\u003cspan className=\"eclipse-dawn\">\u003c/span>\u003cspan className=\"stars\">\u003ci style={{ \"--x\": \"56%\", \"--y\": \"22%\", \"--s\": \"2px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"70%\", \"--y\": \"40%\", \"--s\": \"3px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"85%\", \"--y\": \"21%\", \"--s\": \"2px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"63%\", \"--y\": \"65%\", \"--s\": \"2px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"92%\", \"--y\": \"55%\", \"--s\": \"1px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"47%\", \"--y\": \"42%\", \"--s\": \"1px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"78%\", \"--y\": \"73%\", \"--s\": \"2px\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--x\": \"34%\", \"--y\": \"19%\", \"--s\": \"2px\" } as CSSProperties}>\u003c/i>\u003c/span>\u003cspan className=\"sun-halo\">\u003c/span>\u003cspan className=\"eclipse-cloud c1\">\u003c/span>\u003cspan className=\"eclipse-cloud c2\">\u003c/span>\u003csvg className=\"eclipse-landscape\" viewBox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan className=\"celestial-body\">\u003cspan className=\"crater crater1\">\u003c/span>\u003cspan className=\"crater crater2\">\u003c/span>\u003cspan className=\"crater crater3\">\u003c/span>\u003cspan className=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/eclipse/styles.css","code":"/* eclipse: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-eclipse {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-eclipse[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-eclipse:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-eclipse:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-eclipse {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-eclipse:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-eclipse:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-eclipse .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-eclipse .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-eclipse .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-eclipse .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-eclipse .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-eclipse .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-eclipse .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-eclipse,\n  .sop-toggle.sop-eclipse * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-eclipse {\n  width: 260px;\n  height: 112px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-sky {\n  position: absolute;\n  inset: 2px 0;\n  border-radius: 60px;\n  background: radial-gradient(ellipse at 70% 80%,#343b70,#121a36 68%);\n  box-shadow: inset 0 3px 9px #0008,0 10px 20px #0007,inset 0 0 0 1px #7786b151;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .eclipse-dawn {\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  background: linear-gradient(155deg,#bc6334 0,#e9a666 35%,#efd2a4 76%,#fff0be);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-eclipse .stars {\n  position: absolute;\n  inset: 0;\n  opacity: calc(1 - var(--p));\n  transform: translateY(calc(var(--p)*-25px));\n}\n\n.sop-toggle.sop-eclipse .stars i {\n  position: absolute;\n  left: var(--x);\n  top: var(--y);\n  width: var(--s);\n  height: var(--s);\n  border-radius: 50%;\n  background: #eeecff;\n  box-shadow: 0 0 5px #bad0ff99;\n}\n\n.sop-toggle.sop-eclipse .stars i:nth-child(3):after {\n  content: \"\";\n  position: absolute;\n  left: -3px;\n  top: .5px;\n  width: 8px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#f2edff,transparent);\n}\n\n.sop-toggle.sop-eclipse .celestial-body {\n  position: absolute;\n  left: 12px;\n  top: 15px;\n  width: 82px;\n  height: 82px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 30% 25%,#f4ebd6 0,#d7d9d9 34%,#8793ad 75%,#656d8c);\n  box-shadow: inset -6px -4px 9px #39425b55,0 0 25px #adb8fe27,2px 7px 9px #070e2277;\n  transform: translateX(calc(var(--p)*154px)) rotate(calc(var(--p)*85deg));\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .celestial-body:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 35% 25%,#fff8df,#fff0bb 42%,#f8c264 75%,#e69243);\n  opacity: var(--p);\n  box-shadow: inset 0 0 9px #fff8d4;\n}\n\n.sop-toggle.sop-eclipse .crater {\n  position: absolute;\n  border-radius: 50%;\n  background: #7b85975c;\n  box-shadow: inset 2px 2px 4px #62718488,1px 1px 1px #e6e6d747;\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .crater1 {\n  width: 23px;\n  height: 23px;\n  left: 18px;\n  top: 19px;\n}\n\n.sop-toggle.sop-eclipse .crater2 {\n  width: 12px;\n  height: 12px;\n  right: 17px;\n  top: 47px;\n}\n\n.sop-toggle.sop-eclipse .crater3 {\n  width: 10px;\n  height: 10px;\n  left: 23px;\n  bottom: 13px;\n}\n\n.sop-toggle.sop-eclipse .moon-shade {\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(circle at 20% 20%,transparent 30%,#14214177);\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .sun-halo {\n  position: absolute;\n  left: 0;\n  top: -29px;\n  width: 175px;\n  height: 175px;\n  border-radius: 50%;\n  background: radial-gradient(circle,#fff6c966 0 30%,#ffe8b829 31% 45%,#fff2b41c 46% 60%,#ffeec508 61%);\n  opacity: var(--p);\n  transform: translateX(calc(var(--p)*108px));\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud {\n  position: absolute;\n  background: #fff5dba8;\n  border-radius: 60%;\n  opacity: var(--p);\n  transform: translateX(calc((1 - var(--p))*-42px));\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud:before,\n.sop-toggle.sop-eclipse .eclipse-cloud:after {\n  content: \"\";\n  position: absolute;\n  border-radius: 50%;\n  background: inherit;\n}\n\n.sop-toggle.sop-eclipse .c1 {\n  width: 69px;\n  height: 13px;\n  left: 23px;\n  top: 32px;\n}\n\n.sop-toggle.sop-eclipse .c1:before {\n  width: 25px;\n  height: 18px;\n  left: 12px;\n  bottom: 3px;\n}\n\n.sop-toggle.sop-eclipse .c1:after {\n  width: 34px;\n  height: 23px;\n  left: 26px;\n  bottom: 0;\n}\n\n.sop-toggle.sop-eclipse .c2 {\n  width: 49px;\n  height: 9px;\n  left: 78px;\n  top: 63px;\n  opacity: calc(var(--p)*.7);\n}\n\n.sop-toggle.sop-eclipse .c2:before {\n  width: 25px;\n  height: 16px;\n  left: 12px;\n  bottom: 1px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-landscape {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #3f4b76;\n  opacity: calc(.8 - var(--p)*.58);\n  transform: translateX(calc(var(--p)*-12px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/eclipse/react/Example.tsx","code":"import React, { useState } from 'react';\nimport EclipseToggle from './EclipseToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cEclipseToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/eclipse/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/eclipse/react/EclipseToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"eclipse\",\n    \"name\": \"Eclipse\",\n    \"initial\": false,\n    \"stiffness\": 155,\n    \"damping\": 23,\n    \"tone\": 330,\n    \"travel\": 154\n};\n/** 月が滑り、静かな夜に朝焼けが満ちる。 */\nexport default function EclipseToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Eclipse トグル'} aria-checked={checked} className={`sop-toggle sop-eclipse ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"eclipse-sky\">\u003cspan className=\"eclipse-dawn\">\u003c/span>\u003cspan className=\"stars\">\u003ci style={{ \"--x\": \"56%\", \"--y\": \"22%\", \"--s\": \"2px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"70%\", \"--y\": \"40%\", \"--s\": \"3px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"85%\", \"--y\": \"21%\", \"--s\": \"2px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"63%\", \"--y\": \"65%\", \"--s\": \"2px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"92%\", \"--y\": \"55%\", \"--s\": \"1px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"47%\", \"--y\": \"42%\", \"--s\": \"1px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"78%\", \"--y\": \"73%\", \"--s\": \"2px\" }}>\u003c/i>\u003ci style={{ \"--x\": \"34%\", \"--y\": \"19%\", \"--s\": \"2px\" }}>\u003c/i>\u003c/span>\u003cspan className=\"sun-halo\">\u003c/span>\u003cspan className=\"eclipse-cloud c1\">\u003c/span>\u003cspan className=\"eclipse-cloud c2\">\u003c/span>\u003csvg className=\"eclipse-landscape\" viewBox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan className=\"celestial-body\">\u003cspan className=\"crater crater1\">\u003c/span>\u003cspan className=\"crater crater2\">\u003c/span>\u003cspan className=\"crater crater3\">\u003c/span>\u003cspan className=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/eclipse/styles.css","code":"/* eclipse: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-eclipse {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-eclipse[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-eclipse:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-eclipse:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-eclipse {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-eclipse:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-eclipse:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-eclipse .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-eclipse .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-eclipse .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-eclipse .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-eclipse .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-eclipse .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-eclipse .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-eclipse,\n  .sop-toggle.sop-eclipse * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-eclipse {\n  width: 260px;\n  height: 112px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-sky {\n  position: absolute;\n  inset: 2px 0;\n  border-radius: 60px;\n  background: radial-gradient(ellipse at 70% 80%,#343b70,#121a36 68%);\n  box-shadow: inset 0 3px 9px #0008,0 10px 20px #0007,inset 0 0 0 1px #7786b151;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .eclipse-dawn {\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  background: linear-gradient(155deg,#bc6334 0,#e9a666 35%,#efd2a4 76%,#fff0be);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-eclipse .stars {\n  position: absolute;\n  inset: 0;\n  opacity: calc(1 - var(--p));\n  transform: translateY(calc(var(--p)*-25px));\n}\n\n.sop-toggle.sop-eclipse .stars i {\n  position: absolute;\n  left: var(--x);\n  top: var(--y);\n  width: var(--s);\n  height: var(--s);\n  border-radius: 50%;\n  background: #eeecff;\n  box-shadow: 0 0 5px #bad0ff99;\n}\n\n.sop-toggle.sop-eclipse .stars i:nth-child(3):after {\n  content: \"\";\n  position: absolute;\n  left: -3px;\n  top: .5px;\n  width: 8px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#f2edff,transparent);\n}\n\n.sop-toggle.sop-eclipse .celestial-body {\n  position: absolute;\n  left: 12px;\n  top: 15px;\n  width: 82px;\n  height: 82px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 30% 25%,#f4ebd6 0,#d7d9d9 34%,#8793ad 75%,#656d8c);\n  box-shadow: inset -6px -4px 9px #39425b55,0 0 25px #adb8fe27,2px 7px 9px #070e2277;\n  transform: translateX(calc(var(--p)*154px)) rotate(calc(var(--p)*85deg));\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .celestial-body:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 35% 25%,#fff8df,#fff0bb 42%,#f8c264 75%,#e69243);\n  opacity: var(--p);\n  box-shadow: inset 0 0 9px #fff8d4;\n}\n\n.sop-toggle.sop-eclipse .crater {\n  position: absolute;\n  border-radius: 50%;\n  background: #7b85975c;\n  box-shadow: inset 2px 2px 4px #62718488,1px 1px 1px #e6e6d747;\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .crater1 {\n  width: 23px;\n  height: 23px;\n  left: 18px;\n  top: 19px;\n}\n\n.sop-toggle.sop-eclipse .crater2 {\n  width: 12px;\n  height: 12px;\n  right: 17px;\n  top: 47px;\n}\n\n.sop-toggle.sop-eclipse .crater3 {\n  width: 10px;\n  height: 10px;\n  left: 23px;\n  bottom: 13px;\n}\n\n.sop-toggle.sop-eclipse .moon-shade {\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(circle at 20% 20%,transparent 30%,#14214177);\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .sun-halo {\n  position: absolute;\n  left: 0;\n  top: -29px;\n  width: 175px;\n  height: 175px;\n  border-radius: 50%;\n  background: radial-gradient(circle,#fff6c966 0 30%,#ffe8b829 31% 45%,#fff2b41c 46% 60%,#ffeec508 61%);\n  opacity: var(--p);\n  transform: translateX(calc(var(--p)*108px));\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud {\n  position: absolute;\n  background: #fff5dba8;\n  border-radius: 60%;\n  opacity: var(--p);\n  transform: translateX(calc((1 - var(--p))*-42px));\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud:before,\n.sop-toggle.sop-eclipse .eclipse-cloud:after {\n  content: \"\";\n  position: absolute;\n  border-radius: 50%;\n  background: inherit;\n}\n\n.sop-toggle.sop-eclipse .c1 {\n  width: 69px;\n  height: 13px;\n  left: 23px;\n  top: 32px;\n}\n\n.sop-toggle.sop-eclipse .c1:before {\n  width: 25px;\n  height: 18px;\n  left: 12px;\n  bottom: 3px;\n}\n\n.sop-toggle.sop-eclipse .c1:after {\n  width: 34px;\n  height: 23px;\n  left: 26px;\n  bottom: 0;\n}\n\n.sop-toggle.sop-eclipse .c2 {\n  width: 49px;\n  height: 9px;\n  left: 78px;\n  top: 63px;\n  opacity: calc(var(--p)*.7);\n}\n\n.sop-toggle.sop-eclipse .c2:before {\n  width: 25px;\n  height: 16px;\n  left: 12px;\n  bottom: 1px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-landscape {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #3f4b76;\n  opacity: calc(.8 - var(--p)*.58);\n  transform: translateX(calc(var(--p)*-12px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/eclipse/react/Example.jsx","code":"import React, { useState } from 'react';\nimport EclipseToggle from './EclipseToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cEclipseToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/eclipse/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/eclipse/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"eclipse\",\n    \"name\": \"Eclipse\",\n    \"initial\": false,\n    \"stiffness\": 155,\n    \"damping\": 23,\n    \"tone\": 330,\n    \"travel\": 154\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for EclipseToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/eclipse/styles.css","code":"/* eclipse: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-eclipse {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-eclipse[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-eclipse:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-eclipse:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-eclipse {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-eclipse:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-eclipse:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-eclipse .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-eclipse .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-eclipse .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-eclipse .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-eclipse .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-eclipse .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-eclipse .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-eclipse,\n  .sop-toggle.sop-eclipse * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-eclipse {\n  width: 260px;\n  height: 112px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-sky {\n  position: absolute;\n  inset: 2px 0;\n  border-radius: 60px;\n  background: radial-gradient(ellipse at 70% 80%,#343b70,#121a36 68%);\n  box-shadow: inset 0 3px 9px #0008,0 10px 20px #0007,inset 0 0 0 1px #7786b151;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .eclipse-dawn {\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  background: linear-gradient(155deg,#bc6334 0,#e9a666 35%,#efd2a4 76%,#fff0be);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-eclipse .stars {\n  position: absolute;\n  inset: 0;\n  opacity: calc(1 - var(--p));\n  transform: translateY(calc(var(--p)*-25px));\n}\n\n.sop-toggle.sop-eclipse .stars i {\n  position: absolute;\n  left: var(--x);\n  top: var(--y);\n  width: var(--s);\n  height: var(--s);\n  border-radius: 50%;\n  background: #eeecff;\n  box-shadow: 0 0 5px #bad0ff99;\n}\n\n.sop-toggle.sop-eclipse .stars i:nth-child(3):after {\n  content: \"\";\n  position: absolute;\n  left: -3px;\n  top: .5px;\n  width: 8px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#f2edff,transparent);\n}\n\n.sop-toggle.sop-eclipse .celestial-body {\n  position: absolute;\n  left: 12px;\n  top: 15px;\n  width: 82px;\n  height: 82px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 30% 25%,#f4ebd6 0,#d7d9d9 34%,#8793ad 75%,#656d8c);\n  box-shadow: inset -6px -4px 9px #39425b55,0 0 25px #adb8fe27,2px 7px 9px #070e2277;\n  transform: translateX(calc(var(--p)*154px)) rotate(calc(var(--p)*85deg));\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .celestial-body:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 35% 25%,#fff8df,#fff0bb 42%,#f8c264 75%,#e69243);\n  opacity: var(--p);\n  box-shadow: inset 0 0 9px #fff8d4;\n}\n\n.sop-toggle.sop-eclipse .crater {\n  position: absolute;\n  border-radius: 50%;\n  background: #7b85975c;\n  box-shadow: inset 2px 2px 4px #62718488,1px 1px 1px #e6e6d747;\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .crater1 {\n  width: 23px;\n  height: 23px;\n  left: 18px;\n  top: 19px;\n}\n\n.sop-toggle.sop-eclipse .crater2 {\n  width: 12px;\n  height: 12px;\n  right: 17px;\n  top: 47px;\n}\n\n.sop-toggle.sop-eclipse .crater3 {\n  width: 10px;\n  height: 10px;\n  left: 23px;\n  bottom: 13px;\n}\n\n.sop-toggle.sop-eclipse .moon-shade {\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(circle at 20% 20%,transparent 30%,#14214177);\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .sun-halo {\n  position: absolute;\n  left: 0;\n  top: -29px;\n  width: 175px;\n  height: 175px;\n  border-radius: 50%;\n  background: radial-gradient(circle,#fff6c966 0 30%,#ffe8b829 31% 45%,#fff2b41c 46% 60%,#ffeec508 61%);\n  opacity: var(--p);\n  transform: translateX(calc(var(--p)*108px));\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud {\n  position: absolute;\n  background: #fff5dba8;\n  border-radius: 60%;\n  opacity: var(--p);\n  transform: translateX(calc((1 - var(--p))*-42px));\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud:before,\n.sop-toggle.sop-eclipse .eclipse-cloud:after {\n  content: \"\";\n  position: absolute;\n  border-radius: 50%;\n  background: inherit;\n}\n\n.sop-toggle.sop-eclipse .c1 {\n  width: 69px;\n  height: 13px;\n  left: 23px;\n  top: 32px;\n}\n\n.sop-toggle.sop-eclipse .c1:before {\n  width: 25px;\n  height: 18px;\n  left: 12px;\n  bottom: 3px;\n}\n\n.sop-toggle.sop-eclipse .c1:after {\n  width: 34px;\n  height: 23px;\n  left: 26px;\n  bottom: 0;\n}\n\n.sop-toggle.sop-eclipse .c2 {\n  width: 49px;\n  height: 9px;\n  left: 78px;\n  top: 63px;\n  opacity: calc(var(--p)*.7);\n}\n\n.sop-toggle.sop-eclipse .c2:before {\n  width: 25px;\n  height: 16px;\n  left: 12px;\n  bottom: 1px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-landscape {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #3f4b76;\n  opacity: calc(.8 - var(--p)*.58);\n  transform: translateX(calc(var(--p)*-12px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/eclipse/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-eclipse');\nif (!element) throw new Error('The Eclipse root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/eclipse/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/eclipse/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Eclipse\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/eclipse/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"eclipse\",\n    \"name\": \"Eclipse\",\n    \"initial\": false,\n    \"stiffness\": 155,\n    \"damping\": 23,\n    \"tone\": 330,\n    \"travel\": 154\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for EclipseToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/eclipse/styles.css","code":"/* eclipse: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-eclipse {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-eclipse[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-eclipse:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-eclipse:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-eclipse {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-eclipse:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-eclipse:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-eclipse .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-eclipse .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-eclipse .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-eclipse .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-eclipse .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-eclipse .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-eclipse .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-eclipse,\n  .sop-toggle.sop-eclipse * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-eclipse {\n  width: 260px;\n  height: 112px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-sky {\n  position: absolute;\n  inset: 2px 0;\n  border-radius: 60px;\n  background: radial-gradient(ellipse at 70% 80%,#343b70,#121a36 68%);\n  box-shadow: inset 0 3px 9px #0008,0 10px 20px #0007,inset 0 0 0 1px #7786b151;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .eclipse-dawn {\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  background: linear-gradient(155deg,#bc6334 0,#e9a666 35%,#efd2a4 76%,#fff0be);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-eclipse .stars {\n  position: absolute;\n  inset: 0;\n  opacity: calc(1 - var(--p));\n  transform: translateY(calc(var(--p)*-25px));\n}\n\n.sop-toggle.sop-eclipse .stars i {\n  position: absolute;\n  left: var(--x);\n  top: var(--y);\n  width: var(--s);\n  height: var(--s);\n  border-radius: 50%;\n  background: #eeecff;\n  box-shadow: 0 0 5px #bad0ff99;\n}\n\n.sop-toggle.sop-eclipse .stars i:nth-child(3):after {\n  content: \"\";\n  position: absolute;\n  left: -3px;\n  top: .5px;\n  width: 8px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#f2edff,transparent);\n}\n\n.sop-toggle.sop-eclipse .celestial-body {\n  position: absolute;\n  left: 12px;\n  top: 15px;\n  width: 82px;\n  height: 82px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 30% 25%,#f4ebd6 0,#d7d9d9 34%,#8793ad 75%,#656d8c);\n  box-shadow: inset -6px -4px 9px #39425b55,0 0 25px #adb8fe27,2px 7px 9px #070e2277;\n  transform: translateX(calc(var(--p)*154px)) rotate(calc(var(--p)*85deg));\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .celestial-body:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 35% 25%,#fff8df,#fff0bb 42%,#f8c264 75%,#e69243);\n  opacity: var(--p);\n  box-shadow: inset 0 0 9px #fff8d4;\n}\n\n.sop-toggle.sop-eclipse .crater {\n  position: absolute;\n  border-radius: 50%;\n  background: #7b85975c;\n  box-shadow: inset 2px 2px 4px #62718488,1px 1px 1px #e6e6d747;\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .crater1 {\n  width: 23px;\n  height: 23px;\n  left: 18px;\n  top: 19px;\n}\n\n.sop-toggle.sop-eclipse .crater2 {\n  width: 12px;\n  height: 12px;\n  right: 17px;\n  top: 47px;\n}\n\n.sop-toggle.sop-eclipse .crater3 {\n  width: 10px;\n  height: 10px;\n  left: 23px;\n  bottom: 13px;\n}\n\n.sop-toggle.sop-eclipse .moon-shade {\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(circle at 20% 20%,transparent 30%,#14214177);\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .sun-halo {\n  position: absolute;\n  left: 0;\n  top: -29px;\n  width: 175px;\n  height: 175px;\n  border-radius: 50%;\n  background: radial-gradient(circle,#fff6c966 0 30%,#ffe8b829 31% 45%,#fff2b41c 46% 60%,#ffeec508 61%);\n  opacity: var(--p);\n  transform: translateX(calc(var(--p)*108px));\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud {\n  position: absolute;\n  background: #fff5dba8;\n  border-radius: 60%;\n  opacity: var(--p);\n  transform: translateX(calc((1 - var(--p))*-42px));\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud:before,\n.sop-toggle.sop-eclipse .eclipse-cloud:after {\n  content: \"\";\n  position: absolute;\n  border-radius: 50%;\n  background: inherit;\n}\n\n.sop-toggle.sop-eclipse .c1 {\n  width: 69px;\n  height: 13px;\n  left: 23px;\n  top: 32px;\n}\n\n.sop-toggle.sop-eclipse .c1:before {\n  width: 25px;\n  height: 18px;\n  left: 12px;\n  bottom: 3px;\n}\n\n.sop-toggle.sop-eclipse .c1:after {\n  width: 34px;\n  height: 23px;\n  left: 26px;\n  bottom: 0;\n}\n\n.sop-toggle.sop-eclipse .c2 {\n  width: 49px;\n  height: 9px;\n  left: 78px;\n  top: 63px;\n  opacity: calc(var(--p)*.7);\n}\n\n.sop-toggle.sop-eclipse .c2:before {\n  width: 25px;\n  height: 16px;\n  left: 12px;\n  bottom: 1px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-landscape {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #3f4b76;\n  opacity: calc(.8 - var(--p)*.58);\n  transform: translateX(calc(var(--p)*-12px));\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/eclipse/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-eclipse');\nif (!element)\n    throw new Error('The Eclipse root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/eclipse/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/eclipse/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Eclipse\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Eclipse — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Eclipse · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"false\" aria-label=\"Eclipse トグル\" class=\"sop-toggle sop-eclipse\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"eclipse-sky\">\u003cspan class=\"eclipse-dawn\">\u003c/span>\u003cspan class=\"stars\">\u003ci style=\"--x:56%;--y:22%;--s:2px\">\u003c/i>\u003ci style=\"--x:70%;--y:40%;--s:3px\">\u003c/i>\u003ci style=\"--x:85%;--y:21%;--s:2px\">\u003c/i>\u003ci style=\"--x:63%;--y:65%;--s:2px\">\u003c/i>\u003ci style=\"--x:92%;--y:55%;--s:1px\">\u003c/i>\u003ci style=\"--x:47%;--y:42%;--s:1px\">\u003c/i>\u003ci style=\"--x:78%;--y:73%;--s:2px\">\u003c/i>\u003ci style=\"--x:34%;--y:19%;--s:2px\">\u003c/i>\u003c/span>\u003cspan class=\"sun-halo\">\u003c/span>\u003cspan class=\"eclipse-cloud c1\">\u003c/span>\u003cspan class=\"eclipse-cloud c2\">\u003c/span>\u003csvg class=\"eclipse-landscape\" viewbox=\"0 0 260 108\">\u003cpath d=\"M0 100 22 85 42 89 69 72 94 86 130 54 160 76 180 62 224 88 240 75 260 88V108H0Z\" fill=\"currentColor\" opacity=\".5\">\u003c/path>\u003cpath d=\"m0 108 45-18 30 7 49-13 30 8 47-13 59 25v4Z\" fill=\"currentColor\">\u003c/path>\u003c/svg>\u003c/span>\u003cspan class=\"celestial-body\">\u003cspan class=\"crater crater1\">\u003c/span>\u003cspan class=\"crater crater2\">\u003c/span>\u003cspan class=\"crater crater3\">\u003c/span>\u003cspan class=\"moon-shade\">\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* eclipse: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-eclipse {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-eclipse[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-eclipse:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-eclipse:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-eclipse {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-eclipse:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-eclipse:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-eclipse .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-eclipse .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-eclipse .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-eclipse .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-eclipse .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-eclipse .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-eclipse .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-eclipse .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-eclipse,\n  .sop-toggle.sop-eclipse * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-eclipse {\n  width: 260px;\n  height: 112px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-sky {\n  position: absolute;\n  inset: 2px 0;\n  border-radius: 60px;\n  background: radial-gradient(ellipse at 70% 80%,#343b70,#121a36 68%);\n  box-shadow: inset 0 3px 9px #0008,0 10px 20px #0007,inset 0 0 0 1px #7786b151;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .eclipse-dawn {\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  background: linear-gradient(155deg,#bc6334 0,#e9a666 35%,#efd2a4 76%,#fff0be);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-eclipse .stars {\n  position: absolute;\n  inset: 0;\n  opacity: calc(1 - var(--p));\n  transform: translateY(calc(var(--p)*-25px));\n}\n\n.sop-toggle.sop-eclipse .stars i {\n  position: absolute;\n  left: var(--x);\n  top: var(--y);\n  width: var(--s);\n  height: var(--s);\n  border-radius: 50%;\n  background: #eeecff;\n  box-shadow: 0 0 5px #bad0ff99;\n}\n\n.sop-toggle.sop-eclipse .stars i:nth-child(3):after {\n  content: \"\";\n  position: absolute;\n  left: -3px;\n  top: .5px;\n  width: 8px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#f2edff,transparent);\n}\n\n.sop-toggle.sop-eclipse .celestial-body {\n  position: absolute;\n  left: 12px;\n  top: 15px;\n  width: 82px;\n  height: 82px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 30% 25%,#f4ebd6 0,#d7d9d9 34%,#8793ad 75%,#656d8c);\n  box-shadow: inset -6px -4px 9px #39425b55,0 0 25px #adb8fe27,2px 7px 9px #070e2277;\n  transform: translateX(calc(var(--p)*154px)) rotate(calc(var(--p)*85deg));\n  overflow: hidden;\n}\n\n.sop-toggle.sop-eclipse .celestial-body:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 35% 25%,#fff8df,#fff0bb 42%,#f8c264 75%,#e69243);\n  opacity: var(--p);\n  box-shadow: inset 0 0 9px #fff8d4;\n}\n\n.sop-toggle.sop-eclipse .crater {\n  position: absolute;\n  border-radius: 50%;\n  background: #7b85975c;\n  box-shadow: inset 2px 2px 4px #62718488,1px 1px 1px #e6e6d747;\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .crater1 {\n  width: 23px;\n  height: 23px;\n  left: 18px;\n  top: 19px;\n}\n\n.sop-toggle.sop-eclipse .crater2 {\n  width: 12px;\n  height: 12px;\n  right: 17px;\n  top: 47px;\n}\n\n.sop-toggle.sop-eclipse .crater3 {\n  width: 10px;\n  height: 10px;\n  left: 23px;\n  bottom: 13px;\n}\n\n.sop-toggle.sop-eclipse .moon-shade {\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(circle at 20% 20%,transparent 30%,#14214177);\n  opacity: calc(1 - var(--p));\n}\n\n.sop-toggle.sop-eclipse .sun-halo {\n  position: absolute;\n  left: 0;\n  top: -29px;\n  width: 175px;\n  height: 175px;\n  border-radius: 50%;\n  background: radial-gradient(circle,#fff6c966 0 30%,#ffe8b829 31% 45%,#fff2b41c 46% 60%,#ffeec508 61%);\n  opacity: var(--p);\n  transform: translateX(calc(var(--p)*108px));\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud {\n  position: absolute;\n  background: #fff5dba8;\n  border-radius: 60%;\n  opacity: var(--p);\n  transform: translateX(calc((1 - var(--p))*-42px));\n  filter: blur(.5px);\n}\n\n.sop-toggle.sop-eclipse .eclipse-cloud:before,\n.sop-toggle.sop-eclipse .eclipse-cloud:after {\n  content: \"\";\n  position: absolute;\n  border-radius: 50%;\n  background: inherit;\n}\n\n.sop-toggle.sop-eclipse .c1 {\n  width: 69px;\n  height: 13px;\n  left: 23px;\n  top: 32px;\n}\n\n.sop-toggle.sop-eclipse .c1:before {\n  width: 25px;\n  height: 18px;\n  left: 12px;\n  bottom: 3px;\n}\n\n.sop-toggle.sop-eclipse .c1:after {\n  width: 34px;\n  height: 23px;\n  left: 26px;\n  bottom: 0;\n}\n\n.sop-toggle.sop-eclipse .c2 {\n  width: 49px;\n  height: 9px;\n  left: 78px;\n  top: 63px;\n  opacity: calc(var(--p)*.7);\n}\n\n.sop-toggle.sop-eclipse .c2:before {\n  width: 25px;\n  height: 16px;\n  left: 12px;\n  bottom: 1px;\n}\n\n.sop-toggle.sop-eclipse .eclipse-landscape {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #3f4b76;\n  opacity: calc(.8 - var(--p)*.58);\n  transform: translateX(calc(var(--p)*-12px));\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/eclipse/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/eclipse/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/eclipse/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"eclipse\",\n    \"name\": \"Eclipse\",\n    \"initial\": false,\n    \"stiffness\": 155,\n    \"damping\": 23,\n    \"tone\": 330,\n    \"travel\": 154\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for EclipseToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/eclipse/demo/main.ts\");})();\n"}},{"id":"bloom","name":"Bloom","category":"toggles","order":4,"version":"2.3.0","tagline":"咲く、というスイッチ。","description":"つぼみがほどけ、花びらがふわりと広がる。","material":"ORGANIC MOTION","motion":"UNFURL","accent":"238,153,137","initial":true,"componentName":"BloomToggle","tags":["Bloom","bloom","トグル","ORGANIC MOTION","つぼみがほどけ、花びらがふわりと広がる。"],"config":{"id":"bloom","name":"Bloom","initial":true,"stiffness":180,"damping":19,"tone":660,"travel":159},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n","usage":"# Bloom / 2.3.0\n\nつぼみがほどけ、花びらがふわりと広がる。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`BloomToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/bloom/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/bloom/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/bloom/react/BloomToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/bloom/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/bloom/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/bloom/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Bloom — 再現仕様\n\nつぼみがほどけ、花びらがふわりと広がる。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 159px、ばねの剛性 180、減衰 19。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-bloom` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 280px;\nheight: 140px;\nleft: 16px;\ntop: 32px;\nwidth: 248px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #231b1c, #372528, #231b1d, #64434478, #08070999, #0005, #b0747036, #a8766140, #bc8983, #0007, #bf625e, #ed9c88, #ffccb0, #e99887, #ba6965, #fce6c96e, #7c413757, #662e3744, #ffddc46b, #c9776c, #ffb9a0, #e79b8b, #d17e78, #c68368, #ffd7aa, #f3b18f, #ba7961, #793c4440, #ffdfa7, #eaaa72, #b46e48, #804531, #ffe3a860, #60362290, #5136209c, #000\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/bloom/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/bloom/react/BloomToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"bloom\",\n    \"name\": \"Bloom\",\n    \"initial\": true,\n    \"stiffness\": 180,\n    \"damping\": 19,\n    \"tone\": 660,\n    \"travel\": 159\n};\n/** つぼみがほどけ、花びらがふわりと広がる。 */\nexport default function BloomToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Bloom トグル'} aria-checked={checked} className={`sop-toggle sop-bloom ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"bloom-track\">\u003csvg className=\"bloom-vines\" viewBox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"1\">\u003c/path>\u003c/svg>\u003cspan className=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan className=\"flower\">\u003cspan className=\"petals\">\u003ci style={{ \"--i\": \"0\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"1\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"2\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"3\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"4\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"5\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"6\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"7\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"8\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"9\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"10\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"11\" } as CSSProperties}>\u003c/i>\u003c/span>\u003cspan className=\"flower-inner\">\u003ci style={{ \"--i\": \"0\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"1\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"2\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"3\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"4\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"5\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"6\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"7\" } as CSSProperties}>\u003c/i>\u003c/span>\u003cspan className=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/bloom/styles.css","code":"/* bloom: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-bloom {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-bloom[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-bloom:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-bloom:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-bloom {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-bloom:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-bloom:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-bloom .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-bloom .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-bloom .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-bloom .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-bloom .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-bloom .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-bloom .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-bloom,\n  .sop-toggle.sop-bloom * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-bloom {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-bloom .bloom-track {\n  position: absolute;\n  left: 16px;\n  top: 32px;\n  width: 248px;\n  height: 80px;\n  border-radius: 50px;\n  background: linear-gradient(165deg,#231b1c,#372528 45%,#231b1d);\n  border: 1px solid #64434478;\n  box-shadow: inset 0 3px 9px #08070999,0 8px 18px #0005,inset 0 -1px 1px #b0747036;\n}\n\n.sop-toggle.sop-bloom .bloom-vines {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #a8766140;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-bloom .bloom-word {\n  position: absolute;\n  top: 34px;\n  left: 45px;\n  font: 8px var(--sop-mono);\n  letter-spacing: 3px;\n  color: #bc8983;\n  opacity: calc(var(--p)*.6);\n}\n\n.sop-toggle.sop-bloom .flower {\n  position: absolute;\n  left: 57px;\n  top: 72px;\n  width: 0;\n  height: 0;\n  transform: translateX(calc(var(--p)*159px)) rotate(calc(var(--p)*85deg));\n  filter: drop-shadow(2px 9px 6px #0007);\n}\n\n.sop-toggle.sop-bloom .petals,\n.sop-toggle.sop-bloom .flower-inner {\n  position: absolute;\n  inset: 0;\n  perspective: 250px;\n}\n\n.sop-toggle.sop-bloom .petals i {\n  position: absolute;\n  left: -16px;\n  bottom: -8px;\n  width: 32px;\n  height: 65px;\n  border-radius: 60% 50% 45% 45%;\n  background: linear-gradient(100deg,#bf625e,#ed9c88 28%,#ffccb0 51%,#e99887 55%,#ba6965);\n  box-shadow: inset 1px 0 2px #fce6c96e,inset -1px 0 2px #7c413757,1px 1px 4px #662e3744;\n  transform-origin: 50% calc(100% - 8px);\n  transform: rotate(calc(var(--i)*30deg)) translateY(calc(var(--p)*-3px)) rotateX(calc((1 - var(--p))*74deg)) scale(calc(.35 + var(--p)*.65));\n  opacity: calc(.1 + var(--p)*.9);\n}\n\n.sop-toggle.sop-bloom .petals i:after {\n  content: \"\";\n  position: absolute;\n  width: 1px;\n  left: 50%;\n  top: 12%;\n  bottom: 10%;\n  background: linear-gradient(transparent,#ffddc46b,transparent);\n}\n\n.sop-toggle.sop-bloom .petals i:nth-child(even) {\n  background: linear-gradient(95deg,#c9776c,#ffb9a0 48%,#e79b8b 52%,#d17e78);\n}\n\n.sop-toggle.sop-bloom .flower-inner i {\n  position: absolute;\n  left: -12px;\n  bottom: -6px;\n  width: 24px;\n  height: 42px;\n  border-radius: 55% 50% 40% 40%;\n  background: linear-gradient(90deg,#c68368,#ffd7aa 49%,#f3b18f 52%,#ba7961);\n  transform-origin: 50% calc(100% - 6px);\n  transform: rotate(calc(var(--i)*45deg + 15deg)) rotateX(calc((1 - var(--p))*78deg)) scale(calc(.4 + var(--p)*.6));\n  box-shadow: 1px 2px 4px #793c4440;\n}\n\n.sop-toggle.sop-bloom .flower-core {\n  position: absolute;\n  inset: -23px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 26%,#ffdfa7,#eaaa72 30%,#b46e48 72%,#804531);\n  box-shadow: inset 1px 2px 3px #ffe3a860,2px 4px 6px #60362290;\n  transform: scale(calc(1 - var(--p)*.26));\n}\n\n.sop-toggle.sop-bloom .flower-core:after {\n  content: \"\";\n  position: absolute;\n  inset: 4px;\n  border-radius: 50%;\n  background-image: radial-gradient(#5136209c .7px,transparent 1.3px);\n  background-size: 4px 4px;\n  opacity: var(--p);\n  mask-image: radial-gradient(circle,#000 35%,transparent 68%);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/bloom/react/Example.tsx","code":"import React, { useState } from 'react';\nimport BloomToggle from './BloomToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cBloomToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/bloom/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/bloom/react/BloomToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"bloom\",\n    \"name\": \"Bloom\",\n    \"initial\": true,\n    \"stiffness\": 180,\n    \"damping\": 19,\n    \"tone\": 660,\n    \"travel\": 159\n};\n/** つぼみがほどけ、花びらがふわりと広がる。 */\nexport default function BloomToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Bloom トグル'} aria-checked={checked} className={`sop-toggle sop-bloom ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"bloom-track\">\u003csvg className=\"bloom-vines\" viewBox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"1\">\u003c/path>\u003c/svg>\u003cspan className=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan className=\"flower\">\u003cspan className=\"petals\">\u003ci style={{ \"--i\": \"0\" }}>\u003c/i>\u003ci style={{ \"--i\": \"1\" }}>\u003c/i>\u003ci style={{ \"--i\": \"2\" }}>\u003c/i>\u003ci style={{ \"--i\": \"3\" }}>\u003c/i>\u003ci style={{ \"--i\": \"4\" }}>\u003c/i>\u003ci style={{ \"--i\": \"5\" }}>\u003c/i>\u003ci style={{ \"--i\": \"6\" }}>\u003c/i>\u003ci style={{ \"--i\": \"7\" }}>\u003c/i>\u003ci style={{ \"--i\": \"8\" }}>\u003c/i>\u003ci style={{ \"--i\": \"9\" }}>\u003c/i>\u003ci style={{ \"--i\": \"10\" }}>\u003c/i>\u003ci style={{ \"--i\": \"11\" }}>\u003c/i>\u003c/span>\u003cspan className=\"flower-inner\">\u003ci style={{ \"--i\": \"0\" }}>\u003c/i>\u003ci style={{ \"--i\": \"1\" }}>\u003c/i>\u003ci style={{ \"--i\": \"2\" }}>\u003c/i>\u003ci style={{ \"--i\": \"3\" }}>\u003c/i>\u003ci style={{ \"--i\": \"4\" }}>\u003c/i>\u003ci style={{ \"--i\": \"5\" }}>\u003c/i>\u003ci style={{ \"--i\": \"6\" }}>\u003c/i>\u003ci style={{ \"--i\": \"7\" }}>\u003c/i>\u003c/span>\u003cspan className=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/bloom/styles.css","code":"/* bloom: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-bloom {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-bloom[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-bloom:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-bloom:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-bloom {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-bloom:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-bloom:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-bloom .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-bloom .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-bloom .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-bloom .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-bloom .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-bloom .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-bloom .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-bloom,\n  .sop-toggle.sop-bloom * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-bloom {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-bloom .bloom-track {\n  position: absolute;\n  left: 16px;\n  top: 32px;\n  width: 248px;\n  height: 80px;\n  border-radius: 50px;\n  background: linear-gradient(165deg,#231b1c,#372528 45%,#231b1d);\n  border: 1px solid #64434478;\n  box-shadow: inset 0 3px 9px #08070999,0 8px 18px #0005,inset 0 -1px 1px #b0747036;\n}\n\n.sop-toggle.sop-bloom .bloom-vines {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #a8766140;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-bloom .bloom-word {\n  position: absolute;\n  top: 34px;\n  left: 45px;\n  font: 8px var(--sop-mono);\n  letter-spacing: 3px;\n  color: #bc8983;\n  opacity: calc(var(--p)*.6);\n}\n\n.sop-toggle.sop-bloom .flower {\n  position: absolute;\n  left: 57px;\n  top: 72px;\n  width: 0;\n  height: 0;\n  transform: translateX(calc(var(--p)*159px)) rotate(calc(var(--p)*85deg));\n  filter: drop-shadow(2px 9px 6px #0007);\n}\n\n.sop-toggle.sop-bloom .petals,\n.sop-toggle.sop-bloom .flower-inner {\n  position: absolute;\n  inset: 0;\n  perspective: 250px;\n}\n\n.sop-toggle.sop-bloom .petals i {\n  position: absolute;\n  left: -16px;\n  bottom: -8px;\n  width: 32px;\n  height: 65px;\n  border-radius: 60% 50% 45% 45%;\n  background: linear-gradient(100deg,#bf625e,#ed9c88 28%,#ffccb0 51%,#e99887 55%,#ba6965);\n  box-shadow: inset 1px 0 2px #fce6c96e,inset -1px 0 2px #7c413757,1px 1px 4px #662e3744;\n  transform-origin: 50% calc(100% - 8px);\n  transform: rotate(calc(var(--i)*30deg)) translateY(calc(var(--p)*-3px)) rotateX(calc((1 - var(--p))*74deg)) scale(calc(.35 + var(--p)*.65));\n  opacity: calc(.1 + var(--p)*.9);\n}\n\n.sop-toggle.sop-bloom .petals i:after {\n  content: \"\";\n  position: absolute;\n  width: 1px;\n  left: 50%;\n  top: 12%;\n  bottom: 10%;\n  background: linear-gradient(transparent,#ffddc46b,transparent);\n}\n\n.sop-toggle.sop-bloom .petals i:nth-child(even) {\n  background: linear-gradient(95deg,#c9776c,#ffb9a0 48%,#e79b8b 52%,#d17e78);\n}\n\n.sop-toggle.sop-bloom .flower-inner i {\n  position: absolute;\n  left: -12px;\n  bottom: -6px;\n  width: 24px;\n  height: 42px;\n  border-radius: 55% 50% 40% 40%;\n  background: linear-gradient(90deg,#c68368,#ffd7aa 49%,#f3b18f 52%,#ba7961);\n  transform-origin: 50% calc(100% - 6px);\n  transform: rotate(calc(var(--i)*45deg + 15deg)) rotateX(calc((1 - var(--p))*78deg)) scale(calc(.4 + var(--p)*.6));\n  box-shadow: 1px 2px 4px #793c4440;\n}\n\n.sop-toggle.sop-bloom .flower-core {\n  position: absolute;\n  inset: -23px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 26%,#ffdfa7,#eaaa72 30%,#b46e48 72%,#804531);\n  box-shadow: inset 1px 2px 3px #ffe3a860,2px 4px 6px #60362290;\n  transform: scale(calc(1 - var(--p)*.26));\n}\n\n.sop-toggle.sop-bloom .flower-core:after {\n  content: \"\";\n  position: absolute;\n  inset: 4px;\n  border-radius: 50%;\n  background-image: radial-gradient(#5136209c .7px,transparent 1.3px);\n  background-size: 4px 4px;\n  opacity: var(--p);\n  mask-image: radial-gradient(circle,#000 35%,transparent 68%);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/bloom/react/Example.jsx","code":"import React, { useState } from 'react';\nimport BloomToggle from './BloomToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cBloomToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/bloom/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/bloom/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"bloom\",\n    \"name\": \"Bloom\",\n    \"initial\": true,\n    \"stiffness\": 180,\n    \"damping\": 19,\n    \"tone\": 660,\n    \"travel\": 159\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for BloomToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/bloom/styles.css","code":"/* bloom: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-bloom {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-bloom[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-bloom:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-bloom:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-bloom {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-bloom:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-bloom:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-bloom .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-bloom .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-bloom .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-bloom .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-bloom .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-bloom .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-bloom .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-bloom,\n  .sop-toggle.sop-bloom * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-bloom {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-bloom .bloom-track {\n  position: absolute;\n  left: 16px;\n  top: 32px;\n  width: 248px;\n  height: 80px;\n  border-radius: 50px;\n  background: linear-gradient(165deg,#231b1c,#372528 45%,#231b1d);\n  border: 1px solid #64434478;\n  box-shadow: inset 0 3px 9px #08070999,0 8px 18px #0005,inset 0 -1px 1px #b0747036;\n}\n\n.sop-toggle.sop-bloom .bloom-vines {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #a8766140;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-bloom .bloom-word {\n  position: absolute;\n  top: 34px;\n  left: 45px;\n  font: 8px var(--sop-mono);\n  letter-spacing: 3px;\n  color: #bc8983;\n  opacity: calc(var(--p)*.6);\n}\n\n.sop-toggle.sop-bloom .flower {\n  position: absolute;\n  left: 57px;\n  top: 72px;\n  width: 0;\n  height: 0;\n  transform: translateX(calc(var(--p)*159px)) rotate(calc(var(--p)*85deg));\n  filter: drop-shadow(2px 9px 6px #0007);\n}\n\n.sop-toggle.sop-bloom .petals,\n.sop-toggle.sop-bloom .flower-inner {\n  position: absolute;\n  inset: 0;\n  perspective: 250px;\n}\n\n.sop-toggle.sop-bloom .petals i {\n  position: absolute;\n  left: -16px;\n  bottom: -8px;\n  width: 32px;\n  height: 65px;\n  border-radius: 60% 50% 45% 45%;\n  background: linear-gradient(100deg,#bf625e,#ed9c88 28%,#ffccb0 51%,#e99887 55%,#ba6965);\n  box-shadow: inset 1px 0 2px #fce6c96e,inset -1px 0 2px #7c413757,1px 1px 4px #662e3744;\n  transform-origin: 50% calc(100% - 8px);\n  transform: rotate(calc(var(--i)*30deg)) translateY(calc(var(--p)*-3px)) rotateX(calc((1 - var(--p))*74deg)) scale(calc(.35 + var(--p)*.65));\n  opacity: calc(.1 + var(--p)*.9);\n}\n\n.sop-toggle.sop-bloom .petals i:after {\n  content: \"\";\n  position: absolute;\n  width: 1px;\n  left: 50%;\n  top: 12%;\n  bottom: 10%;\n  background: linear-gradient(transparent,#ffddc46b,transparent);\n}\n\n.sop-toggle.sop-bloom .petals i:nth-child(even) {\n  background: linear-gradient(95deg,#c9776c,#ffb9a0 48%,#e79b8b 52%,#d17e78);\n}\n\n.sop-toggle.sop-bloom .flower-inner i {\n  position: absolute;\n  left: -12px;\n  bottom: -6px;\n  width: 24px;\n  height: 42px;\n  border-radius: 55% 50% 40% 40%;\n  background: linear-gradient(90deg,#c68368,#ffd7aa 49%,#f3b18f 52%,#ba7961);\n  transform-origin: 50% calc(100% - 6px);\n  transform: rotate(calc(var(--i)*45deg + 15deg)) rotateX(calc((1 - var(--p))*78deg)) scale(calc(.4 + var(--p)*.6));\n  box-shadow: 1px 2px 4px #793c4440;\n}\n\n.sop-toggle.sop-bloom .flower-core {\n  position: absolute;\n  inset: -23px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 26%,#ffdfa7,#eaaa72 30%,#b46e48 72%,#804531);\n  box-shadow: inset 1px 2px 3px #ffe3a860,2px 4px 6px #60362290;\n  transform: scale(calc(1 - var(--p)*.26));\n}\n\n.sop-toggle.sop-bloom .flower-core:after {\n  content: \"\";\n  position: absolute;\n  inset: 4px;\n  border-radius: 50%;\n  background-image: radial-gradient(#5136209c .7px,transparent 1.3px);\n  background-size: 4px 4px;\n  opacity: var(--p);\n  mask-image: radial-gradient(circle,#000 35%,transparent 68%);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/bloom/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-bloom');\nif (!element) throw new Error('The Bloom root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/bloom/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/bloom/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Bloom\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/bloom/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"bloom\",\n    \"name\": \"Bloom\",\n    \"initial\": true,\n    \"stiffness\": 180,\n    \"damping\": 19,\n    \"tone\": 660,\n    \"travel\": 159\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for BloomToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/bloom/styles.css","code":"/* bloom: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-bloom {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-bloom[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-bloom:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-bloom:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-bloom {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-bloom:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-bloom:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-bloom .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-bloom .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-bloom .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-bloom .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-bloom .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-bloom .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-bloom .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-bloom,\n  .sop-toggle.sop-bloom * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-bloom {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-bloom .bloom-track {\n  position: absolute;\n  left: 16px;\n  top: 32px;\n  width: 248px;\n  height: 80px;\n  border-radius: 50px;\n  background: linear-gradient(165deg,#231b1c,#372528 45%,#231b1d);\n  border: 1px solid #64434478;\n  box-shadow: inset 0 3px 9px #08070999,0 8px 18px #0005,inset 0 -1px 1px #b0747036;\n}\n\n.sop-toggle.sop-bloom .bloom-vines {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #a8766140;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-bloom .bloom-word {\n  position: absolute;\n  top: 34px;\n  left: 45px;\n  font: 8px var(--sop-mono);\n  letter-spacing: 3px;\n  color: #bc8983;\n  opacity: calc(var(--p)*.6);\n}\n\n.sop-toggle.sop-bloom .flower {\n  position: absolute;\n  left: 57px;\n  top: 72px;\n  width: 0;\n  height: 0;\n  transform: translateX(calc(var(--p)*159px)) rotate(calc(var(--p)*85deg));\n  filter: drop-shadow(2px 9px 6px #0007);\n}\n\n.sop-toggle.sop-bloom .petals,\n.sop-toggle.sop-bloom .flower-inner {\n  position: absolute;\n  inset: 0;\n  perspective: 250px;\n}\n\n.sop-toggle.sop-bloom .petals i {\n  position: absolute;\n  left: -16px;\n  bottom: -8px;\n  width: 32px;\n  height: 65px;\n  border-radius: 60% 50% 45% 45%;\n  background: linear-gradient(100deg,#bf625e,#ed9c88 28%,#ffccb0 51%,#e99887 55%,#ba6965);\n  box-shadow: inset 1px 0 2px #fce6c96e,inset -1px 0 2px #7c413757,1px 1px 4px #662e3744;\n  transform-origin: 50% calc(100% - 8px);\n  transform: rotate(calc(var(--i)*30deg)) translateY(calc(var(--p)*-3px)) rotateX(calc((1 - var(--p))*74deg)) scale(calc(.35 + var(--p)*.65));\n  opacity: calc(.1 + var(--p)*.9);\n}\n\n.sop-toggle.sop-bloom .petals i:after {\n  content: \"\";\n  position: absolute;\n  width: 1px;\n  left: 50%;\n  top: 12%;\n  bottom: 10%;\n  background: linear-gradient(transparent,#ffddc46b,transparent);\n}\n\n.sop-toggle.sop-bloom .petals i:nth-child(even) {\n  background: linear-gradient(95deg,#c9776c,#ffb9a0 48%,#e79b8b 52%,#d17e78);\n}\n\n.sop-toggle.sop-bloom .flower-inner i {\n  position: absolute;\n  left: -12px;\n  bottom: -6px;\n  width: 24px;\n  height: 42px;\n  border-radius: 55% 50% 40% 40%;\n  background: linear-gradient(90deg,#c68368,#ffd7aa 49%,#f3b18f 52%,#ba7961);\n  transform-origin: 50% calc(100% - 6px);\n  transform: rotate(calc(var(--i)*45deg + 15deg)) rotateX(calc((1 - var(--p))*78deg)) scale(calc(.4 + var(--p)*.6));\n  box-shadow: 1px 2px 4px #793c4440;\n}\n\n.sop-toggle.sop-bloom .flower-core {\n  position: absolute;\n  inset: -23px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 26%,#ffdfa7,#eaaa72 30%,#b46e48 72%,#804531);\n  box-shadow: inset 1px 2px 3px #ffe3a860,2px 4px 6px #60362290;\n  transform: scale(calc(1 - var(--p)*.26));\n}\n\n.sop-toggle.sop-bloom .flower-core:after {\n  content: \"\";\n  position: absolute;\n  inset: 4px;\n  border-radius: 50%;\n  background-image: radial-gradient(#5136209c .7px,transparent 1.3px);\n  background-size: 4px 4px;\n  opacity: var(--p);\n  mask-image: radial-gradient(circle,#000 35%,transparent 68%);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/bloom/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-bloom');\nif (!element)\n    throw new Error('The Bloom root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/bloom/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/bloom/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Bloom\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Bloom — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Bloom · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"true\" aria-label=\"Bloom トグル\" class=\"sop-toggle sop-bloom\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"bloom-track\">\u003csvg class=\"bloom-vines\" viewbox=\"0 0 248 80\">\u003cpath d=\"M25 40h170M72 40q12-18 35-16-8 19-35 16m18 0q16 19 38 16-11-19-38-16m52 0q8-15 29-14-5 15-29 14\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"bloom-word\">GROW\u003c/span>\u003c/span>\u003cspan class=\"flower\">\u003cspan class=\"petals\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003ci style=\"--i:10\">\u003c/i>\u003ci style=\"--i:11\">\u003c/i>\u003c/span>\u003cspan class=\"flower-inner\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003c/span>\u003cspan class=\"flower-core\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* bloom: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-bloom {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-bloom[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-bloom:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-bloom:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-bloom {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-bloom:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-bloom:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-bloom .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-bloom .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-bloom .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-bloom .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-bloom .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-bloom .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-bloom .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-bloom .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-bloom,\n  .sop-toggle.sop-bloom * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-bloom {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-bloom .bloom-track {\n  position: absolute;\n  left: 16px;\n  top: 32px;\n  width: 248px;\n  height: 80px;\n  border-radius: 50px;\n  background: linear-gradient(165deg,#231b1c,#372528 45%,#231b1d);\n  border: 1px solid #64434478;\n  box-shadow: inset 0 3px 9px #08070999,0 8px 18px #0005,inset 0 -1px 1px #b0747036;\n}\n\n.sop-toggle.sop-bloom .bloom-vines {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  color: #a8766140;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-bloom .bloom-word {\n  position: absolute;\n  top: 34px;\n  left: 45px;\n  font: 8px var(--sop-mono);\n  letter-spacing: 3px;\n  color: #bc8983;\n  opacity: calc(var(--p)*.6);\n}\n\n.sop-toggle.sop-bloom .flower {\n  position: absolute;\n  left: 57px;\n  top: 72px;\n  width: 0;\n  height: 0;\n  transform: translateX(calc(var(--p)*159px)) rotate(calc(var(--p)*85deg));\n  filter: drop-shadow(2px 9px 6px #0007);\n}\n\n.sop-toggle.sop-bloom .petals,\n.sop-toggle.sop-bloom .flower-inner {\n  position: absolute;\n  inset: 0;\n  perspective: 250px;\n}\n\n.sop-toggle.sop-bloom .petals i {\n  position: absolute;\n  left: -16px;\n  bottom: -8px;\n  width: 32px;\n  height: 65px;\n  border-radius: 60% 50% 45% 45%;\n  background: linear-gradient(100deg,#bf625e,#ed9c88 28%,#ffccb0 51%,#e99887 55%,#ba6965);\n  box-shadow: inset 1px 0 2px #fce6c96e,inset -1px 0 2px #7c413757,1px 1px 4px #662e3744;\n  transform-origin: 50% calc(100% - 8px);\n  transform: rotate(calc(var(--i)*30deg)) translateY(calc(var(--p)*-3px)) rotateX(calc((1 - var(--p))*74deg)) scale(calc(.35 + var(--p)*.65));\n  opacity: calc(.1 + var(--p)*.9);\n}\n\n.sop-toggle.sop-bloom .petals i:after {\n  content: \"\";\n  position: absolute;\n  width: 1px;\n  left: 50%;\n  top: 12%;\n  bottom: 10%;\n  background: linear-gradient(transparent,#ffddc46b,transparent);\n}\n\n.sop-toggle.sop-bloom .petals i:nth-child(even) {\n  background: linear-gradient(95deg,#c9776c,#ffb9a0 48%,#e79b8b 52%,#d17e78);\n}\n\n.sop-toggle.sop-bloom .flower-inner i {\n  position: absolute;\n  left: -12px;\n  bottom: -6px;\n  width: 24px;\n  height: 42px;\n  border-radius: 55% 50% 40% 40%;\n  background: linear-gradient(90deg,#c68368,#ffd7aa 49%,#f3b18f 52%,#ba7961);\n  transform-origin: 50% calc(100% - 6px);\n  transform: rotate(calc(var(--i)*45deg + 15deg)) rotateX(calc((1 - var(--p))*78deg)) scale(calc(.4 + var(--p)*.6));\n  box-shadow: 1px 2px 4px #793c4440;\n}\n\n.sop-toggle.sop-bloom .flower-core {\n  position: absolute;\n  inset: -23px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 26%,#ffdfa7,#eaaa72 30%,#b46e48 72%,#804531);\n  box-shadow: inset 1px 2px 3px #ffe3a860,2px 4px 6px #60362290;\n  transform: scale(calc(1 - var(--p)*.26));\n}\n\n.sop-toggle.sop-bloom .flower-core:after {\n  content: \"\";\n  position: absolute;\n  inset: 4px;\n  border-radius: 50%;\n  background-image: radial-gradient(#5136209c .7px,transparent 1.3px);\n  background-size: 4px 4px;\n  opacity: var(--p);\n  mask-image: radial-gradient(circle,#000 35%,transparent 68%);\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/bloom/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/bloom/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/bloom/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"bloom\",\n    \"name\": \"Bloom\",\n    \"initial\": true,\n    \"stiffness\": 180,\n    \"damping\": 19,\n    \"tone\": 660,\n    \"travel\": 159\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for BloomToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/bloom/demo/main.ts\");})();\n"}},{"id":"volt","name":"Volt","category":"toggles","order":5,"version":"2.3.0","tagline":"小さな、覚醒。","description":"接点が離れ、ガラス管の中で電光が走る。","material":"CONTAINED ENERGY","motion":"IGNITE","accent":"132,202,247","initial":false,"componentName":"VoltToggle","tags":["Volt","volt","トグル","CONTAINED ENERGY","接点が離れ、ガラス管の中で電光が走る。"],"config":{"id":"volt","name":"Volt","initial":false,"stiffness":330,"damping":23,"tone":110,"travel":173},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Volt / 2.3.0\n\n接点が離れ、ガラス管の中で電光が走る。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`VoltToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/volt/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/volt/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/volt/react/VoltToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/volt/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/volt/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/volt/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Volt — 再現仕様\n\n接点が離れ、ガラス管の中で電光が走る。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 173px、ばねの剛性 330、減衰 23。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-volt` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 274px;\nheight: 118px;\nleft: 77px;\ntop: -6px;\nleft: 7px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #69787e, #839aa055, #49647422, #0b1319b3, #0b1721, #264e6326, #bfe6fa22, #99ccea27, #0007, #805d3b, #bf873230, #bddfff42, #d2e9f64a, #d3f3ff0a, #8ce4f406, #0006, #373b3b, #969489, #424b4c, #a7aaa0, #353c3e, #0008, #d7e2e399, #24292a, #293c47, #ccdde0, #647d88, #263a46, #a6bdc3, #203641, #a9c3cc8c, #000c, #48575f, #12212a, #91b3bc66, #000, #8acaff, #9adbff\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/volt/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/volt/react/VoltToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"volt\",\n    \"name\": \"Volt\",\n    \"initial\": false,\n    \"stiffness\": 330,\n    \"damping\": 23,\n    \"tone\": 110,\n    \"travel\": 173\n};\n/** 接点が離れ、ガラス管の中で電光が走る。 */\nexport default function VoltToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Volt トグル'} aria-checked={checked} className={`sop-toggle sop-volt ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan className=\"volt-tube\">\u003cspan className=\"volt-filament\">\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan className=\"volt-gloss\">\u003c/span>\u003cspan className=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan className=\"volt-cap cap-left\">\u003c/span>\u003cspan className=\"volt-cap cap-right\">\u003c/span>\u003cspan className=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/volt/styles.css","code":"/* volt: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-volt {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-volt[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-volt:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-volt:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-volt {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-volt:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-volt:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-volt .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-volt .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-volt .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-volt .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-volt .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-volt .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-volt,\n  .sop-toggle.sop-volt * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-volt {\n  width: 274px;\n  height: 118px;\n}\n\n.sop-toggle.sop-volt .volt-label {\n  position: absolute;\n  left: 77px;\n  top: -6px;\n  font-size: 6px;\n  letter-spacing: 1.2px;\n  color: #69787e;\n}\n\n.sop-toggle.sop-volt .volt-tube {\n  position: absolute;\n  left: 7px;\n  top: 16px;\n  width: 260px;\n  height: 88px;\n  border-radius: 45px;\n  border: 1px solid #839aa055;\n  background: linear-gradient(180deg,#49647422,#0b1319b3 25%,#0b1721 58%,#264e6326);\n  box-shadow: inset 0 2px 2px #bfe6fa22,inset 0 -2px 2px #99ccea27,0 10px 23px #0007;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-volt .volt-filament {\n  position: absolute;\n  left: 16px;\n  right: 16px;\n  top: 42px;\n  height: 2px;\n  background: #805d3b;\n  box-shadow: 0 0 4px #bf873230;\n  opacity: calc(.5 + var(--p)*.5);\n}\n\n.sop-toggle.sop-volt .volt-gloss {\n  position: absolute;\n  inset: 3px 10px;\n  border-top: 2px solid #bddfff42;\n  border-bottom: 1px solid #d2e9f64a;\n  border-radius: 50px;\n  z-index: 4;\n  background: linear-gradient(#d3f3ff0a,transparent 24%,transparent 72%,#8ce4f406);\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .volt-cap {\n  position: absolute;\n  top: 22px;\n  width: 27px;\n  height: 76px;\n  border-radius: 7px;\n  background: repeating-linear-gradient(0deg,transparent 0 3px,#0006 3px 4px),linear-gradient(90deg,#373b3b,#969489 22%,#424b4c 45%,#a7aaa0 67%,#353c3e);\n  box-shadow: 0 7px 10px #0008,inset 0 1px 1px #d7e2e399;\n  z-index: 5;\n  border: 1px solid #24292a;\n}\n\n.sop-toggle.sop-volt .cap-left {\n  left: 0;\n}\n\n.sop-toggle.sop-volt .cap-right {\n  right: 0;\n}\n\n.sop-toggle.sop-volt .volt-electrode {\n  position: absolute;\n  left: 20px;\n  top: 21px;\n  width: 44px;\n  height: 44px;\n  border-radius: 50%;\n  background: conic-gradient(from -60deg,#293c47,#ccdde0,#647d88,#263a46,#a6bdc3,#203641);\n  border: 1px solid #a9c3cc8c;\n  box-shadow: 0 3px 4px #000c;\n  transform: translateX(calc(var(--p)*173px)) rotate(calc(var(--p)*70deg));\n  z-index: 3;\n}\n\n.sop-toggle.sop-volt .volt-electrode i {\n  position: absolute;\n  inset: 6px;\n  background: radial-gradient(circle at 35% 25%,#48575f,#12212a 70%);\n  border-radius: 50%;\n  border: 1px solid #91b3bc66;\n  box-shadow: inset 1px 1px 3px #000;\n}\n\n.sop-toggle.sop-volt .volt-electrode i:before {\n  content: \"\";\n  position: absolute;\n  left: 11px;\n  top: 5px;\n  width: 8px;\n  height: 20px;\n  background: #8acaff;\n  clip-path: polygon(45% 0,100% 0,65% 40%,100% 40%,15% 100%,37% 53%,0 53%);\n  opacity: calc(.3 + var(--p)*.7);\n  filter: drop-shadow(0 0 3px #9adbff);\n}\n\n.sop-toggle.sop-volt .volt-badge {\n  position: absolute;\n  left: 111px;\n  bottom: -9px;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.5px;\n  color: #77838a;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/volt/react/Example.tsx","code":"import React, { useState } from 'react';\nimport VoltToggle from './VoltToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cVoltToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/volt/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/volt/react/VoltToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"volt\",\n    \"name\": \"Volt\",\n    \"initial\": false,\n    \"stiffness\": 330,\n    \"damping\": 23,\n    \"tone\": 110,\n    \"travel\": 173\n};\n/** 接点が離れ、ガラス管の中で電光が走る。 */\nexport default function VoltToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Volt トグル'} aria-checked={checked} className={`sop-toggle sop-volt ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan className=\"volt-tube\">\u003cspan className=\"volt-filament\">\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan className=\"volt-gloss\">\u003c/span>\u003cspan className=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan className=\"volt-cap cap-left\">\u003c/span>\u003cspan className=\"volt-cap cap-right\">\u003c/span>\u003cspan className=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/volt/styles.css","code":"/* volt: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-volt {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-volt[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-volt:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-volt:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-volt {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-volt:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-volt:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-volt .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-volt .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-volt .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-volt .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-volt .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-volt .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-volt,\n  .sop-toggle.sop-volt * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-volt {\n  width: 274px;\n  height: 118px;\n}\n\n.sop-toggle.sop-volt .volt-label {\n  position: absolute;\n  left: 77px;\n  top: -6px;\n  font-size: 6px;\n  letter-spacing: 1.2px;\n  color: #69787e;\n}\n\n.sop-toggle.sop-volt .volt-tube {\n  position: absolute;\n  left: 7px;\n  top: 16px;\n  width: 260px;\n  height: 88px;\n  border-radius: 45px;\n  border: 1px solid #839aa055;\n  background: linear-gradient(180deg,#49647422,#0b1319b3 25%,#0b1721 58%,#264e6326);\n  box-shadow: inset 0 2px 2px #bfe6fa22,inset 0 -2px 2px #99ccea27,0 10px 23px #0007;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-volt .volt-filament {\n  position: absolute;\n  left: 16px;\n  right: 16px;\n  top: 42px;\n  height: 2px;\n  background: #805d3b;\n  box-shadow: 0 0 4px #bf873230;\n  opacity: calc(.5 + var(--p)*.5);\n}\n\n.sop-toggle.sop-volt .volt-gloss {\n  position: absolute;\n  inset: 3px 10px;\n  border-top: 2px solid #bddfff42;\n  border-bottom: 1px solid #d2e9f64a;\n  border-radius: 50px;\n  z-index: 4;\n  background: linear-gradient(#d3f3ff0a,transparent 24%,transparent 72%,#8ce4f406);\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .volt-cap {\n  position: absolute;\n  top: 22px;\n  width: 27px;\n  height: 76px;\n  border-radius: 7px;\n  background: repeating-linear-gradient(0deg,transparent 0 3px,#0006 3px 4px),linear-gradient(90deg,#373b3b,#969489 22%,#424b4c 45%,#a7aaa0 67%,#353c3e);\n  box-shadow: 0 7px 10px #0008,inset 0 1px 1px #d7e2e399;\n  z-index: 5;\n  border: 1px solid #24292a;\n}\n\n.sop-toggle.sop-volt .cap-left {\n  left: 0;\n}\n\n.sop-toggle.sop-volt .cap-right {\n  right: 0;\n}\n\n.sop-toggle.sop-volt .volt-electrode {\n  position: absolute;\n  left: 20px;\n  top: 21px;\n  width: 44px;\n  height: 44px;\n  border-radius: 50%;\n  background: conic-gradient(from -60deg,#293c47,#ccdde0,#647d88,#263a46,#a6bdc3,#203641);\n  border: 1px solid #a9c3cc8c;\n  box-shadow: 0 3px 4px #000c;\n  transform: translateX(calc(var(--p)*173px)) rotate(calc(var(--p)*70deg));\n  z-index: 3;\n}\n\n.sop-toggle.sop-volt .volt-electrode i {\n  position: absolute;\n  inset: 6px;\n  background: radial-gradient(circle at 35% 25%,#48575f,#12212a 70%);\n  border-radius: 50%;\n  border: 1px solid #91b3bc66;\n  box-shadow: inset 1px 1px 3px #000;\n}\n\n.sop-toggle.sop-volt .volt-electrode i:before {\n  content: \"\";\n  position: absolute;\n  left: 11px;\n  top: 5px;\n  width: 8px;\n  height: 20px;\n  background: #8acaff;\n  clip-path: polygon(45% 0,100% 0,65% 40%,100% 40%,15% 100%,37% 53%,0 53%);\n  opacity: calc(.3 + var(--p)*.7);\n  filter: drop-shadow(0 0 3px #9adbff);\n}\n\n.sop-toggle.sop-volt .volt-badge {\n  position: absolute;\n  left: 111px;\n  bottom: -9px;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.5px;\n  color: #77838a;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/volt/react/Example.jsx","code":"import React, { useState } from 'react';\nimport VoltToggle from './VoltToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cVoltToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/volt/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/volt/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"volt\",\n    \"name\": \"Volt\",\n    \"initial\": false,\n    \"stiffness\": 330,\n    \"damping\": 23,\n    \"tone\": 110,\n    \"travel\": 173\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for VoltToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/volt/styles.css","code":"/* volt: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-volt {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-volt[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-volt:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-volt:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-volt {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-volt:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-volt:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-volt .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-volt .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-volt .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-volt .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-volt .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-volt .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-volt,\n  .sop-toggle.sop-volt * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-volt {\n  width: 274px;\n  height: 118px;\n}\n\n.sop-toggle.sop-volt .volt-label {\n  position: absolute;\n  left: 77px;\n  top: -6px;\n  font-size: 6px;\n  letter-spacing: 1.2px;\n  color: #69787e;\n}\n\n.sop-toggle.sop-volt .volt-tube {\n  position: absolute;\n  left: 7px;\n  top: 16px;\n  width: 260px;\n  height: 88px;\n  border-radius: 45px;\n  border: 1px solid #839aa055;\n  background: linear-gradient(180deg,#49647422,#0b1319b3 25%,#0b1721 58%,#264e6326);\n  box-shadow: inset 0 2px 2px #bfe6fa22,inset 0 -2px 2px #99ccea27,0 10px 23px #0007;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-volt .volt-filament {\n  position: absolute;\n  left: 16px;\n  right: 16px;\n  top: 42px;\n  height: 2px;\n  background: #805d3b;\n  box-shadow: 0 0 4px #bf873230;\n  opacity: calc(.5 + var(--p)*.5);\n}\n\n.sop-toggle.sop-volt .volt-gloss {\n  position: absolute;\n  inset: 3px 10px;\n  border-top: 2px solid #bddfff42;\n  border-bottom: 1px solid #d2e9f64a;\n  border-radius: 50px;\n  z-index: 4;\n  background: linear-gradient(#d3f3ff0a,transparent 24%,transparent 72%,#8ce4f406);\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .volt-cap {\n  position: absolute;\n  top: 22px;\n  width: 27px;\n  height: 76px;\n  border-radius: 7px;\n  background: repeating-linear-gradient(0deg,transparent 0 3px,#0006 3px 4px),linear-gradient(90deg,#373b3b,#969489 22%,#424b4c 45%,#a7aaa0 67%,#353c3e);\n  box-shadow: 0 7px 10px #0008,inset 0 1px 1px #d7e2e399;\n  z-index: 5;\n  border: 1px solid #24292a;\n}\n\n.sop-toggle.sop-volt .cap-left {\n  left: 0;\n}\n\n.sop-toggle.sop-volt .cap-right {\n  right: 0;\n}\n\n.sop-toggle.sop-volt .volt-electrode {\n  position: absolute;\n  left: 20px;\n  top: 21px;\n  width: 44px;\n  height: 44px;\n  border-radius: 50%;\n  background: conic-gradient(from -60deg,#293c47,#ccdde0,#647d88,#263a46,#a6bdc3,#203641);\n  border: 1px solid #a9c3cc8c;\n  box-shadow: 0 3px 4px #000c;\n  transform: translateX(calc(var(--p)*173px)) rotate(calc(var(--p)*70deg));\n  z-index: 3;\n}\n\n.sop-toggle.sop-volt .volt-electrode i {\n  position: absolute;\n  inset: 6px;\n  background: radial-gradient(circle at 35% 25%,#48575f,#12212a 70%);\n  border-radius: 50%;\n  border: 1px solid #91b3bc66;\n  box-shadow: inset 1px 1px 3px #000;\n}\n\n.sop-toggle.sop-volt .volt-electrode i:before {\n  content: \"\";\n  position: absolute;\n  left: 11px;\n  top: 5px;\n  width: 8px;\n  height: 20px;\n  background: #8acaff;\n  clip-path: polygon(45% 0,100% 0,65% 40%,100% 40%,15% 100%,37% 53%,0 53%);\n  opacity: calc(.3 + var(--p)*.7);\n  filter: drop-shadow(0 0 3px #9adbff);\n}\n\n.sop-toggle.sop-volt .volt-badge {\n  position: absolute;\n  left: 111px;\n  bottom: -9px;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.5px;\n  color: #77838a;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/volt/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-volt');\nif (!element) throw new Error('The Volt root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/volt/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/volt/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Volt\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/volt/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"volt\",\n    \"name\": \"Volt\",\n    \"initial\": false,\n    \"stiffness\": 330,\n    \"damping\": 23,\n    \"tone\": 110,\n    \"travel\": 173\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for VoltToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/volt/styles.css","code":"/* volt: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-volt {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-volt[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-volt:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-volt:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-volt {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-volt:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-volt:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-volt .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-volt .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-volt .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-volt .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-volt .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-volt .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-volt,\n  .sop-toggle.sop-volt * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-volt {\n  width: 274px;\n  height: 118px;\n}\n\n.sop-toggle.sop-volt .volt-label {\n  position: absolute;\n  left: 77px;\n  top: -6px;\n  font-size: 6px;\n  letter-spacing: 1.2px;\n  color: #69787e;\n}\n\n.sop-toggle.sop-volt .volt-tube {\n  position: absolute;\n  left: 7px;\n  top: 16px;\n  width: 260px;\n  height: 88px;\n  border-radius: 45px;\n  border: 1px solid #839aa055;\n  background: linear-gradient(180deg,#49647422,#0b1319b3 25%,#0b1721 58%,#264e6326);\n  box-shadow: inset 0 2px 2px #bfe6fa22,inset 0 -2px 2px #99ccea27,0 10px 23px #0007;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-volt .volt-filament {\n  position: absolute;\n  left: 16px;\n  right: 16px;\n  top: 42px;\n  height: 2px;\n  background: #805d3b;\n  box-shadow: 0 0 4px #bf873230;\n  opacity: calc(.5 + var(--p)*.5);\n}\n\n.sop-toggle.sop-volt .volt-gloss {\n  position: absolute;\n  inset: 3px 10px;\n  border-top: 2px solid #bddfff42;\n  border-bottom: 1px solid #d2e9f64a;\n  border-radius: 50px;\n  z-index: 4;\n  background: linear-gradient(#d3f3ff0a,transparent 24%,transparent 72%,#8ce4f406);\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .volt-cap {\n  position: absolute;\n  top: 22px;\n  width: 27px;\n  height: 76px;\n  border-radius: 7px;\n  background: repeating-linear-gradient(0deg,transparent 0 3px,#0006 3px 4px),linear-gradient(90deg,#373b3b,#969489 22%,#424b4c 45%,#a7aaa0 67%,#353c3e);\n  box-shadow: 0 7px 10px #0008,inset 0 1px 1px #d7e2e399;\n  z-index: 5;\n  border: 1px solid #24292a;\n}\n\n.sop-toggle.sop-volt .cap-left {\n  left: 0;\n}\n\n.sop-toggle.sop-volt .cap-right {\n  right: 0;\n}\n\n.sop-toggle.sop-volt .volt-electrode {\n  position: absolute;\n  left: 20px;\n  top: 21px;\n  width: 44px;\n  height: 44px;\n  border-radius: 50%;\n  background: conic-gradient(from -60deg,#293c47,#ccdde0,#647d88,#263a46,#a6bdc3,#203641);\n  border: 1px solid #a9c3cc8c;\n  box-shadow: 0 3px 4px #000c;\n  transform: translateX(calc(var(--p)*173px)) rotate(calc(var(--p)*70deg));\n  z-index: 3;\n}\n\n.sop-toggle.sop-volt .volt-electrode i {\n  position: absolute;\n  inset: 6px;\n  background: radial-gradient(circle at 35% 25%,#48575f,#12212a 70%);\n  border-radius: 50%;\n  border: 1px solid #91b3bc66;\n  box-shadow: inset 1px 1px 3px #000;\n}\n\n.sop-toggle.sop-volt .volt-electrode i:before {\n  content: \"\";\n  position: absolute;\n  left: 11px;\n  top: 5px;\n  width: 8px;\n  height: 20px;\n  background: #8acaff;\n  clip-path: polygon(45% 0,100% 0,65% 40%,100% 40%,15% 100%,37% 53%,0 53%);\n  opacity: calc(.3 + var(--p)*.7);\n  filter: drop-shadow(0 0 3px #9adbff);\n}\n\n.sop-toggle.sop-volt .volt-badge {\n  position: absolute;\n  left: 111px;\n  bottom: -9px;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.5px;\n  color: #77838a;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/volt/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-volt');\nif (!element)\n    throw new Error('The Volt root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/volt/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/volt/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Volt\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Volt — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Volt · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"false\" aria-label=\"Volt トグル\" class=\"sop-toggle sop-volt\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"volt-label mono\">01   /   HIGH VOLTAGE\u003c/span>\u003cspan class=\"volt-tube\">\u003cspan class=\"volt-filament\">\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"176\" width=\"520\">\u003c/canvas>\u003cspan class=\"volt-gloss\">\u003c/span>\u003cspan class=\"volt-electrode\">\u003ci>\u003c/i>\u003c/span>\u003c/span>\u003cspan class=\"volt-cap cap-left\">\u003c/span>\u003cspan class=\"volt-cap cap-right\">\u003c/span>\u003cspan class=\"volt-badge\">HV — 220\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* volt: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-volt {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-volt[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-volt:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-volt:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-volt {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-volt:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-volt:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-volt .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-volt .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-volt .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-volt .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-volt .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-volt .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-volt .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-volt,\n  .sop-toggle.sop-volt * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-volt {\n  width: 274px;\n  height: 118px;\n}\n\n.sop-toggle.sop-volt .volt-label {\n  position: absolute;\n  left: 77px;\n  top: -6px;\n  font-size: 6px;\n  letter-spacing: 1.2px;\n  color: #69787e;\n}\n\n.sop-toggle.sop-volt .volt-tube {\n  position: absolute;\n  left: 7px;\n  top: 16px;\n  width: 260px;\n  height: 88px;\n  border-radius: 45px;\n  border: 1px solid #839aa055;\n  background: linear-gradient(180deg,#49647422,#0b1319b3 25%,#0b1721 58%,#264e6326);\n  box-shadow: inset 0 2px 2px #bfe6fa22,inset 0 -2px 2px #99ccea27,0 10px 23px #0007;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-volt .volt-filament {\n  position: absolute;\n  left: 16px;\n  right: 16px;\n  top: 42px;\n  height: 2px;\n  background: #805d3b;\n  box-shadow: 0 0 4px #bf873230;\n  opacity: calc(.5 + var(--p)*.5);\n}\n\n.sop-toggle.sop-volt .volt-gloss {\n  position: absolute;\n  inset: 3px 10px;\n  border-top: 2px solid #bddfff42;\n  border-bottom: 1px solid #d2e9f64a;\n  border-radius: 50px;\n  z-index: 4;\n  background: linear-gradient(#d3f3ff0a,transparent 24%,transparent 72%,#8ce4f406);\n  pointer-events: none;\n}\n\n.sop-toggle.sop-volt .volt-cap {\n  position: absolute;\n  top: 22px;\n  width: 27px;\n  height: 76px;\n  border-radius: 7px;\n  background: repeating-linear-gradient(0deg,transparent 0 3px,#0006 3px 4px),linear-gradient(90deg,#373b3b,#969489 22%,#424b4c 45%,#a7aaa0 67%,#353c3e);\n  box-shadow: 0 7px 10px #0008,inset 0 1px 1px #d7e2e399;\n  z-index: 5;\n  border: 1px solid #24292a;\n}\n\n.sop-toggle.sop-volt .cap-left {\n  left: 0;\n}\n\n.sop-toggle.sop-volt .cap-right {\n  right: 0;\n}\n\n.sop-toggle.sop-volt .volt-electrode {\n  position: absolute;\n  left: 20px;\n  top: 21px;\n  width: 44px;\n  height: 44px;\n  border-radius: 50%;\n  background: conic-gradient(from -60deg,#293c47,#ccdde0,#647d88,#263a46,#a6bdc3,#203641);\n  border: 1px solid #a9c3cc8c;\n  box-shadow: 0 3px 4px #000c;\n  transform: translateX(calc(var(--p)*173px)) rotate(calc(var(--p)*70deg));\n  z-index: 3;\n}\n\n.sop-toggle.sop-volt .volt-electrode i {\n  position: absolute;\n  inset: 6px;\n  background: radial-gradient(circle at 35% 25%,#48575f,#12212a 70%);\n  border-radius: 50%;\n  border: 1px solid #91b3bc66;\n  box-shadow: inset 1px 1px 3px #000;\n}\n\n.sop-toggle.sop-volt .volt-electrode i:before {\n  content: \"\";\n  position: absolute;\n  left: 11px;\n  top: 5px;\n  width: 8px;\n  height: 20px;\n  background: #8acaff;\n  clip-path: polygon(45% 0,100% 0,65% 40%,100% 40%,15% 100%,37% 53%,0 53%);\n  opacity: calc(.3 + var(--p)*.7);\n  filter: drop-shadow(0 0 3px #9adbff);\n}\n\n.sop-toggle.sop-volt .volt-badge {\n  position: absolute;\n  left: 111px;\n  bottom: -9px;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.5px;\n  color: #77838a;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/volt/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/volt/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/volt/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"volt\",\n    \"name\": \"Volt\",\n    \"initial\": false,\n    \"stiffness\": 330,\n    \"damping\": 23,\n    \"tone\": 110,\n    \"travel\": 173\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for VoltToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/volt/demo/main.ts\");})();\n"}},{"id":"orbit","name":"Orbit","category":"toggles","order":6,"version":"2.3.0","tagline":"引き寄せられる、軌道。","description":"磁性の球が弧を描き、もう一つの極へ。","material":"MAGNETIC FIELD","motion":"REVOLVE","accent":"162,218,185","initial":true,"componentName":"OrbitToggle","tags":["Orbit","orbit","トグル","MAGNETIC FIELD","磁性の球が弧を描き、もう一つの極へ。"],"config":{"id":"orbit","name":"Orbit","initial":true,"stiffness":140,"damping":18,"tone":420,"travel":192},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Orbit / 2.3.0\n\n磁性の球が弧を描き、もう一つの極へ。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`OrbitToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/orbit/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/orbit/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/orbit/react/OrbitToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/orbit/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/orbit/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/orbit/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Orbit — 再現仕様\n\n磁性の球が弧を描き、もう一つの極へ。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 192px、ばねの剛性 140、減衰 18。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-orbit` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 280px;\nheight: 150px;\nwidth: 100%;\nheight: 100%;\nleft: 30px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #000, #0a1510e0, #11191255, #0004, #a2d1af70, #a4c2a6, #d5ead6, #92b89c, #3f6f59, #142c21, #081a12, #a7dfb652, #ecffe98f, #000b, #83d49a14, #e9ffe199, #d8ffe454, #7b9280\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/orbit/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/orbit/react/OrbitToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"orbit\",\n    \"name\": \"Orbit\",\n    \"initial\": true,\n    \"stiffness\": 140,\n    \"damping\": 18,\n    \"tone\": 420,\n    \"travel\": 192\n};\n/** 磁性の球が弧を描き、もう一つの極へ。 */\nexport default function OrbitToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Orbit トグル'} aria-checked={checked} className={`sop-toggle sop-orbit ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"orbit-shadow\">\u003c/span>\u003csvg className=\"orbit-rails\" viewBox=\"0 0 280 150\">\u003cdefs>\u003clinearGradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stopColor=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stopColor=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stopColor=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stopColor=\"#768a7e\">\u003c/stop>\u003c/linearGradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" strokeWidth=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" strokeWidth=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" strokeWidth=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" strokeWidth=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" strokeWidth=\"1\">\u003c/path>\u003c/svg>\u003cspan className=\"orbit-center\">\u003cspan className=\"orbit-cross\">\u003c/span>\u003cspan className=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan className=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan className=\"orbit-end end-a\">0\u003c/span>\u003cspan className=\"orbit-end end-b\">1\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/orbit/styles.css","code":"/* orbit: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-orbit {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-orbit[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-orbit:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-orbit:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-orbit {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-orbit:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-orbit:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-orbit .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-orbit .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-orbit .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-orbit .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-orbit .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-orbit .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-orbit .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-orbit,\n  .sop-toggle.sop-orbit * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-orbit {\n  width: 280px;\n  height: 150px;\n}\n\n.sop-toggle.sop-orbit .orbit-rails {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  filter: drop-shadow(0 4px 3px #000);\n}\n\n.sop-toggle.sop-orbit .orbit-shadow {\n  position: absolute;\n  left: 30px;\n  top: 46px;\n  width: 220px;\n  height: 73px;\n  border-radius: 50%;\n  background: radial-gradient(ellipse,#0a1510e0,#11191255 70%,transparent 75%);\n  box-shadow: 0 12px 18px #0004;\n}\n\n.sop-toggle.sop-orbit .orbit-center {\n  position: absolute;\n  left: 122px;\n  top: 59px;\n  width: 36px;\n  height: 35px;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-orbit .orbit-cross {\n  position: absolute;\n  left: 13px;\n  top: 0;\n  width: 10px;\n  height: 10px;\n  background: linear-gradient(90deg,transparent 4px,#a2d1af70 4px 5px,transparent 5px),linear-gradient(transparent 4px,#a2d1af70 4px 5px,transparent 5px);\n}\n\n.sop-toggle.sop-orbit .orbit-caption {\n  position: absolute;\n  left: 0;\n  top: 21px;\n  width: 36px;\n  text-align: center;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1px;\n  color: #a4c2a6;\n  white-space: nowrap;\n}\n\n.sop-toggle.sop-orbit .orbit-orb {\n  position: absolute;\n  left: var(--ox,44px);\n  top: var(--oy,73px);\n  width: 49px;\n  height: 49px;\n  margin: -24.5px 0 0 -24.5px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 21%,#d5ead6,#92b89c 15%,#3f6f59 36%,#142c21 66%,#081a12 84%);\n  box-shadow: inset -2px -2px 3px #a7dfb652,inset 1px 1px 2px #ecffe98f,2px 8px 7px #000b,0 0 18px #83d49a14;\n}\n\n.sop-toggle.sop-orbit .orbit-orb span {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  border-top: 1px solid #e9ffe199;\n  transform: rotate(-27deg);\n  filter: blur(.4px);\n}\n\n.sop-toggle.sop-orbit .orbit-orb:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 15%,#d8ffe454,transparent 50%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-orbit .orbit-end {\n  position: absolute;\n  top: 108px;\n  font: 7px var(--sop-mono);\n  color: #7b9280;\n}\n\n.sop-toggle.sop-orbit .end-a {\n  left: 42px;\n}\n\n.sop-toggle.sop-orbit .end-b {\n  right: 41px;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/orbit/react/Example.tsx","code":"import React, { useState } from 'react';\nimport OrbitToggle from './OrbitToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cOrbitToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/orbit/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/orbit/react/OrbitToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"orbit\",\n    \"name\": \"Orbit\",\n    \"initial\": true,\n    \"stiffness\": 140,\n    \"damping\": 18,\n    \"tone\": 420,\n    \"travel\": 192\n};\n/** 磁性の球が弧を描き、もう一つの極へ。 */\nexport default function OrbitToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Orbit トグル'} aria-checked={checked} className={`sop-toggle sop-orbit ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"orbit-shadow\">\u003c/span>\u003csvg className=\"orbit-rails\" viewBox=\"0 0 280 150\">\u003cdefs>\u003clinearGradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stopColor=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stopColor=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stopColor=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stopColor=\"#768a7e\">\u003c/stop>\u003c/linearGradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" strokeWidth=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" strokeWidth=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" strokeWidth=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" strokeWidth=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" strokeWidth=\"1\">\u003c/path>\u003c/svg>\u003cspan className=\"orbit-center\">\u003cspan className=\"orbit-cross\">\u003c/span>\u003cspan className=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan className=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan className=\"orbit-end end-a\">0\u003c/span>\u003cspan className=\"orbit-end end-b\">1\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/orbit/styles.css","code":"/* orbit: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-orbit {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-orbit[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-orbit:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-orbit:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-orbit {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-orbit:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-orbit:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-orbit .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-orbit .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-orbit .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-orbit .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-orbit .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-orbit .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-orbit .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-orbit,\n  .sop-toggle.sop-orbit * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-orbit {\n  width: 280px;\n  height: 150px;\n}\n\n.sop-toggle.sop-orbit .orbit-rails {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  filter: drop-shadow(0 4px 3px #000);\n}\n\n.sop-toggle.sop-orbit .orbit-shadow {\n  position: absolute;\n  left: 30px;\n  top: 46px;\n  width: 220px;\n  height: 73px;\n  border-radius: 50%;\n  background: radial-gradient(ellipse,#0a1510e0,#11191255 70%,transparent 75%);\n  box-shadow: 0 12px 18px #0004;\n}\n\n.sop-toggle.sop-orbit .orbit-center {\n  position: absolute;\n  left: 122px;\n  top: 59px;\n  width: 36px;\n  height: 35px;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-orbit .orbit-cross {\n  position: absolute;\n  left: 13px;\n  top: 0;\n  width: 10px;\n  height: 10px;\n  background: linear-gradient(90deg,transparent 4px,#a2d1af70 4px 5px,transparent 5px),linear-gradient(transparent 4px,#a2d1af70 4px 5px,transparent 5px);\n}\n\n.sop-toggle.sop-orbit .orbit-caption {\n  position: absolute;\n  left: 0;\n  top: 21px;\n  width: 36px;\n  text-align: center;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1px;\n  color: #a4c2a6;\n  white-space: nowrap;\n}\n\n.sop-toggle.sop-orbit .orbit-orb {\n  position: absolute;\n  left: var(--ox,44px);\n  top: var(--oy,73px);\n  width: 49px;\n  height: 49px;\n  margin: -24.5px 0 0 -24.5px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 21%,#d5ead6,#92b89c 15%,#3f6f59 36%,#142c21 66%,#081a12 84%);\n  box-shadow: inset -2px -2px 3px #a7dfb652,inset 1px 1px 2px #ecffe98f,2px 8px 7px #000b,0 0 18px #83d49a14;\n}\n\n.sop-toggle.sop-orbit .orbit-orb span {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  border-top: 1px solid #e9ffe199;\n  transform: rotate(-27deg);\n  filter: blur(.4px);\n}\n\n.sop-toggle.sop-orbit .orbit-orb:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 15%,#d8ffe454,transparent 50%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-orbit .orbit-end {\n  position: absolute;\n  top: 108px;\n  font: 7px var(--sop-mono);\n  color: #7b9280;\n}\n\n.sop-toggle.sop-orbit .end-a {\n  left: 42px;\n}\n\n.sop-toggle.sop-orbit .end-b {\n  right: 41px;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/orbit/react/Example.jsx","code":"import React, { useState } from 'react';\nimport OrbitToggle from './OrbitToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cOrbitToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/orbit/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/orbit/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"orbit\",\n    \"name\": \"Orbit\",\n    \"initial\": true,\n    \"stiffness\": 140,\n    \"damping\": 18,\n    \"tone\": 420,\n    \"travel\": 192\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for OrbitToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/orbit/styles.css","code":"/* orbit: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-orbit {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-orbit[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-orbit:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-orbit:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-orbit {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-orbit:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-orbit:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-orbit .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-orbit .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-orbit .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-orbit .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-orbit .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-orbit .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-orbit .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-orbit,\n  .sop-toggle.sop-orbit * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-orbit {\n  width: 280px;\n  height: 150px;\n}\n\n.sop-toggle.sop-orbit .orbit-rails {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  filter: drop-shadow(0 4px 3px #000);\n}\n\n.sop-toggle.sop-orbit .orbit-shadow {\n  position: absolute;\n  left: 30px;\n  top: 46px;\n  width: 220px;\n  height: 73px;\n  border-radius: 50%;\n  background: radial-gradient(ellipse,#0a1510e0,#11191255 70%,transparent 75%);\n  box-shadow: 0 12px 18px #0004;\n}\n\n.sop-toggle.sop-orbit .orbit-center {\n  position: absolute;\n  left: 122px;\n  top: 59px;\n  width: 36px;\n  height: 35px;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-orbit .orbit-cross {\n  position: absolute;\n  left: 13px;\n  top: 0;\n  width: 10px;\n  height: 10px;\n  background: linear-gradient(90deg,transparent 4px,#a2d1af70 4px 5px,transparent 5px),linear-gradient(transparent 4px,#a2d1af70 4px 5px,transparent 5px);\n}\n\n.sop-toggle.sop-orbit .orbit-caption {\n  position: absolute;\n  left: 0;\n  top: 21px;\n  width: 36px;\n  text-align: center;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1px;\n  color: #a4c2a6;\n  white-space: nowrap;\n}\n\n.sop-toggle.sop-orbit .orbit-orb {\n  position: absolute;\n  left: var(--ox,44px);\n  top: var(--oy,73px);\n  width: 49px;\n  height: 49px;\n  margin: -24.5px 0 0 -24.5px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 21%,#d5ead6,#92b89c 15%,#3f6f59 36%,#142c21 66%,#081a12 84%);\n  box-shadow: inset -2px -2px 3px #a7dfb652,inset 1px 1px 2px #ecffe98f,2px 8px 7px #000b,0 0 18px #83d49a14;\n}\n\n.sop-toggle.sop-orbit .orbit-orb span {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  border-top: 1px solid #e9ffe199;\n  transform: rotate(-27deg);\n  filter: blur(.4px);\n}\n\n.sop-toggle.sop-orbit .orbit-orb:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 15%,#d8ffe454,transparent 50%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-orbit .orbit-end {\n  position: absolute;\n  top: 108px;\n  font: 7px var(--sop-mono);\n  color: #7b9280;\n}\n\n.sop-toggle.sop-orbit .end-a {\n  left: 42px;\n}\n\n.sop-toggle.sop-orbit .end-b {\n  right: 41px;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/orbit/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-orbit');\nif (!element) throw new Error('The Orbit root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/orbit/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/orbit/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Orbit\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/orbit/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"orbit\",\n    \"name\": \"Orbit\",\n    \"initial\": true,\n    \"stiffness\": 140,\n    \"damping\": 18,\n    \"tone\": 420,\n    \"travel\": 192\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for OrbitToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/orbit/styles.css","code":"/* orbit: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-orbit {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-orbit[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-orbit:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-orbit:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-orbit {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-orbit:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-orbit:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-orbit .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-orbit .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-orbit .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-orbit .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-orbit .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-orbit .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-orbit .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-orbit,\n  .sop-toggle.sop-orbit * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-orbit {\n  width: 280px;\n  height: 150px;\n}\n\n.sop-toggle.sop-orbit .orbit-rails {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  filter: drop-shadow(0 4px 3px #000);\n}\n\n.sop-toggle.sop-orbit .orbit-shadow {\n  position: absolute;\n  left: 30px;\n  top: 46px;\n  width: 220px;\n  height: 73px;\n  border-radius: 50%;\n  background: radial-gradient(ellipse,#0a1510e0,#11191255 70%,transparent 75%);\n  box-shadow: 0 12px 18px #0004;\n}\n\n.sop-toggle.sop-orbit .orbit-center {\n  position: absolute;\n  left: 122px;\n  top: 59px;\n  width: 36px;\n  height: 35px;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-orbit .orbit-cross {\n  position: absolute;\n  left: 13px;\n  top: 0;\n  width: 10px;\n  height: 10px;\n  background: linear-gradient(90deg,transparent 4px,#a2d1af70 4px 5px,transparent 5px),linear-gradient(transparent 4px,#a2d1af70 4px 5px,transparent 5px);\n}\n\n.sop-toggle.sop-orbit .orbit-caption {\n  position: absolute;\n  left: 0;\n  top: 21px;\n  width: 36px;\n  text-align: center;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1px;\n  color: #a4c2a6;\n  white-space: nowrap;\n}\n\n.sop-toggle.sop-orbit .orbit-orb {\n  position: absolute;\n  left: var(--ox,44px);\n  top: var(--oy,73px);\n  width: 49px;\n  height: 49px;\n  margin: -24.5px 0 0 -24.5px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 21%,#d5ead6,#92b89c 15%,#3f6f59 36%,#142c21 66%,#081a12 84%);\n  box-shadow: inset -2px -2px 3px #a7dfb652,inset 1px 1px 2px #ecffe98f,2px 8px 7px #000b,0 0 18px #83d49a14;\n}\n\n.sop-toggle.sop-orbit .orbit-orb span {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  border-top: 1px solid #e9ffe199;\n  transform: rotate(-27deg);\n  filter: blur(.4px);\n}\n\n.sop-toggle.sop-orbit .orbit-orb:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 15%,#d8ffe454,transparent 50%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-orbit .orbit-end {\n  position: absolute;\n  top: 108px;\n  font: 7px var(--sop-mono);\n  color: #7b9280;\n}\n\n.sop-toggle.sop-orbit .end-a {\n  left: 42px;\n}\n\n.sop-toggle.sop-orbit .end-b {\n  right: 41px;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/orbit/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-orbit');\nif (!element)\n    throw new Error('The Orbit root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/orbit/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/orbit/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Orbit\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Orbit — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Orbit · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"true\" aria-label=\"Orbit トグル\" class=\"sop-toggle sop-orbit\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"orbit-shadow\">\u003c/span>\u003csvg class=\"orbit-rails\" viewbox=\"0 0 280 150\">\u003cdefs>\u003clineargradient id=\"orbitRail\" x1=\"0\" x2=\"0\" y1=\"0\" y2=\"1\">\u003cstop stop-color=\"#89958b\">\u003c/stop>\u003cstop offset=\".3\" stop-color=\"#303a35\">\u003c/stop>\u003cstop offset=\".6\" stop-color=\"#141c18\">\u003c/stop>\u003cstop offset=\"1\" stop-color=\"#768a7e\">\u003c/stop>\u003c/lineargradient>\u003c/defs>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"118\" ry=\"56\" stroke=\"#364339\" stroke-dasharray=\"1 7\" stroke-width=\".8\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"75\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"#080d0a\" stroke-width=\"9\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"96\" ry=\"36\" stroke=\"url(#orbitRail)\" stroke-width=\"4\">\u003c/ellipse>\u003cellipse cx=\"140\" cy=\"73\" fill=\"none\" rx=\"77\" ry=\"24\" stroke=\"#37483b\" stroke-width=\".7\">\u003c/ellipse>\u003cpath d=\"M140 7v8m0 121v8M12 75h8m240 0h8\" stroke=\"#82988a\" stroke-width=\"1\">\u003c/path>\u003c/svg>\u003cspan class=\"orbit-center\">\u003cspan class=\"orbit-cross\">\u003c/span>\u003cspan class=\"orbit-caption\">N   /   S\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"300\" width=\"560\">\u003c/canvas>\u003cspan class=\"orbit-orb\">\u003cspan>\u003c/span>\u003c/span>\u003cspan class=\"orbit-end end-a\">0\u003c/span>\u003cspan class=\"orbit-end end-b\">1\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* orbit: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-orbit {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-orbit[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-orbit:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-orbit:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-orbit {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-orbit:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-orbit:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-orbit .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-orbit .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-orbit .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-orbit .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-orbit .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-orbit .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-orbit .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-orbit .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-orbit,\n  .sop-toggle.sop-orbit * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-orbit {\n  width: 280px;\n  height: 150px;\n}\n\n.sop-toggle.sop-orbit .orbit-rails {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  filter: drop-shadow(0 4px 3px #000);\n}\n\n.sop-toggle.sop-orbit .orbit-shadow {\n  position: absolute;\n  left: 30px;\n  top: 46px;\n  width: 220px;\n  height: 73px;\n  border-radius: 50%;\n  background: radial-gradient(ellipse,#0a1510e0,#11191255 70%,transparent 75%);\n  box-shadow: 0 12px 18px #0004;\n}\n\n.sop-toggle.sop-orbit .orbit-center {\n  position: absolute;\n  left: 122px;\n  top: 59px;\n  width: 36px;\n  height: 35px;\n  opacity: calc(.3 + var(--p)*.7);\n}\n\n.sop-toggle.sop-orbit .orbit-cross {\n  position: absolute;\n  left: 13px;\n  top: 0;\n  width: 10px;\n  height: 10px;\n  background: linear-gradient(90deg,transparent 4px,#a2d1af70 4px 5px,transparent 5px),linear-gradient(transparent 4px,#a2d1af70 4px 5px,transparent 5px);\n}\n\n.sop-toggle.sop-orbit .orbit-caption {\n  position: absolute;\n  left: 0;\n  top: 21px;\n  width: 36px;\n  text-align: center;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1px;\n  color: #a4c2a6;\n  white-space: nowrap;\n}\n\n.sop-toggle.sop-orbit .orbit-orb {\n  position: absolute;\n  left: var(--ox,44px);\n  top: var(--oy,73px);\n  width: 49px;\n  height: 49px;\n  margin: -24.5px 0 0 -24.5px;\n  border-radius: 50%;\n  background: radial-gradient(circle at 33% 21%,#d5ead6,#92b89c 15%,#3f6f59 36%,#142c21 66%,#081a12 84%);\n  box-shadow: inset -2px -2px 3px #a7dfb652,inset 1px 1px 2px #ecffe98f,2px 8px 7px #000b,0 0 18px #83d49a14;\n}\n\n.sop-toggle.sop-orbit .orbit-orb span {\n  position: absolute;\n  inset: 5px;\n  border-radius: 50%;\n  border-top: 1px solid #e9ffe199;\n  transform: rotate(-27deg);\n  filter: blur(.4px);\n}\n\n.sop-toggle.sop-orbit .orbit-orb:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: 50%;\n  background: radial-gradient(circle at 25% 15%,#d8ffe454,transparent 50%);\n  opacity: var(--p);\n}\n\n.sop-toggle.sop-orbit .orbit-end {\n  position: absolute;\n  top: 108px;\n  font: 7px var(--sop-mono);\n  color: #7b9280;\n}\n\n.sop-toggle.sop-orbit .end-a {\n  left: 42px;\n}\n\n.sop-toggle.sop-orbit .end-b {\n  right: 41px;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/orbit/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/orbit/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/orbit/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"orbit\",\n    \"name\": \"Orbit\",\n    \"initial\": true,\n    \"stiffness\": 140,\n    \"damping\": 18,\n    \"tone\": 420,\n    \"travel\": 192\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for OrbitToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/orbit/demo/main.ts\");})();\n"}},{"id":"reel","name":"Analog","category":"toggles","order":7,"version":"2.3.0","tagline":"あの頃の、手応え。","description":"カチッと送り出す、リールと慣性のリズム。","material":"ANALOG MECHANISM","motion":"WIND","accent":"215,173,117","initial":false,"componentName":"AnalogToggle","tags":["Analog","reel","トグル","ANALOG MECHANISM","カチッと送り出す、リールと慣性のリズム。"],"config":{"id":"reel","name":"Analog","initial":false,"stiffness":430,"damping":28,"tone":170,"travel":134},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Analog / 2.3.0\n\nカチッと送り出す、リールと慣性のリズム。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`AnalogToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/reel/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/reel/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/reel/react/AnalogToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/reel/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/reel/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/reel/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Analog — 再現仕様\n\nカチッと送り出す、リールと慣性のリズム。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 134px、ばねの剛性 430、減衰 28。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-reel` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 268px;\nheight: 133px;\nborder-radius: 15px;\nborder-radius: 14px;\nleft: 26px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #6e66545c, #e6c39003, #464035, #26231f, #322d25, #1c1c19, #0009, #b4a38569, #0a0c0a, #c1ab86, #8f8068, #12120e, #b28b504d, #98825e30, #81786667, #1a1b1699, #51483980, #b2a28b16, #0007, #a99c83, #33332a, #938673, #11150fcc, #121911, #bbb097, #121610, #000, #7b674d5c, #78664377, #d3ae62, #e4a24940, #10120e, #93837144, #0c100b, #887d64, #d1b586, #a78c61, #c3a775\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/reel/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/reel/react/AnalogToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"reel\",\n    \"name\": \"Analog\",\n    \"initial\": false,\n    \"stiffness\": 430,\n    \"damping\": 28,\n    \"tone\": 170,\n    \"travel\": 134\n};\n/** カチッと送り出す、リールと慣性のリズム。 */\nexport default function AnalogToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Analog トグル'} aria-checked={checked} className={`sop-toggle sop-reel ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"cassette-body\">\u003ci className=\"screw s1\">\u003c/i>\u003ci className=\"screw s2\">\u003c/i>\u003ci className=\"screw s3\">\u003c/i>\u003ci className=\"screw s4\">\u003c/i>\u003cspan className=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan className=\"tape-line\">\u003c/span>\u003cspan className=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan className=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/reel/styles.css","code":"/* reel: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-reel {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-reel[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-reel:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-reel:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-reel {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-reel:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-reel:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-reel .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-reel .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-reel .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-reel .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-reel .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-reel .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-reel .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-reel,\n  .sop-toggle.sop-reel * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-reel {\n  width: 268px;\n  height: 133px;\n  border-radius: 15px;\n}\n\n.sop-toggle.sop-reel .cassette-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 14px;\n  border: 1px solid #6e66545c;\n  background: repeating-linear-gradient(90deg,#e6c39003 0 1px,transparent 1px 3px),linear-gradient(155deg,#464035,#26231f 23%,#322d25 70%,#1c1c19);\n  box-shadow: 0 13px 22px #0009,inset 0 1px 1px #b4a38569,inset 0 -2px 1px #0a0c0a,0 2px 1px #0a0c0a;\n}\n\n.sop-toggle.sop-reel .cassette-label {\n  position: absolute;\n  left: 26px;\n  right: 26px;\n  top: 12px;\n  color: #c1ab86;\n  font-size: 7px;\n  letter-spacing: 1.5px;\n}\n\n.sop-toggle.sop-reel .cassette-label>span {\n  float: right;\n  font-size: 6px;\n  letter-spacing: 1px;\n  color: #8f8068;\n}\n\n.sop-toggle.sop-reel .tape-line {\n  position: absolute;\n  left: 46px;\n  right: 46px;\n  top: 34px;\n  height: 47px;\n  border: 2px solid #12120e;\n  border-bottom-color: #b28b504d;\n  border-radius: 35px;\n  box-shadow: 0 0 0 1px #98825e30;\n}\n\n.sop-toggle.sop-reel .spool {\n  position: absolute;\n  top: 29px;\n  width: 57px;\n  height: 57px;\n  border-radius: 50%;\n  border: 1px solid #81786667;\n  background: repeating-radial-gradient(circle,#1a1b1699 0 1px,#51483980 1px 2px);\n  box-shadow: inset 0 0 0 4px #b2a28b16,0 2px 4px #0007;\n}\n\n.sop-toggle.sop-reel .spool-a {\n  left: 33px;\n  transform: rotate(calc(var(--p)*600deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool-b {\n  right: 33px;\n  transform: rotate(calc(var(--p)*760deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool>i {\n  position: absolute;\n  inset: 11px;\n  border-radius: 50%;\n  background: conic-gradient(#a99c83 0 30deg,#33332a 30deg 80deg,#a99c83 80deg 110deg,#33332a 110deg 160deg,#a99c83 160deg 190deg,#33332a 190deg 240deg,#a99c83 240deg 270deg,#33332a 270deg 320deg,#a99c83 320deg);\n  border: 2px solid #938673;\n  box-shadow: 0 0 0 2px #11150fcc;\n}\n\n.sop-toggle.sop-reel .spool>i:after {\n  content: \"\";\n  position: absolute;\n  inset: 10px;\n  border-radius: 50%;\n  background: #121911;\n  box-shadow: 0 0 0 2px #bbb097;\n}\n\n.sop-toggle.sop-reel .cassette-window {\n  position: absolute;\n  left: 105px;\n  top: 39px;\n  width: 55px;\n  height: 30px;\n  background: #121610;\n  box-shadow: inset 0 2px 5px #000,0 1px 0 #7b674d5c;\n  border: 1px solid #78664377;\n  border-radius: 4px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n\n.sop-toggle.sop-reel .cassette-window i {\n  width: 4px;\n  height: calc(3px + var(--p)*14px);\n  background: #d3ae62;\n  opacity: calc(.2 + var(--p)*.75);\n  box-shadow: 0 0 4px #e4a24940;\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(2n) {\n  height: calc(3px + var(--p)*9px);\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(3) {\n  height: calc(3px + var(--p)*20px);\n}\n\n.sop-toggle.sop-reel .reel-slot {\n  position: absolute;\n  left: 27px;\n  right: 27px;\n  bottom: 13px;\n  height: 24px;\n  background: #10120e;\n  border-radius: 4px;\n  box-shadow: inset 0 2px 3px #000,0 1px 0 #93837144;\n  border: 1px solid #0c100b;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 0 18px;\n  color: #887d64;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.2px;\n}\n\n.sop-toggle.sop-reel .reel-slider {\n  position: absolute;\n  left: 2px;\n  top: 1px;\n  width: 71px;\n  height: 20px;\n  background: linear-gradient(#d1b586,#a78c61 15%,#c3a775 40%,#a68c60 82%,#6f5d3f);\n  border-radius: 3px;\n  box-shadow: 0 2px 3px #000b,inset 0 1px 1px #f4dfb766;\n  border: 1px solid #ac906752;\n  transform: translateX(calc(var(--p)*134px));\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 3px;\n}\n\n.sop-toggle.sop-reel .reel-slider>i {\n  display: block;\n  width: 1px;\n  height: 9px;\n  background: #66502f;\n  box-shadow: 1px 0 0 #efdbb952;\n}\n\n.sop-toggle.sop-reel .reel-slider>b {\n  font-size: 6px;\n  color: #403b2b;\n  position: absolute;\n  right: 9px;\n  top: 5px;\n  font-weight: 400;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/reel/react/Example.tsx","code":"import React, { useState } from 'react';\nimport AnalogToggle from './AnalogToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cAnalogToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/reel/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/reel/react/AnalogToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"reel\",\n    \"name\": \"Analog\",\n    \"initial\": false,\n    \"stiffness\": 430,\n    \"damping\": 28,\n    \"tone\": 170,\n    \"travel\": 134\n};\n/** カチッと送り出す、リールと慣性のリズム。 */\nexport default function AnalogToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Analog トグル'} aria-checked={checked} className={`sop-toggle sop-reel ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"cassette-body\">\u003ci className=\"screw s1\">\u003c/i>\u003ci className=\"screw s2\">\u003c/i>\u003ci className=\"screw s3\">\u003c/i>\u003ci className=\"screw s4\">\u003c/i>\u003cspan className=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan className=\"tape-line\">\u003c/span>\u003cspan className=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan className=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/reel/styles.css","code":"/* reel: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-reel {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-reel[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-reel:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-reel:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-reel {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-reel:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-reel:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-reel .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-reel .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-reel .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-reel .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-reel .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-reel .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-reel .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-reel,\n  .sop-toggle.sop-reel * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-reel {\n  width: 268px;\n  height: 133px;\n  border-radius: 15px;\n}\n\n.sop-toggle.sop-reel .cassette-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 14px;\n  border: 1px solid #6e66545c;\n  background: repeating-linear-gradient(90deg,#e6c39003 0 1px,transparent 1px 3px),linear-gradient(155deg,#464035,#26231f 23%,#322d25 70%,#1c1c19);\n  box-shadow: 0 13px 22px #0009,inset 0 1px 1px #b4a38569,inset 0 -2px 1px #0a0c0a,0 2px 1px #0a0c0a;\n}\n\n.sop-toggle.sop-reel .cassette-label {\n  position: absolute;\n  left: 26px;\n  right: 26px;\n  top: 12px;\n  color: #c1ab86;\n  font-size: 7px;\n  letter-spacing: 1.5px;\n}\n\n.sop-toggle.sop-reel .cassette-label>span {\n  float: right;\n  font-size: 6px;\n  letter-spacing: 1px;\n  color: #8f8068;\n}\n\n.sop-toggle.sop-reel .tape-line {\n  position: absolute;\n  left: 46px;\n  right: 46px;\n  top: 34px;\n  height: 47px;\n  border: 2px solid #12120e;\n  border-bottom-color: #b28b504d;\n  border-radius: 35px;\n  box-shadow: 0 0 0 1px #98825e30;\n}\n\n.sop-toggle.sop-reel .spool {\n  position: absolute;\n  top: 29px;\n  width: 57px;\n  height: 57px;\n  border-radius: 50%;\n  border: 1px solid #81786667;\n  background: repeating-radial-gradient(circle,#1a1b1699 0 1px,#51483980 1px 2px);\n  box-shadow: inset 0 0 0 4px #b2a28b16,0 2px 4px #0007;\n}\n\n.sop-toggle.sop-reel .spool-a {\n  left: 33px;\n  transform: rotate(calc(var(--p)*600deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool-b {\n  right: 33px;\n  transform: rotate(calc(var(--p)*760deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool>i {\n  position: absolute;\n  inset: 11px;\n  border-radius: 50%;\n  background: conic-gradient(#a99c83 0 30deg,#33332a 30deg 80deg,#a99c83 80deg 110deg,#33332a 110deg 160deg,#a99c83 160deg 190deg,#33332a 190deg 240deg,#a99c83 240deg 270deg,#33332a 270deg 320deg,#a99c83 320deg);\n  border: 2px solid #938673;\n  box-shadow: 0 0 0 2px #11150fcc;\n}\n\n.sop-toggle.sop-reel .spool>i:after {\n  content: \"\";\n  position: absolute;\n  inset: 10px;\n  border-radius: 50%;\n  background: #121911;\n  box-shadow: 0 0 0 2px #bbb097;\n}\n\n.sop-toggle.sop-reel .cassette-window {\n  position: absolute;\n  left: 105px;\n  top: 39px;\n  width: 55px;\n  height: 30px;\n  background: #121610;\n  box-shadow: inset 0 2px 5px #000,0 1px 0 #7b674d5c;\n  border: 1px solid #78664377;\n  border-radius: 4px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n\n.sop-toggle.sop-reel .cassette-window i {\n  width: 4px;\n  height: calc(3px + var(--p)*14px);\n  background: #d3ae62;\n  opacity: calc(.2 + var(--p)*.75);\n  box-shadow: 0 0 4px #e4a24940;\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(2n) {\n  height: calc(3px + var(--p)*9px);\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(3) {\n  height: calc(3px + var(--p)*20px);\n}\n\n.sop-toggle.sop-reel .reel-slot {\n  position: absolute;\n  left: 27px;\n  right: 27px;\n  bottom: 13px;\n  height: 24px;\n  background: #10120e;\n  border-radius: 4px;\n  box-shadow: inset 0 2px 3px #000,0 1px 0 #93837144;\n  border: 1px solid #0c100b;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 0 18px;\n  color: #887d64;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.2px;\n}\n\n.sop-toggle.sop-reel .reel-slider {\n  position: absolute;\n  left: 2px;\n  top: 1px;\n  width: 71px;\n  height: 20px;\n  background: linear-gradient(#d1b586,#a78c61 15%,#c3a775 40%,#a68c60 82%,#6f5d3f);\n  border-radius: 3px;\n  box-shadow: 0 2px 3px #000b,inset 0 1px 1px #f4dfb766;\n  border: 1px solid #ac906752;\n  transform: translateX(calc(var(--p)*134px));\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 3px;\n}\n\n.sop-toggle.sop-reel .reel-slider>i {\n  display: block;\n  width: 1px;\n  height: 9px;\n  background: #66502f;\n  box-shadow: 1px 0 0 #efdbb952;\n}\n\n.sop-toggle.sop-reel .reel-slider>b {\n  font-size: 6px;\n  color: #403b2b;\n  position: absolute;\n  right: 9px;\n  top: 5px;\n  font-weight: 400;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/reel/react/Example.jsx","code":"import React, { useState } from 'react';\nimport AnalogToggle from './AnalogToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cAnalogToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/reel/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/reel/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"reel\",\n    \"name\": \"Analog\",\n    \"initial\": false,\n    \"stiffness\": 430,\n    \"damping\": 28,\n    \"tone\": 170,\n    \"travel\": 134\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for AnalogToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/reel/styles.css","code":"/* reel: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-reel {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-reel[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-reel:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-reel:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-reel {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-reel:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-reel:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-reel .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-reel .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-reel .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-reel .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-reel .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-reel .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-reel .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-reel,\n  .sop-toggle.sop-reel * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-reel {\n  width: 268px;\n  height: 133px;\n  border-radius: 15px;\n}\n\n.sop-toggle.sop-reel .cassette-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 14px;\n  border: 1px solid #6e66545c;\n  background: repeating-linear-gradient(90deg,#e6c39003 0 1px,transparent 1px 3px),linear-gradient(155deg,#464035,#26231f 23%,#322d25 70%,#1c1c19);\n  box-shadow: 0 13px 22px #0009,inset 0 1px 1px #b4a38569,inset 0 -2px 1px #0a0c0a,0 2px 1px #0a0c0a;\n}\n\n.sop-toggle.sop-reel .cassette-label {\n  position: absolute;\n  left: 26px;\n  right: 26px;\n  top: 12px;\n  color: #c1ab86;\n  font-size: 7px;\n  letter-spacing: 1.5px;\n}\n\n.sop-toggle.sop-reel .cassette-label>span {\n  float: right;\n  font-size: 6px;\n  letter-spacing: 1px;\n  color: #8f8068;\n}\n\n.sop-toggle.sop-reel .tape-line {\n  position: absolute;\n  left: 46px;\n  right: 46px;\n  top: 34px;\n  height: 47px;\n  border: 2px solid #12120e;\n  border-bottom-color: #b28b504d;\n  border-radius: 35px;\n  box-shadow: 0 0 0 1px #98825e30;\n}\n\n.sop-toggle.sop-reel .spool {\n  position: absolute;\n  top: 29px;\n  width: 57px;\n  height: 57px;\n  border-radius: 50%;\n  border: 1px solid #81786667;\n  background: repeating-radial-gradient(circle,#1a1b1699 0 1px,#51483980 1px 2px);\n  box-shadow: inset 0 0 0 4px #b2a28b16,0 2px 4px #0007;\n}\n\n.sop-toggle.sop-reel .spool-a {\n  left: 33px;\n  transform: rotate(calc(var(--p)*600deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool-b {\n  right: 33px;\n  transform: rotate(calc(var(--p)*760deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool>i {\n  position: absolute;\n  inset: 11px;\n  border-radius: 50%;\n  background: conic-gradient(#a99c83 0 30deg,#33332a 30deg 80deg,#a99c83 80deg 110deg,#33332a 110deg 160deg,#a99c83 160deg 190deg,#33332a 190deg 240deg,#a99c83 240deg 270deg,#33332a 270deg 320deg,#a99c83 320deg);\n  border: 2px solid #938673;\n  box-shadow: 0 0 0 2px #11150fcc;\n}\n\n.sop-toggle.sop-reel .spool>i:after {\n  content: \"\";\n  position: absolute;\n  inset: 10px;\n  border-radius: 50%;\n  background: #121911;\n  box-shadow: 0 0 0 2px #bbb097;\n}\n\n.sop-toggle.sop-reel .cassette-window {\n  position: absolute;\n  left: 105px;\n  top: 39px;\n  width: 55px;\n  height: 30px;\n  background: #121610;\n  box-shadow: inset 0 2px 5px #000,0 1px 0 #7b674d5c;\n  border: 1px solid #78664377;\n  border-radius: 4px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n\n.sop-toggle.sop-reel .cassette-window i {\n  width: 4px;\n  height: calc(3px + var(--p)*14px);\n  background: #d3ae62;\n  opacity: calc(.2 + var(--p)*.75);\n  box-shadow: 0 0 4px #e4a24940;\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(2n) {\n  height: calc(3px + var(--p)*9px);\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(3) {\n  height: calc(3px + var(--p)*20px);\n}\n\n.sop-toggle.sop-reel .reel-slot {\n  position: absolute;\n  left: 27px;\n  right: 27px;\n  bottom: 13px;\n  height: 24px;\n  background: #10120e;\n  border-radius: 4px;\n  box-shadow: inset 0 2px 3px #000,0 1px 0 #93837144;\n  border: 1px solid #0c100b;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 0 18px;\n  color: #887d64;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.2px;\n}\n\n.sop-toggle.sop-reel .reel-slider {\n  position: absolute;\n  left: 2px;\n  top: 1px;\n  width: 71px;\n  height: 20px;\n  background: linear-gradient(#d1b586,#a78c61 15%,#c3a775 40%,#a68c60 82%,#6f5d3f);\n  border-radius: 3px;\n  box-shadow: 0 2px 3px #000b,inset 0 1px 1px #f4dfb766;\n  border: 1px solid #ac906752;\n  transform: translateX(calc(var(--p)*134px));\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 3px;\n}\n\n.sop-toggle.sop-reel .reel-slider>i {\n  display: block;\n  width: 1px;\n  height: 9px;\n  background: #66502f;\n  box-shadow: 1px 0 0 #efdbb952;\n}\n\n.sop-toggle.sop-reel .reel-slider>b {\n  font-size: 6px;\n  color: #403b2b;\n  position: absolute;\n  right: 9px;\n  top: 5px;\n  font-weight: 400;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/reel/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-reel');\nif (!element) throw new Error('The Analog root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/reel/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/reel/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Analog\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/reel/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"reel\",\n    \"name\": \"Analog\",\n    \"initial\": false,\n    \"stiffness\": 430,\n    \"damping\": 28,\n    \"tone\": 170,\n    \"travel\": 134\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for AnalogToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/reel/styles.css","code":"/* reel: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-reel {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-reel[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-reel:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-reel:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-reel {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-reel:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-reel:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-reel .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-reel .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-reel .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-reel .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-reel .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-reel .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-reel .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-reel,\n  .sop-toggle.sop-reel * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-reel {\n  width: 268px;\n  height: 133px;\n  border-radius: 15px;\n}\n\n.sop-toggle.sop-reel .cassette-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 14px;\n  border: 1px solid #6e66545c;\n  background: repeating-linear-gradient(90deg,#e6c39003 0 1px,transparent 1px 3px),linear-gradient(155deg,#464035,#26231f 23%,#322d25 70%,#1c1c19);\n  box-shadow: 0 13px 22px #0009,inset 0 1px 1px #b4a38569,inset 0 -2px 1px #0a0c0a,0 2px 1px #0a0c0a;\n}\n\n.sop-toggle.sop-reel .cassette-label {\n  position: absolute;\n  left: 26px;\n  right: 26px;\n  top: 12px;\n  color: #c1ab86;\n  font-size: 7px;\n  letter-spacing: 1.5px;\n}\n\n.sop-toggle.sop-reel .cassette-label>span {\n  float: right;\n  font-size: 6px;\n  letter-spacing: 1px;\n  color: #8f8068;\n}\n\n.sop-toggle.sop-reel .tape-line {\n  position: absolute;\n  left: 46px;\n  right: 46px;\n  top: 34px;\n  height: 47px;\n  border: 2px solid #12120e;\n  border-bottom-color: #b28b504d;\n  border-radius: 35px;\n  box-shadow: 0 0 0 1px #98825e30;\n}\n\n.sop-toggle.sop-reel .spool {\n  position: absolute;\n  top: 29px;\n  width: 57px;\n  height: 57px;\n  border-radius: 50%;\n  border: 1px solid #81786667;\n  background: repeating-radial-gradient(circle,#1a1b1699 0 1px,#51483980 1px 2px);\n  box-shadow: inset 0 0 0 4px #b2a28b16,0 2px 4px #0007;\n}\n\n.sop-toggle.sop-reel .spool-a {\n  left: 33px;\n  transform: rotate(calc(var(--p)*600deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool-b {\n  right: 33px;\n  transform: rotate(calc(var(--p)*760deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool>i {\n  position: absolute;\n  inset: 11px;\n  border-radius: 50%;\n  background: conic-gradient(#a99c83 0 30deg,#33332a 30deg 80deg,#a99c83 80deg 110deg,#33332a 110deg 160deg,#a99c83 160deg 190deg,#33332a 190deg 240deg,#a99c83 240deg 270deg,#33332a 270deg 320deg,#a99c83 320deg);\n  border: 2px solid #938673;\n  box-shadow: 0 0 0 2px #11150fcc;\n}\n\n.sop-toggle.sop-reel .spool>i:after {\n  content: \"\";\n  position: absolute;\n  inset: 10px;\n  border-radius: 50%;\n  background: #121911;\n  box-shadow: 0 0 0 2px #bbb097;\n}\n\n.sop-toggle.sop-reel .cassette-window {\n  position: absolute;\n  left: 105px;\n  top: 39px;\n  width: 55px;\n  height: 30px;\n  background: #121610;\n  box-shadow: inset 0 2px 5px #000,0 1px 0 #7b674d5c;\n  border: 1px solid #78664377;\n  border-radius: 4px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n\n.sop-toggle.sop-reel .cassette-window i {\n  width: 4px;\n  height: calc(3px + var(--p)*14px);\n  background: #d3ae62;\n  opacity: calc(.2 + var(--p)*.75);\n  box-shadow: 0 0 4px #e4a24940;\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(2n) {\n  height: calc(3px + var(--p)*9px);\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(3) {\n  height: calc(3px + var(--p)*20px);\n}\n\n.sop-toggle.sop-reel .reel-slot {\n  position: absolute;\n  left: 27px;\n  right: 27px;\n  bottom: 13px;\n  height: 24px;\n  background: #10120e;\n  border-radius: 4px;\n  box-shadow: inset 0 2px 3px #000,0 1px 0 #93837144;\n  border: 1px solid #0c100b;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 0 18px;\n  color: #887d64;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.2px;\n}\n\n.sop-toggle.sop-reel .reel-slider {\n  position: absolute;\n  left: 2px;\n  top: 1px;\n  width: 71px;\n  height: 20px;\n  background: linear-gradient(#d1b586,#a78c61 15%,#c3a775 40%,#a68c60 82%,#6f5d3f);\n  border-radius: 3px;\n  box-shadow: 0 2px 3px #000b,inset 0 1px 1px #f4dfb766;\n  border: 1px solid #ac906752;\n  transform: translateX(calc(var(--p)*134px));\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 3px;\n}\n\n.sop-toggle.sop-reel .reel-slider>i {\n  display: block;\n  width: 1px;\n  height: 9px;\n  background: #66502f;\n  box-shadow: 1px 0 0 #efdbb952;\n}\n\n.sop-toggle.sop-reel .reel-slider>b {\n  font-size: 6px;\n  color: #403b2b;\n  position: absolute;\n  right: 9px;\n  top: 5px;\n  font-weight: 400;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/reel/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-reel');\nif (!element)\n    throw new Error('The Analog root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/reel/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/reel/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Analog\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Analog — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Analog · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"false\" aria-label=\"Analog トグル\" class=\"sop-toggle sop-reel\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"cassette-body\">\u003ci class=\"screw s1\">\u003c/i>\u003ci class=\"screw s2\">\u003c/i>\u003ci class=\"screw s3\">\u003c/i>\u003ci class=\"screw s4\">\u003c/i>\u003cspan class=\"cassette-label mono\">S / P \u003cspan>STEREO · TYPE II\u003c/span>\u003c/span>\u003cspan class=\"tape-line\">\u003c/span>\u003cspan class=\"spool spool-a\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"spool spool-b\">\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"cassette-window\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"reel-slot\">\u003cspan>STOP\u003c/span>\u003cspan>PLAY\u003c/span>\u003cspan class=\"reel-slider\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003cb>▶\u003c/b>\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* reel: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-reel {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-reel[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-reel:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-reel:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-reel {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-reel:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-reel:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-reel .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-reel .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-reel .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-reel .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-reel .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-reel .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-reel .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-reel .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-reel,\n  .sop-toggle.sop-reel * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-reel {\n  width: 268px;\n  height: 133px;\n  border-radius: 15px;\n}\n\n.sop-toggle.sop-reel .cassette-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 14px;\n  border: 1px solid #6e66545c;\n  background: repeating-linear-gradient(90deg,#e6c39003 0 1px,transparent 1px 3px),linear-gradient(155deg,#464035,#26231f 23%,#322d25 70%,#1c1c19);\n  box-shadow: 0 13px 22px #0009,inset 0 1px 1px #b4a38569,inset 0 -2px 1px #0a0c0a,0 2px 1px #0a0c0a;\n}\n\n.sop-toggle.sop-reel .cassette-label {\n  position: absolute;\n  left: 26px;\n  right: 26px;\n  top: 12px;\n  color: #c1ab86;\n  font-size: 7px;\n  letter-spacing: 1.5px;\n}\n\n.sop-toggle.sop-reel .cassette-label>span {\n  float: right;\n  font-size: 6px;\n  letter-spacing: 1px;\n  color: #8f8068;\n}\n\n.sop-toggle.sop-reel .tape-line {\n  position: absolute;\n  left: 46px;\n  right: 46px;\n  top: 34px;\n  height: 47px;\n  border: 2px solid #12120e;\n  border-bottom-color: #b28b504d;\n  border-radius: 35px;\n  box-shadow: 0 0 0 1px #98825e30;\n}\n\n.sop-toggle.sop-reel .spool {\n  position: absolute;\n  top: 29px;\n  width: 57px;\n  height: 57px;\n  border-radius: 50%;\n  border: 1px solid #81786667;\n  background: repeating-radial-gradient(circle,#1a1b1699 0 1px,#51483980 1px 2px);\n  box-shadow: inset 0 0 0 4px #b2a28b16,0 2px 4px #0007;\n}\n\n.sop-toggle.sop-reel .spool-a {\n  left: 33px;\n  transform: rotate(calc(var(--p)*600deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool-b {\n  right: 33px;\n  transform: rotate(calc(var(--p)*760deg + var(--reel-angle,0deg)));\n}\n\n.sop-toggle.sop-reel .spool>i {\n  position: absolute;\n  inset: 11px;\n  border-radius: 50%;\n  background: conic-gradient(#a99c83 0 30deg,#33332a 30deg 80deg,#a99c83 80deg 110deg,#33332a 110deg 160deg,#a99c83 160deg 190deg,#33332a 190deg 240deg,#a99c83 240deg 270deg,#33332a 270deg 320deg,#a99c83 320deg);\n  border: 2px solid #938673;\n  box-shadow: 0 0 0 2px #11150fcc;\n}\n\n.sop-toggle.sop-reel .spool>i:after {\n  content: \"\";\n  position: absolute;\n  inset: 10px;\n  border-radius: 50%;\n  background: #121911;\n  box-shadow: 0 0 0 2px #bbb097;\n}\n\n.sop-toggle.sop-reel .cassette-window {\n  position: absolute;\n  left: 105px;\n  top: 39px;\n  width: 55px;\n  height: 30px;\n  background: #121610;\n  box-shadow: inset 0 2px 5px #000,0 1px 0 #7b674d5c;\n  border: 1px solid #78664377;\n  border-radius: 4px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n\n.sop-toggle.sop-reel .cassette-window i {\n  width: 4px;\n  height: calc(3px + var(--p)*14px);\n  background: #d3ae62;\n  opacity: calc(.2 + var(--p)*.75);\n  box-shadow: 0 0 4px #e4a24940;\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(2n) {\n  height: calc(3px + var(--p)*9px);\n}\n\n.sop-toggle.sop-reel .cassette-window i:nth-child(3) {\n  height: calc(3px + var(--p)*20px);\n}\n\n.sop-toggle.sop-reel .reel-slot {\n  position: absolute;\n  left: 27px;\n  right: 27px;\n  bottom: 13px;\n  height: 24px;\n  background: #10120e;\n  border-radius: 4px;\n  box-shadow: inset 0 2px 3px #000,0 1px 0 #93837144;\n  border: 1px solid #0c100b;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 0 18px;\n  color: #887d64;\n  font: 6px var(--sop-mono);\n  letter-spacing: 1.2px;\n}\n\n.sop-toggle.sop-reel .reel-slider {\n  position: absolute;\n  left: 2px;\n  top: 1px;\n  width: 71px;\n  height: 20px;\n  background: linear-gradient(#d1b586,#a78c61 15%,#c3a775 40%,#a68c60 82%,#6f5d3f);\n  border-radius: 3px;\n  box-shadow: 0 2px 3px #000b,inset 0 1px 1px #f4dfb766;\n  border: 1px solid #ac906752;\n  transform: translateX(calc(var(--p)*134px));\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 3px;\n}\n\n.sop-toggle.sop-reel .reel-slider>i {\n  display: block;\n  width: 1px;\n  height: 9px;\n  background: #66502f;\n  box-shadow: 1px 0 0 #efdbb952;\n}\n\n.sop-toggle.sop-reel .reel-slider>b {\n  font-size: 6px;\n  color: #403b2b;\n  position: absolute;\n  right: 9px;\n  top: 5px;\n  font-weight: 400;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/reel/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/reel/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/reel/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"reel\",\n    \"name\": \"Analog\",\n    \"initial\": false,\n    \"stiffness\": 430,\n    \"damping\": 28,\n    \"tone\": 170,\n    \"travel\": 134\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for AnalogToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/reel/demo/main.ts\");})();\n"}},{"id":"fold","name":"Fold","category":"toggles","order":8,"version":"2.3.0","tagline":"余白を、ひらく。","description":"一枚の紙が伸び、幾何学の陰影が現れる。","material":"FOLDED GEOMETRY","motion":"EXPAND","accent":"232,158,115","initial":false,"componentName":"FoldToggle","tags":["Fold","fold","トグル","FOLDED GEOMETRY","一枚の紙が伸び、幾何学の陰影が現れる。"],"config":{"id":"fold","name":"Fold","initial":false,"stiffness":250,"damping":21,"tone":280,"travel":177},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Fold / 2.3.0\n\n一枚の紙が伸び、幾何学の陰影が現れる。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`FoldToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/fold/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/fold/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/fold/react/FoldToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/fold/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/fold/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/fold/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Fold — 再現仕様\n\n一枚の紙が伸び、幾何学の陰影が現れる。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 177px、ばねの剛性 250、減衰 21。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-fold` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 268px;\nheight: 118px;\nborder-radius: 10px;\nleft: 4px;\ntop: 13px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #392a24, #231d1a, #3f2e25, #85624743, #0907049e, #0007, #b1865540, #ba8a6d, #0008, #bf7956, #edb487, #ffd1a2, #ffd1a459, #885139, #c4855b, #d9976a, #ffd4ad, #e6aa7d, #c48762, #dd9a72, #68392055, #ffe2c880, #a3674d, #ffcca499, #8d685055\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/fold/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/fold/react/FoldToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"fold\",\n    \"name\": \"Fold\",\n    \"initial\": false,\n    \"stiffness\": 250,\n    \"damping\": 21,\n    \"tone\": 280,\n    \"travel\": 177\n};\n/** 一枚の紙が伸び、幾何学の陰影が現れる。 */\nexport default function FoldToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Fold トグル'} aria-checked={checked} className={`sop-toggle sop-fold ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"fold-base\">\u003cspan className=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan className=\"fold-paper\">\u003ci style={{ \"--i\": \"0\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"1\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"2\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"3\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"4\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"5\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"6\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"7\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"8\" } as CSSProperties}>\u003c/i>\u003ci style={{ \"--i\": \"9\" } as CSSProperties}>\u003c/i>\u003c/span>\u003cspan className=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"fold-guide\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/fold/styles.css","code":"/* fold: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-fold {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-fold[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-fold:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-fold:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-fold {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-fold:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-fold:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-fold .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-fold .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-fold .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-fold .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-fold .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-fold .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-fold .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-fold,\n  .sop-toggle.sop-fold * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-fold {\n  width: 268px;\n  height: 118px;\n  border-radius: 10px;\n}\n\n.sop-toggle.sop-fold .fold-base {\n  position: absolute;\n  left: 4px;\n  top: 13px;\n  width: 260px;\n  height: 89px;\n  border-radius: 5px;\n  background: linear-gradient(170deg,#392a24,#231d1a 65%,#3f2e25);\n  border: 1px solid #85624743;\n  box-shadow: inset 0 2px 8px #0907049e,0 12px 16px #0007,0 1px 1px #b1865540;\n}\n\n.sop-toggle.sop-fold .fold-marks {\n  position: absolute;\n  left: 24px;\n  right: 24px;\n  top: 29px;\n  color: #ba8a6d;\n  font: 18px var(--sop-mono);\n  opacity: .5;\n}\n\n.sop-toggle.sop-fold .fold-marks>span {\n  float: right;\n}\n\n.sop-toggle.sop-fold .fold-paper {\n  position: absolute;\n  left: 34px;\n  top: 29px;\n  height: 55px;\n  width: calc(18px + var(--p)*163px);\n  display: flex;\n  filter: drop-shadow(0 8px 4px #0008);\n  perspective: 250px;\n  transform: skewY(calc(var(--v)*-.4deg));\n}\n\n.sop-toggle.sop-fold .fold-paper i {\n  display: block;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  background: linear-gradient(90deg,#bf7956,#edb487 80%,#ffd1a2);\n  clip-path: polygon(0 4%,100% 0,100% 100%,0 96%);\n  transform: skewY(calc(-7deg - (1 - var(--p))*22deg));\n  border-right: 1px solid #ffd1a459;\n}\n\n.sop-toggle.sop-fold .fold-paper i:nth-child(even) {\n  background: linear-gradient(90deg,#885139,#c4855b 73%,#d9976a);\n  transform: skewY(calc(7deg + (1 - var(--p))*22deg));\n  clip-path: polygon(0 0,100% 4%,100% 96%,0 100%);\n}\n\n.sop-toggle.sop-fold .fold-tab {\n  position: absolute;\n  left: 16px;\n  top: 22px;\n  width: 54px;\n  height: 69px;\n  background: linear-gradient(130deg,#ffd4ad,#e6aa7d 49%,#c48762 50%,#dd9a72 80%);\n  clip-path: polygon(0 6%,82% 0,100% 15%,100% 91%,14% 100%,0 83%);\n  transform: translateX(calc(var(--p)*177px)) rotateY(calc(var(--v)*3deg));\n  filter: drop-shadow(0 8px 5px #0007);\n}\n\n.sop-toggle.sop-fold .fold-tab>span {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(130deg,transparent 49%,#68392055 50%,transparent 51%);\n  box-shadow: inset 1px 1px 1px #ffe2c880;\n}\n\n.sop-toggle.sop-fold .fold-tab>i {\n  position: absolute;\n  left: 13px;\n  top: 29px;\n  width: 27px;\n  height: 1px;\n  background: #a3674d;\n  box-shadow: 0 1px 0 #ffcca499;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-fold .fold-guide {\n  position: absolute;\n  left: 83px;\n  bottom: -2px;\n  width: 105px;\n  height: 1px;\n  background: repeating-linear-gradient(90deg,#8d685055 0 3px,transparent 3px 7px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/fold/react/Example.tsx","code":"import React, { useState } from 'react';\nimport FoldToggle from './FoldToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cFoldToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/fold/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/fold/react/FoldToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"fold\",\n    \"name\": \"Fold\",\n    \"initial\": false,\n    \"stiffness\": 250,\n    \"damping\": 21,\n    \"tone\": 280,\n    \"travel\": 177\n};\n/** 一枚の紙が伸び、幾何学の陰影が現れる。 */\nexport default function FoldToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Fold トグル'} aria-checked={checked} className={`sop-toggle sop-fold ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"fold-base\">\u003cspan className=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan className=\"fold-paper\">\u003ci style={{ \"--i\": \"0\" }}>\u003c/i>\u003ci style={{ \"--i\": \"1\" }}>\u003c/i>\u003ci style={{ \"--i\": \"2\" }}>\u003c/i>\u003ci style={{ \"--i\": \"3\" }}>\u003c/i>\u003ci style={{ \"--i\": \"4\" }}>\u003c/i>\u003ci style={{ \"--i\": \"5\" }}>\u003c/i>\u003ci style={{ \"--i\": \"6\" }}>\u003c/i>\u003ci style={{ \"--i\": \"7\" }}>\u003c/i>\u003ci style={{ \"--i\": \"8\" }}>\u003c/i>\u003ci style={{ \"--i\": \"9\" }}>\u003c/i>\u003c/span>\u003cspan className=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan className=\"fold-guide\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/fold/styles.css","code":"/* fold: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-fold {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-fold[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-fold:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-fold:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-fold {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-fold:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-fold:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-fold .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-fold .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-fold .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-fold .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-fold .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-fold .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-fold .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-fold,\n  .sop-toggle.sop-fold * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-fold {\n  width: 268px;\n  height: 118px;\n  border-radius: 10px;\n}\n\n.sop-toggle.sop-fold .fold-base {\n  position: absolute;\n  left: 4px;\n  top: 13px;\n  width: 260px;\n  height: 89px;\n  border-radius: 5px;\n  background: linear-gradient(170deg,#392a24,#231d1a 65%,#3f2e25);\n  border: 1px solid #85624743;\n  box-shadow: inset 0 2px 8px #0907049e,0 12px 16px #0007,0 1px 1px #b1865540;\n}\n\n.sop-toggle.sop-fold .fold-marks {\n  position: absolute;\n  left: 24px;\n  right: 24px;\n  top: 29px;\n  color: #ba8a6d;\n  font: 18px var(--sop-mono);\n  opacity: .5;\n}\n\n.sop-toggle.sop-fold .fold-marks>span {\n  float: right;\n}\n\n.sop-toggle.sop-fold .fold-paper {\n  position: absolute;\n  left: 34px;\n  top: 29px;\n  height: 55px;\n  width: calc(18px + var(--p)*163px);\n  display: flex;\n  filter: drop-shadow(0 8px 4px #0008);\n  perspective: 250px;\n  transform: skewY(calc(var(--v)*-.4deg));\n}\n\n.sop-toggle.sop-fold .fold-paper i {\n  display: block;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  background: linear-gradient(90deg,#bf7956,#edb487 80%,#ffd1a2);\n  clip-path: polygon(0 4%,100% 0,100% 100%,0 96%);\n  transform: skewY(calc(-7deg - (1 - var(--p))*22deg));\n  border-right: 1px solid #ffd1a459;\n}\n\n.sop-toggle.sop-fold .fold-paper i:nth-child(even) {\n  background: linear-gradient(90deg,#885139,#c4855b 73%,#d9976a);\n  transform: skewY(calc(7deg + (1 - var(--p))*22deg));\n  clip-path: polygon(0 0,100% 4%,100% 96%,0 100%);\n}\n\n.sop-toggle.sop-fold .fold-tab {\n  position: absolute;\n  left: 16px;\n  top: 22px;\n  width: 54px;\n  height: 69px;\n  background: linear-gradient(130deg,#ffd4ad,#e6aa7d 49%,#c48762 50%,#dd9a72 80%);\n  clip-path: polygon(0 6%,82% 0,100% 15%,100% 91%,14% 100%,0 83%);\n  transform: translateX(calc(var(--p)*177px)) rotateY(calc(var(--v)*3deg));\n  filter: drop-shadow(0 8px 5px #0007);\n}\n\n.sop-toggle.sop-fold .fold-tab>span {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(130deg,transparent 49%,#68392055 50%,transparent 51%);\n  box-shadow: inset 1px 1px 1px #ffe2c880;\n}\n\n.sop-toggle.sop-fold .fold-tab>i {\n  position: absolute;\n  left: 13px;\n  top: 29px;\n  width: 27px;\n  height: 1px;\n  background: #a3674d;\n  box-shadow: 0 1px 0 #ffcca499;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-fold .fold-guide {\n  position: absolute;\n  left: 83px;\n  bottom: -2px;\n  width: 105px;\n  height: 1px;\n  background: repeating-linear-gradient(90deg,#8d685055 0 3px,transparent 3px 7px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/fold/react/Example.jsx","code":"import React, { useState } from 'react';\nimport FoldToggle from './FoldToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cFoldToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/fold/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/fold/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"fold\",\n    \"name\": \"Fold\",\n    \"initial\": false,\n    \"stiffness\": 250,\n    \"damping\": 21,\n    \"tone\": 280,\n    \"travel\": 177\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for FoldToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/fold/styles.css","code":"/* fold: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-fold {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-fold[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-fold:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-fold:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-fold {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-fold:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-fold:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-fold .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-fold .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-fold .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-fold .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-fold .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-fold .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-fold .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-fold,\n  .sop-toggle.sop-fold * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-fold {\n  width: 268px;\n  height: 118px;\n  border-radius: 10px;\n}\n\n.sop-toggle.sop-fold .fold-base {\n  position: absolute;\n  left: 4px;\n  top: 13px;\n  width: 260px;\n  height: 89px;\n  border-radius: 5px;\n  background: linear-gradient(170deg,#392a24,#231d1a 65%,#3f2e25);\n  border: 1px solid #85624743;\n  box-shadow: inset 0 2px 8px #0907049e,0 12px 16px #0007,0 1px 1px #b1865540;\n}\n\n.sop-toggle.sop-fold .fold-marks {\n  position: absolute;\n  left: 24px;\n  right: 24px;\n  top: 29px;\n  color: #ba8a6d;\n  font: 18px var(--sop-mono);\n  opacity: .5;\n}\n\n.sop-toggle.sop-fold .fold-marks>span {\n  float: right;\n}\n\n.sop-toggle.sop-fold .fold-paper {\n  position: absolute;\n  left: 34px;\n  top: 29px;\n  height: 55px;\n  width: calc(18px + var(--p)*163px);\n  display: flex;\n  filter: drop-shadow(0 8px 4px #0008);\n  perspective: 250px;\n  transform: skewY(calc(var(--v)*-.4deg));\n}\n\n.sop-toggle.sop-fold .fold-paper i {\n  display: block;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  background: linear-gradient(90deg,#bf7956,#edb487 80%,#ffd1a2);\n  clip-path: polygon(0 4%,100% 0,100% 100%,0 96%);\n  transform: skewY(calc(-7deg - (1 - var(--p))*22deg));\n  border-right: 1px solid #ffd1a459;\n}\n\n.sop-toggle.sop-fold .fold-paper i:nth-child(even) {\n  background: linear-gradient(90deg,#885139,#c4855b 73%,#d9976a);\n  transform: skewY(calc(7deg + (1 - var(--p))*22deg));\n  clip-path: polygon(0 0,100% 4%,100% 96%,0 100%);\n}\n\n.sop-toggle.sop-fold .fold-tab {\n  position: absolute;\n  left: 16px;\n  top: 22px;\n  width: 54px;\n  height: 69px;\n  background: linear-gradient(130deg,#ffd4ad,#e6aa7d 49%,#c48762 50%,#dd9a72 80%);\n  clip-path: polygon(0 6%,82% 0,100% 15%,100% 91%,14% 100%,0 83%);\n  transform: translateX(calc(var(--p)*177px)) rotateY(calc(var(--v)*3deg));\n  filter: drop-shadow(0 8px 5px #0007);\n}\n\n.sop-toggle.sop-fold .fold-tab>span {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(130deg,transparent 49%,#68392055 50%,transparent 51%);\n  box-shadow: inset 1px 1px 1px #ffe2c880;\n}\n\n.sop-toggle.sop-fold .fold-tab>i {\n  position: absolute;\n  left: 13px;\n  top: 29px;\n  width: 27px;\n  height: 1px;\n  background: #a3674d;\n  box-shadow: 0 1px 0 #ffcca499;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-fold .fold-guide {\n  position: absolute;\n  left: 83px;\n  bottom: -2px;\n  width: 105px;\n  height: 1px;\n  background: repeating-linear-gradient(90deg,#8d685055 0 3px,transparent 3px 7px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/fold/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-fold');\nif (!element) throw new Error('The Fold root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/fold/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/fold/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Fold\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/fold/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"fold\",\n    \"name\": \"Fold\",\n    \"initial\": false,\n    \"stiffness\": 250,\n    \"damping\": 21,\n    \"tone\": 280,\n    \"travel\": 177\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for FoldToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/fold/styles.css","code":"/* fold: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-fold {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-fold[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-fold:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-fold:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-fold {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-fold:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-fold:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-fold .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-fold .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-fold .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-fold .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-fold .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-fold .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-fold .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-fold,\n  .sop-toggle.sop-fold * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-fold {\n  width: 268px;\n  height: 118px;\n  border-radius: 10px;\n}\n\n.sop-toggle.sop-fold .fold-base {\n  position: absolute;\n  left: 4px;\n  top: 13px;\n  width: 260px;\n  height: 89px;\n  border-radius: 5px;\n  background: linear-gradient(170deg,#392a24,#231d1a 65%,#3f2e25);\n  border: 1px solid #85624743;\n  box-shadow: inset 0 2px 8px #0907049e,0 12px 16px #0007,0 1px 1px #b1865540;\n}\n\n.sop-toggle.sop-fold .fold-marks {\n  position: absolute;\n  left: 24px;\n  right: 24px;\n  top: 29px;\n  color: #ba8a6d;\n  font: 18px var(--sop-mono);\n  opacity: .5;\n}\n\n.sop-toggle.sop-fold .fold-marks>span {\n  float: right;\n}\n\n.sop-toggle.sop-fold .fold-paper {\n  position: absolute;\n  left: 34px;\n  top: 29px;\n  height: 55px;\n  width: calc(18px + var(--p)*163px);\n  display: flex;\n  filter: drop-shadow(0 8px 4px #0008);\n  perspective: 250px;\n  transform: skewY(calc(var(--v)*-.4deg));\n}\n\n.sop-toggle.sop-fold .fold-paper i {\n  display: block;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  background: linear-gradient(90deg,#bf7956,#edb487 80%,#ffd1a2);\n  clip-path: polygon(0 4%,100% 0,100% 100%,0 96%);\n  transform: skewY(calc(-7deg - (1 - var(--p))*22deg));\n  border-right: 1px solid #ffd1a459;\n}\n\n.sop-toggle.sop-fold .fold-paper i:nth-child(even) {\n  background: linear-gradient(90deg,#885139,#c4855b 73%,#d9976a);\n  transform: skewY(calc(7deg + (1 - var(--p))*22deg));\n  clip-path: polygon(0 0,100% 4%,100% 96%,0 100%);\n}\n\n.sop-toggle.sop-fold .fold-tab {\n  position: absolute;\n  left: 16px;\n  top: 22px;\n  width: 54px;\n  height: 69px;\n  background: linear-gradient(130deg,#ffd4ad,#e6aa7d 49%,#c48762 50%,#dd9a72 80%);\n  clip-path: polygon(0 6%,82% 0,100% 15%,100% 91%,14% 100%,0 83%);\n  transform: translateX(calc(var(--p)*177px)) rotateY(calc(var(--v)*3deg));\n  filter: drop-shadow(0 8px 5px #0007);\n}\n\n.sop-toggle.sop-fold .fold-tab>span {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(130deg,transparent 49%,#68392055 50%,transparent 51%);\n  box-shadow: inset 1px 1px 1px #ffe2c880;\n}\n\n.sop-toggle.sop-fold .fold-tab>i {\n  position: absolute;\n  left: 13px;\n  top: 29px;\n  width: 27px;\n  height: 1px;\n  background: #a3674d;\n  box-shadow: 0 1px 0 #ffcca499;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-fold .fold-guide {\n  position: absolute;\n  left: 83px;\n  bottom: -2px;\n  width: 105px;\n  height: 1px;\n  background: repeating-linear-gradient(90deg,#8d685055 0 3px,transparent 3px 7px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/fold/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-fold');\nif (!element)\n    throw new Error('The Fold root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/fold/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/fold/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Fold\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Fold — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Fold · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"false\" aria-label=\"Fold トグル\" class=\"sop-toggle sop-fold\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"fold-base\">\u003cspan class=\"fold-marks\">−\u003cspan>+\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"fold-paper\">\u003ci style=\"--i:0\">\u003c/i>\u003ci style=\"--i:1\">\u003c/i>\u003ci style=\"--i:2\">\u003c/i>\u003ci style=\"--i:3\">\u003c/i>\u003ci style=\"--i:4\">\u003c/i>\u003ci style=\"--i:5\">\u003c/i>\u003ci style=\"--i:6\">\u003c/i>\u003ci style=\"--i:7\">\u003c/i>\u003ci style=\"--i:8\">\u003c/i>\u003ci style=\"--i:9\">\u003c/i>\u003c/span>\u003cspan class=\"fold-tab\">\u003cspan>\u003c/span>\u003ci>\u003c/i>\u003c/span>\u003cspan class=\"fold-guide\">\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* fold: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-fold {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-fold[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-fold:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-fold:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-fold {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-fold:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-fold:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-fold .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-fold .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-fold .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-fold .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-fold .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-fold .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-fold .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-fold .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-fold,\n  .sop-toggle.sop-fold * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-fold {\n  width: 268px;\n  height: 118px;\n  border-radius: 10px;\n}\n\n.sop-toggle.sop-fold .fold-base {\n  position: absolute;\n  left: 4px;\n  top: 13px;\n  width: 260px;\n  height: 89px;\n  border-radius: 5px;\n  background: linear-gradient(170deg,#392a24,#231d1a 65%,#3f2e25);\n  border: 1px solid #85624743;\n  box-shadow: inset 0 2px 8px #0907049e,0 12px 16px #0007,0 1px 1px #b1865540;\n}\n\n.sop-toggle.sop-fold .fold-marks {\n  position: absolute;\n  left: 24px;\n  right: 24px;\n  top: 29px;\n  color: #ba8a6d;\n  font: 18px var(--sop-mono);\n  opacity: .5;\n}\n\n.sop-toggle.sop-fold .fold-marks>span {\n  float: right;\n}\n\n.sop-toggle.sop-fold .fold-paper {\n  position: absolute;\n  left: 34px;\n  top: 29px;\n  height: 55px;\n  width: calc(18px + var(--p)*163px);\n  display: flex;\n  filter: drop-shadow(0 8px 4px #0008);\n  perspective: 250px;\n  transform: skewY(calc(var(--v)*-.4deg));\n}\n\n.sop-toggle.sop-fold .fold-paper i {\n  display: block;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  background: linear-gradient(90deg,#bf7956,#edb487 80%,#ffd1a2);\n  clip-path: polygon(0 4%,100% 0,100% 100%,0 96%);\n  transform: skewY(calc(-7deg - (1 - var(--p))*22deg));\n  border-right: 1px solid #ffd1a459;\n}\n\n.sop-toggle.sop-fold .fold-paper i:nth-child(even) {\n  background: linear-gradient(90deg,#885139,#c4855b 73%,#d9976a);\n  transform: skewY(calc(7deg + (1 - var(--p))*22deg));\n  clip-path: polygon(0 0,100% 4%,100% 96%,0 100%);\n}\n\n.sop-toggle.sop-fold .fold-tab {\n  position: absolute;\n  left: 16px;\n  top: 22px;\n  width: 54px;\n  height: 69px;\n  background: linear-gradient(130deg,#ffd4ad,#e6aa7d 49%,#c48762 50%,#dd9a72 80%);\n  clip-path: polygon(0 6%,82% 0,100% 15%,100% 91%,14% 100%,0 83%);\n  transform: translateX(calc(var(--p)*177px)) rotateY(calc(var(--v)*3deg));\n  filter: drop-shadow(0 8px 5px #0007);\n}\n\n.sop-toggle.sop-fold .fold-tab>span {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(130deg,transparent 49%,#68392055 50%,transparent 51%);\n  box-shadow: inset 1px 1px 1px #ffe2c880;\n}\n\n.sop-toggle.sop-fold .fold-tab>i {\n  position: absolute;\n  left: 13px;\n  top: 29px;\n  width: 27px;\n  height: 1px;\n  background: #a3674d;\n  box-shadow: 0 1px 0 #ffcca499;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-fold .fold-guide {\n  position: absolute;\n  left: 83px;\n  bottom: -2px;\n  width: 105px;\n  height: 1px;\n  background: repeating-linear-gradient(90deg,#8d685055 0 3px,transparent 3px 7px);\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/fold/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/fold/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/fold/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"fold\",\n    \"name\": \"Fold\",\n    \"initial\": false,\n    \"stiffness\": 250,\n    \"damping\": 21,\n    \"tone\": 280,\n    \"travel\": 177\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for FoldToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/fold/demo/main.ts\");})();\n"}},{"id":"prism","name":"Prism","category":"toggles","order":9,"version":"2.3.0","tagline":"ひとつの光、無数の色。","description":"結晶が回転し、光のスペクトルを解き放つ。","material":"REFRACTED SPECTRUM","motion":"REFRACT","accent":"190,171,250","initial":true,"componentName":"PrismToggle","tags":["Prism","prism","トグル","REFRACTED SPECTRUM","結晶が回転し、光のスペクトルを解き放つ。"],"config":{"id":"prism","name":"Prism","initial":true,"stiffness":170,"damping":17,"tone":880,"travel":160},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Prism / 2.3.0\n\n結晶が回転し、光のスペクトルを解き放つ。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`PrismToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/prism/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/prism/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/prism/react/PrismToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/prism/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/prism/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/prism/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Prism — 再現仕様\n\n結晶が回転し、光のスペクトルを解き放つ。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 160px、ばねの剛性 170、減衰 17。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-prism` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 280px;\nheight: 140px;\nleft: 11px;\ntop: 26px;\nwidth: 258px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #3b31441c, #141921, #45345124, #9188b445, #ced6ff35, #c3bfff55, #b9cce722, #dec3f900, #92eed04a, #94e4fa20, #9988ff50, #ecafda33, #f4dda03b, #deedc047, #e7cafa00, #ffabd24a, #b7d0ff66, #a4f3d452, #c8d2ff29, #000b, #dcc3ff20, #e9f8ff, #989ac6, #e8b6eb, #daedbf, #3a8593, #cbb7ee, #fdc3d5, #bdc9ef, #6b527e, #ecd0ce, #589797, #b5dad1, #a5a9d4, #f0ebff, #8699bf, #e9d5e6, #8fc4d0\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/prism/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/prism/react/PrismToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"prism\",\n    \"name\": \"Prism\",\n    \"initial\": true,\n    \"stiffness\": 170,\n    \"damping\": 17,\n    \"tone\": 880,\n    \"travel\": 160\n};\n/** 結晶が回転し、光のスペクトルを解き放つ。 */\nexport default function PrismToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Prism トグル'} aria-checked={checked} className={`sop-toggle sop-prism ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"prism-caustic\">\u003c/span>\u003cspan className=\"prism-base\">\u003cspan className=\"prism-spectrum\">\u003c/span>\u003cspan className=\"prism-wire w1\">\u003c/span>\u003cspan className=\"prism-wire w2\">\u003c/span>\u003cspan className=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan className=\"prism-crystal\">\u003cspan className=\"crystal-back\">\u003c/span>\u003cspan className=\"crystal-facet\">\u003c/span>\u003cspan className=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan className=\"prism-spectral-line\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/prism/styles.css","code":"/* prism: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-prism {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-prism[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-prism:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-prism:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-prism {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-prism:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-prism:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-prism .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-prism .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-prism .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-prism .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-prism .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-prism .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-prism .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-prism,\n  .sop-toggle.sop-prism * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-prism {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-prism .prism-base {\n  position: absolute;\n  left: 11px;\n  top: 26px;\n  width: 258px;\n  height: 89px;\n  border-radius: 7px;\n  background: linear-gradient(130deg,#3b31441c,#141921 50%,#45345124);\n  border: 1px solid #9188b445;\n  clip-path: polygon(14px 0,calc(100% - 14px) 0,100% 14px,100% calc(100% - 14px),calc(100% - 14px) 100%,14px 100%,0 calc(100% - 14px),0 14px);\n  box-shadow: inset 0 1px 1px #ced6ff35,inset 0 -1px 2px #c3bfff55;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-prism .prism-base:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #b9cce722;\n  border-radius: 6px;\n}\n\n.sop-toggle.sop-prism .prism-spectrum {\n  position: absolute;\n  inset: -35px 0;\n  background: conic-gradient(from 20deg at 45% 50%,#dec3f900,#92eed04a,#94e4fa20,#9988ff50,#ecafda33,#f4dda03b,#deedc047,#e7cafa00);\n  transform: rotate(calc(var(--p)*65deg));\n  filter: blur(5px);\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-prism .prism-wire {\n  position: absolute;\n  left: 0;\n  right: 0;\n  height: 1px;\n  background: linear-gradient(90deg,#ffabd24a,#b7d0ff66,#a4f3d452);\n  transform-origin: 50% 50%;\n  opacity: calc(.25 + var(--p)*.5);\n}\n\n.sop-toggle.sop-prism .w1 {\n  top: 35%;\n  transform: rotate(8deg);\n}\n\n.sop-toggle.sop-prism .w2 {\n  top: 65%;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-prism .w3 {\n  top: 50%;\n  background: #c8d2ff29;\n}\n\n.sop-toggle.sop-prism .prism-crystal {\n  position: absolute;\n  left: 19px;\n  top: 22px;\n  width: 82px;\n  height: 96px;\n  transform: translateX(calc(var(--p)*160px)) rotate(calc(var(--p)*180deg));\n  filter: drop-shadow(0 5px 3px #000b) drop-shadow(0 0 7px #dcc3ff20);\n}\n\n.sop-toggle.sop-prism .crystal-back {\n  position: absolute;\n  inset: 0;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#e9f8ff,#989ac6,#e8b6eb,#daedbf,#3a8593,#cbb7ee,#fdc3d5,#e9f8ff);\n}\n\n.sop-toggle.sop-prism .crystal-facet {\n  position: absolute;\n  inset: 3px;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#bdc9ef 0 60deg,#6b527e 60deg 120deg,#ecd0ce 120deg 180deg,#589797 180deg 240deg,#b5dad1 240deg 300deg,#a5a9d4 300deg);\n  opacity: .9;\n}\n\n.sop-toggle.sop-prism .crystal-core {\n  position: absolute;\n  left: 21px;\n  right: 21px;\n  top: 25px;\n  bottom: 25px;\n  clip-path: polygon(50% 0,100% 25%,100% 75%,50% 100%,0 75%,0 25%);\n  background: conic-gradient(from 30deg,#f0ebff,#8699bf,#e9d5e6,#8fc4d0,#d0f2df,#c9c3fa);\n  box-shadow: inset 0 0 6px #fff;\n}\n\n.sop-toggle.sop-prism .prism-caustic {\n  position: absolute;\n  left: 0;\n  top: 70px;\n  width: 260px;\n  height: 65px;\n  background: conic-gradient(from -30deg at 80% 0%,transparent 0deg,#d791c655 35deg,#d9c18a50 60deg,#92daba44 88deg,#69afde50 120deg,#a088d247 145deg,transparent 170deg);\n  filter: blur(13px);\n  opacity: calc(var(--p)*.7);\n  transform: translateY(calc(var(--energy)*10px));\n}\n\n.sop-toggle.sop-prism .prism-spectral-line {\n  position: absolute;\n  left: 40px;\n  right: 40px;\n  bottom: 7px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#d59db088,#dcd5a68a,#a4c8cc80,#a1aed96e,transparent);\n  opacity: var(--p);\n  filter: blur(.5px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/prism/react/Example.tsx","code":"import React, { useState } from 'react';\nimport PrismToggle from './PrismToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cPrismToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/prism/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/prism/react/PrismToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"prism\",\n    \"name\": \"Prism\",\n    \"initial\": true,\n    \"stiffness\": 170,\n    \"damping\": 17,\n    \"tone\": 880,\n    \"travel\": 160\n};\n/** 結晶が回転し、光のスペクトルを解き放つ。 */\nexport default function PrismToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Prism トグル'} aria-checked={checked} className={`sop-toggle sop-prism ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"prism-caustic\">\u003c/span>\u003cspan className=\"prism-base\">\u003cspan className=\"prism-spectrum\">\u003c/span>\u003cspan className=\"prism-wire w1\">\u003c/span>\u003cspan className=\"prism-wire w2\">\u003c/span>\u003cspan className=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas className=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan className=\"prism-crystal\">\u003cspan className=\"crystal-back\">\u003c/span>\u003cspan className=\"crystal-facet\">\u003c/span>\u003cspan className=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan className=\"prism-spectral-line\">\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/prism/styles.css","code":"/* prism: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-prism {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-prism[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-prism:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-prism:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-prism {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-prism:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-prism:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-prism .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-prism .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-prism .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-prism .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-prism .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-prism .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-prism .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-prism,\n  .sop-toggle.sop-prism * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-prism {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-prism .prism-base {\n  position: absolute;\n  left: 11px;\n  top: 26px;\n  width: 258px;\n  height: 89px;\n  border-radius: 7px;\n  background: linear-gradient(130deg,#3b31441c,#141921 50%,#45345124);\n  border: 1px solid #9188b445;\n  clip-path: polygon(14px 0,calc(100% - 14px) 0,100% 14px,100% calc(100% - 14px),calc(100% - 14px) 100%,14px 100%,0 calc(100% - 14px),0 14px);\n  box-shadow: inset 0 1px 1px #ced6ff35,inset 0 -1px 2px #c3bfff55;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-prism .prism-base:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #b9cce722;\n  border-radius: 6px;\n}\n\n.sop-toggle.sop-prism .prism-spectrum {\n  position: absolute;\n  inset: -35px 0;\n  background: conic-gradient(from 20deg at 45% 50%,#dec3f900,#92eed04a,#94e4fa20,#9988ff50,#ecafda33,#f4dda03b,#deedc047,#e7cafa00);\n  transform: rotate(calc(var(--p)*65deg));\n  filter: blur(5px);\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-prism .prism-wire {\n  position: absolute;\n  left: 0;\n  right: 0;\n  height: 1px;\n  background: linear-gradient(90deg,#ffabd24a,#b7d0ff66,#a4f3d452);\n  transform-origin: 50% 50%;\n  opacity: calc(.25 + var(--p)*.5);\n}\n\n.sop-toggle.sop-prism .w1 {\n  top: 35%;\n  transform: rotate(8deg);\n}\n\n.sop-toggle.sop-prism .w2 {\n  top: 65%;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-prism .w3 {\n  top: 50%;\n  background: #c8d2ff29;\n}\n\n.sop-toggle.sop-prism .prism-crystal {\n  position: absolute;\n  left: 19px;\n  top: 22px;\n  width: 82px;\n  height: 96px;\n  transform: translateX(calc(var(--p)*160px)) rotate(calc(var(--p)*180deg));\n  filter: drop-shadow(0 5px 3px #000b) drop-shadow(0 0 7px #dcc3ff20);\n}\n\n.sop-toggle.sop-prism .crystal-back {\n  position: absolute;\n  inset: 0;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#e9f8ff,#989ac6,#e8b6eb,#daedbf,#3a8593,#cbb7ee,#fdc3d5,#e9f8ff);\n}\n\n.sop-toggle.sop-prism .crystal-facet {\n  position: absolute;\n  inset: 3px;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#bdc9ef 0 60deg,#6b527e 60deg 120deg,#ecd0ce 120deg 180deg,#589797 180deg 240deg,#b5dad1 240deg 300deg,#a5a9d4 300deg);\n  opacity: .9;\n}\n\n.sop-toggle.sop-prism .crystal-core {\n  position: absolute;\n  left: 21px;\n  right: 21px;\n  top: 25px;\n  bottom: 25px;\n  clip-path: polygon(50% 0,100% 25%,100% 75%,50% 100%,0 75%,0 25%);\n  background: conic-gradient(from 30deg,#f0ebff,#8699bf,#e9d5e6,#8fc4d0,#d0f2df,#c9c3fa);\n  box-shadow: inset 0 0 6px #fff;\n}\n\n.sop-toggle.sop-prism .prism-caustic {\n  position: absolute;\n  left: 0;\n  top: 70px;\n  width: 260px;\n  height: 65px;\n  background: conic-gradient(from -30deg at 80% 0%,transparent 0deg,#d791c655 35deg,#d9c18a50 60deg,#92daba44 88deg,#69afde50 120deg,#a088d247 145deg,transparent 170deg);\n  filter: blur(13px);\n  opacity: calc(var(--p)*.7);\n  transform: translateY(calc(var(--energy)*10px));\n}\n\n.sop-toggle.sop-prism .prism-spectral-line {\n  position: absolute;\n  left: 40px;\n  right: 40px;\n  bottom: 7px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#d59db088,#dcd5a68a,#a4c8cc80,#a1aed96e,transparent);\n  opacity: var(--p);\n  filter: blur(.5px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/prism/react/Example.jsx","code":"import React, { useState } from 'react';\nimport PrismToggle from './PrismToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cPrismToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/prism/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/prism/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"prism\",\n    \"name\": \"Prism\",\n    \"initial\": true,\n    \"stiffness\": 170,\n    \"damping\": 17,\n    \"tone\": 880,\n    \"travel\": 160\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for PrismToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/prism/styles.css","code":"/* prism: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-prism {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-prism[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-prism:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-prism:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-prism {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-prism:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-prism:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-prism .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-prism .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-prism .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-prism .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-prism .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-prism .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-prism .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-prism,\n  .sop-toggle.sop-prism * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-prism {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-prism .prism-base {\n  position: absolute;\n  left: 11px;\n  top: 26px;\n  width: 258px;\n  height: 89px;\n  border-radius: 7px;\n  background: linear-gradient(130deg,#3b31441c,#141921 50%,#45345124);\n  border: 1px solid #9188b445;\n  clip-path: polygon(14px 0,calc(100% - 14px) 0,100% 14px,100% calc(100% - 14px),calc(100% - 14px) 100%,14px 100%,0 calc(100% - 14px),0 14px);\n  box-shadow: inset 0 1px 1px #ced6ff35,inset 0 -1px 2px #c3bfff55;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-prism .prism-base:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #b9cce722;\n  border-radius: 6px;\n}\n\n.sop-toggle.sop-prism .prism-spectrum {\n  position: absolute;\n  inset: -35px 0;\n  background: conic-gradient(from 20deg at 45% 50%,#dec3f900,#92eed04a,#94e4fa20,#9988ff50,#ecafda33,#f4dda03b,#deedc047,#e7cafa00);\n  transform: rotate(calc(var(--p)*65deg));\n  filter: blur(5px);\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-prism .prism-wire {\n  position: absolute;\n  left: 0;\n  right: 0;\n  height: 1px;\n  background: linear-gradient(90deg,#ffabd24a,#b7d0ff66,#a4f3d452);\n  transform-origin: 50% 50%;\n  opacity: calc(.25 + var(--p)*.5);\n}\n\n.sop-toggle.sop-prism .w1 {\n  top: 35%;\n  transform: rotate(8deg);\n}\n\n.sop-toggle.sop-prism .w2 {\n  top: 65%;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-prism .w3 {\n  top: 50%;\n  background: #c8d2ff29;\n}\n\n.sop-toggle.sop-prism .prism-crystal {\n  position: absolute;\n  left: 19px;\n  top: 22px;\n  width: 82px;\n  height: 96px;\n  transform: translateX(calc(var(--p)*160px)) rotate(calc(var(--p)*180deg));\n  filter: drop-shadow(0 5px 3px #000b) drop-shadow(0 0 7px #dcc3ff20);\n}\n\n.sop-toggle.sop-prism .crystal-back {\n  position: absolute;\n  inset: 0;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#e9f8ff,#989ac6,#e8b6eb,#daedbf,#3a8593,#cbb7ee,#fdc3d5,#e9f8ff);\n}\n\n.sop-toggle.sop-prism .crystal-facet {\n  position: absolute;\n  inset: 3px;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#bdc9ef 0 60deg,#6b527e 60deg 120deg,#ecd0ce 120deg 180deg,#589797 180deg 240deg,#b5dad1 240deg 300deg,#a5a9d4 300deg);\n  opacity: .9;\n}\n\n.sop-toggle.sop-prism .crystal-core {\n  position: absolute;\n  left: 21px;\n  right: 21px;\n  top: 25px;\n  bottom: 25px;\n  clip-path: polygon(50% 0,100% 25%,100% 75%,50% 100%,0 75%,0 25%);\n  background: conic-gradient(from 30deg,#f0ebff,#8699bf,#e9d5e6,#8fc4d0,#d0f2df,#c9c3fa);\n  box-shadow: inset 0 0 6px #fff;\n}\n\n.sop-toggle.sop-prism .prism-caustic {\n  position: absolute;\n  left: 0;\n  top: 70px;\n  width: 260px;\n  height: 65px;\n  background: conic-gradient(from -30deg at 80% 0%,transparent 0deg,#d791c655 35deg,#d9c18a50 60deg,#92daba44 88deg,#69afde50 120deg,#a088d247 145deg,transparent 170deg);\n  filter: blur(13px);\n  opacity: calc(var(--p)*.7);\n  transform: translateY(calc(var(--energy)*10px));\n}\n\n.sop-toggle.sop-prism .prism-spectral-line {\n  position: absolute;\n  left: 40px;\n  right: 40px;\n  bottom: 7px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#d59db088,#dcd5a68a,#a4c8cc80,#a1aed96e,transparent);\n  opacity: var(--p);\n  filter: blur(.5px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/prism/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-prism');\nif (!element) throw new Error('The Prism root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/prism/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/prism/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Prism\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/prism/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"prism\",\n    \"name\": \"Prism\",\n    \"initial\": true,\n    \"stiffness\": 170,\n    \"damping\": 17,\n    \"tone\": 880,\n    \"travel\": 160\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for PrismToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/prism/styles.css","code":"/* prism: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-prism {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-prism[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-prism:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-prism:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-prism {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-prism:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-prism:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-prism .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-prism .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-prism .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-prism .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-prism .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-prism .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-prism .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-prism,\n  .sop-toggle.sop-prism * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-prism {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-prism .prism-base {\n  position: absolute;\n  left: 11px;\n  top: 26px;\n  width: 258px;\n  height: 89px;\n  border-radius: 7px;\n  background: linear-gradient(130deg,#3b31441c,#141921 50%,#45345124);\n  border: 1px solid #9188b445;\n  clip-path: polygon(14px 0,calc(100% - 14px) 0,100% 14px,100% calc(100% - 14px),calc(100% - 14px) 100%,14px 100%,0 calc(100% - 14px),0 14px);\n  box-shadow: inset 0 1px 1px #ced6ff35,inset 0 -1px 2px #c3bfff55;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-prism .prism-base:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #b9cce722;\n  border-radius: 6px;\n}\n\n.sop-toggle.sop-prism .prism-spectrum {\n  position: absolute;\n  inset: -35px 0;\n  background: conic-gradient(from 20deg at 45% 50%,#dec3f900,#92eed04a,#94e4fa20,#9988ff50,#ecafda33,#f4dda03b,#deedc047,#e7cafa00);\n  transform: rotate(calc(var(--p)*65deg));\n  filter: blur(5px);\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-prism .prism-wire {\n  position: absolute;\n  left: 0;\n  right: 0;\n  height: 1px;\n  background: linear-gradient(90deg,#ffabd24a,#b7d0ff66,#a4f3d452);\n  transform-origin: 50% 50%;\n  opacity: calc(.25 + var(--p)*.5);\n}\n\n.sop-toggle.sop-prism .w1 {\n  top: 35%;\n  transform: rotate(8deg);\n}\n\n.sop-toggle.sop-prism .w2 {\n  top: 65%;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-prism .w3 {\n  top: 50%;\n  background: #c8d2ff29;\n}\n\n.sop-toggle.sop-prism .prism-crystal {\n  position: absolute;\n  left: 19px;\n  top: 22px;\n  width: 82px;\n  height: 96px;\n  transform: translateX(calc(var(--p)*160px)) rotate(calc(var(--p)*180deg));\n  filter: drop-shadow(0 5px 3px #000b) drop-shadow(0 0 7px #dcc3ff20);\n}\n\n.sop-toggle.sop-prism .crystal-back {\n  position: absolute;\n  inset: 0;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#e9f8ff,#989ac6,#e8b6eb,#daedbf,#3a8593,#cbb7ee,#fdc3d5,#e9f8ff);\n}\n\n.sop-toggle.sop-prism .crystal-facet {\n  position: absolute;\n  inset: 3px;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#bdc9ef 0 60deg,#6b527e 60deg 120deg,#ecd0ce 120deg 180deg,#589797 180deg 240deg,#b5dad1 240deg 300deg,#a5a9d4 300deg);\n  opacity: .9;\n}\n\n.sop-toggle.sop-prism .crystal-core {\n  position: absolute;\n  left: 21px;\n  right: 21px;\n  top: 25px;\n  bottom: 25px;\n  clip-path: polygon(50% 0,100% 25%,100% 75%,50% 100%,0 75%,0 25%);\n  background: conic-gradient(from 30deg,#f0ebff,#8699bf,#e9d5e6,#8fc4d0,#d0f2df,#c9c3fa);\n  box-shadow: inset 0 0 6px #fff;\n}\n\n.sop-toggle.sop-prism .prism-caustic {\n  position: absolute;\n  left: 0;\n  top: 70px;\n  width: 260px;\n  height: 65px;\n  background: conic-gradient(from -30deg at 80% 0%,transparent 0deg,#d791c655 35deg,#d9c18a50 60deg,#92daba44 88deg,#69afde50 120deg,#a088d247 145deg,transparent 170deg);\n  filter: blur(13px);\n  opacity: calc(var(--p)*.7);\n  transform: translateY(calc(var(--energy)*10px));\n}\n\n.sop-toggle.sop-prism .prism-spectral-line {\n  position: absolute;\n  left: 40px;\n  right: 40px;\n  bottom: 7px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#d59db088,#dcd5a68a,#a4c8cc80,#a1aed96e,transparent);\n  opacity: var(--p);\n  filter: blur(.5px);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/prism/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-prism');\nif (!element)\n    throw new Error('The Prism root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/prism/markup.html","code":"\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/prism/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Prism\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Prism — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Prism · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"true\" aria-label=\"Prism トグル\" class=\"sop-toggle sop-prism\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"prism-caustic\">\u003c/span>\u003cspan class=\"prism-base\">\u003cspan class=\"prism-spectrum\">\u003c/span>\u003cspan class=\"prism-wire w1\">\u003c/span>\u003cspan class=\"prism-wire w2\">\u003c/span>\u003cspan class=\"prism-wire w3\">\u003c/span>\u003c/span>\u003ccanvas class=\"object-canvas\" height=\"280\" width=\"560\">\u003c/canvas>\u003cspan class=\"prism-crystal\">\u003cspan class=\"crystal-back\">\u003c/span>\u003cspan class=\"crystal-facet\">\u003c/span>\u003cspan class=\"crystal-core\">\u003c/span>\u003c/span>\u003cspan class=\"prism-spectral-line\">\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* prism: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-prism {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-prism[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-prism:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-prism:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-prism {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-prism:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-prism:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-prism .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-prism .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-prism .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-prism .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-prism .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-prism .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-prism .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-prism .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-prism,\n  .sop-toggle.sop-prism * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-prism {\n  width: 280px;\n  height: 140px;\n}\n\n.sop-toggle.sop-prism .prism-base {\n  position: absolute;\n  left: 11px;\n  top: 26px;\n  width: 258px;\n  height: 89px;\n  border-radius: 7px;\n  background: linear-gradient(130deg,#3b31441c,#141921 50%,#45345124);\n  border: 1px solid #9188b445;\n  clip-path: polygon(14px 0,calc(100% - 14px) 0,100% 14px,100% calc(100% - 14px),calc(100% - 14px) 100%,14px 100%,0 calc(100% - 14px),0 14px);\n  box-shadow: inset 0 1px 1px #ced6ff35,inset 0 -1px 2px #c3bfff55;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-prism .prism-base:before {\n  content: \"\";\n  position: absolute;\n  inset: 5px;\n  border: 1px solid #b9cce722;\n  border-radius: 6px;\n}\n\n.sop-toggle.sop-prism .prism-spectrum {\n  position: absolute;\n  inset: -35px 0;\n  background: conic-gradient(from 20deg at 45% 50%,#dec3f900,#92eed04a,#94e4fa20,#9988ff50,#ecafda33,#f4dda03b,#deedc047,#e7cafa00);\n  transform: rotate(calc(var(--p)*65deg));\n  filter: blur(5px);\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-prism .prism-wire {\n  position: absolute;\n  left: 0;\n  right: 0;\n  height: 1px;\n  background: linear-gradient(90deg,#ffabd24a,#b7d0ff66,#a4f3d452);\n  transform-origin: 50% 50%;\n  opacity: calc(.25 + var(--p)*.5);\n}\n\n.sop-toggle.sop-prism .w1 {\n  top: 35%;\n  transform: rotate(8deg);\n}\n\n.sop-toggle.sop-prism .w2 {\n  top: 65%;\n  transform: rotate(-8deg);\n}\n\n.sop-toggle.sop-prism .w3 {\n  top: 50%;\n  background: #c8d2ff29;\n}\n\n.sop-toggle.sop-prism .prism-crystal {\n  position: absolute;\n  left: 19px;\n  top: 22px;\n  width: 82px;\n  height: 96px;\n  transform: translateX(calc(var(--p)*160px)) rotate(calc(var(--p)*180deg));\n  filter: drop-shadow(0 5px 3px #000b) drop-shadow(0 0 7px #dcc3ff20);\n}\n\n.sop-toggle.sop-prism .crystal-back {\n  position: absolute;\n  inset: 0;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#e9f8ff,#989ac6,#e8b6eb,#daedbf,#3a8593,#cbb7ee,#fdc3d5,#e9f8ff);\n}\n\n.sop-toggle.sop-prism .crystal-facet {\n  position: absolute;\n  inset: 3px;\n  clip-path: polygon(50% 0,93% 25%,93% 75%,50% 100%,7% 75%,7% 25%);\n  background: conic-gradient(from 30deg,#bdc9ef 0 60deg,#6b527e 60deg 120deg,#ecd0ce 120deg 180deg,#589797 180deg 240deg,#b5dad1 240deg 300deg,#a5a9d4 300deg);\n  opacity: .9;\n}\n\n.sop-toggle.sop-prism .crystal-core {\n  position: absolute;\n  left: 21px;\n  right: 21px;\n  top: 25px;\n  bottom: 25px;\n  clip-path: polygon(50% 0,100% 25%,100% 75%,50% 100%,0 75%,0 25%);\n  background: conic-gradient(from 30deg,#f0ebff,#8699bf,#e9d5e6,#8fc4d0,#d0f2df,#c9c3fa);\n  box-shadow: inset 0 0 6px #fff;\n}\n\n.sop-toggle.sop-prism .prism-caustic {\n  position: absolute;\n  left: 0;\n  top: 70px;\n  width: 260px;\n  height: 65px;\n  background: conic-gradient(from -30deg at 80% 0%,transparent 0deg,#d791c655 35deg,#d9c18a50 60deg,#92daba44 88deg,#69afde50 120deg,#a088d247 145deg,transparent 170deg);\n  filter: blur(13px);\n  opacity: calc(var(--p)*.7);\n  transform: translateY(calc(var(--energy)*10px));\n}\n\n.sop-toggle.sop-prism .prism-spectral-line {\n  position: absolute;\n  left: 40px;\n  right: 40px;\n  bottom: 7px;\n  height: 1px;\n  background: linear-gradient(90deg,transparent,#d59db088,#dcd5a68a,#a4c8cc80,#a1aed96e,transparent);\n  opacity: var(--p);\n  filter: blur(.5px);\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/prism/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/prism/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/prism/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"prism\",\n    \"name\": \"Prism\",\n    \"initial\": true,\n    \"stiffness\": 170,\n    \"damping\": 17,\n    \"tone\": 880,\n    \"travel\": 160\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for PrismToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/prism/demo/main.ts\");})();\n"}},{"id":"signal","name":"Signal","category":"toggles","order":10,"version":"2.3.0","tagline":"静寂から、律動へ。","description":"ロッカーを倒すと、ドットの波形が目を覚ます。","material":"DIGITAL PULSE","motion":"PULSE","accent":"192,229,135","initial":false,"componentName":"SignalToggle","tags":["Signal","signal","トグル","DIGITAL PULSE","ロッカーを倒すと、ドットの波形が目を覚ます。"],"config":{"id":"signal","name":"Signal","initial":false,"stiffness":460,"damping":29,"tone":130,"travel":140},"related":["original-surface"],"props":[["checked","boolean","外部からON/OFFを制御します。"],["defaultChecked","boolean","外部制御しない場合の初期状態です。"],["onCheckedChange","(checked: boolean) => void","操作により要求された状態を受け取ります。"],["disabled","boolean","操作を無効にします。"],["aria-label","string","機能を示す名前。例：通知を有効にする。"],["className / style","標準のReact属性","外側の配置やサイズを調整します。"]],"markup":"\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","usage":"# Signal / 2.3.0\n\nロッカーを倒すと、ドットの波形が目を覚ます。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`SignalToggle` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/toggles/signal/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `checked` (boolean): 外部からON/OFFを制御します。\n- `defaultChecked` (boolean): 外部制御しない場合の初期状態です。\n- `onCheckedChange` ((checked: boolean) => void): 操作により要求された状態を受け取ります。\n- `disabled` (boolean): 操作を無効にします。\n- `aria-label` (string): 機能を示す名前。例：通知を有効にする。\n- `className / style` (標準のReact属性): 外側の配置やサイズを調整します。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/signal/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/toggles/signal/react/SignalToggle.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/toggles/signal/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/toggles/signal/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/toggles/signal/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Signal — 再現仕様\n\nロッカーを倒すと、ドットの波形が目を覚ます。 展示全体ではなく、このトグルを単独の再利用可能な部品として実装する。\n\n## 固定する外観\n元の `markup.html` のレイヤー順を保つ。サイズ、各レイヤーの位置、色、グラデーション、影、素材感は `styles.css` を正本とする。CSS/SVG/Canvasで描かれた装飾を、絵文字、画像、一般的な単色スイッチへ置換しない。周囲の展示番号や見出し、背景カードはトグルに埋め込まない。\n\n## 動き\nOFFは左、ONは右。移動距離 140px、ばねの剛性 460、減衰 29。`--p` は0〜1を目標に動く連続値で、途中のオーバーシュートも含む。`--v` は速度、`--energy` は変形や余韻の強さ。元の計算と演出を維持し、単なる一定速度のスライドへ置換しない。`renderer` の素材別処理も必要ファイルに含める。\n\n## 操作と状態\nクリック、Enter、Spaceで切り替える。左右キーでOFF/ONを指定。左右ドラッグは7pxのしきい値を持ち、縦のタッチ操作はページのスクロールに使う。無効状態では操作しない。公開状態をアプリから制御できる。アニメーションを減らす設定では状態に直接移動し、装飾の連続描画を止める。\n\n## 組み込みの条件\nCSSはルートの `.sop-signal` に閉じる。同一ページへの複数配置、各個体の独立した状態、アンマウント後のイベント/RAF/Observer解除を維持する。Reactのcontrolled/uncontrolledをどちらも支援する。参照先に存在しない共通ファイルを残さない。音は任意であり、描画の依存にしない。\n\n## 確認すること\nOFF / ON / 切り替え途中の3状態を参照実装と比較する。サイズと陰影、つまみの形、素材の特徴、停止後の余韻が一致していること。統合先では初期状態とラベルだけを目的に合わせて変更し、指定がない視覚値は維持する。\n\n## 外観の数値（元CSSから抽出）\n\n```css\nborder-radius: 60px;\nwidth: 100%;\nheight: 100%;\nwidth: 100%;\nheight: 100%;\nwidth: 7px;\nheight: 7px;\nborder-radius: 50%;\nleft: 1px;\ntop: 3px;\nheight: 1px;\ntop: 10px;\nleft: 11px;\ntop: 10px;\nleft: 11px;\nwidth: 268px;\nheight: 122px;\nborder-radius: 13px;\nborder-radius: 12px;\nleft: 17px;\n```\n\n色: #d1e6b2, #8b8d8d, #333737, #111313, #5d6162, #060707, #171919, #343b32, #20261f, #1c211b, #31362e, #68705b66, #0009, #070d06, #abb39941, #a7b399, #a3d563, #b9e57d60, #081005, #4e5d3a9c, #000, #b8c09e27, #d4f2ab10, #f1ffcc19, #080e06, #b1c59833, #111d0c, #515e43, #3a472d, #25331c, #35432a, #4d5b3c, #d0e2b458, #081104, #81906430, #a8b69a, #c7f18e, #c9ff7e9e, #748667, #a4c786\n\n全レイヤーの寸法、陰影、グラデーションはコード込み形式のstyles.cssに記載しています。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/toggles/signal/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/toggles/signal/react/SignalToggle.tsx","code":"'use client';\nimport React, { type CSSProperties } from 'react';\nimport { useToggle, type ToggleProps } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"signal\",\n    \"name\": \"Signal\",\n    \"initial\": false,\n    \"stiffness\": 460,\n    \"damping\": 29,\n    \"tone\": 130,\n    \"travel\": 140\n};\n/** ロッカーを倒すと、ドットの波形が目を覚ます。 */\nexport default function SignalToggle(props: ToggleProps) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Signal トグル'} aria-checked={checked} className={`sop-toggle sop-signal ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"signal-body\">\u003cspan className=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan className=\"signal-screen\">\u003ccanvas className=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan className=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan className=\"rocker-mount\">\u003cspan className=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan className=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"tsx","group":"component"},{"name":"src/parts/toggles/signal/styles.css","code":"/* signal: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-signal {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-signal[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-signal:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-signal:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-signal {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-signal:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-signal:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-signal .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-signal .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-signal .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-signal .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-signal .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-signal .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-signal .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-signal,\n  .sop-toggle.sop-signal * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-signal {\n  width: 268px;\n  height: 122px;\n  border-radius: 13px;\n}\n\n.sop-toggle.sop-signal .signal-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 12px;\n  background: linear-gradient(150deg,#343b32,#20261f 35%,#1c211b 79%,#31362e);\n  border: 1px solid #68705b66;\n  box-shadow: 0 12px 22px #0009,0 2px 1px #070d06,inset 0 1px 1px #abb39941;\n}\n\n.sop-toggle.sop-signal .signal-header {\n  position: absolute;\n  left: 17px;\n  top: 12px;\n  right: 17px;\n  color: #a7b399;\n  font-size: 6px;\n  letter-spacing: 1.3px;\n}\n\n.sop-toggle.sop-signal .signal-header i {\n  position: absolute;\n  right: 2px;\n  top: 0;\n  width: 4px;\n  height: 4px;\n  border-radius: 50%;\n  background: #a3d563;\n  box-shadow: 0 0 5px #b9e57d60;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-signal .signal-screen {\n  position: absolute;\n  left: 15px;\n  top: 29px;\n  width: 165px;\n  height: 59px;\n  border-radius: 5px;\n  background: #081005;\n  border: 1px solid #4e5d3a9c;\n  box-shadow: inset 0 1px 5px #000,0 1px 0 #b8c09e27;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-signal .signal-screen .object-canvas {\n  left: 6px;\n  top: 1px;\n  width: 152px;\n  height: 56px;\n}\n\n.sop-toggle.sop-signal .screen-glare {\n  position: absolute;\n  inset: 0;\n  border-radius: 4px;\n  background: linear-gradient(155deg,#d4f2ab10,transparent 43%);\n  box-shadow: inset 0 1px 1px #f1ffcc19;\n}\n\n.sop-toggle.sop-signal .rocker-mount {\n  position: absolute;\n  right: 17px;\n  top: 26px;\n  width: 54px;\n  height: 68px;\n  background: #080e06;\n  border-radius: 6px;\n  box-shadow: inset 0 0 3px #000,0 1px 1px #b1c59833;\n  border: 1px solid #111d0c;\n  perspective: 140px;\n}\n\n.sop-toggle.sop-signal .signal-rocker {\n  position: absolute;\n  inset: 4px;\n  border-radius: 4px;\n  background: linear-gradient(#515e43,#3a472d 30%,#25331c 57%,#35432a 89%,#4d5b3c);\n  box-shadow: 0 3px 4px #000,inset 0 1px 1px #d0e2b458,inset 0 -1px 1px #081104;\n  border: 1px solid #81906430;\n  transform: rotateX(calc(-19deg + var(--p)*38deg));\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: space-around;\n  padding: 3px 0;\n}\n\n.sop-toggle.sop-signal .signal-rocker>span {\n  font: 10px Arial;\n  color: #a8b69a;\n  text-shadow: 0 1px 1px #000;\n}\n\n.sop-toggle.sop-signal .signal-rocker>i {\n  height: 2px;\n  width: 16px;\n  background: #c7f18e;\n  box-shadow: 0 0 6px #c9ff7e9e;\n  opacity: calc(.08 + var(--p)*.85);\n  border-radius: 1px;\n}\n\n.sop-toggle.sop-signal .signal-bottom {\n  position: absolute;\n  bottom: 14px;\n  left: 18px;\n  right: 82px;\n  font-size: 5px;\n  color: #748667;\n  letter-spacing: 1px;\n  display: flex;\n  justify-content: space-between;\n}\n\n.sop-toggle.sop-signal .signal-bottom>span:first-child {\n  color: #a4c786;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.ts","code":"'use client';\nimport { useEffect, useRef, useState, type ButtonHTMLAttributes } from 'react';\nimport { createToggleController, type ToggleConfig, type ToggleController } from './toggle-controller';\nexport interface ToggleProps extends Omit\u003cButtonHTMLAttributes\u003cHTMLButtonElement>, 'onChange' | 'children' | 'defaultChecked'> {\n    checked?: boolean;\n    defaultChecked?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config: ToggleConfig, props: ToggleProps) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef\u003cHTMLButtonElement | null>(null);\n    const control = useRef\u003cToggleController | null>(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => { if (props.disabled)\n        control.current?.cancelInteraction(); }, [props.disabled]);\n    return { element, checked };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/signal/react/Example.tsx","code":"import React, { useState } from 'react';\nimport SignalToggle from './SignalToggle';\n\nexport default function Example() {\n  const [enabled, setEnabled] = useState(false);\n  return \u003cSignalToggle\n    checked={enabled}\n    onCheckedChange={setEnabled}\n    aria-label=\"通知を有効にする\"\n  />;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/toggles/signal/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/toggles/signal/react/SignalToggle.jsx","code":"'use client';\nimport React from 'react';\nimport { useToggle } from '../../../../shared/use-toggle';\nimport '../styles.css';\nconst config = {\n    \"id\": \"signal\",\n    \"name\": \"Signal\",\n    \"initial\": false,\n    \"stiffness\": 460,\n    \"damping\": 29,\n    \"tone\": 130,\n    \"travel\": 140\n};\n/** ロッカーを倒すと、ドットの波形が目を覚ます。 */\nexport default function SignalToggle(props) {\n    const { element, checked } = useToggle(config, props);\n    const { checked: _checked, defaultChecked: _initial, onCheckedChange: _change, className = '', ...buttonProps } = props;\n    return (\u003cbutton {...buttonProps} ref={element} type=\"button\" role=\"switch\" aria-label={props['aria-label'] ?? 'Signal トグル'} aria-checked={checked} className={`sop-toggle sop-signal ${className}`}>\n      \u003cspan aria-hidden=\"true\" className=\"switch-art\">\u003cspan className=\"signal-body\">\u003cspan className=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan className=\"signal-screen\">\u003ccanvas className=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan className=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan className=\"rocker-mount\">\u003cspan className=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan className=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\n    \u003c/button>);\n}\n","language":"jsx","group":"component"},{"name":"src/parts/toggles/signal/styles.css","code":"/* signal: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-signal {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-signal[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-signal:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-signal:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-signal {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-signal:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-signal:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-signal .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-signal .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-signal .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-signal .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-signal .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-signal .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-signal .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-signal,\n  .sop-toggle.sop-signal * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-signal {\n  width: 268px;\n  height: 122px;\n  border-radius: 13px;\n}\n\n.sop-toggle.sop-signal .signal-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 12px;\n  background: linear-gradient(150deg,#343b32,#20261f 35%,#1c211b 79%,#31362e);\n  border: 1px solid #68705b66;\n  box-shadow: 0 12px 22px #0009,0 2px 1px #070d06,inset 0 1px 1px #abb39941;\n}\n\n.sop-toggle.sop-signal .signal-header {\n  position: absolute;\n  left: 17px;\n  top: 12px;\n  right: 17px;\n  color: #a7b399;\n  font-size: 6px;\n  letter-spacing: 1.3px;\n}\n\n.sop-toggle.sop-signal .signal-header i {\n  position: absolute;\n  right: 2px;\n  top: 0;\n  width: 4px;\n  height: 4px;\n  border-radius: 50%;\n  background: #a3d563;\n  box-shadow: 0 0 5px #b9e57d60;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-signal .signal-screen {\n  position: absolute;\n  left: 15px;\n  top: 29px;\n  width: 165px;\n  height: 59px;\n  border-radius: 5px;\n  background: #081005;\n  border: 1px solid #4e5d3a9c;\n  box-shadow: inset 0 1px 5px #000,0 1px 0 #b8c09e27;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-signal .signal-screen .object-canvas {\n  left: 6px;\n  top: 1px;\n  width: 152px;\n  height: 56px;\n}\n\n.sop-toggle.sop-signal .screen-glare {\n  position: absolute;\n  inset: 0;\n  border-radius: 4px;\n  background: linear-gradient(155deg,#d4f2ab10,transparent 43%);\n  box-shadow: inset 0 1px 1px #f1ffcc19;\n}\n\n.sop-toggle.sop-signal .rocker-mount {\n  position: absolute;\n  right: 17px;\n  top: 26px;\n  width: 54px;\n  height: 68px;\n  background: #080e06;\n  border-radius: 6px;\n  box-shadow: inset 0 0 3px #000,0 1px 1px #b1c59833;\n  border: 1px solid #111d0c;\n  perspective: 140px;\n}\n\n.sop-toggle.sop-signal .signal-rocker {\n  position: absolute;\n  inset: 4px;\n  border-radius: 4px;\n  background: linear-gradient(#515e43,#3a472d 30%,#25331c 57%,#35432a 89%,#4d5b3c);\n  box-shadow: 0 3px 4px #000,inset 0 1px 1px #d0e2b458,inset 0 -1px 1px #081104;\n  border: 1px solid #81906430;\n  transform: rotateX(calc(-19deg + var(--p)*38deg));\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: space-around;\n  padding: 3px 0;\n}\n\n.sop-toggle.sop-signal .signal-rocker>span {\n  font: 10px Arial;\n  color: #a8b69a;\n  text-shadow: 0 1px 1px #000;\n}\n\n.sop-toggle.sop-signal .signal-rocker>i {\n  height: 2px;\n  width: 16px;\n  background: #c7f18e;\n  box-shadow: 0 0 6px #c9ff7e9e;\n  opacity: calc(.08 + var(--p)*.85);\n  border-radius: 1px;\n}\n\n.sop-toggle.sop-signal .signal-bottom {\n  position: absolute;\n  bottom: 14px;\n  left: 18px;\n  right: 82px;\n  font-size: 5px;\n  color: #748667;\n  letter-spacing: 1px;\n  display: flex;\n  justify-content: space-between;\n}\n\n.sop-toggle.sop-signal .signal-bottom>span:first-child {\n  color: #a4c786;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/use-toggle.js","code":"'use client';\nimport { useEffect, useRef, useState } from 'react';\nimport { createToggleController } from './toggle-controller';\n/** React owns the committed state/ARIA; the controller owns only transient artwork values. */\nexport function useToggle(config, props) {\n    const [internal, setInternal] = useState(props.defaultChecked ?? config.initial);\n    const checked = props.checked ?? internal;\n    const element = useRef(null);\n    const control = useRef(null);\n    const latest = useRef(props);\n    latest.current = props;\n    const initial = useRef(checked);\n    useEffect(() => {\n        const button = element.current;\n        if (!button)\n            return;\n        const controller = createToggleController(button, config, {\n            checked: initial.current, controlled: true, manageAria: false,\n            onCheckedChange(next) {\n                if (latest.current.checked === undefined)\n                    setInternal(next);\n                latest.current.onCheckedChange?.(next);\n            }\n        });\n        control.current = controller;\n        return () => { controller.destroy(); control.current = null; };\n    }, [config]);\n    useEffect(() => { control.current?.setChecked(checked); }, [checked]);\n    useEffect(() => {\n        if (props.disabled)\n            control.current?.cancelInteraction();\n    }, [props.disabled]);\n    return { element, checked };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/signal/react/Example.jsx","code":"import React, { useState } from 'react';\nimport SignalToggle from './SignalToggle';\nexport default function Example() {\n    const [enabled, setEnabled] = useState(false);\n    return \u003cSignalToggle checked={enabled} onCheckedChange={setEnabled} aria-label=\"通知を有効にする\"/>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/toggles/signal/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/toggles/signal/vanilla/init.ts","code":"import { createToggleController, type ToggleOptions } from '../../../../shared/toggle-controller';\nconst config = {\n    \"id\": \"signal\",\n    \"name\": \"Signal\",\n    \"initial\": false,\n    \"stiffness\": 460,\n    \"damping\": 29,\n    \"tone\": 130,\n    \"travel\": 140\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element: HTMLElement, options: ToggleOptions = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for SignalToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"typescript","group":"component"},{"name":"src/parts/toggles/signal/styles.css","code":"/* signal: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-signal {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-signal[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-signal:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-signal:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-signal {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-signal:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-signal:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-signal .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-signal .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-signal .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-signal .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-signal .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-signal .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-signal .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-signal,\n  .sop-toggle.sop-signal * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-signal {\n  width: 268px;\n  height: 122px;\n  border-radius: 13px;\n}\n\n.sop-toggle.sop-signal .signal-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 12px;\n  background: linear-gradient(150deg,#343b32,#20261f 35%,#1c211b 79%,#31362e);\n  border: 1px solid #68705b66;\n  box-shadow: 0 12px 22px #0009,0 2px 1px #070d06,inset 0 1px 1px #abb39941;\n}\n\n.sop-toggle.sop-signal .signal-header {\n  position: absolute;\n  left: 17px;\n  top: 12px;\n  right: 17px;\n  color: #a7b399;\n  font-size: 6px;\n  letter-spacing: 1.3px;\n}\n\n.sop-toggle.sop-signal .signal-header i {\n  position: absolute;\n  right: 2px;\n  top: 0;\n  width: 4px;\n  height: 4px;\n  border-radius: 50%;\n  background: #a3d563;\n  box-shadow: 0 0 5px #b9e57d60;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-signal .signal-screen {\n  position: absolute;\n  left: 15px;\n  top: 29px;\n  width: 165px;\n  height: 59px;\n  border-radius: 5px;\n  background: #081005;\n  border: 1px solid #4e5d3a9c;\n  box-shadow: inset 0 1px 5px #000,0 1px 0 #b8c09e27;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-signal .signal-screen .object-canvas {\n  left: 6px;\n  top: 1px;\n  width: 152px;\n  height: 56px;\n}\n\n.sop-toggle.sop-signal .screen-glare {\n  position: absolute;\n  inset: 0;\n  border-radius: 4px;\n  background: linear-gradient(155deg,#d4f2ab10,transparent 43%);\n  box-shadow: inset 0 1px 1px #f1ffcc19;\n}\n\n.sop-toggle.sop-signal .rocker-mount {\n  position: absolute;\n  right: 17px;\n  top: 26px;\n  width: 54px;\n  height: 68px;\n  background: #080e06;\n  border-radius: 6px;\n  box-shadow: inset 0 0 3px #000,0 1px 1px #b1c59833;\n  border: 1px solid #111d0c;\n  perspective: 140px;\n}\n\n.sop-toggle.sop-signal .signal-rocker {\n  position: absolute;\n  inset: 4px;\n  border-radius: 4px;\n  background: linear-gradient(#515e43,#3a472d 30%,#25331c 57%,#35432a 89%,#4d5b3c);\n  box-shadow: 0 3px 4px #000,inset 0 1px 1px #d0e2b458,inset 0 -1px 1px #081104;\n  border: 1px solid #81906430;\n  transform: rotateX(calc(-19deg + var(--p)*38deg));\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: space-around;\n  padding: 3px 0;\n}\n\n.sop-toggle.sop-signal .signal-rocker>span {\n  font: 10px Arial;\n  color: #a8b69a;\n  text-shadow: 0 1px 1px #000;\n}\n\n.sop-toggle.sop-signal .signal-rocker>i {\n  height: 2px;\n  width: 16px;\n  background: #c7f18e;\n  box-shadow: 0 0 6px #c9ff7e9e;\n  opacity: calc(.08 + var(--p)*.85);\n  border-radius: 1px;\n}\n\n.sop-toggle.sop-signal .signal-bottom {\n  position: absolute;\n  bottom: 14px;\n  left: 18px;\n  right: 82px;\n  font-size: 5px;\n  color: #748667;\n  letter-spacing: 1px;\n  display: flex;\n  justify-content: space-between;\n}\n\n.sop-toggle.sop-signal .signal-bottom>span:first-child {\n  color: #a4c786;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.ts","code":"import { Spring, isDrag, dragValue } from './motion';\nimport { ObjectRenderer } from './renderer';\nexport interface ToggleConfig {\n    id: string;\n    name: string;\n    initial: boolean;\n    stiffness: number;\n    damping: number;\n    travel: number;\n    tone: number;\n}\nexport interface ToggleOptions {\n    checked?: boolean;\n    controlled?: boolean;\n    manageAria?: boolean;\n    onCheckedChange?: (checked: boolean) => void;\n}\nexport interface ToggleController {\n    setChecked: (checked: boolean, immediate?: boolean) => void;\n    getChecked: () => boolean;\n    cancelInteraction: () => void;\n    resize: () => void;\n    destroy: () => void;\n    setPaused: (paused: boolean) => void;\n}\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root: HTMLElement) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map\u003cstring, string>();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button: HTMLButtonElement, config: ToggleConfig, options: ToggleOptions = {}): ToggleController {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer: {\n        id: number;\n        x: number;\n        y: number;\n        value: number;\n        scale: number;\n        dragging: boolean;\n    } | null = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time: number) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value: boolean, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value: boolean) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => { if (pointer)\n        cancelInteraction(); }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) { paused = value; if (paused) {\n            cancelInteraction();\n            pause();\n        }\n        else\n            requestFrame(); },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/motion.ts","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value: number;\n    target: number;\n    velocity = 0;\n    stiffness: number;\n    damping: number;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value: number) { this.target = clamp(value); }\n    snap(value: number) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt: number) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx: number, dy: number, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start: number, dx: number, travel: number, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value: number) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"typescript","group":"shared"},{"name":"src/shared/renderer.ts","code":"import { clamp, orbitPosition } from \"./motion\";\nimport type { ToggleConfig } from \"./toggle-controller\";\ninterface Particle {\n    x: number;\n    y: number;\n    vx: number;\n    vy: number;\n    life: number;\n    age: number;\n    size: number;\n    hue: number;\n}\nconst LETTERS: Record\u003cstring, string[]> = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button: HTMLButtonElement;\n    config: ToggleConfig;\n    canvas: HTMLCanvasElement | null;\n    ctx: CanvasRenderingContext2D | null;\n    particles: Particle[];\n    reelAngle: number;\n    effectAge: number;\n    lastValue: number | null;\n    lastEnergy: number;\n    size: {\n        w: number;\n        h: number;\n    };\n    constructor(button: HTMLButtonElement, config: ToggleConfig) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on: boolean, reduced: boolean) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value: number, velocity: number, t: number, dt: number, reduced: boolean) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx: CanvasRenderingContext2D, w: number, h: number, p: number, t: number) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx: CanvasRenderingContext2D, dt: number) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/toggles/signal/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-signal');\nif (!element) throw new Error('The Signal root was not found.');\nconst controller = init(element, {\n  checked: false,\n  onCheckedChange(checked) {\n    console.log(\"checked:\", checked);\n  }\n});\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/toggles/signal/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/signal/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Signal\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/toggles/signal/vanilla/init.js","code":"import { createToggleController } from '../../../../shared/toggle-controller.js';\nconst config = {\n    \"id\": \"signal\",\n    \"name\": \"Signal\",\n    \"initial\": false,\n    \"stiffness\": 460,\n    \"damping\": 29,\n    \"tone\": 130,\n    \"travel\": 140\n};\n/** Pass the component button, not a selector or a gallery card. */\nexport function init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for SignalToggle.');\n    return createToggleController(element, config, options);\n}\n","language":"javascript","group":"component"},{"name":"src/parts/toggles/signal/styles.css","code":"/* signal: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-signal {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-signal[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-signal:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-signal:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-signal {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-signal:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-signal:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-signal .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-signal .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-signal .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-signal .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-signal .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-signal .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-signal .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-signal,\n  .sop-toggle.sop-signal * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-signal {\n  width: 268px;\n  height: 122px;\n  border-radius: 13px;\n}\n\n.sop-toggle.sop-signal .signal-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 12px;\n  background: linear-gradient(150deg,#343b32,#20261f 35%,#1c211b 79%,#31362e);\n  border: 1px solid #68705b66;\n  box-shadow: 0 12px 22px #0009,0 2px 1px #070d06,inset 0 1px 1px #abb39941;\n}\n\n.sop-toggle.sop-signal .signal-header {\n  position: absolute;\n  left: 17px;\n  top: 12px;\n  right: 17px;\n  color: #a7b399;\n  font-size: 6px;\n  letter-spacing: 1.3px;\n}\n\n.sop-toggle.sop-signal .signal-header i {\n  position: absolute;\n  right: 2px;\n  top: 0;\n  width: 4px;\n  height: 4px;\n  border-radius: 50%;\n  background: #a3d563;\n  box-shadow: 0 0 5px #b9e57d60;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-signal .signal-screen {\n  position: absolute;\n  left: 15px;\n  top: 29px;\n  width: 165px;\n  height: 59px;\n  border-radius: 5px;\n  background: #081005;\n  border: 1px solid #4e5d3a9c;\n  box-shadow: inset 0 1px 5px #000,0 1px 0 #b8c09e27;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-signal .signal-screen .object-canvas {\n  left: 6px;\n  top: 1px;\n  width: 152px;\n  height: 56px;\n}\n\n.sop-toggle.sop-signal .screen-glare {\n  position: absolute;\n  inset: 0;\n  border-radius: 4px;\n  background: linear-gradient(155deg,#d4f2ab10,transparent 43%);\n  box-shadow: inset 0 1px 1px #f1ffcc19;\n}\n\n.sop-toggle.sop-signal .rocker-mount {\n  position: absolute;\n  right: 17px;\n  top: 26px;\n  width: 54px;\n  height: 68px;\n  background: #080e06;\n  border-radius: 6px;\n  box-shadow: inset 0 0 3px #000,0 1px 1px #b1c59833;\n  border: 1px solid #111d0c;\n  perspective: 140px;\n}\n\n.sop-toggle.sop-signal .signal-rocker {\n  position: absolute;\n  inset: 4px;\n  border-radius: 4px;\n  background: linear-gradient(#515e43,#3a472d 30%,#25331c 57%,#35432a 89%,#4d5b3c);\n  box-shadow: 0 3px 4px #000,inset 0 1px 1px #d0e2b458,inset 0 -1px 1px #081104;\n  border: 1px solid #81906430;\n  transform: rotateX(calc(-19deg + var(--p)*38deg));\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: space-around;\n  padding: 3px 0;\n}\n\n.sop-toggle.sop-signal .signal-rocker>span {\n  font: 10px Arial;\n  color: #a8b69a;\n  text-shadow: 0 1px 1px #000;\n}\n\n.sop-toggle.sop-signal .signal-rocker>i {\n  height: 2px;\n  width: 16px;\n  background: #c7f18e;\n  box-shadow: 0 0 6px #c9ff7e9e;\n  opacity: calc(.08 + var(--p)*.85);\n  border-radius: 1px;\n}\n\n.sop-toggle.sop-signal .signal-bottom {\n  position: absolute;\n  bottom: 14px;\n  left: 18px;\n  right: 82px;\n  font-size: 5px;\n  color: #748667;\n  letter-spacing: 1px;\n  display: flex;\n  justify-content: space-between;\n}\n\n.sop-toggle.sop-signal .signal-bottom>span:first-child {\n  color: #a4c786;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/toggle-controller.js","code":"import { Spring, isDrag, dragValue } from './motion.js';\nimport { ObjectRenderer } from './renderer.js';\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nexport function createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && isDrag(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap(dragValue(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/motion.js","code":"/* Original spring physics, independent of the gallery and React. */\nexport const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexport class Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = clamp(value); }\n    snap(value) { this.value = this.target = clamp(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexport function isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nexport function dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return clamp(start);\n    return clamp(start + dx / (travel * Math.max(0.1, scale)));\n}\nexport function orbitPosition(value) {\n    const p = clamp(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n","language":"javascript","group":"shared"},{"name":"src/shared/renderer.js","code":"import { clamp, orbitPosition } from \"./motion.js\";\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nexport class ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = clamp(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = orbitPosition(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/toggles/signal/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-signal');\nif (!element)\n    throw new Error('The Signal root was not found.');\nconst controller = init(element, {\n    checked: false,\n    onCheckedChange(checked) {\n        console.log(\"checked:\", checked);\n    }\n});\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/toggles/signal/markup.html","code":"\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n","language":"markup","group":"example"},{"name":"src/parts/toggles/signal/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Signal\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Signal — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Signal · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cbutton aria-checked=\"false\" aria-label=\"Signal トグル\" class=\"sop-toggle sop-signal\" role=\"switch\" type=\"button\">\u003cspan aria-hidden=\"true\" class=\"switch-art\">\u003cspan class=\"signal-body\">\u003cspan class=\"signal-header mono\">SIGNAL GENERATOR \u003ci>\u003c/i>\u003c/span>\u003cspan class=\"signal-screen\">\u003ccanvas class=\"object-canvas\" height=\"112\" width=\"304\">\u003c/canvas>\u003cspan class=\"screen-glare\">\u003c/span>\u003c/span>\u003cspan class=\"rocker-mount\">\u003cspan class=\"signal-rocker\">\u003cspan>I\u003c/span>\u003ci>\u003c/i>\u003cspan>O\u003c/span>\u003c/span>\u003c/span>\u003cspan class=\"signal-bottom mono\">\u003cspan>● REC\u003c/span>\u003cspan>48 kHz / 24 bit\u003c/span>\u003c/span>\u003c/span>\u003c/span>\u003c/button>\n\u003c/div>\u003cp class=\"hint\">クリック・ドラッグ・キーボードで操作できます。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* signal: isolated component styles, generated from style.css + shared base. */\n.sop-toggle.sop-signal {\n  --sop-mono:  \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace;\n  box-sizing: border-box;\n  color: inherit;\n  font: inherit;\n  -webkit-tap-highlight-color: transparent;\n}\n\n.sop-toggle.sop-signal[aria-checked=\"true\"] {\n  --p: 1;\n}\n\n.sop-toggle.sop-signal:disabled {\n  cursor: not-allowed;\n  opacity: .45;\n  filter: saturate(.35);\n}\n\n.sop-toggle.sop-signal:focus-visible {\n  outline: 2px solid #d1e6b2;\n  outline-offset: 10px;\n}\n\n.sop-toggle.sop-signal {\n  --p: 0;\n  --v: 0;\n  --energy: 0;\n  --time: 0;\n  position: relative;\n  display: block;\n  flex: none;\n  background: none;\n  border: 0;\n  padding: 0;\n  margin: 0;\n  outline-offset: 10px!important;\n  touch-action: pan-y;\n  user-select: none;\n  border-radius: 60px;\n  cursor: pointer;\n  transform: translateY(0);\n  transition: filter .3s;\n  -webkit-user-select: none;\n}\n\n.sop-toggle.sop-signal:hover {\n  filter: brightness(1.075);\n}\n\n.sop-toggle.sop-signal:active {\n  cursor: grabbing;\n}\n\n.sop-toggle.sop-signal .switch-art {\n  display: block;\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n  transform: translateY(calc(var(--press,0)*1.5px));\n  transition: transform .12s;\n}\n\n.sop-toggle.sop-signal .switch-art * {\n  box-sizing: border-box;\n}\n\n.sop-toggle.sop-signal .object-canvas {\n  position: absolute;\n  inset: 0;\n  display: block;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.sop-toggle.sop-signal .screw {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(125deg,#8b8d8d,#333737 48%,#111313 50%,#5d6162);\n  box-shadow: 0 0 1px 1px #060707;\n}\n\n.sop-toggle.sop-signal .screw:after {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #171919;\n  transform: rotate(-35deg);\n}\n\n.sop-toggle.sop-signal .s1 {\n  top: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s2 {\n  top: 10px;\n  right: 11px;\n}\n\n.sop-toggle.sop-signal .s3 {\n  bottom: 10px;\n  left: 11px;\n}\n\n.sop-toggle.sop-signal .s4 {\n  bottom: 10px;\n  right: 11px;\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-toggle.sop-signal,\n  .sop-toggle.sop-signal * {\n    animation: none!important;\n    transition: none!important;\n  }\n\n}\n\n.sop-toggle.sop-signal {\n  width: 268px;\n  height: 122px;\n  border-radius: 13px;\n}\n\n.sop-toggle.sop-signal .signal-body {\n  position: absolute;\n  inset: 0;\n  border-radius: 12px;\n  background: linear-gradient(150deg,#343b32,#20261f 35%,#1c211b 79%,#31362e);\n  border: 1px solid #68705b66;\n  box-shadow: 0 12px 22px #0009,0 2px 1px #070d06,inset 0 1px 1px #abb39941;\n}\n\n.sop-toggle.sop-signal .signal-header {\n  position: absolute;\n  left: 17px;\n  top: 12px;\n  right: 17px;\n  color: #a7b399;\n  font-size: 6px;\n  letter-spacing: 1.3px;\n}\n\n.sop-toggle.sop-signal .signal-header i {\n  position: absolute;\n  right: 2px;\n  top: 0;\n  width: 4px;\n  height: 4px;\n  border-radius: 50%;\n  background: #a3d563;\n  box-shadow: 0 0 5px #b9e57d60;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n.sop-toggle.sop-signal .signal-screen {\n  position: absolute;\n  left: 15px;\n  top: 29px;\n  width: 165px;\n  height: 59px;\n  border-radius: 5px;\n  background: #081005;\n  border: 1px solid #4e5d3a9c;\n  box-shadow: inset 0 1px 5px #000,0 1px 0 #b8c09e27;\n  overflow: hidden;\n}\n\n.sop-toggle.sop-signal .signal-screen .object-canvas {\n  left: 6px;\n  top: 1px;\n  width: 152px;\n  height: 56px;\n}\n\n.sop-toggle.sop-signal .screen-glare {\n  position: absolute;\n  inset: 0;\n  border-radius: 4px;\n  background: linear-gradient(155deg,#d4f2ab10,transparent 43%);\n  box-shadow: inset 0 1px 1px #f1ffcc19;\n}\n\n.sop-toggle.sop-signal .rocker-mount {\n  position: absolute;\n  right: 17px;\n  top: 26px;\n  width: 54px;\n  height: 68px;\n  background: #080e06;\n  border-radius: 6px;\n  box-shadow: inset 0 0 3px #000,0 1px 1px #b1c59833;\n  border: 1px solid #111d0c;\n  perspective: 140px;\n}\n\n.sop-toggle.sop-signal .signal-rocker {\n  position: absolute;\n  inset: 4px;\n  border-radius: 4px;\n  background: linear-gradient(#515e43,#3a472d 30%,#25331c 57%,#35432a 89%,#4d5b3c);\n  box-shadow: 0 3px 4px #000,inset 0 1px 1px #d0e2b458,inset 0 -1px 1px #081104;\n  border: 1px solid #81906430;\n  transform: rotateX(calc(-19deg + var(--p)*38deg));\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: space-around;\n  padding: 3px 0;\n}\n\n.sop-toggle.sop-signal .signal-rocker>span {\n  font: 10px Arial;\n  color: #a8b69a;\n  text-shadow: 0 1px 1px #000;\n}\n\n.sop-toggle.sop-signal .signal-rocker>i {\n  height: 2px;\n  width: 16px;\n  background: #c7f18e;\n  box-shadow: 0 0 6px #c9ff7e9e;\n  opacity: calc(.08 + var(--p)*.85);\n  border-radius: 1px;\n}\n\n.sop-toggle.sop-signal .signal-bottom {\n  position: absolute;\n  bottom: 14px;\n  left: 18px;\n  right: 82px;\n  font-size: 5px;\n  color: #748667;\n  letter-spacing: 1px;\n  display: flex;\n  justify-content: space-between;\n}\n\n.sop-toggle.sop-signal .signal-bottom>span:first-child {\n  color: #a4c786;\n  opacity: calc(.2 + var(--p)*.8);\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/toggles/signal/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/toggles/signal/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/toggles/signal/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst toggle_controller_1 = require(\"src/shared/toggle-controller.ts\");\nconst config = {\n    \"id\": \"signal\",\n    \"name\": \"Signal\",\n    \"initial\": false,\n    \"stiffness\": 460,\n    \"damping\": 29,\n    \"tone\": 130,\n    \"travel\": 140\n};\n/** Pass the component button, not a selector or a gallery card. */\nfunction init(element, options = {}) {\n    if (!(element instanceof HTMLButtonElement))\n        throw new TypeError('A button is required for SignalToggle.');\n    return (0, toggle_controller_1.createToggleController)(element, config, options);\n}\n\n},\n\"src/shared/toggle-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createToggleController = createToggleController;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst renderer_1 = require(\"src/shared/renderer.ts\");\nlet nextInstance = 0;\n/** SVG resources are local to each instance, including multiple copies of Orbit. */\nfunction scopeSvgResources(root) {\n    if (root.dataset.sopInstance)\n        return;\n    const prefix = `sop-${++nextInstance}-${Math.random().toString(36).slice(2, 10)}-`;\n    root.dataset.sopInstance = prefix;\n    const ids = new Map();\n    root.querySelectorAll('[id]').forEach(element => {\n        ids.set(element.id, prefix + element.id);\n        element.id = prefix + element.id;\n    });\n    root.querySelectorAll('*').forEach(element => {\n        for (const attribute of [...element.attributes]) {\n            let value = attribute.value;\n            for (const [before, after] of ids) {\n                value = value.replaceAll(`url(#${before})`, `url(#${after})`);\n                if ((attribute.name === 'href' || attribute.name === 'xlink:href') && value === `#${before}`)\n                    value = `#${after}`;\n            }\n            if (value !== attribute.value)\n                element.setAttribute(attribute.name, value);\n        }\n    });\n}\n/** Owns only this button's animation. No document-wide selectors or gallery dependency. */\nfunction createToggleController(button, config, options = {}) {\n    if (!(button instanceof HTMLButtonElement))\n        throw new TypeError('A button element is required.');\n    const events = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let reduced = media.matches;\n    let checked = options.checked ?? config.initial;\n    const spring = new motion_1.Spring(checked ? 1 : 0, config);\n    scopeSvgResources(button);\n    const renderer = new renderer_1.ObjectRenderer(button, config);\n    let paused = false;\n    let destroyed = false, visible = true, raf = 0, lastTime = 0, elapsed = 0;\n    let suppressClick = false;\n    let pointer = null;\n    const continuous = ['volt', 'orbit', 'reel', 'prism', 'signal'].includes(config.id);\n    function render() { renderer.frame(spring.value, spring.velocity, elapsed, 0, reduced); }\n    function requestFrame() {\n        if (!raf && !destroyed && !paused && visible && !document.hidden)\n            raf = requestAnimationFrame(frame);\n    }\n    function pause() { cancelAnimationFrame(raf); raf = 0; lastTime = 0; }\n    function frame(time) {\n        raf = 0;\n        if (destroyed || paused || !visible || document.hidden) {\n            lastTime = 0;\n            return;\n        }\n        const dt = lastTime ? Math.min((time - lastTime) / 1000, .05) : 1 / 60;\n        lastTime = time;\n        elapsed += dt;\n        if (!pointer?.dragging)\n            spring.advance(dt);\n        renderer.frame(spring.value, spring.velocity, elapsed, dt, reduced);\n        if (!spring.settled || (!reduced && (renderer.particles.length > 0 || checked && continuous)))\n            requestFrame();\n        else\n            lastTime = 0;\n    }\n    function setChecked(value, immediate = false) {\n        if (destroyed)\n            return;\n        const changed = checked !== value;\n        checked = value;\n        if (options.manageAria !== false)\n            button.setAttribute('aria-checked', String(checked));\n        if (reduced || immediate || !visible)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        if (changed)\n            renderer.trigger(checked, reduced);\n        if (reduced || immediate || !visible)\n            render();\n        requestFrame();\n    }\n    function requestChange(value) {\n        if (button.disabled || destroyed)\n            return;\n        // In controlled mode the consumer, not this controller, owns the committed value.\n        if (!options.controlled)\n            setChecked(value);\n        options.onCheckedChange?.(value);\n        button.dispatchEvent(new CustomEvent('sop:change', { bubbles: true, detail: { checked: value, id: config.id } }));\n        // A parent may intentionally decline a controlled update. Return from any dragged position.\n        if (options.controlled) {\n            spring.setTarget(checked ? 1 : 0);\n            requestFrame();\n        }\n    }\n    function cancelInteraction() {\n        if (pointer) {\n            const id = pointer.id;\n            pointer = null;\n            try {\n                if (button.hasPointerCapture(id))\n                    button.releasePointerCapture(id);\n            }\n            catch { /* Pointer already ended. */ }\n        }\n        button.style.setProperty('--press', '0');\n        if (reduced)\n            spring.snap(checked ? 1 : 0);\n        else\n            spring.setTarget(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }\n    button.addEventListener('click', event => {\n        if (button.disabled)\n            return;\n        if (suppressClick && event.detail !== 0) {\n            suppressClick = false;\n            event.preventDefault();\n            return;\n        }\n        suppressClick = false;\n        requestChange(!checked);\n    }, { signal: events.signal });\n    button.addEventListener('keydown', event => {\n        if (button.disabled)\n            return;\n        if (['ArrowLeft', 'ArrowDown', 'Home'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(false);\n        }\n        if (['ArrowRight', 'ArrowUp', 'End'].includes(event.key)) {\n            event.preventDefault();\n            requestChange(true);\n        }\n        if (event.key === 'Escape' && pointer) {\n            event.preventDefault();\n            event.stopPropagation();\n            cancelInteraction();\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointerdown', event => {\n        if (button.disabled || !event.isPrimary || (event.pointerType === 'mouse' && event.button !== 0) || pointer)\n            return;\n        suppressClick = false;\n        pointer = { id: event.pointerId, x: event.clientX, y: event.clientY, value: spring.value,\n            scale: button.getBoundingClientRect().width / button.offsetWidth, dragging: false };\n        button.style.setProperty('--press', '1');\n        try {\n            button.setPointerCapture(event.pointerId);\n        }\n        catch { /* Capture is optional. */ }\n    }, { signal: events.signal });\n    button.addEventListener('pointermove', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        if (button.disabled) {\n            cancelInteraction();\n            return;\n        }\n        const dx = event.clientX - pointer.x, dy = event.clientY - pointer.y;\n        if (!pointer.dragging && (0, motion_1.isDrag)(dx, dy))\n            pointer.dragging = true;\n        if (!pointer.dragging)\n            return;\n        spring.snap((0, motion_1.dragValue)(pointer.value, dx, config.travel, pointer.scale));\n        render();\n    }, { signal: events.signal });\n    button.addEventListener('pointerup', event => {\n        if (!pointer || pointer.id !== event.pointerId)\n            return;\n        const dragged = pointer.dragging;\n        pointer = null;\n        button.style.setProperty('--press', '0');\n        if (dragged) {\n            suppressClick = true;\n            requestChange(spring.value >= .5);\n        }\n    }, { signal: events.signal });\n    button.addEventListener('pointercancel', cancelInteraction, { signal: events.signal });\n    button.addEventListener('lostpointercapture', () => {\n        if (pointer)\n            cancelInteraction();\n    }, { signal: events.signal });\n    media.addEventListener('change', () => {\n        reduced = media.matches;\n        renderer.particles = [];\n        spring.snap(checked ? 1 : 0);\n        render();\n        requestFrame();\n    }, { signal: events.signal });\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            cancelInteraction();\n            pause();\n        }\n        else {\n            renderer.resize();\n            requestFrame();\n        }\n    }, { signal: events.signal });\n    const resizeObserver = new ResizeObserver(() => { renderer.resize(); render(); });\n    resizeObserver.observe(button);\n    const intersection = new IntersectionObserver(entries => {\n        visible = entries[0].isIntersecting;\n        if (visible) {\n            renderer.resize();\n            render();\n            requestFrame();\n        }\n        else {\n            cancelInteraction();\n            spring.snap(checked ? 1 : 0);\n            renderer.particles = [];\n            pause();\n        }\n    });\n    intersection.observe(button);\n    setChecked(checked, true);\n    return {\n        setChecked, getChecked: () => checked, cancelInteraction,\n        setPaused(value) {\n            paused = value;\n            if (paused) {\n                cancelInteraction();\n                pause();\n            }\n            else\n                requestFrame();\n        },\n        resize: () => { renderer.resize(); render(); },\n        destroy() {\n            if (destroyed)\n                return;\n            destroyed = true;\n            cancelInteraction();\n            pause();\n            events.abort();\n            resizeObserver.disconnect();\n            intersection.disconnect();\n            renderer.particles = [];\n        }\n    };\n}\n\n},\n\"src/shared/motion.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Spring = exports.clamp = void 0;\nexports.isDrag = isDrag;\nexports.dragValue = dragValue;\nexports.orbitPosition = orbitPosition;\n/* Original spring physics, independent of the gallery and React. */\nconst clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));\nexports.clamp = clamp;\nclass Spring {\n    value;\n    target;\n    velocity = 0;\n    stiffness;\n    damping;\n    constructor(value = 0, { stiffness = 250, damping = 22 } = {}) {\n        this.value = value;\n        this.target = value;\n        this.velocity = 0;\n        this.stiffness = stiffness;\n        this.damping = damping;\n    }\n    setTarget(value) { this.target = (0, exports.clamp)(value); }\n    snap(value) { this.value = this.target = (0, exports.clamp)(value); this.velocity = 0; }\n    get settled() { return Math.abs(this.value - this.target) \u003c 0.0001 && Math.abs(this.velocity) \u003c 0.001; }\n    advance(dt) {\n        if (!Number.isFinite(dt) || dt \u003c= 0)\n            return this.value;\n        // Small fixed substeps make springs stable on low-refresh devices and after a slow frame.\n        let remaining = Math.min(dt, 0.064);\n        while (remaining > 0) {\n            const step = Math.min(remaining, 1 / 240);\n            this.velocity += ((this.target - this.value) * this.stiffness - this.velocity * this.damping) * step;\n            this.value += this.velocity * step;\n            remaining -= step;\n        }\n        if (this.settled) {\n            this.value = this.target;\n            this.velocity = 0;\n        }\n        return this.value;\n    }\n}\nexports.Spring = Spring;\nfunction isDrag(dx, dy, threshold = 7) {\n    return Math.abs(dx) > threshold && Math.abs(dx) > Math.abs(dy) * 1.12;\n}\nfunction dragValue(start, dx, travel, scale = 1) {\n    if (!Number.isFinite(travel) || travel \u003c= 0)\n        return (0, exports.clamp)(start);\n    return (0, exports.clamp)(start + dx / (travel * Math.max(0.1, scale)));\n}\nfunction orbitPosition(value) {\n    const p = (0, exports.clamp)(value, -0.08, 1.08);\n    return { x: 140 - 96 * Math.cos(p * Math.PI), y: 73 - 36 * Math.sin(p * Math.PI) };\n}\n\n},\n\"src/shared/renderer.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.ObjectRenderer = void 0;\nconst motion_1 = require(\"src/shared/motion.ts\");\nconst LETTERS = {\n    O: ['01110', '11011', '10001', '10001', '10001', '11011', '01110'],\n    N: ['10001', '11001', '11001', '10101', '10011', '10011', '10001'],\n    F: ['11111', '10000', '10000', '11110', '10000', '10000', '10000']\n};\nclass ObjectRenderer {\n    button;\n    config;\n    canvas;\n    ctx;\n    particles;\n    reelAngle;\n    effectAge;\n    lastValue;\n    lastEnergy;\n    size;\n    constructor(button, config) {\n        this.button = button;\n        this.config = config;\n        this.canvas = button.querySelector('canvas');\n        this.ctx = this.canvas ? this.canvas.getContext('2d') : null;\n        this.particles = [];\n        this.reelAngle = 0;\n        this.effectAge = 5;\n        this.lastValue = null;\n        this.lastEnergy = 0;\n        this.size = { w: 0, h: 0 };\n        this.resize();\n    }\n    resize() {\n        if (!this.canvas || !this.ctx)\n            return;\n        // clientWidth, unlike getBoundingClientRect, does not include responsive CSS scaling.\n        const w = this.canvas.clientWidth || 280;\n        const h = this.canvas.clientHeight || 140;\n        const dpr = Math.min(window.devicePixelRatio || 1, 2);\n        this.canvas.width = Math.round(w * dpr);\n        this.canvas.height = Math.round(h * dpr);\n        this.ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        this.size = { w, h };\n    }\n    trigger(on, reduced) {\n        this.effectAge = 0;\n        if (reduced || !this.ctx)\n            return;\n        const { w, h } = this.size;\n        if (this.config.id === 'bloom' || this.config.id === 'prism') {\n            const count = this.config.id === 'bloom' ? 16 : 11;\n            for (let i = 0; i \u003c count; i++) {\n                const angle = Math.random() * Math.PI * 2;\n                const speed = 12 + Math.random() * 38;\n                this.particles.push({ x: on ? w * .75 : w * .25, y: h * .48,\n                    vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed - 18,\n                    life: .65 + Math.random() * .6, age: 0, size: .5 + Math.random() * 1.3, hue: 25 + Math.random() * 50 });\n            }\n            // Rapid clicking must not grow an unbounded particle list.\n            this.particles = this.particles.slice(-70);\n        }\n    }\n    frame(value, velocity, t, dt, reduced) {\n        const p = (0, motion_1.clamp)(value);\n        const energy = reduced ? 0 : Math.min(Math.abs(velocity) / 7, 1);\n        if (this.lastValue !== value || Math.abs(energy - this.lastEnergy) > .0001) {\n            this.button.style.setProperty('--p', value.toFixed(5));\n            this.button.style.setProperty('--v', velocity.toFixed(4));\n            this.button.style.setProperty('--energy', energy.toFixed(4));\n            if (this.config.id === 'orbit') {\n                const pos = (0, motion_1.orbitPosition)(value);\n                this.button.style.setProperty('--ox', pos.x.toFixed(3) + 'px');\n                this.button.style.setProperty('--oy', pos.y.toFixed(3) + 'px');\n            }\n            this.lastValue = value;\n            this.lastEnergy = energy;\n        }\n        this.effectAge += dt;\n        if (this.config.id === 'reel' && p > .001 && !reduced) {\n            this.reelAngle = (this.reelAngle + dt * p * 100) % 360;\n            this.button.style.setProperty('--reel-angle', this.reelAngle.toFixed(3) + 'deg');\n        }\n        if (!this.ctx)\n            return;\n        const ctx = this.ctx, { w, h } = this.size;\n        ctx.clearRect(0, 0, w, h);\n        switch (this.config.id) {\n            case 'volt':\n                this.volt(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'orbit':\n                this.orbit(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'signal':\n                this.signal(ctx, w, h, p, reduced ? 0 : t);\n                break;\n            case 'prism':\n                this.prism(ctx, w, h, p, reduced ? 0 : t);\n                break;\n        }\n        if (!reduced)\n            this.drawParticles(ctx, dt);\n    }\n    volt(ctx, w, h, p, t) {\n        if (p \u003c .025)\n            return;\n        const end = 42 + 173 * p;\n        const pulse = .72 + .2 * Math.sin(t * 11);\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const glow = ctx.createRadialGradient(end * .6, h * .5, 0, end * .6, h * .5, end * .55);\n        glow.addColorStop(0, `rgba(80,167,245,${.17 * p})`);\n        glow.addColorStop(1, 'rgba(60,145,255,0)');\n        ctx.fillStyle = glow;\n        ctx.fillRect(0, 0, w, h);\n        for (let strand = 0; strand \u003c 3; strand++) {\n            ctx.beginPath();\n            const segments = 34;\n            for (let i = 0; i \u003c= segments; i++) {\n                const f = i / segments;\n                const x = 20 + (end - 20) * f;\n                const envelope = Math.sin(f * Math.PI);\n                const turbulence = Math.sin(f * 35 - t * 15 + strand * 3) * 8 + Math.sin(f * 79 + t * 23 + strand) * 3;\n                const y = h / 2 + turbulence * envelope * p * (strand === 0 ? 1 : .8) + Math.sin(f * Math.PI) * (strand - 1) * 9 * p;\n                if (i === 0)\n                    ctx.moveTo(x, y);\n                else\n                    ctx.lineTo(x, y);\n            }\n            ctx.strokeStyle = strand === 0 ? `rgba(205,237,255,${pulse * p})` : `rgba(97,172,255,${.48 * p})`;\n            ctx.lineWidth = strand === 0 ? 1.15 : .7;\n            ctx.shadowBlur = strand === 0 ? 8 : 4;\n            ctx.shadowColor = '#6baaff';\n            ctx.stroke();\n        }\n        [20, end].forEach(x => {\n            ctx.fillStyle = `rgba(209,239,255,${p * .8})`;\n            ctx.shadowBlur = 15;\n            ctx.beginPath();\n            ctx.ellipse(x, h / 2, 2, 5 * p, 0, 0, Math.PI * 2);\n            ctx.fill();\n        });\n        ctx.restore();\n    }\n    orbit(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        for (let i = 0; i \u003c 3; i++) {\n            const angle = t * .5 + i * Math.PI * 2 / 3;\n            for (let j = 0; j \u003c 18; j++) {\n                const a = angle - j * .018;\n                const x = w / 2 + 118 * Math.cos(a);\n                const y = 75 + 56 * Math.sin(a);\n                ctx.fillStyle = `rgba(180,232,194,${p * (1 - j / 18) * .65})`;\n                ctx.beginPath();\n                ctx.arc(x, y, j === 0 ? 1.5 : .7, 0, Math.PI * 2);\n                ctx.fill();\n            }\n        }\n        ctx.restore();\n    }\n    prism(ctx, w, h, p, t) {\n        if (p \u003c .02)\n            return;\n        ctx.save();\n        ctx.globalCompositeOperation = 'lighter';\n        const x = 59 + p * 160;\n        for (let i = 0; i \u003c 5; i++) {\n            const endX = 30 + i * 41 + Math.sin(t * .25 + i) * 8;\n            const gradient = ctx.createLinearGradient(x, 69, endX, 124);\n            gradient.addColorStop(0, `hsla(${160 + i * 35},80%,80%,0)`);\n            gradient.addColorStop(.35, `hsla(${160 + i * 35},75%,75%,${.16 * p})`);\n            gradient.addColorStop(1, `hsla(${160 + i * 35},70%,70%,0)`);\n            ctx.fillStyle = gradient;\n            ctx.beginPath();\n            ctx.moveTo(x, 69);\n            ctx.lineTo(endX - 13, 131);\n            ctx.lineTo(endX + 13, 131);\n            ctx.closePath();\n            ctx.fill();\n        }\n        ctx.restore();\n    }\n    signal(ctx, w, h, p, t) {\n        const columns = 38, rows = 13, step = 4;\n        const word = p > .55 ? 'ON' : 'OFF';\n        const offset = Math.floor((columns - (word.length * 6 - 1)) / 2);\n        for (let row = 0; row \u003c rows; row++) {\n            for (let col = 0; col \u003c columns; col++) {\n                let lit = false;\n                const letterIndex = Math.floor((col - offset) / 6);\n                const letterCol = (col - offset) % 6;\n                if (letterIndex >= 0 && letterIndex \u003c word.length && letterCol \u003c 5 && letterCol >= 0 && row >= 3 && row \u003c 10) {\n                    lit = LETTERS[word[letterIndex]][row - 3][letterCol] === '1';\n                }\n                const wave = Math.sin(t * 3 + col * .38) * Math.sin(t * 1.1 + col * .2);\n                const isWave = p > .6 && Math.abs(row - 6) \u003c Math.abs(wave) * 4.5 && (col \u003c offset - 2 || col > offset + word.length * 6);\n                const x = col * step + 1.7, y = row * step + 3.6;\n                ctx.beginPath();\n                ctx.arc(x, y, 1, 0, Math.PI * 2);\n                ctx.fillStyle = lit ? (p > .55 ? 'rgba(196,239,123,.9)' : 'rgba(166,148,92,.65)') : isWave ? 'rgba(166,214,98,.62)' : 'rgba(110,138,69,.11)';\n                ctx.fill();\n            }\n        }\n    }\n    drawParticles(ctx, dt) {\n        for (const particle of this.particles) {\n            particle.age += dt;\n            particle.x += particle.vx * dt;\n            particle.y += particle.vy * dt;\n            particle.vx *= Math.exp(-dt * .8);\n            particle.vy += dt * 7;\n            const alpha = Math.max(0, 1 - particle.age / particle.life) * .7;\n            ctx.fillStyle = this.config.id === 'bloom' ? `rgba(245,200,135,${alpha})` : `hsla(${particle.hue + 145},80%,83%,${alpha})`;\n            ctx.beginPath();\n            ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);\n            ctx.fill();\n        }\n        this.particles = this.particles.filter(p => p.age \u003c p.life);\n    }\n}\nexports.ObjectRenderer = ObjectRenderer;\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/toggles/signal/demo/main.ts\");})();\n"}},{"id":"original-surface","name":"Original Surface","category":"blocks","order":11,"version":"2.3.0","tagline":"静かな、余白。","description":"今のギャラリーを包む、オリジナルの面。","material":"DARK / ORIGINAL","motion":"FOLLOW","accent":"209,230,178","initial":false,"componentName":"OriginalSurface","tags":["Original Surface","ブロック","カード","背景","今のギャラリーを包む、オリジナルの面。"],"config":{"tilt":false},"related":["chrome"],"design":{"background":"#18191a","border":"1px solid #2c2d2c","radius":"10px","hoverBackground":"#1a1b1c","hoverBorder":"#484b44","pointerLight":"400px radial gradient / accent alpha .045 / transparent 60%","transition":"400ms background + border, 300ms pointer opacity"},"props":[["children","ReactNode","文章、画像、ボタンなど好きな中身を配置できます。"],["className / style","標準のReact属性","幅、余白、外側のレイアウトを設定します。"],["--sop-padding","CSSカスタムプロパティ","内側の余白。初期値は28pxです。"],["--sop-accent","R,G,B","Original Surfaceの追従光の色を指定できます。"]],"markup":"\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","usage":"# Original Surface / 2.3.0\n\n今のギャラリーを包む、オリジナルの面。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`OriginalSurface` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/blocks/original-surface/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `children` (ReactNode): 文章、画像、ボタンなど好きな中身を配置できます。\n- `className / style` (標準のReact属性): 幅、余白、外側のレイアウトを設定します。\n- `--sop-padding` (CSSカスタムプロパティ): 内側の余白。初期値は28pxです。\n- `--sop-accent` (R,G,B): Original Surfaceの追従光の色を指定できます。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/original-surface/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/blocks/original-surface/react/OriginalSurface.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/blocks/original-surface/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/blocks/original-surface/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/blocks/original-surface/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Original Surface — 再現仕様\n\n基準となる元の背景カード。暗い均一な面とほぼ感じる程度の追従光を重視し、発光の強さを上げすぎない。展示用の番号、ON/OFF、見出し、詳細ボタンは部品に含めない。\n\n## 基準値\n```json\n{\n  \"background\": \"#18191a\",\n  \"border\": \"1px solid #2c2d2c\",\n  \"radius\": \"10px\",\n  \"hoverBackground\": \"#1a1b1c\",\n  \"hoverBorder\": \"#484b44\",\n  \"pointerLight\": \"400px radial gradient / accent alpha .045 / transparent 60%\",\n  \"transition\": \"400ms background + border, 300ms pointer opacity\"\n}\n```\n\n## 部品の境界\n外観とポインター反応を持つ汎用コンテナ。文章・画像・ボタン・トグルを自由に子要素へ配置する。展示用の見出しや番号、詳細画面、特定の見本文言は組み込まない。`.sop-surface-content` は内側のレイヤーで、`--sop-padding` で余白を調整できる。\n\n## 動作\nルートは `.sop-original-surface`。マウスの位置を基準に反射を更新する。タッチ時は静かな既定位置を維持して、縦スクロールを妨げない。prefers-reduced-motionで動きを止める。画面から外したとき、イベントとRAF/Observerを確実に解除する。子のボタンを押しても、コンテナが意図しない動作をしない。\n\n## 再現確認\n元のCSSを正本とし、輪郭・面・陰影・色の強さ・動きの順に照合する。中身を空にした場合、文章を増やした場合、狭い幅、同じ部品の複数配置でも崩れないこと。元コードから外観を変更せず、統合先に合わせて子要素と幅・余白のみを調整する。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/blocks/original-surface/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/blocks/original-surface/react/OriginalSurface.tsx","code":"'use client';\nimport React, { useEffect, useRef, type HTMLAttributes } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\nexport interface OriginalSurfaceProps extends HTMLAttributes\u003cHTMLDivElement> {\n}\n/** 今のギャラリーを包む、オリジナルの面。 The content is yours. */\nexport default function OriginalSurface({ children, className = '', ...props }: OriginalSurfaceProps) {\n    const element = useRef\u003cHTMLDivElement | null>(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-original-surface ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"tsx","group":"component"},{"name":"src/parts/blocks/original-surface/styles.css","code":"/* original-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-original-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-original-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-original-surface,\n  .sop-surface.sop-original-surface:before,\n  .sop-surface.sop-original-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-original-surface {\n  border: 1px solid #2c2d2c;\n  border-radius: 10px;\n  background: #18191a;\n  transition: border-color .4s,background .4s;\n}\n\n.sop-surface.sop-original-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(ellipse at 50% 48%,rgba(var(--sop-accent,209,230,178),.025),transparent 67%);\n}\n\n.sop-surface.sop-original-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(400px circle at var(--sop-x,50%) var(--sop-y,50%),rgba(var(--sop-accent,209,230,178),.045),transparent 60%);\n  opacity: 0;\n  transition: opacity .3s;\n}\n\n.sop-surface.sop-original-surface:hover {\n  border-color: #484b44;\n  background: #1a1b1c;\n}\n\n.sop-surface.sop-original-surface:hover:after {\n  opacity: 1;\n}\n\n.sop-surface.sop-original-surface:focus-within {\n  border-color: #69725f;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/original-surface/react/Example.tsx","code":"import React from 'react';\nimport OriginalSurface from './OriginalSurface';\n\nexport default function Example() {\n  return \u003cOriginalSurface style={{maxWidth: 420}}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/OriginalSurface>;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/blocks/original-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/blocks/original-surface/react/OriginalSurface.jsx","code":"'use client';\nimport React, { useEffect, useRef } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\n/** 今のギャラリーを包む、オリジナルの面。 The content is yours. */\nexport default function OriginalSurface({ children, className = '', ...props }) {\n    const element = useRef(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-original-surface ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"jsx","group":"component"},{"name":"src/parts/blocks/original-surface/styles.css","code":"/* original-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-original-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-original-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-original-surface,\n  .sop-surface.sop-original-surface:before,\n  .sop-surface.sop-original-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-original-surface {\n  border: 1px solid #2c2d2c;\n  border-radius: 10px;\n  background: #18191a;\n  transition: border-color .4s,background .4s;\n}\n\n.sop-surface.sop-original-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(ellipse at 50% 48%,rgba(var(--sop-accent,209,230,178),.025),transparent 67%);\n}\n\n.sop-surface.sop-original-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(400px circle at var(--sop-x,50%) var(--sop-y,50%),rgba(var(--sop-accent,209,230,178),.045),transparent 60%);\n  opacity: 0;\n  transition: opacity .3s;\n}\n\n.sop-surface.sop-original-surface:hover {\n  border-color: #484b44;\n  background: #1a1b1c;\n}\n\n.sop-surface.sop-original-surface:hover:after {\n  opacity: 1;\n}\n\n.sop-surface.sop-original-surface:focus-within {\n  border-color: #69725f;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/original-surface/react/Example.jsx","code":"import React from 'react';\nimport OriginalSurface from './OriginalSurface';\nexport default function Example() {\n    return \u003cOriginalSurface style={{ maxWidth: 420 }}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/OriginalSurface>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/blocks/original-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/blocks/original-surface/vanilla/init.ts","code":"import { createSurfaceController, type SurfaceOptions } from '../../../../shared/surface-controller';\nexport function init(element: HTMLElement, options: SurfaceOptions = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"typescript","group":"component"},{"name":"src/parts/blocks/original-surface/styles.css","code":"/* original-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-original-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-original-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-original-surface,\n  .sop-surface.sop-original-surface:before,\n  .sop-surface.sop-original-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-original-surface {\n  border: 1px solid #2c2d2c;\n  border-radius: 10px;\n  background: #18191a;\n  transition: border-color .4s,background .4s;\n}\n\n.sop-surface.sop-original-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(ellipse at 50% 48%,rgba(var(--sop-accent,209,230,178),.025),transparent 67%);\n}\n\n.sop-surface.sop-original-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(400px circle at var(--sop-x,50%) var(--sop-y,50%),rgba(var(--sop-accent,209,230,178),.045),transparent 60%);\n  opacity: 0;\n  transition: opacity .3s;\n}\n\n.sop-surface.sop-original-surface:hover {\n  border-color: #484b44;\n  background: #1a1b1c;\n}\n\n.sop-surface.sop-original-surface:hover:after {\n  opacity: 1;\n}\n\n.sop-surface.sop-original-surface:focus-within {\n  border-color: #69725f;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/original-surface/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-original-surface');\nif (!element) throw new Error('The Original Surface root was not found.');\nconst controller = init(element);\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/blocks/original-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/original-surface/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Original Surface\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/blocks/original-surface/vanilla/init.js","code":"import { createSurfaceController } from '../../../../shared/surface-controller.js';\nexport function init(element, options = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"javascript","group":"component"},{"name":"src/parts/blocks/original-surface/styles.css","code":"/* original-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-original-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-original-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-original-surface,\n  .sop-surface.sop-original-surface:before,\n  .sop-surface.sop-original-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-original-surface {\n  border: 1px solid #2c2d2c;\n  border-radius: 10px;\n  background: #18191a;\n  transition: border-color .4s,background .4s;\n}\n\n.sop-surface.sop-original-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(ellipse at 50% 48%,rgba(var(--sop-accent,209,230,178),.025),transparent 67%);\n}\n\n.sop-surface.sop-original-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(400px circle at var(--sop-x,50%) var(--sop-y,50%),rgba(var(--sop-accent,209,230,178),.045),transparent 60%);\n  opacity: 0;\n  transition: opacity .3s;\n}\n\n.sop-surface.sop-original-surface:hover {\n  border-color: #484b44;\n  background: #1a1b1c;\n}\n\n.sop-surface.sop-original-surface:hover:after {\n  opacity: 1;\n}\n\n.sop-surface.sop-original-surface:focus-within {\n  border-color: #69725f;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/original-surface/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-original-surface');\nif (!element)\n    throw new Error('The Original Surface root was not found.');\nconst controller = init(element);\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/blocks/original-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/original-surface/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Original Surface\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Original Surface — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Original Surface · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cdiv class=\"sop-surface sop-original-surface\">\u003cdiv class=\"sop-surface-content\">\u003ch2>Original Surface\u003c/h2>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003cbutton type=\"button\">サンプルボタン\u003c/button>\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\u003c/div>\u003cp class=\"hint\">中身を自由に入れ替えられる、独立した背景パーツです。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* original-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-original-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-original-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-original-surface,\n  .sop-surface.sop-original-surface:before,\n  .sop-surface.sop-original-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-original-surface {\n  border: 1px solid #2c2d2c;\n  border-radius: 10px;\n  background: #18191a;\n  transition: border-color .4s,background .4s;\n}\n\n.sop-surface.sop-original-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(ellipse at 50% 48%,rgba(var(--sop-accent,209,230,178),.025),transparent 67%);\n}\n\n.sop-surface.sop-original-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background: radial-gradient(400px circle at var(--sop-x,50%) var(--sop-y,50%),rgba(var(--sop-accent,209,230,178),.045),transparent 60%);\n  opacity: 0;\n  transition: opacity .3s;\n}\n\n.sop-surface.sop-original-surface:hover {\n  border-color: #484b44;\n  background: #1a1b1c;\n}\n\n.sop-surface.sop-original-surface:hover:after {\n  opacity: 1;\n}\n\n.sop-surface.sop-original-surface:focus-within {\n  border-color: #69725f;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/blocks/original-surface/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/blocks/original-surface/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/blocks/original-surface/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst surface_controller_1 = require(\"src/shared/surface-controller.ts\");\nfunction init(element, options = {}) {\n    return (0, surface_controller_1.createSurfaceController)(element, { tilt: false, ...options });\n}\n\n},\n\"src/shared/surface-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createSurfaceController = createSurfaceController;\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nfunction createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/blocks/original-surface/demo/main.ts\");})();\n"}},{"id":"frosted-glass","name":"Frosted Glass","category":"blocks","order":12,"version":"2.3.0","tagline":"光を、通す。","description":"曇ったガラスの向こうに、色と奥行き。","material":"FROST / TRANSLUCENT","motion":"REFRACT","accent":"149,208,221","initial":false,"componentName":"FrostedGlass","tags":["Frosted Glass","ブロック","カード","背景","曇ったガラスの向こうに、色と奥行き。"],"config":{"tilt":true},"related":["original-surface"],"design":{"background":"three-stop translucent linear gradient","blur":"18px","saturate":"1.3","border":"1px #c6f0ff40","radius":"18px","innerBorder":"6px inset, 13px radius","tilt":"maximum X ±3deg / Y ±4deg","transition":"500ms cubic-bezier(.16,1,.3,1)"},"props":[["children","ReactNode","文章、画像、ボタンなど好きな中身を配置できます。"],["className / style","標準のReact属性","幅、余白、外側のレイアウトを設定します。"],["--sop-padding","CSSカスタムプロパティ","内側の余白。初期値は28pxです。"],["--sop-accent","R,G,B","Original Surfaceの追従光の色を指定できます。"]],"markup":"\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","usage":"# Frosted Glass / 2.3.0\n\n曇ったガラスの向こうに、色と奥行き。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`FrostedGlass` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/blocks/frosted-glass/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `children` (ReactNode): 文章、画像、ボタンなど好きな中身を配置できます。\n- `className / style` (標準のReact属性): 幅、余白、外側のレイアウトを設定します。\n- `--sop-padding` (CSSカスタムプロパティ): 内側の余白。初期値は28pxです。\n- `--sop-accent` (R,G,B): Original Surfaceの追従光の色を指定できます。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/frosted-glass/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/blocks/frosted-glass/react/FrostedGlass.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/blocks/frosted-glass/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/blocks/frosted-glass/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/blocks/frosted-glass/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Frosted Glass — 再現仕様\n\nすりガラスそのものは半透明。背面の色や形をぼかして透かす。独立した背景装飾は展示のためのものであり、ガラスの子要素に固定しない。透け方は利用先の背面に依存する。\n\n## 基準値\n```json\n{\n  \"background\": \"three-stop translucent linear gradient\",\n  \"blur\": \"18px\",\n  \"saturate\": \"1.3\",\n  \"border\": \"1px #c6f0ff40\",\n  \"radius\": \"18px\",\n  \"innerBorder\": \"6px inset, 13px radius\",\n  \"tilt\": \"maximum X ±3deg / Y ±4deg\",\n  \"transition\": \"500ms cubic-bezier(.16,1,.3,1)\"\n}\n```\n\n## 部品の境界\n外観とポインター反応を持つ汎用コンテナ。文章・画像・ボタン・トグルを自由に子要素へ配置する。展示用の見出しや番号、詳細画面、特定の見本文言は組み込まない。`.sop-surface-content` は内側のレイヤーで、`--sop-padding` で余白を調整できる。\n\n## 動作\nルートは `.sop-frosted-glass`。マウスの位置を基準に反射を更新する。タッチ時は静かな既定位置を維持して、縦スクロールを妨げない。prefers-reduced-motionで動きを止める。画面から外したとき、イベントとRAF/Observerを確実に解除する。子のボタンを押しても、コンテナが意図しない動作をしない。\n\n## 再現確認\n元のCSSを正本とし、輪郭・面・陰影・色の強さ・動きの順に照合する。中身を空にした場合、文章を増やした場合、狭い幅、同じ部品の複数配置でも崩れないこと。元コードから外観を変更せず、統合先に合わせて子要素と幅・余白のみを調整する。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/blocks/frosted-glass/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/blocks/frosted-glass/react/FrostedGlass.tsx","code":"'use client';\nimport React, { useEffect, useRef, type HTMLAttributes } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\nexport interface FrostedGlassProps extends HTMLAttributes\u003cHTMLDivElement> {\n}\n/** 曇ったガラスの向こうに、色と奥行き。 The content is yours. */\nexport default function FrostedGlass({ children, className = '', ...props }: FrostedGlassProps) {\n    const element = useRef\u003cHTMLDivElement | null>(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: true });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-frosted-glass ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"tsx","group":"component"},{"name":"src/parts/blocks/frosted-glass/styles.css","code":"/* frosted-glass: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-frosted-glass {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-frosted-glass .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-frosted-glass,\n  .sop-surface.sop-frosted-glass:before,\n  .sop-surface.sop-frosted-glass:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-frosted-glass {\n  border: 1px solid #c6f0ff40;\n  border-radius: 18px;\n  background: linear-gradient(135deg,#c2e9f817,#95b6df06 50%,#dfbeec13);\n  backdrop-filter: blur(18px) saturate(1.3);\n  -webkit-backdrop-filter: blur(18px) saturate(1.3);\n  box-shadow: inset 0 1px 0 #eeffff45,inset 0 -1px 0 #a4defc20,0 20px 45px #0004;\n  transition: box-shadow .5s,border-color .5s,transform .5s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-frosted-glass:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  z-index: 0;\n  background: radial-gradient(230px circle at var(--sop-x,32%) var(--sop-y,10%),#d8faff26,transparent 75%),linear-gradient(125deg,#ffffff07,transparent 45%);\n  border-radius: inherit;\n}\n\n.sop-surface.sop-frosted-glass:after {\n  content: \"\";\n  position: absolute;\n  inset: 6px;\n  border: 1px solid #d7f4ff0d;\n  border-radius: 13px;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-frosted-glass:hover {\n  border-color: #c7eeff77;\n  box-shadow: inset 0 1px 0 #ffffff66,inset 0 -1px 0 #a4defc40,0 28px 50px #0005;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/frosted-glass/react/Example.tsx","code":"import React from 'react';\nimport FrostedGlass from './FrostedGlass';\n\nexport default function Example() {\n  return \u003cFrostedGlass style={{maxWidth: 420}}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/FrostedGlass>;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/blocks/frosted-glass/markup.html","code":"\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/blocks/frosted-glass/react/FrostedGlass.jsx","code":"'use client';\nimport React, { useEffect, useRef } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\n/** 曇ったガラスの向こうに、色と奥行き。 The content is yours. */\nexport default function FrostedGlass({ children, className = '', ...props }) {\n    const element = useRef(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: true });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-frosted-glass ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"jsx","group":"component"},{"name":"src/parts/blocks/frosted-glass/styles.css","code":"/* frosted-glass: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-frosted-glass {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-frosted-glass .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-frosted-glass,\n  .sop-surface.sop-frosted-glass:before,\n  .sop-surface.sop-frosted-glass:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-frosted-glass {\n  border: 1px solid #c6f0ff40;\n  border-radius: 18px;\n  background: linear-gradient(135deg,#c2e9f817,#95b6df06 50%,#dfbeec13);\n  backdrop-filter: blur(18px) saturate(1.3);\n  -webkit-backdrop-filter: blur(18px) saturate(1.3);\n  box-shadow: inset 0 1px 0 #eeffff45,inset 0 -1px 0 #a4defc20,0 20px 45px #0004;\n  transition: box-shadow .5s,border-color .5s,transform .5s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-frosted-glass:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  z-index: 0;\n  background: radial-gradient(230px circle at var(--sop-x,32%) var(--sop-y,10%),#d8faff26,transparent 75%),linear-gradient(125deg,#ffffff07,transparent 45%);\n  border-radius: inherit;\n}\n\n.sop-surface.sop-frosted-glass:after {\n  content: \"\";\n  position: absolute;\n  inset: 6px;\n  border: 1px solid #d7f4ff0d;\n  border-radius: 13px;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-frosted-glass:hover {\n  border-color: #c7eeff77;\n  box-shadow: inset 0 1px 0 #ffffff66,inset 0 -1px 0 #a4defc40,0 28px 50px #0005;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/frosted-glass/react/Example.jsx","code":"import React from 'react';\nimport FrostedGlass from './FrostedGlass';\nexport default function Example() {\n    return \u003cFrostedGlass style={{ maxWidth: 420 }}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/FrostedGlass>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/blocks/frosted-glass/markup.html","code":"\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/blocks/frosted-glass/vanilla/init.ts","code":"import { createSurfaceController, type SurfaceOptions } from '../../../../shared/surface-controller';\nexport function init(element: HTMLElement, options: SurfaceOptions = {}) {\n    return createSurfaceController(element, { tilt: true, ...options });\n}\n","language":"typescript","group":"component"},{"name":"src/parts/blocks/frosted-glass/styles.css","code":"/* frosted-glass: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-frosted-glass {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-frosted-glass .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-frosted-glass,\n  .sop-surface.sop-frosted-glass:before,\n  .sop-surface.sop-frosted-glass:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-frosted-glass {\n  border: 1px solid #c6f0ff40;\n  border-radius: 18px;\n  background: linear-gradient(135deg,#c2e9f817,#95b6df06 50%,#dfbeec13);\n  backdrop-filter: blur(18px) saturate(1.3);\n  -webkit-backdrop-filter: blur(18px) saturate(1.3);\n  box-shadow: inset 0 1px 0 #eeffff45,inset 0 -1px 0 #a4defc20,0 20px 45px #0004;\n  transition: box-shadow .5s,border-color .5s,transform .5s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-frosted-glass:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  z-index: 0;\n  background: radial-gradient(230px circle at var(--sop-x,32%) var(--sop-y,10%),#d8faff26,transparent 75%),linear-gradient(125deg,#ffffff07,transparent 45%);\n  border-radius: inherit;\n}\n\n.sop-surface.sop-frosted-glass:after {\n  content: \"\";\n  position: absolute;\n  inset: 6px;\n  border: 1px solid #d7f4ff0d;\n  border-radius: 13px;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-frosted-glass:hover {\n  border-color: #c7eeff77;\n  box-shadow: inset 0 1px 0 #ffffff66,inset 0 -1px 0 #a4defc40,0 28px 50px #0005;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/frosted-glass/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-frosted-glass');\nif (!element) throw new Error('The Frosted Glass root was not found.');\nconst controller = init(element);\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/blocks/frosted-glass/markup.html","code":"\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/frosted-glass/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Frosted Glass\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/blocks/frosted-glass/vanilla/init.js","code":"import { createSurfaceController } from '../../../../shared/surface-controller.js';\nexport function init(element, options = {}) {\n    return createSurfaceController(element, { tilt: true, ...options });\n}\n","language":"javascript","group":"component"},{"name":"src/parts/blocks/frosted-glass/styles.css","code":"/* frosted-glass: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-frosted-glass {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-frosted-glass .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-frosted-glass,\n  .sop-surface.sop-frosted-glass:before,\n  .sop-surface.sop-frosted-glass:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-frosted-glass {\n  border: 1px solid #c6f0ff40;\n  border-radius: 18px;\n  background: linear-gradient(135deg,#c2e9f817,#95b6df06 50%,#dfbeec13);\n  backdrop-filter: blur(18px) saturate(1.3);\n  -webkit-backdrop-filter: blur(18px) saturate(1.3);\n  box-shadow: inset 0 1px 0 #eeffff45,inset 0 -1px 0 #a4defc20,0 20px 45px #0004;\n  transition: box-shadow .5s,border-color .5s,transform .5s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-frosted-glass:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  z-index: 0;\n  background: radial-gradient(230px circle at var(--sop-x,32%) var(--sop-y,10%),#d8faff26,transparent 75%),linear-gradient(125deg,#ffffff07,transparent 45%);\n  border-radius: inherit;\n}\n\n.sop-surface.sop-frosted-glass:after {\n  content: \"\";\n  position: absolute;\n  inset: 6px;\n  border: 1px solid #d7f4ff0d;\n  border-radius: 13px;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-frosted-glass:hover {\n  border-color: #c7eeff77;\n  box-shadow: inset 0 1px 0 #ffffff66,inset 0 -1px 0 #a4defc40,0 28px 50px #0005;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/frosted-glass/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-frosted-glass');\nif (!element)\n    throw new Error('The Frosted Glass root was not found.');\nconst controller = init(element);\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/blocks/frosted-glass/markup.html","code":"\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/frosted-glass/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Frosted Glass\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Frosted Glass — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Frosted Glass · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cdiv class=\"sop-surface sop-frosted-glass\">\u003cdiv class=\"sop-surface-content\">\u003ch2>Frosted Glass\u003c/h2>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003cbutton type=\"button\">サンプルボタン\u003c/button>\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\u003c/div>\u003cp class=\"hint\">中身を自由に入れ替えられる、独立した背景パーツです。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* frosted-glass: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-frosted-glass {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-frosted-glass .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-frosted-glass,\n  .sop-surface.sop-frosted-glass:before,\n  .sop-surface.sop-frosted-glass:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-frosted-glass {\n  border: 1px solid #c6f0ff40;\n  border-radius: 18px;\n  background: linear-gradient(135deg,#c2e9f817,#95b6df06 50%,#dfbeec13);\n  backdrop-filter: blur(18px) saturate(1.3);\n  -webkit-backdrop-filter: blur(18px) saturate(1.3);\n  box-shadow: inset 0 1px 0 #eeffff45,inset 0 -1px 0 #a4defc20,0 20px 45px #0004;\n  transition: box-shadow .5s,border-color .5s,transform .5s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-frosted-glass:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  z-index: 0;\n  background: radial-gradient(230px circle at var(--sop-x,32%) var(--sop-y,10%),#d8faff26,transparent 75%),linear-gradient(125deg,#ffffff07,transparent 45%);\n  border-radius: inherit;\n}\n\n.sop-surface.sop-frosted-glass:after {\n  content: \"\";\n  position: absolute;\n  inset: 6px;\n  border: 1px solid #d7f4ff0d;\n  border-radius: 13px;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-frosted-glass:hover {\n  border-color: #c7eeff77;\n  box-shadow: inset 0 1px 0 #ffffff66,inset 0 -1px 0 #a4defc40,0 28px 50px #0005;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/blocks/frosted-glass/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/blocks/frosted-glass/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/blocks/frosted-glass/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst surface_controller_1 = require(\"src/shared/surface-controller.ts\");\nfunction init(element, options = {}) {\n    return (0, surface_controller_1.createSurfaceController)(element, { tilt: true, ...options });\n}\n\n},\n\"src/shared/surface-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createSurfaceController = createSurfaceController;\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nfunction createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/blocks/frosted-glass/demo/main.ts\");})();\n"}},{"id":"machined-panel","name":"Machined Panel","category":"blocks","order":13,"version":"2.3.0","tagline":"削り出された、静寂。","description":"精密な輪郭と、指先を追う金属の反射。","material":"ALUMINIUM / MILLED","motion":"SHEEN","accent":"202,209,190","initial":false,"componentName":"MachinedPanel","tags":["Machined Panel","ブロック","カード","背景","精密な輪郭と、指先を追う金属の反射。"],"config":{"tilt":false},"related":["original-surface"],"design":{"radius":"12px","innerRim":"8px inset / 6px radius","grain":"horizontal 1px at 3px interval","hardware":"4 screws / 7px diameter / 17px inset","shadow":"inset highlights + 3px lower lip + 18px diffuse shadow","pointerLight":"450px ellipse / alpha .094"},"props":[["children","ReactNode","文章、画像、ボタンなど好きな中身を配置できます。"],["className / style","標準のReact属性","幅、余白、外側のレイアウトを設定します。"],["--sop-padding","CSSカスタムプロパティ","内側の余白。初期値は28pxです。"],["--sop-accent","R,G,B","Original Surfaceの追従光の色を指定できます。"]],"markup":"\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","usage":"# Machined Panel / 2.3.0\n\n精密な輪郭と、指先を追う金属の反射。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`MachinedPanel` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/blocks/machined-panel/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `children` (ReactNode): 文章、画像、ボタンなど好きな中身を配置できます。\n- `className / style` (標準のReact属性): 幅、余白、外側のレイアウトを設定します。\n- `--sop-padding` (CSSカスタムプロパティ): 内側の余白。初期値は28pxです。\n- `--sop-accent` (R,G,B): Original Surfaceの追従光の色を指定できます。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/machined-panel/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/blocks/machined-panel/react/MachinedPanel.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/blocks/machined-panel/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/blocks/machined-panel/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/blocks/machined-panel/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Machined Panel — 再現仕様\n\n削り出した金属板。3px周期の微細な筋、内側の刻線、4つの小さなねじ、下端の段差を保つ。クロームの鏡面ではなく、落ち着いたダークアルミニウムの反射。\n\n## 基準値\n```json\n{\n  \"radius\": \"12px\",\n  \"innerRim\": \"8px inset / 6px radius\",\n  \"grain\": \"horizontal 1px at 3px interval\",\n  \"hardware\": \"4 screws / 7px diameter / 17px inset\",\n  \"shadow\": \"inset highlights + 3px lower lip + 18px diffuse shadow\",\n  \"pointerLight\": \"450px ellipse / alpha .094\"\n}\n```\n\n## 部品の境界\n外観とポインター反応を持つ汎用コンテナ。文章・画像・ボタン・トグルを自由に子要素へ配置する。展示用の見出しや番号、詳細画面、特定の見本文言は組み込まない。`.sop-surface-content` は内側のレイヤーで、`--sop-padding` で余白を調整できる。\n\n## 動作\nルートは `.sop-machined-panel`。マウスの位置を基準に反射を更新する。タッチ時は静かな既定位置を維持して、縦スクロールを妨げない。prefers-reduced-motionで動きを止める。画面から外したとき、イベントとRAF/Observerを確実に解除する。子のボタンを押しても、コンテナが意図しない動作をしない。\n\n## 再現確認\n元のCSSを正本とし、輪郭・面・陰影・色の強さ・動きの順に照合する。中身を空にした場合、文章を増やした場合、狭い幅、同じ部品の複数配置でも崩れないこと。元コードから外観を変更せず、統合先に合わせて子要素と幅・余白のみを調整する。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/blocks/machined-panel/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/blocks/machined-panel/react/MachinedPanel.tsx","code":"'use client';\nimport React, { useEffect, useRef, type HTMLAttributes } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\nexport interface MachinedPanelProps extends HTMLAttributes\u003cHTMLDivElement> {\n}\n/** 精密な輪郭と、指先を追う金属の反射。 The content is yours. */\nexport default function MachinedPanel({ children, className = '', ...props }: MachinedPanelProps) {\n    const element = useRef\u003cHTMLDivElement | null>(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-machined-panel ${className}`}>\n    \u003cspan className=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"tsx","group":"component"},{"name":"src/parts/blocks/machined-panel/styles.css","code":"/* machined-panel: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-machined-panel {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-machined-panel .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-machined-panel,\n  .sop-surface.sop-machined-panel:before,\n  .sop-surface.sop-machined-panel:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-machined-panel {\n  border-radius: 12px;\n  border: 1px solid #777e7470;\n  background: repeating-linear-gradient(0deg,#e4ecdd04 0 1px,transparent 1px 3px),linear-gradient(142deg,#474c46 0,#242925 30%,#191e1b 65%,#353c34);\n  box-shadow: inset 0 1px 0 #d3dcc666,inset 0 -2px 0 #080d0a,0 3px 0 #0a100b,0 18px 30px #0006;\n  transition: border-color .4s;\n}\n\n.sop-surface.sop-machined-panel:before {\n  content: \"\";\n  position: absolute;\n  inset: 8px;\n  border: 1px solid #b7c4ad22;\n  border-radius: 6px;\n  box-shadow: 0 0 0 1px #030a0370;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(450px ellipse at var(--sop-x,20%) var(--sop-y,10%),#d6e3c718,transparent 64%);\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:hover {\n  border-color: #a3ae8b99;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware {\n  position: absolute;\n  inset: 17px;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(135deg,#abb3a3,#323b2f 40%,#0b100b 45%,#91a180 65%,#475540);\n  box-shadow: 0 0 0 1px #10190e,inset 0 1px 0 #efffdb50;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:before {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #121a0f;\n  transform: rotate(-30deg);\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(1) {\n  top: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(2) {\n  top: 0;\n  right: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(3) {\n  bottom: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(4) {\n  bottom: 0;\n  right: 0;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/machined-panel/react/Example.tsx","code":"import React from 'react';\nimport MachinedPanel from './MachinedPanel';\n\nexport default function Example() {\n  return \u003cMachinedPanel style={{maxWidth: 420}}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/MachinedPanel>;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/blocks/machined-panel/markup.html","code":"\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/blocks/machined-panel/react/MachinedPanel.jsx","code":"'use client';\nimport React, { useEffect, useRef } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\n/** 精密な輪郭と、指先を追う金属の反射。 The content is yours. */\nexport default function MachinedPanel({ children, className = '', ...props }) {\n    const element = useRef(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-machined-panel ${className}`}>\n    \u003cspan className=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"jsx","group":"component"},{"name":"src/parts/blocks/machined-panel/styles.css","code":"/* machined-panel: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-machined-panel {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-machined-panel .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-machined-panel,\n  .sop-surface.sop-machined-panel:before,\n  .sop-surface.sop-machined-panel:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-machined-panel {\n  border-radius: 12px;\n  border: 1px solid #777e7470;\n  background: repeating-linear-gradient(0deg,#e4ecdd04 0 1px,transparent 1px 3px),linear-gradient(142deg,#474c46 0,#242925 30%,#191e1b 65%,#353c34);\n  box-shadow: inset 0 1px 0 #d3dcc666,inset 0 -2px 0 #080d0a,0 3px 0 #0a100b,0 18px 30px #0006;\n  transition: border-color .4s;\n}\n\n.sop-surface.sop-machined-panel:before {\n  content: \"\";\n  position: absolute;\n  inset: 8px;\n  border: 1px solid #b7c4ad22;\n  border-radius: 6px;\n  box-shadow: 0 0 0 1px #030a0370;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(450px ellipse at var(--sop-x,20%) var(--sop-y,10%),#d6e3c718,transparent 64%);\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:hover {\n  border-color: #a3ae8b99;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware {\n  position: absolute;\n  inset: 17px;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(135deg,#abb3a3,#323b2f 40%,#0b100b 45%,#91a180 65%,#475540);\n  box-shadow: 0 0 0 1px #10190e,inset 0 1px 0 #efffdb50;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:before {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #121a0f;\n  transform: rotate(-30deg);\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(1) {\n  top: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(2) {\n  top: 0;\n  right: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(3) {\n  bottom: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(4) {\n  bottom: 0;\n  right: 0;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/machined-panel/react/Example.jsx","code":"import React from 'react';\nimport MachinedPanel from './MachinedPanel';\nexport default function Example() {\n    return \u003cMachinedPanel style={{ maxWidth: 420 }}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/MachinedPanel>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/blocks/machined-panel/markup.html","code":"\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/blocks/machined-panel/vanilla/init.ts","code":"import { createSurfaceController, type SurfaceOptions } from '../../../../shared/surface-controller';\nexport function init(element: HTMLElement, options: SurfaceOptions = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"typescript","group":"component"},{"name":"src/parts/blocks/machined-panel/styles.css","code":"/* machined-panel: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-machined-panel {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-machined-panel .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-machined-panel,\n  .sop-surface.sop-machined-panel:before,\n  .sop-surface.sop-machined-panel:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-machined-panel {\n  border-radius: 12px;\n  border: 1px solid #777e7470;\n  background: repeating-linear-gradient(0deg,#e4ecdd04 0 1px,transparent 1px 3px),linear-gradient(142deg,#474c46 0,#242925 30%,#191e1b 65%,#353c34);\n  box-shadow: inset 0 1px 0 #d3dcc666,inset 0 -2px 0 #080d0a,0 3px 0 #0a100b,0 18px 30px #0006;\n  transition: border-color .4s;\n}\n\n.sop-surface.sop-machined-panel:before {\n  content: \"\";\n  position: absolute;\n  inset: 8px;\n  border: 1px solid #b7c4ad22;\n  border-radius: 6px;\n  box-shadow: 0 0 0 1px #030a0370;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(450px ellipse at var(--sop-x,20%) var(--sop-y,10%),#d6e3c718,transparent 64%);\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:hover {\n  border-color: #a3ae8b99;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware {\n  position: absolute;\n  inset: 17px;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(135deg,#abb3a3,#323b2f 40%,#0b100b 45%,#91a180 65%,#475540);\n  box-shadow: 0 0 0 1px #10190e,inset 0 1px 0 #efffdb50;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:before {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #121a0f;\n  transform: rotate(-30deg);\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(1) {\n  top: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(2) {\n  top: 0;\n  right: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(3) {\n  bottom: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(4) {\n  bottom: 0;\n  right: 0;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/machined-panel/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-machined-panel');\nif (!element) throw new Error('The Machined Panel root was not found.');\nconst controller = init(element);\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/blocks/machined-panel/markup.html","code":"\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/machined-panel/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Machined Panel\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/blocks/machined-panel/vanilla/init.js","code":"import { createSurfaceController } from '../../../../shared/surface-controller.js';\nexport function init(element, options = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"javascript","group":"component"},{"name":"src/parts/blocks/machined-panel/styles.css","code":"/* machined-panel: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-machined-panel {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-machined-panel .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-machined-panel,\n  .sop-surface.sop-machined-panel:before,\n  .sop-surface.sop-machined-panel:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-machined-panel {\n  border-radius: 12px;\n  border: 1px solid #777e7470;\n  background: repeating-linear-gradient(0deg,#e4ecdd04 0 1px,transparent 1px 3px),linear-gradient(142deg,#474c46 0,#242925 30%,#191e1b 65%,#353c34);\n  box-shadow: inset 0 1px 0 #d3dcc666,inset 0 -2px 0 #080d0a,0 3px 0 #0a100b,0 18px 30px #0006;\n  transition: border-color .4s;\n}\n\n.sop-surface.sop-machined-panel:before {\n  content: \"\";\n  position: absolute;\n  inset: 8px;\n  border: 1px solid #b7c4ad22;\n  border-radius: 6px;\n  box-shadow: 0 0 0 1px #030a0370;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(450px ellipse at var(--sop-x,20%) var(--sop-y,10%),#d6e3c718,transparent 64%);\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:hover {\n  border-color: #a3ae8b99;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware {\n  position: absolute;\n  inset: 17px;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(135deg,#abb3a3,#323b2f 40%,#0b100b 45%,#91a180 65%,#475540);\n  box-shadow: 0 0 0 1px #10190e,inset 0 1px 0 #efffdb50;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:before {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #121a0f;\n  transform: rotate(-30deg);\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(1) {\n  top: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(2) {\n  top: 0;\n  right: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(3) {\n  bottom: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(4) {\n  bottom: 0;\n  right: 0;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/machined-panel/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-machined-panel');\nif (!element)\n    throw new Error('The Machined Panel root was not found.');\nconst controller = init(element);\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/blocks/machined-panel/markup.html","code":"\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/machined-panel/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Machined Panel\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Machined Panel — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Machined Panel · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cdiv class=\"sop-surface sop-machined-panel\">\u003cspan class=\"sop-hardware\" aria-hidden=\"true\">\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003ci>\u003c/i>\u003c/span>\u003cdiv class=\"sop-surface-content\">\u003ch2>Machined Panel\u003c/h2>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003cbutton type=\"button\">サンプルボタン\u003c/button>\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\u003c/div>\u003cp class=\"hint\">中身を自由に入れ替えられる、独立した背景パーツです。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* machined-panel: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-machined-panel {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-machined-panel .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-machined-panel,\n  .sop-surface.sop-machined-panel:before,\n  .sop-surface.sop-machined-panel:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-machined-panel {\n  border-radius: 12px;\n  border: 1px solid #777e7470;\n  background: repeating-linear-gradient(0deg,#e4ecdd04 0 1px,transparent 1px 3px),linear-gradient(142deg,#474c46 0,#242925 30%,#191e1b 65%,#353c34);\n  box-shadow: inset 0 1px 0 #d3dcc666,inset 0 -2px 0 #080d0a,0 3px 0 #0a100b,0 18px 30px #0006;\n  transition: border-color .4s;\n}\n\n.sop-surface.sop-machined-panel:before {\n  content: \"\";\n  position: absolute;\n  inset: 8px;\n  border: 1px solid #b7c4ad22;\n  border-radius: 6px;\n  box-shadow: 0 0 0 1px #030a0370;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(450px ellipse at var(--sop-x,20%) var(--sop-y,10%),#d6e3c718,transparent 64%);\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-machined-panel:hover {\n  border-color: #a3ae8b99;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware {\n  position: absolute;\n  inset: 17px;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i {\n  position: absolute;\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: linear-gradient(135deg,#abb3a3,#323b2f 40%,#0b100b 45%,#91a180 65%,#475540);\n  box-shadow: 0 0 0 1px #10190e,inset 0 1px 0 #efffdb50;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:before {\n  content: \"\";\n  position: absolute;\n  left: 1px;\n  right: 1px;\n  top: 3px;\n  height: 1px;\n  background: #121a0f;\n  transform: rotate(-30deg);\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(1) {\n  top: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(2) {\n  top: 0;\n  right: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(3) {\n  bottom: 0;\n  left: 0;\n}\n\n.sop-surface.sop-machined-panel .sop-hardware i:nth-child(4) {\n  bottom: 0;\n  right: 0;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/blocks/machined-panel/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/blocks/machined-panel/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/blocks/machined-panel/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst surface_controller_1 = require(\"src/shared/surface-controller.ts\");\nfunction init(element, options = {}) {\n    return (0, surface_controller_1.createSurfaceController)(element, { tilt: false, ...options });\n}\n\n},\n\"src/shared/surface-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createSurfaceController = createSurfaceController;\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nfunction createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/blocks/machined-panel/demo/main.ts\");})();\n"}},{"id":"luminous-frame","name":"Luminous Frame","category":"blocks","order":14,"version":"2.3.0","tagline":"境界に、光。","description":"静かな面の輪郭を、光がゆっくり巡る。","material":"LIGHT / PERIMETER","motion":"ORBIT","accent":"191,234,171","initial":false,"componentName":"LuminousFrame","tags":["Luminous Frame","ブロック","カード","背景","静かな面の輪郭を、光がゆっくり巡る。"],"config":{"tilt":false},"related":["original-surface"],"design":{"radius":"16px","lightWidth":"1px exposed inner border","rotation":"8 seconds / linear / full 360deg","face":"#141a16","lightPeak":"#d8ffec","glow":"hover only / subtle alpha .063"},"props":[["children","ReactNode","文章、画像、ボタンなど好きな中身を配置できます。"],["className / style","標準のReact属性","幅、余白、外側のレイアウトを設定します。"],["--sop-padding","CSSカスタムプロパティ","内側の余白。初期値は28pxです。"],["--sop-accent","R,G,B","Original Surfaceの追従光の色を指定できます。"]],"markup":"\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","usage":"# Luminous Frame / 2.3.0\n\n静かな面の輪郭を、光がゆっくり巡る。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`LuminousFrame` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/blocks/luminous-frame/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `children` (ReactNode): 文章、画像、ボタンなど好きな中身を配置できます。\n- `className / style` (標準のReact属性): 幅、余白、外側のレイアウトを設定します。\n- `--sop-padding` (CSSカスタムプロパティ): 内側の余白。初期値は28pxです。\n- `--sop-accent` (R,G,B): Original Surfaceの追従光の色を指定できます。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/luminous-frame/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/blocks/luminous-frame/react/LuminousFrame.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/blocks/luminous-frame/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/blocks/luminous-frame/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/blocks/luminous-frame/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Luminous Frame — 再現仕様\n\n中央の面は動かさず、細い輪郭のみを光が巡る。巨大なグラデーション背景へ変えない。8秒で一周する淡い緑から青緑の光を保つ。\n\n## 基準値\n```json\n{\n  \"radius\": \"16px\",\n  \"lightWidth\": \"1px exposed inner border\",\n  \"rotation\": \"8 seconds / linear / full 360deg\",\n  \"face\": \"#141a16\",\n  \"lightPeak\": \"#d8ffec\",\n  \"glow\": \"hover only / subtle alpha .063\"\n}\n```\n\n## 部品の境界\n外観とポインター反応を持つ汎用コンテナ。文章・画像・ボタン・トグルを自由に子要素へ配置する。展示用の見出しや番号、詳細画面、特定の見本文言は組み込まない。`.sop-surface-content` は内側のレイヤーで、`--sop-padding` で余白を調整できる。\n\n## 動作\nルートは `.sop-luminous-frame`。マウスの位置を基準に反射を更新する。タッチ時は静かな既定位置を維持して、縦スクロールを妨げない。prefers-reduced-motionで動きを止める。画面から外したとき、イベントとRAF/Observerを確実に解除する。子のボタンを押しても、コンテナが意図しない動作をしない。\n\n## 再現確認\n元のCSSを正本とし、輪郭・面・陰影・色の強さ・動きの順に照合する。中身を空にした場合、文章を増やした場合、狭い幅、同じ部品の複数配置でも崩れないこと。元コードから外観を変更せず、統合先に合わせて子要素と幅・余白のみを調整する。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/blocks/luminous-frame/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/blocks/luminous-frame/react/LuminousFrame.tsx","code":"'use client';\nimport React, { useEffect, useRef, type HTMLAttributes } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\nexport interface LuminousFrameProps extends HTMLAttributes\u003cHTMLDivElement> {\n}\n/** 静かな面の輪郭を、光がゆっくり巡る。 The content is yours. */\nexport default function LuminousFrame({ children, className = '', ...props }: LuminousFrameProps) {\n    const element = useRef\u003cHTMLDivElement | null>(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-luminous-frame ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"tsx","group":"component"},{"name":"src/parts/blocks/luminous-frame/styles.css","code":"/* luminous-frame: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-luminous-frame {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-luminous-frame .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-luminous-frame,\n  .sop-surface.sop-luminous-frame:before,\n  .sop-surface.sop-luminous-frame:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-luminous-frame {\n  border-radius: 16px;\n  border: 1px solid #9ae4b226;\n  background: #151b18;\n  box-shadow: 0 16px 38px #0005;\n  isolation: isolate;\n}\n\n.sop-surface.sop-luminous-frame:before {\n  content: \"\";\n  position: absolute;\n  inset: -120%;\n  background: conic-gradient(from 45deg,transparent 0 58%,#607c5430 64%,#c9edb488 70%,#d8ffec 73%,#78cdbe55 78%,transparent 84%);\n  animation: sop-frame-turn 8s linear infinite;\n  animation-play-state: var(--sop-play,running);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:after {\n  content: \"\";\n  position: absolute;\n  inset: 1px;\n  border-radius: 14px;\n  background: radial-gradient(ellipse at 50% 0,#31473625,transparent 60%),#141a16;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:hover {\n  box-shadow: 0 16px 38px #0005,0 0 28px #b4efa410;\n}\n\n@keyframes sop-frame-turn {\nto{transform:rotate(360deg)}\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/luminous-frame/react/Example.tsx","code":"import React from 'react';\nimport LuminousFrame from './LuminousFrame';\n\nexport default function Example() {\n  return \u003cLuminousFrame style={{maxWidth: 420}}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/LuminousFrame>;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/blocks/luminous-frame/markup.html","code":"\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/blocks/luminous-frame/react/LuminousFrame.jsx","code":"'use client';\nimport React, { useEffect, useRef } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\n/** 静かな面の輪郭を、光がゆっくり巡る。 The content is yours. */\nexport default function LuminousFrame({ children, className = '', ...props }) {\n    const element = useRef(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-luminous-frame ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"jsx","group":"component"},{"name":"src/parts/blocks/luminous-frame/styles.css","code":"/* luminous-frame: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-luminous-frame {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-luminous-frame .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-luminous-frame,\n  .sop-surface.sop-luminous-frame:before,\n  .sop-surface.sop-luminous-frame:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-luminous-frame {\n  border-radius: 16px;\n  border: 1px solid #9ae4b226;\n  background: #151b18;\n  box-shadow: 0 16px 38px #0005;\n  isolation: isolate;\n}\n\n.sop-surface.sop-luminous-frame:before {\n  content: \"\";\n  position: absolute;\n  inset: -120%;\n  background: conic-gradient(from 45deg,transparent 0 58%,#607c5430 64%,#c9edb488 70%,#d8ffec 73%,#78cdbe55 78%,transparent 84%);\n  animation: sop-frame-turn 8s linear infinite;\n  animation-play-state: var(--sop-play,running);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:after {\n  content: \"\";\n  position: absolute;\n  inset: 1px;\n  border-radius: 14px;\n  background: radial-gradient(ellipse at 50% 0,#31473625,transparent 60%),#141a16;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:hover {\n  box-shadow: 0 16px 38px #0005,0 0 28px #b4efa410;\n}\n\n@keyframes sop-frame-turn {\nto{transform:rotate(360deg)}\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/luminous-frame/react/Example.jsx","code":"import React from 'react';\nimport LuminousFrame from './LuminousFrame';\nexport default function Example() {\n    return \u003cLuminousFrame style={{ maxWidth: 420 }}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/LuminousFrame>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/blocks/luminous-frame/markup.html","code":"\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/blocks/luminous-frame/vanilla/init.ts","code":"import { createSurfaceController, type SurfaceOptions } from '../../../../shared/surface-controller';\nexport function init(element: HTMLElement, options: SurfaceOptions = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"typescript","group":"component"},{"name":"src/parts/blocks/luminous-frame/styles.css","code":"/* luminous-frame: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-luminous-frame {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-luminous-frame .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-luminous-frame,\n  .sop-surface.sop-luminous-frame:before,\n  .sop-surface.sop-luminous-frame:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-luminous-frame {\n  border-radius: 16px;\n  border: 1px solid #9ae4b226;\n  background: #151b18;\n  box-shadow: 0 16px 38px #0005;\n  isolation: isolate;\n}\n\n.sop-surface.sop-luminous-frame:before {\n  content: \"\";\n  position: absolute;\n  inset: -120%;\n  background: conic-gradient(from 45deg,transparent 0 58%,#607c5430 64%,#c9edb488 70%,#d8ffec 73%,#78cdbe55 78%,transparent 84%);\n  animation: sop-frame-turn 8s linear infinite;\n  animation-play-state: var(--sop-play,running);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:after {\n  content: \"\";\n  position: absolute;\n  inset: 1px;\n  border-radius: 14px;\n  background: radial-gradient(ellipse at 50% 0,#31473625,transparent 60%),#141a16;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:hover {\n  box-shadow: 0 16px 38px #0005,0 0 28px #b4efa410;\n}\n\n@keyframes sop-frame-turn {\nto{transform:rotate(360deg)}\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/luminous-frame/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-luminous-frame');\nif (!element) throw new Error('The Luminous Frame root was not found.');\nconst controller = init(element);\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/blocks/luminous-frame/markup.html","code":"\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/luminous-frame/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Luminous Frame\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/blocks/luminous-frame/vanilla/init.js","code":"import { createSurfaceController } from '../../../../shared/surface-controller.js';\nexport function init(element, options = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"javascript","group":"component"},{"name":"src/parts/blocks/luminous-frame/styles.css","code":"/* luminous-frame: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-luminous-frame {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-luminous-frame .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-luminous-frame,\n  .sop-surface.sop-luminous-frame:before,\n  .sop-surface.sop-luminous-frame:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-luminous-frame {\n  border-radius: 16px;\n  border: 1px solid #9ae4b226;\n  background: #151b18;\n  box-shadow: 0 16px 38px #0005;\n  isolation: isolate;\n}\n\n.sop-surface.sop-luminous-frame:before {\n  content: \"\";\n  position: absolute;\n  inset: -120%;\n  background: conic-gradient(from 45deg,transparent 0 58%,#607c5430 64%,#c9edb488 70%,#d8ffec 73%,#78cdbe55 78%,transparent 84%);\n  animation: sop-frame-turn 8s linear infinite;\n  animation-play-state: var(--sop-play,running);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:after {\n  content: \"\";\n  position: absolute;\n  inset: 1px;\n  border-radius: 14px;\n  background: radial-gradient(ellipse at 50% 0,#31473625,transparent 60%),#141a16;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:hover {\n  box-shadow: 0 16px 38px #0005,0 0 28px #b4efa410;\n}\n\n@keyframes sop-frame-turn {\nto{transform:rotate(360deg)}\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/luminous-frame/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-luminous-frame');\nif (!element)\n    throw new Error('The Luminous Frame root was not found.');\nconst controller = init(element);\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/blocks/luminous-frame/markup.html","code":"\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/luminous-frame/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Luminous Frame\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Luminous Frame — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Luminous Frame · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cdiv class=\"sop-surface sop-luminous-frame\">\u003cdiv class=\"sop-surface-content\">\u003ch2>Luminous Frame\u003c/h2>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003cbutton type=\"button\">サンプルボタン\u003c/button>\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\u003c/div>\u003cp class=\"hint\">中身を自由に入れ替えられる、独立した背景パーツです。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* luminous-frame: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-luminous-frame {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-luminous-frame .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-luminous-frame,\n  .sop-surface.sop-luminous-frame:before,\n  .sop-surface.sop-luminous-frame:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-luminous-frame {\n  border-radius: 16px;\n  border: 1px solid #9ae4b226;\n  background: #151b18;\n  box-shadow: 0 16px 38px #0005;\n  isolation: isolate;\n}\n\n.sop-surface.sop-luminous-frame:before {\n  content: \"\";\n  position: absolute;\n  inset: -120%;\n  background: conic-gradient(from 45deg,transparent 0 58%,#607c5430 64%,#c9edb488 70%,#d8ffec 73%,#78cdbe55 78%,transparent 84%);\n  animation: sop-frame-turn 8s linear infinite;\n  animation-play-state: var(--sop-play,running);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:after {\n  content: \"\";\n  position: absolute;\n  inset: 1px;\n  border-radius: 14px;\n  background: radial-gradient(ellipse at 50% 0,#31473625,transparent 60%),#141a16;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-luminous-frame:hover {\n  box-shadow: 0 16px 38px #0005,0 0 28px #b4efa410;\n}\n\n@keyframes sop-frame-turn {\nto{transform:rotate(360deg)}\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/blocks/luminous-frame/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/blocks/luminous-frame/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/blocks/luminous-frame/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst surface_controller_1 = require(\"src/shared/surface-controller.ts\");\nfunction init(element, options = {}) {\n    return (0, surface_controller_1.createSurfaceController)(element, { tilt: false, ...options });\n}\n\n},\n\"src/shared/surface-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createSurfaceController = createSurfaceController;\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nfunction createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/blocks/luminous-frame/demo/main.ts\");})();\n"}},{"id":"folded-paper","name":"Folded Paper","category":"blocks","order":15,"version":"2.3.0","tagline":"余白を、折る。","description":"温かな紙の厚みと、持ち上がる小さな角。","material":"PAPER / TACTILE","motion":"LIFT","accent":"225,189,149","initial":false,"componentName":"FoldedPaper","tags":["Folded Paper","ブロック","カード","背景","温かな紙の厚みと、持ち上がる小さな角。"],"config":{"tilt":false},"related":["original-surface"],"design":{"face":"#f1e7d3 to #e8d9be","text":"#53473a","fold":"top-right 30px","radius":"3px except cut corner","guideLine":"24px inset / #ad785b33","hover":"translateY(-4px) rotate(-.5deg), 550ms"},"props":[["children","ReactNode","文章、画像、ボタンなど好きな中身を配置できます。"],["className / style","標準のReact属性","幅、余白、外側のレイアウトを設定します。"],["--sop-padding","CSSカスタムプロパティ","内側の余白。初期値は28pxです。"],["--sop-accent","R,G,B","Original Surfaceの追従光の色を指定できます。"]],"markup":"\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","usage":"# Folded Paper / 2.3.0\n\n温かな紙の厚みと、持ち上がる小さな角。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`FoldedPaper` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/blocks/folded-paper/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `children` (ReactNode): 文章、画像、ボタンなど好きな中身を配置できます。\n- `className / style` (標準のReact属性): 幅、余白、外側のレイアウトを設定します。\n- `--sop-padding` (CSSカスタムプロパティ): 内側の余白。初期値は28pxです。\n- `--sop-accent` (R,G,B): Original Surfaceの追従光の色を指定できます。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/folded-paper/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/blocks/folded-paper/react/FoldedPaper.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/blocks/folded-paper/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/blocks/folded-paper/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/blocks/folded-paper/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Folded Paper — 再現仕様\n\n暗い展示空間に置かれた暖かな紙。右上30pxの折り返し、左の細い罫線、繊細な紙目を維持する。ホバー時は4px浮き、0.5度だけ傾く。フォームや文章を子要素に配置できる。\n\n## 基準値\n```json\n{\n  \"face\": \"#f1e7d3 to #e8d9be\",\n  \"text\": \"#53473a\",\n  \"fold\": \"top-right 30px\",\n  \"radius\": \"3px except cut corner\",\n  \"guideLine\": \"24px inset / #ad785b33\",\n  \"hover\": \"translateY(-4px) rotate(-.5deg), 550ms\"\n}\n```\n\n## 部品の境界\n外観とポインター反応を持つ汎用コンテナ。文章・画像・ボタン・トグルを自由に子要素へ配置する。展示用の見出しや番号、詳細画面、特定の見本文言は組み込まない。`.sop-surface-content` は内側のレイヤーで、`--sop-padding` で余白を調整できる。\n\n## 動作\nルートは `.sop-folded-paper`。マウスの位置を基準に反射を更新する。タッチ時は静かな既定位置を維持して、縦スクロールを妨げない。prefers-reduced-motionで動きを止める。画面から外したとき、イベントとRAF/Observerを確実に解除する。子のボタンを押しても、コンテナが意図しない動作をしない。\n\n## 再現確認\n元のCSSを正本とし、輪郭・面・陰影・色の強さ・動きの順に照合する。中身を空にした場合、文章を増やした場合、狭い幅、同じ部品の複数配置でも崩れないこと。元コードから外観を変更せず、統合先に合わせて子要素と幅・余白のみを調整する。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/blocks/folded-paper/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/blocks/folded-paper/react/FoldedPaper.tsx","code":"'use client';\nimport React, { useEffect, useRef, type HTMLAttributes } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\nexport interface FoldedPaperProps extends HTMLAttributes\u003cHTMLDivElement> {\n}\n/** 温かな紙の厚みと、持ち上がる小さな角。 The content is yours. */\nexport default function FoldedPaper({ children, className = '', ...props }: FoldedPaperProps) {\n    const element = useRef\u003cHTMLDivElement | null>(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-folded-paper ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"tsx","group":"component"},{"name":"src/parts/blocks/folded-paper/styles.css","code":"/* folded-paper: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-folded-paper {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-folded-paper .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-folded-paper,\n  .sop-surface.sop-folded-paper:before,\n  .sop-surface.sop-folded-paper:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-folded-paper {\n  --sop-fold: 30px;\n  border: 0;\n  border-radius: 3px 0 3px 3px;\n  color: #53473a;\n  background: repeating-linear-gradient(0deg,#92775f04 0 1px,transparent 1px 3px),linear-gradient(125deg,#f1e7d3,#e8d9be);\n  clip-path: polygon(0 0,calc(100% - var(--sop-fold)) 0,100% var(--sop-fold),100% 100%,0 100%);\n  box-shadow: inset 0 1px 0 #fff9,inset 0 -1px 0 #8d71592b;\n  transition: transform .55s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-folded-paper:before {\n  content: \"\";\n  position: absolute;\n  left: 24px;\n  top: 24px;\n  bottom: 24px;\n  width: 1px;\n  background: #ad785b33;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-folded-paper:after {\n  content: \"\";\n  position: absolute;\n  right: 0;\n  top: 0;\n  width: var(--sop-fold);\n  height: var(--sop-fold);\n  background: linear-gradient(225deg,#bea98a,#dbccb2 48%,#f8ecd6 52%);\n  box-shadow: -3px 4px 5px #5d493821;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-folded-paper:hover {\n  transform: translateY(-4px) rotate(-.5deg);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/folded-paper/react/Example.tsx","code":"import React from 'react';\nimport FoldedPaper from './FoldedPaper';\n\nexport default function Example() {\n  return \u003cFoldedPaper style={{maxWidth: 420}}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/FoldedPaper>;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/blocks/folded-paper/markup.html","code":"\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/blocks/folded-paper/react/FoldedPaper.jsx","code":"'use client';\nimport React, { useEffect, useRef } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\n/** 温かな紙の厚みと、持ち上がる小さな角。 The content is yours. */\nexport default function FoldedPaper({ children, className = '', ...props }) {\n    const element = useRef(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: false });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-folded-paper ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"jsx","group":"component"},{"name":"src/parts/blocks/folded-paper/styles.css","code":"/* folded-paper: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-folded-paper {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-folded-paper .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-folded-paper,\n  .sop-surface.sop-folded-paper:before,\n  .sop-surface.sop-folded-paper:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-folded-paper {\n  --sop-fold: 30px;\n  border: 0;\n  border-radius: 3px 0 3px 3px;\n  color: #53473a;\n  background: repeating-linear-gradient(0deg,#92775f04 0 1px,transparent 1px 3px),linear-gradient(125deg,#f1e7d3,#e8d9be);\n  clip-path: polygon(0 0,calc(100% - var(--sop-fold)) 0,100% var(--sop-fold),100% 100%,0 100%);\n  box-shadow: inset 0 1px 0 #fff9,inset 0 -1px 0 #8d71592b;\n  transition: transform .55s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-folded-paper:before {\n  content: \"\";\n  position: absolute;\n  left: 24px;\n  top: 24px;\n  bottom: 24px;\n  width: 1px;\n  background: #ad785b33;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-folded-paper:after {\n  content: \"\";\n  position: absolute;\n  right: 0;\n  top: 0;\n  width: var(--sop-fold);\n  height: var(--sop-fold);\n  background: linear-gradient(225deg,#bea98a,#dbccb2 48%,#f8ecd6 52%);\n  box-shadow: -3px 4px 5px #5d493821;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-folded-paper:hover {\n  transform: translateY(-4px) rotate(-.5deg);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/folded-paper/react/Example.jsx","code":"import React from 'react';\nimport FoldedPaper from './FoldedPaper';\nexport default function Example() {\n    return \u003cFoldedPaper style={{ maxWidth: 420 }}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/FoldedPaper>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/blocks/folded-paper/markup.html","code":"\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/blocks/folded-paper/vanilla/init.ts","code":"import { createSurfaceController, type SurfaceOptions } from '../../../../shared/surface-controller';\nexport function init(element: HTMLElement, options: SurfaceOptions = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"typescript","group":"component"},{"name":"src/parts/blocks/folded-paper/styles.css","code":"/* folded-paper: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-folded-paper {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-folded-paper .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-folded-paper,\n  .sop-surface.sop-folded-paper:before,\n  .sop-surface.sop-folded-paper:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-folded-paper {\n  --sop-fold: 30px;\n  border: 0;\n  border-radius: 3px 0 3px 3px;\n  color: #53473a;\n  background: repeating-linear-gradient(0deg,#92775f04 0 1px,transparent 1px 3px),linear-gradient(125deg,#f1e7d3,#e8d9be);\n  clip-path: polygon(0 0,calc(100% - var(--sop-fold)) 0,100% var(--sop-fold),100% 100%,0 100%);\n  box-shadow: inset 0 1px 0 #fff9,inset 0 -1px 0 #8d71592b;\n  transition: transform .55s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-folded-paper:before {\n  content: \"\";\n  position: absolute;\n  left: 24px;\n  top: 24px;\n  bottom: 24px;\n  width: 1px;\n  background: #ad785b33;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-folded-paper:after {\n  content: \"\";\n  position: absolute;\n  right: 0;\n  top: 0;\n  width: var(--sop-fold);\n  height: var(--sop-fold);\n  background: linear-gradient(225deg,#bea98a,#dbccb2 48%,#f8ecd6 52%);\n  box-shadow: -3px 4px 5px #5d493821;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-folded-paper:hover {\n  transform: translateY(-4px) rotate(-.5deg);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/folded-paper/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-folded-paper');\nif (!element) throw new Error('The Folded Paper root was not found.');\nconst controller = init(element);\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/blocks/folded-paper/markup.html","code":"\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/folded-paper/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Folded Paper\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/blocks/folded-paper/vanilla/init.js","code":"import { createSurfaceController } from '../../../../shared/surface-controller.js';\nexport function init(element, options = {}) {\n    return createSurfaceController(element, { tilt: false, ...options });\n}\n","language":"javascript","group":"component"},{"name":"src/parts/blocks/folded-paper/styles.css","code":"/* folded-paper: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-folded-paper {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-folded-paper .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-folded-paper,\n  .sop-surface.sop-folded-paper:before,\n  .sop-surface.sop-folded-paper:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-folded-paper {\n  --sop-fold: 30px;\n  border: 0;\n  border-radius: 3px 0 3px 3px;\n  color: #53473a;\n  background: repeating-linear-gradient(0deg,#92775f04 0 1px,transparent 1px 3px),linear-gradient(125deg,#f1e7d3,#e8d9be);\n  clip-path: polygon(0 0,calc(100% - var(--sop-fold)) 0,100% var(--sop-fold),100% 100%,0 100%);\n  box-shadow: inset 0 1px 0 #fff9,inset 0 -1px 0 #8d71592b;\n  transition: transform .55s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-folded-paper:before {\n  content: \"\";\n  position: absolute;\n  left: 24px;\n  top: 24px;\n  bottom: 24px;\n  width: 1px;\n  background: #ad785b33;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-folded-paper:after {\n  content: \"\";\n  position: absolute;\n  right: 0;\n  top: 0;\n  width: var(--sop-fold);\n  height: var(--sop-fold);\n  background: linear-gradient(225deg,#bea98a,#dbccb2 48%,#f8ecd6 52%);\n  box-shadow: -3px 4px 5px #5d493821;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-folded-paper:hover {\n  transform: translateY(-4px) rotate(-.5deg);\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/folded-paper/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-folded-paper');\nif (!element)\n    throw new Error('The Folded Paper root was not found.');\nconst controller = init(element);\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/blocks/folded-paper/markup.html","code":"\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/folded-paper/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Folded Paper\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Folded Paper — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Folded Paper · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cdiv class=\"sop-surface sop-folded-paper\">\u003cdiv class=\"sop-surface-content\">\u003ch2>Folded Paper\u003c/h2>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003cbutton type=\"button\">サンプルボタン\u003c/button>\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\u003c/div>\u003cp class=\"hint\">中身を自由に入れ替えられる、独立した背景パーツです。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* folded-paper: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-folded-paper {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-folded-paper .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-folded-paper,\n  .sop-surface.sop-folded-paper:before,\n  .sop-surface.sop-folded-paper:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-folded-paper {\n  --sop-fold: 30px;\n  border: 0;\n  border-radius: 3px 0 3px 3px;\n  color: #53473a;\n  background: repeating-linear-gradient(0deg,#92775f04 0 1px,transparent 1px 3px),linear-gradient(125deg,#f1e7d3,#e8d9be);\n  clip-path: polygon(0 0,calc(100% - var(--sop-fold)) 0,100% var(--sop-fold),100% 100%,0 100%);\n  box-shadow: inset 0 1px 0 #fff9,inset 0 -1px 0 #8d71592b;\n  transition: transform .55s cubic-bezier(.16,1,.3,1);\n}\n\n.sop-surface.sop-folded-paper:before {\n  content: \"\";\n  position: absolute;\n  left: 24px;\n  top: 24px;\n  bottom: 24px;\n  width: 1px;\n  background: #ad785b33;\n  pointer-events: none;\n  z-index: 0;\n}\n\n.sop-surface.sop-folded-paper:after {\n  content: \"\";\n  position: absolute;\n  right: 0;\n  top: 0;\n  width: var(--sop-fold);\n  height: var(--sop-fold);\n  background: linear-gradient(225deg,#bea98a,#dbccb2 48%,#f8ecd6 52%);\n  box-shadow: -3px 4px 5px #5d493821;\n  z-index: 2;\n  pointer-events: none;\n}\n\n.sop-surface.sop-folded-paper:hover {\n  transform: translateY(-4px) rotate(-.5deg);\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/blocks/folded-paper/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/blocks/folded-paper/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/blocks/folded-paper/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst surface_controller_1 = require(\"src/shared/surface-controller.ts\");\nfunction init(element, options = {}) {\n    return (0, surface_controller_1.createSurfaceController)(element, { tilt: false, ...options });\n}\n\n},\n\"src/shared/surface-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createSurfaceController = createSurfaceController;\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nfunction createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/blocks/folded-paper/demo/main.ts\");})();\n"}},{"id":"iridescent-surface","name":"Iridescent Surface","category":"blocks","order":16,"version":"2.3.0","tagline":"ひとつではない、色。","description":"見る位置で揺らぐ、偏光のスペクトル。","material":"SPECTRUM / OPTICAL","motion":"SHIFT","accent":"189,182,230","initial":false,"componentName":"IridescentSurface","tags":["Iridescent Surface","ブロック","カード","背景","見る位置で揺らぐ、偏光のスペクトル。"],"config":{"tilt":true},"related":["original-surface"],"design":{"radius":"16px","base":"#1b1b24","spectralAngle":"110 + normalizedX*70 - normalizedY*25 degrees","texture":"120deg, 1px at 5px interval","tilt":"X ±3deg / Y ±4deg","palette":"muted lavender / sage / rose / blue-grey"},"props":[["children","ReactNode","文章、画像、ボタンなど好きな中身を配置できます。"],["className / style","標準のReact属性","幅、余白、外側のレイアウトを設定します。"],["--sop-padding","CSSカスタムプロパティ","内側の余白。初期値は28pxです。"],["--sop-accent","R,G,B","Original Surfaceの追従光の色を指定できます。"]],"markup":"\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","usage":"# Iridescent Surface / 2.3.0\n\n見る位置で揺らぐ、偏光のスペクトル。\n\n## React + TypeScript / JavaScript\nZIPを展開し、`src/parts/` と `src/shared/` の階層を崩さずまとめて配置し、`IridescentSurface` をimportします。CSSはコンポーネントから読み込まれます。React 18以降を前提とするソースです。JSX版はTSXから型を除去したものです。Next.jsなどではクライアントコンポーネントとして使います。\n\n## 通常のHTML / TypeScript\n`markup.html` の要素を配置し、`styles.css` を読み込み、`init(element, options)` を実行します。`init`の返り値の `destroy()` を、画面や部品を取り外すときに必ず呼び出します。TS版は利用先のビルド環境で変換して使います。JS版の `src/parts/blocks/iridescent-surface/vanilla/index.html` はZIPのルートを公開するローカルサーバーから開きます。ZIP内の `preview/index.html` はダブルクリックでも開ける独立デモです。\n\n## そのまま保たれるもの\n外観のCSS、素材別の動き、マウスとキーボードの操作、動きを減らす設定。音は展示サイト専用の任意機能で、配布パーツには含めません。展示枠やサンプル文言は部品本体から分離しています。\n\n## 調整\n- `children` (ReactNode): 文章、画像、ボタンなど好きな中身を配置できます。\n- `className / style` (標準のReact属性): 幅、余白、外側のレイアウトを設定します。\n- `--sop-padding` (CSSカスタムプロパティ): 内側の余白。初期値は28pxです。\n- `--sop-accent` (R,G,B): Original Surfaceの追従光の色を指定できます。\n\n## コピーと依存関係\nコード画面の「コピー」で表示中ファイルの本文をコピーし、「ファイルを保存」でそのファイルだけをダウンロードできます。関連ファイル一式は「パーツZIP」で取得してください。共通処理を含む全ファイルを同じ構成で配置してください。JavaScript/TypeScript版の実行時外部依存はありません。React版はReactが必要です。\n\n## ディレクトリ構成を保って導入\n\nこのパッケージは、元のリポジトリ内の相対パスを保持しています。\nファイルだけを一か所に集めたり、`src/shared/` を外したりせず、ZIP内の `src/` をフォルダーごとコピーしてください。\n既存プロジェクトとの衝突を避ける場合は、`src/` 全体を `components/state-of-play/iridescent-surface/` などの専用フォルダーへ入れ、入口へのimportだけを変更します。\n内部の `parts/` と `shared/` の位置関係はそのままにしてください。\n\n- React入口: `src/parts/blocks/iridescent-surface/react/IridescentSurface.tsx`（JSX版は `.jsx`）\n- React使用例: `src/parts/blocks/iridescent-surface/react/Example.tsx`（JSX版は `.jsx`）\n- 通常サイト入口: `src/parts/blocks/iridescent-surface/vanilla/init.ts`（JS版は `.js`）\n- スタイル: `src/parts/blocks/iridescent-surface/styles.css`\n- 共通処理: `src/shared/`\n- 独立デモ: `preview/index.html`、`preview/styles.css`、`preview/app.js`\n\n詳細欄のファイルツリー、コピーしたソース内の相対import、ZIPの保存パスは同じ構成です。\n「ファイルを保存」はブラウザーの仕様上、選択ファイルの名前のみで保存します。ディレクトリごとの導入には「パーツZIP」を使ってください。\n","prompt":"# Iridescent Surface — 再現仕様\n\n鮮烈な虹色ではなく、低彩度のラベンダー・セージ・ローズを重ねた偏光面。ポインター位置で色帯の方向と反射位置が変わり、控えめな3D傾斜が追従する。\n\n## 基準値\n```json\n{\n  \"radius\": \"16px\",\n  \"base\": \"#1b1b24\",\n  \"spectralAngle\": \"110 + normalizedX*70 - normalizedY*25 degrees\",\n  \"texture\": \"120deg, 1px at 5px interval\",\n  \"tilt\": \"X ±3deg / Y ±4deg\",\n  \"palette\": \"muted lavender / sage / rose / blue-grey\"\n}\n```\n\n## 部品の境界\n外観とポインター反応を持つ汎用コンテナ。文章・画像・ボタン・トグルを自由に子要素へ配置する。展示用の見出しや番号、詳細画面、特定の見本文言は組み込まない。`.sop-surface-content` は内側のレイヤーで、`--sop-padding` で余白を調整できる。\n\n## 動作\nルートは `.sop-iridescent-surface`。マウスの位置を基準に反射を更新する。タッチ時は静かな既定位置を維持して、縦スクロールを妨げない。prefers-reduced-motionで動きを止める。画面から外したとき、イベントとRAF/Observerを確実に解除する。子のボタンを押しても、コンテナが意図しない動作をしない。\n\n## 再現確認\n元のCSSを正本とし、輪郭・面・陰影・色の強さ・動きの順に照合する。中身を空にした場合、文章を増やした場合、狭い幅、同じ部品の複数配置でも崩れないこと。元コードから外観を変更せず、統合先に合わせて子要素と幅・余白のみを調整する。\n\n## 配置パスの保持\n\nソースコードの見出しに記載されたファイル名は、ZIPルートからの相対パスです。\n`src/parts/blocks/iridescent-surface/` と `src/shared/` を含む階層を維持し、ファイルを同じ階層に平坦化しないでください。\n別のフォルダーへ組み込む場合は、この `src/` の下の構造をまとめて移します。\n読み込み元と読み込み先の相対位置、CSSのパス、通常JS版の `.js` 拡張子を維持してください。\n","files":{"tsx":[{"name":"src/parts/blocks/iridescent-surface/react/IridescentSurface.tsx","code":"'use client';\nimport React, { useEffect, useRef, type HTMLAttributes } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\nexport interface IridescentSurfaceProps extends HTMLAttributes\u003cHTMLDivElement> {\n}\n/** 見る位置で揺らぐ、偏光のスペクトル。 The content is yours. */\nexport default function IridescentSurface({ children, className = '', ...props }: IridescentSurfaceProps) {\n    const element = useRef\u003cHTMLDivElement | null>(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: true });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-iridescent-surface ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"tsx","group":"component"},{"name":"src/parts/blocks/iridescent-surface/styles.css","code":"/* iridescent-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-iridescent-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-iridescent-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-iridescent-surface,\n  .sop-surface.sop-iridescent-surface:before,\n  .sop-surface.sop-iridescent-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-iridescent-surface {\n  border: 1px solid #c4c6f84d;\n  border-radius: 16px;\n  background: radial-gradient(ellipse at var(--sop-x,25%) var(--sop-y,25%),#b9b1dd16,transparent 65%),linear-gradient(var(--sop-angle,128deg),#b5c2ac19 0,#b9b1df3c 22%,#73889e22 40%,#a3bfa326 62%,#c49bab38 80%,#baba8c19),#1b1b24;\n  box-shadow: inset 0 1px 0 #f0dfff30,inset 0 -1px 0 #baffd51a,0 18px 35px #0005;\n  transition: transform .5s cubic-bezier(.16,1,.3,1),border-color .4s;\n}\n\n.sop-surface.sop-iridescent-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: repeating-linear-gradient(120deg,transparent 0 4px,#fcf0ff04 4px 5px);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(300px circle at var(--sop-x,30%) var(--sop-y,10%),#e9e5ff26,transparent 65%);\n  mix-blend-mode: screen;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:hover {\n  border-color: #d9d4f780;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/iridescent-surface/react/Example.tsx","code":"import React from 'react';\nimport IridescentSurface from './IridescentSurface';\n\nexport default function Example() {\n  return \u003cIridescentSurface style={{maxWidth: 420}}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/IridescentSurface>;\n}\n","language":"tsx","group":"example"},{"name":"src/parts/blocks/iridescent-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"jsx":[{"name":"src/parts/blocks/iridescent-surface/react/IridescentSurface.jsx","code":"'use client';\nimport React, { useEffect, useRef } from 'react';\nimport { createSurfaceController } from '../../../../shared/surface-controller';\nimport '../styles.css';\n/** 見る位置で揺らぐ、偏光のスペクトル。 The content is yours. */\nexport default function IridescentSurface({ children, className = '', ...props }) {\n    const element = useRef(null);\n    useEffect(() => {\n        if (!element.current)\n            return;\n        const controller = createSurfaceController(element.current, { tilt: true });\n        return () => controller.destroy();\n    }, []);\n    return \u003cdiv {...props} ref={element} className={`sop-surface sop-iridescent-surface ${className}`}>\n    \n    \u003cdiv className=\"sop-surface-content\">{children}\u003c/div>\n  \u003c/div>;\n}\n","language":"jsx","group":"component"},{"name":"src/parts/blocks/iridescent-surface/styles.css","code":"/* iridescent-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-iridescent-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-iridescent-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-iridescent-surface,\n  .sop-surface.sop-iridescent-surface:before,\n  .sop-surface.sop-iridescent-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-iridescent-surface {\n  border: 1px solid #c4c6f84d;\n  border-radius: 16px;\n  background: radial-gradient(ellipse at var(--sop-x,25%) var(--sop-y,25%),#b9b1dd16,transparent 65%),linear-gradient(var(--sop-angle,128deg),#b5c2ac19 0,#b9b1df3c 22%,#73889e22 40%,#a3bfa326 62%,#c49bab38 80%,#baba8c19),#1b1b24;\n  box-shadow: inset 0 1px 0 #f0dfff30,inset 0 -1px 0 #baffd51a,0 18px 35px #0005;\n  transition: transform .5s cubic-bezier(.16,1,.3,1),border-color .4s;\n}\n\n.sop-surface.sop-iridescent-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: repeating-linear-gradient(120deg,transparent 0 4px,#fcf0ff04 4px 5px);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(300px circle at var(--sop-x,30%) var(--sop-y,10%),#e9e5ff26,transparent 65%);\n  mix-blend-mode: screen;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:hover {\n  border-color: #d9d4f780;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/iridescent-surface/react/Example.jsx","code":"import React from 'react';\nimport IridescentSurface from './IridescentSurface';\nexport default function Example() {\n    return \u003cIridescentSurface style={{ maxWidth: 420 }}>\n    \u003ch3>Your next idea.\u003c/h3>\n    \u003cp>ここに、あなたのコンテンツを。\u003c/p>\n    \u003cbutton type=\"button\">はじめる\u003c/button>\n  \u003c/IridescentSurface>;\n}\n","language":"jsx","group":"example"},{"name":"src/parts/blocks/iridescent-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"}],"ts":[{"name":"src/parts/blocks/iridescent-surface/vanilla/init.ts","code":"import { createSurfaceController, type SurfaceOptions } from '../../../../shared/surface-controller';\nexport function init(element: HTMLElement, options: SurfaceOptions = {}) {\n    return createSurfaceController(element, { tilt: true, ...options });\n}\n","language":"typescript","group":"component"},{"name":"src/parts/blocks/iridescent-surface/styles.css","code":"/* iridescent-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-iridescent-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-iridescent-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-iridescent-surface,\n  .sop-surface.sop-iridescent-surface:before,\n  .sop-surface.sop-iridescent-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-iridescent-surface {\n  border: 1px solid #c4c6f84d;\n  border-radius: 16px;\n  background: radial-gradient(ellipse at var(--sop-x,25%) var(--sop-y,25%),#b9b1dd16,transparent 65%),linear-gradient(var(--sop-angle,128deg),#b5c2ac19 0,#b9b1df3c 22%,#73889e22 40%,#a3bfa326 62%,#c49bab38 80%,#baba8c19),#1b1b24;\n  box-shadow: inset 0 1px 0 #f0dfff30,inset 0 -1px 0 #baffd51a,0 18px 35px #0005;\n  transition: transform .5s cubic-bezier(.16,1,.3,1),border-color .4s;\n}\n\n.sop-surface.sop-iridescent-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: repeating-linear-gradient(120deg,transparent 0 4px,#fcf0ff04 4px 5px);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(300px circle at var(--sop-x,30%) var(--sop-y,10%),#e9e5ff26,transparent 65%);\n  mix-blend-mode: screen;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:hover {\n  border-color: #d9d4f780;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.ts","code":"export interface SurfaceOptions {\n    intensity?: number;\n    tilt?: boolean;\n}\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element: HTMLElement, { intensity = 1, tilt = false }: SurfaceOptions = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"typescript","group":"shared"},{"name":"src/parts/blocks/iridescent-surface/vanilla/main.ts","code":"import { init } from './init';\n\nconst element = document.querySelector\u003cHTMLElement>('.sop-iridescent-surface');\nif (!element) throw new Error('The Iridescent Surface root was not found.');\nconst controller = init(element);\n\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"typescript","group":"example"},{"name":"src/parts/blocks/iridescent-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/iridescent-surface/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Iridescent Surface\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.ts\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}],"js":[{"name":"src/parts/blocks/iridescent-surface/vanilla/init.js","code":"import { createSurfaceController } from '../../../../shared/surface-controller.js';\nexport function init(element, options = {}) {\n    return createSurfaceController(element, { tilt: true, ...options });\n}\n","language":"javascript","group":"component"},{"name":"src/parts/blocks/iridescent-surface/styles.css","code":"/* iridescent-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-iridescent-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-iridescent-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-iridescent-surface,\n  .sop-surface.sop-iridescent-surface:before,\n  .sop-surface.sop-iridescent-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-iridescent-surface {\n  border: 1px solid #c4c6f84d;\n  border-radius: 16px;\n  background: radial-gradient(ellipse at var(--sop-x,25%) var(--sop-y,25%),#b9b1dd16,transparent 65%),linear-gradient(var(--sop-angle,128deg),#b5c2ac19 0,#b9b1df3c 22%,#73889e22 40%,#a3bfa326 62%,#c49bab38 80%,#baba8c19),#1b1b24;\n  box-shadow: inset 0 1px 0 #f0dfff30,inset 0 -1px 0 #baffd51a,0 18px 35px #0005;\n  transition: transform .5s cubic-bezier(.16,1,.3,1),border-color .4s;\n}\n\n.sop-surface.sop-iridescent-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: repeating-linear-gradient(120deg,transparent 0 4px,#fcf0ff04 4px 5px);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(300px circle at var(--sop-x,30%) var(--sop-y,10%),#e9e5ff26,transparent 65%);\n  mix-blend-mode: screen;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:hover {\n  border-color: #d9d4f780;\n}\n\n","language":"css","group":"component"},{"name":"src/shared/surface-controller.js","code":"/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nexport function createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n","language":"javascript","group":"shared"},{"name":"src/parts/blocks/iridescent-surface/vanilla/main.js","code":"import { init } from './init.js';\nconst element = document.querySelector('.sop-iridescent-surface');\nif (!element)\n    throw new Error('The Iridescent Surface root was not found.');\nconst controller = init(element);\n// On SPA navigation or when removing the component: controller.destroy();\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n","language":"javascript","group":"example"},{"name":"src/parts/blocks/iridescent-surface/markup.html","code":"\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n","language":"markup","group":"example"},{"name":"src/parts/blocks/iridescent-surface/vanilla/index.html","code":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\n\u003chead>\n  \u003cmeta charset=\"UTF-8\">\n  \u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n  \u003ctitle>Iridescent Surface\u003c/title>\n  \u003clink rel=\"stylesheet\" href=\"../styles.css\">\n\u003c/head>\n\u003cbody>\n\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\n  \u003cscript type=\"module\" src=\"./main.js\">\u003c/script>\n\u003c/body>\n\u003c/html>\n","language":"markup","group":"example"}]},"preview":{"index.html":"\u003c!doctype html>\n\u003chtml lang=\"ja\">\u003chead>\u003cmeta charset=\"utf-8\">\u003cmeta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\u003ctitle>Iridescent Surface — standalone demo\u003c/title>\u003clink rel=\"stylesheet\" href=\"./styles.css\">\u003c/head>\u003cbody>\u003cmain>\u003ch1>Iridescent Surface · 2.2.0\u003c/h1>\u003cdiv class=\"demo-root\">\u003cdiv class=\"sop-surface sop-iridescent-surface\">\u003cdiv class=\"sop-surface-content\">\u003ch2>Iridescent Surface\u003c/h2>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003cbutton type=\"button\">サンプルボタン\u003c/button>\u003c!-- slot: freely replace the content -->\u003ch3>Your next idea.\u003c/h3>\u003cp>ここに、あなたのコンテンツを。\u003c/p>\u003c/div>\u003c/div>\n\u003c/div>\u003cp class=\"hint\">中身を自由に入れ替えられる、独立した背景パーツです。\u003c/p>\u003c/main>\u003cscript src=\"./app.js\" defer>\u003c/script>\u003c/body>\u003c/html>\n","styles.css":"/* Demo layout only; never required by the component. */\n:root{color-scheme:dark}body{margin:0;min-height:100vh;display:grid;place-items:center;background:#14161a;color:#e8ebef;font:14px/1.7 Arial,'Yu Gothic',sans-serif}main{width:min(420px,calc(100% - 40px));padding:36px 0}h1{font-size:15px;font-weight:400;margin-bottom:55px;color:#b9c7b4}.demo-root{display:flex;justify-content:center;min-width:0}.hint{font-size:11px;color:#9aa3ae;margin-top:48px}button:not(.sop-toggle){color:inherit;background:transparent;border:1px solid #62696e;border-radius:6px;padding:9px 12px}main>.demo-root>.sop-surface{width:100%}\n/* iridescent-surface: isolated component styles, generated from style.css + shared base. */\n.sop-surface.sop-iridescent-surface {\n  --sop-x: 50%;\n  --sop-y: 50%;\n  --sop-rx: 0deg;\n  --sop-ry: 0deg;\n  position: relative;\n  box-sizing: border-box;\n  isolation: isolate;\n  overflow: hidden;\n  min-width: 0;\n  width: 100%;\n  color: #eeeee6;\n  transform: perspective(900px) rotateX(var(--sop-rx)) rotateY(var(--sop-ry));\n}\n\n.sop-surface.sop-iridescent-surface .sop-surface-content {\n  position: relative;\n  z-index: 1;\n  box-sizing: border-box;\n  padding: var(--sop-padding,28px);\n}\n\n@media(prefers-reduced-motion:reduce) {\n  .sop-surface.sop-iridescent-surface,\n  .sop-surface.sop-iridescent-surface:before,\n  .sop-surface.sop-iridescent-surface:after {\n    animation: none!important;\n    transition: none!important;\n    transform: none!important;\n  }\n\n}\n\n.sop-surface.sop-iridescent-surface {\n  border: 1px solid #c4c6f84d;\n  border-radius: 16px;\n  background: radial-gradient(ellipse at var(--sop-x,25%) var(--sop-y,25%),#b9b1dd16,transparent 65%),linear-gradient(var(--sop-angle,128deg),#b5c2ac19 0,#b9b1df3c 22%,#73889e22 40%,#a3bfa326 62%,#c49bab38 80%,#baba8c19),#1b1b24;\n  box-shadow: inset 0 1px 0 #f0dfff30,inset 0 -1px 0 #baffd51a,0 18px 35px #0005;\n  transition: transform .5s cubic-bezier(.16,1,.3,1),border-color .4s;\n}\n\n.sop-surface.sop-iridescent-surface:before {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: repeating-linear-gradient(120deg,transparent 0 4px,#fcf0ff04 4px 5px);\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  background: radial-gradient(300px circle at var(--sop-x,30%) var(--sop-y,10%),#e9e5ff26,transparent 65%);\n  mix-blend-mode: screen;\n  z-index: 0;\n  pointer-events: none;\n}\n\n.sop-surface.sop-iridescent-surface:hover {\n  border-color: #d9d4f780;\n}\n\n","app.js":"/* Generated from src/ by npm run build. Do not edit this file. */\n(function(){'use strict';const modules={\n\"src/parts/blocks/iridescent-surface/demo/main.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nconst init_1 = require(\"src/parts/blocks/iridescent-surface/vanilla/init.ts\");\nconst root = document.querySelector('.demo-root > :first-child');\nif (!root)\n    throw new Error('Missing demo root');\nconst controller = (0, init_1.init)(root);\nwindow.addEventListener('pagehide', () => controller.destroy(), { once: true });\n\n},\n\"src/parts/blocks/iridescent-surface/vanilla/init.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.init = init;\nconst surface_controller_1 = require(\"src/shared/surface-controller.ts\");\nfunction init(element, options = {}) {\n    return (0, surface_controller_1.createSurfaceController)(element, { tilt: true, ...options });\n}\n\n},\n\"src/shared/surface-controller.ts\":function(module,exports,require){\n\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.createSurfaceController = createSurfaceController;\n/** Pointer light and optional tilt. CSS supplies a motion-reduced and touch-safe resting state. */\nfunction createSurfaceController(element, { intensity = 1, tilt = false } = {}) {\n    const abort = new AbortController();\n    const media = window.matchMedia('(prefers-reduced-motion: reduce)');\n    let raf = 0, disposed = false, x = .5, y = .5;\n    function paint() {\n        raf = 0;\n        if (disposed)\n            return;\n        element.style.setProperty('--sop-x', `${x * 100}%`);\n        element.style.setProperty('--sop-y', `${y * 100}%`);\n        element.style.setProperty('--sop-angle', `${110 + x * 70 - y * 25}deg`);\n        element.style.setProperty('--sop-rx', `${tilt && !media.matches ? (y - .5) * -6 * intensity : 0}deg`);\n        element.style.setProperty('--sop-ry', `${tilt && !media.matches ? (x - .5) * 8 * intensity : 0}deg`);\n    }\n    function reset() {\n        x = y = .5;\n        cancelAnimationFrame(raf);\n        raf = 0;\n        element.classList.remove('sop-hover');\n        paint();\n    }\n    element.addEventListener('pointermove', event => {\n        if (media.matches || event.pointerType !== 'mouse')\n            return;\n        const rect = element.getBoundingClientRect();\n        x = Math.max(0, Math.min(1, (event.clientX - rect.left) / rect.width));\n        y = Math.max(0, Math.min(1, (event.clientY - rect.top) / rect.height));\n        element.classList.add('sop-hover');\n        if (!raf)\n            raf = requestAnimationFrame(paint);\n    }, { passive: true, signal: abort.signal });\n    element.addEventListener('pointerleave', reset, { signal: abort.signal });\n    media.addEventListener('change', reset, { signal: abort.signal });\n    return { destroy() { reset(); disposed = true; abort.abort(); } };\n}\n\n}\n};const cache={};function require(id){if(cache[id])return cache[id].exports;const module=cache[id]={exports:{}};if(!modules[id])throw new Error('Unknown module '+id);modules[id](module,module.exports,require);return module.exports;}require(\"src/parts/blocks/iridescent-surface/demo/main.ts\");})();\n"}}];
